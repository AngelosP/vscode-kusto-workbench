# Architecture Convergence

## Purpose

This is the living work queue for moving Kusto Workbench toward [GOLDEN_OUTCOME.md](GOLDEN_OUTCOME.md).

It records:

- Where the current implementation already matches the golden outcome.
- Where authority, contracts, and behavior still diverge.
- Which gap has the highest priority now.
- The smallest vertical migration that can prove the next boundary.
- The evidence required before selecting another gap.

[ARCHITECTURE.md](ARCHITECTURE.md) remains the factual description of the current implementation. The golden outcome is stable. This document changes after every architecture iteration.

## Operating Principle

Convergence is not a rewrite and not a file-splitting exercise.

Each iteration must move one real state transition behind one explicit owner, preserve the external behavior and file formats, delete the displaced authority, and add a guard that prevents the old ownership pattern from returning.

A smaller file is not evidence of progress. Fewer competing authorities and stronger executable contracts are evidence of progress.

## Baseline

Assessment date: 2026-08-15

The comparison was refreshed from the clean synchronized tree at pushed PRO-25 commit `4787cd11e7d5c5d3b0ccc06bc50dd10963cf0462` (`feat: Implement Power BI publish protocol with message validation and handling`), whose parent is immutable PRO-24 commit `465386b4c3973a18ef692823d29f96a71c555feb`. Local `HEAD`/`main`, `origin/main`, and `origin/HEAD` all resolve to that commit with zero upstream divergence. The combined commit contains the complete PRO-25 implementation, tests, synchronized bundle baselines, and closure documentation together with the previously concurrent `src/host/extension.ts` and agent-open-file-targeting E2E changes; broad qualification covered the complete committed tree. The worktree was clean at this assessment. The comparison includes the full implemented feature set described by the README, package contributions, persisted formats, architecture documentation, browser extension, and test suites.

The current architecture is not uniformly legacy. Several high-risk contexts already provide good models for the rest of the application.

## Current Alignment

| Golden outcome boundary | Current implementation | Assessment |
| ----------------------- | ---------------------- | ---------- |
| Exact Kusto schema ownership | [`KustoEditorSchemaCoordinator`](src/webview/core/kusto-editor-schema-coordinator.ts), exact message routing, model leases, target generations, and tombstones | Strong foundation |
| Serialized shared Kusto worker | [`KustoWorkerMutationPort`](src/webview/shared/kusto-worker-mutation-port.ts) with detached settlement and inline recovery | Strong foundation |
| SQL target and execution ownership | [`SqlEditorLifecycleCoordinator`](src/host/sql/sqlEditorLifecycleCoordinator.ts), [`SqlEditorSessionRegistry`](src/host/sql/sqlEditorSessionRegistry.ts), and [`SqlExecutionBroker`](src/host/sql/sqlExecutionBroker.ts) | Strong foundation |
| SQL editor/language lifecycle forwarding ownership | [`HostSqlEditorLifecycleApplicationHandler`](src/host/sqlEditorLifecycleApplicationHandler.ts), exact nine-route admission/settlement, Production-gated development auth, ordered account-map/token writes, and disposal | Strong bounded host-application boundary |
| Kusto schema request forwarding ownership | [`HostKustoSchemaRequestApplicationHandler`](src/host/kustoSchemaRequestApplicationHandler.ts), exact primary/cross-cluster admission, argument shaping, settlement, and disposal while `SchemaService` retains all algorithms | Strong bounded host-application boundary |
| Kusto schema request/delivery protocol | [`kustoSchemaProtocol.ts`](src/shared/kustoSchemaProtocol.ts), one six-message discriminated type/runtime parser owner, host request admission, typed service delivery, and webview validation before every schema effect | Strong bounded protocol slice |
| Kusto database-discovery protocol | [`kustoDatabaseDiscoveryProtocol.ts`](src/shared/kustoDatabaseDiscoveryProtocol.ts), one four-message discriminated type/runtime parser owner, request admission at both boundaries, typed service delivery, and dispatcher/E2E-waiter validation before every database effect | Strong bounded protocol slice |
| SQL database-discovery protocol | [`sqlDatabaseDiscoveryProtocol.ts`](src/shared/sqlDatabaseDiscoveryProtocol.ts), one five-message discriminated type/runtime parser owner, request admission at both boundaries, typed handler delivery, and dispatcher/router validation before every request-ledger or UI effect | Strong bounded protocol slice |
| SQL schema request/delivery protocol | [`sqlSchemaProtocol.ts`](src/shared/sqlSchemaProtocol.ts), one two-message discriminated type/runtime parser owner, request admission at both boundaries, typed handler delivery, and dispatcher/router validation before every catalog or schema-info effect | Strong bounded protocol slice |
| SQL STS editor-language protocol | [`sqlStsEditorLanguageProtocol.ts`](src/shared/sqlStsEditorLanguageProtocol.ts), one eight-message descriptor-safe type/runtime-admission owner, local and transport request admission, typed coordinator delivery, and dispatcher/router validation before resolver, diagnostics, readiness, owner, or UI effects | Strong bounded protocol slice |
| SQL connections snapshot protocol | [`sqlConnectionsProjectionProtocol.ts`](src/shared/sqlConnectionsProjectionProtocol.ts), one two-message descriptor-safe type/runtime-admission owner, captured request/host application values, typed handler delivery, and validation before revision or projection effects | Strong bounded protocol slice |
| Kusto connections snapshot protocol | [`kustoConnectionsProjectionProtocol.ts`](src/shared/kustoConnectionsProjectionProtocol.ts), one two-message descriptor-safe type/runtime-admission owner, JSON-stable canonical projection, captured staged/application values, transactional privacy/result application, acknowledgement reentrancy fencing, and validation before revision or projection effects | Strong bounded protocol slice |
| Kusto publication transaction protocol | [`kustoPublicationProtocol.ts`](src/shared/kustoPublicationProtocol.ts), one four-message descriptor-safe type/runtime-admission owner across Query Editor, Connection Manager, and Cached Values, with exact waiter/stage/timer/payload/acknowledgement admission and opaque outer payload capture | Strong bounded protocol slice |
| Main-webview envelope capture | [`runtimeMessageEnvelope.ts`](src/shared/runtimeMessageEnvelope.ts) captures one inert own-data value plus its original one-shot descriptor snapshot as the first startup-gateway operation; PRO-21 through PRO-25 and artifact CSV consume the retained snapshot while unrelated payload schemas remain domain-owned | Strong shallow transport guard, not a global schema |
| KQL language request/response protocol | [`kqlLanguageProtocol.ts`](src/shared/kqlLanguageProtocol.ts), one two-message discriminated type/runtime parser owner, request admission at both boundaries, typed handler delivery, and response/method validation before resolver settlement | Strong bounded protocol slice |
| Control-command syntax request/result protocol | [`controlCommandSyntaxProtocol.ts`](src/shared/controlCommandSyntaxProtocol.ts), one two-message discriminated type/runtime parser owner, request admission before pending/transport/lookup effects, typed handler delivery, and exact command/request response settlement | Strong bounded protocol slice |
| Resource URI request/result protocol | [`resourceUriProtocol.ts`](src/shared/resourceUriProtocol.ts), one two-message discriminated type/runtime parser owner across main Query Editor and plain `.md` adapters, request admission before host effects, typed delivery, and result validation before resolver/timer/rendering effects | Strong bounded protocol slice |
| URL content request/delivery protocol | [`urlContentProtocol.ts`](src/shared/urlContentProtocol.ts), one three-message discriminated type/runtime parser owner, atomic descriptor-snapshotted admission before transport/fetch and dual dispatcher/component effects, and typed delivery | Strong bounded protocol slice |
| Python execution request/terminal protocol | [`pythonExecutionProtocol.ts`](src/shared/pythonExecutionProtocol.ts), one three-message discriminated type/runtime parser owner, atomic descriptor-snapshotted admission before reservation/transport/process and dispatcher/leaf terminal effects, typed delivery, and exact box-only settlement | Strong bounded protocol slice |
| Artifact CSV save intent/transfer protocol | [`artifactCsvSaveProtocol.ts`](src/shared/artifactCsvSaveProtocol.ts), one five-message discriminated type/runtime parser owner, atomic descriptor-snapshotted admission before transport/startup/picker/nonce/pending-export/file/download effects, typed host delivery, and exact export/box/artifact settlement | Strong bounded protocol slice |
| Exact SQL schema request ownership | [`HostSqlSchemaRequestApplicationHandler`](src/host/sqlSchemaRequestApplicationHandler.ts), exact lifecycle owner capture, request-current disk/memory cache admission, generation-leased readers, and application-acknowledged publication | Strong bounded host-application boundary |
| SQL connection projection ownership | [`HostSqlConnectionsProjectionApplicationHandler`](src/host/sqlConnectionsProjectionApplicationHandler.ts), one revisioned trigger queue, canonical policy/principal joining, owner-filtered cache/favorites projection, global-block fail-closed behavior, and exact publication | Strong bounded host-application boundary |
| Kusto connection projection ownership | [`HostKustoConnectionsProjectionApplicationHandler`](src/host/kustoConnectionsProjectionApplicationHandler.ts), one revisioned trigger queue, editing-preference/first-time/account/physical-connection/selection/cache/favorites reads, canonical policy-lock admission, correlated retry, and application-acknowledged publication | Strong bounded host-application boundary |
| Persisted-result sanitation ownership | [`HostPersistedResultSanitizationApplicationHandler`](src/host/persistedResultSanitizationApplicationHandler.ts), synchronous/fresh/fail-closed and lock-held publication sanitation, exact Kusto-then-SQL admission, owner/comparison/artifact cleanup, and both invalidation events | Strong bounded cross-language host-application boundary |
| Compatibility sidecar mechanics and close | [`CompatSidecarFormat`](src/host/compatSidecarFormat.ts), [`CompatSidecarStore`](src/host/compatSidecarStore.ts), [`CompatSidecarSession`](src/host/compatSidecarSession.ts), and panel-scoped [`CompatSidecarCloseCoordinator`](src/host/compatSidecarCloseCoordinator.ts) | Strong shared core and close owner |
| Compatibility projection and reload ownership | Panel-scoped [`CompatSidecarProjectionCoordinator`](src/host/compatSidecarProjectionCoordinator.ts) owns KQL/SQL projection generations, active/pending source identity, bounded initial recovery, reload currentness, persist-generation admission, and rollback-failure fencing | Strong shared composition owner |
| Compatibility persist-snapshot application ownership | Panel-scoped [`CompatSidecarPersistCoordinator`](src/host/compatSidecarPersistCoordinator.ts) owns KQL/SQL final admission, terminal decisions, session/projection application, source verification/rollback, draft/state commits, acknowledgement, beforeunload, and final settlement while providers expose typed physical effects only | Strong shared composition owner |
| Compatibility persistence protocol | [`compatibilityPersistenceProtocol.ts`](src/shared/compatibilityPersistenceProtocol.ts), host-created panel UUIDs, strict KQL/SQL lifecycle schemas, request/projection replay, and validation before revision, queue, text, sidecar, final-waiter, or reload effects | Strong bounded protocol slice |
| Lossless notebook codec and kind capabilities | [`kqlxOverlay.ts`](src/host/kqlxOverlay.ts) preserves exact unknown data; [`documentSectionCapabilities.ts`](src/shared/documentSectionCapabilities.ts) owns every known `.kqlx`/`.sqlx`/`.mdx` allow/default/add decision across parser, host, webview, tools, upgrades, and browser | Strong foundation |
| Kusto physical identity fencing | Connection, schema, and database operations capture endpoint and authority identity before asynchronous work | Strong foundation |
| SQL Leave No Trace policy | Cross-window policy, revocation generations, protected one-shot runtime, and guarded admission | Strong but SQL-specific |
| Portable dashboard admission | [`portableDashboardCompiler.ts`](src/shared/portableDashboardCompiler.ts) owns standards-based provenance v1 parsing, browser-tree/source-range target and replacement admission, display/source-schema validation, typed diagnostics, and normalized IR across preview, agent, export, and publish paths | Strong compiler boundary |
| Power BI project artifacts | [`powerBiProjectArtifacts.ts`](src/host/powerBiProjectArtifacts.ts) compiles one sorted, portable-path-admitted, copy-on-read byte manifest; [`powerBiProjectWriter.ts`](src/host/powerBiProjectWriter.ts) writes it locally and [`powerBiPublish.ts`](src/host/powerBiPublish.ts) projects Fabric parts directly without filesystem staging | Strong compiler/adapter boundary |
| Dashboard application workflow | [`HostDashboardApplicationHandler`](src/host/dashboardApplicationHandler.ts) owns all dashboard requests, cancellation, first-commit admission, publish application/compensation leases, and cleanup; `QueryEditorProvider` only composes transport | Strong initial host-application boundary |
| Artifact CSV save workflow | [`HostArtifactCsvSaveApplicationHandler`](src/host/artifactCsvSaveApplicationHandler.ts) owns picker admission, nonce challenges, cancellation, deadlines, replay tombstones, exact correlation, and file publication; [`artifactCsvSaveProtocol.ts`](src/shared/artifactCsvSaveProtocol.ts) validates all five wire messages while `QueryEditorProvider` only composes transport | Strong bounded host-application and protocol boundary |
| Imported CSV save workflow | [`HostImportedCsvSaveApplicationHandler`](src/host/importedCsvSaveApplicationHandler.ts) owns empty-data UX, picker admission, URI-preserving extension handling, exact UTF-8 publication, saved-file actions, failures, and disposal; `QueryEditorProvider` only composes routing | Strong bounded host-application boundary |
| Query-sharing workflow | [`HostQuerySharingApplicationHandler`](src/host/querySharingApplicationHandler.ts) owns ADX-link encoding, Kusto/SQL rich and plain formatting, host clipboard effects, response publication, notifications, and disposal; `QueryEditorProvider` only composes lookup and transport | Strong bounded host-application boundary |
| Query-sharing protocol | [`querySharingProtocol.ts`](src/shared/querySharingProtocol.ts) solely owns descriptor-safe `copyAdeLink`, `shareToClipboard`, and `shareContentReady` admission before transport, lookup/formatting, host delivery, or clipboard effects | Strong bounded protocol slice |
| Editing-preferences protocol | [`editingPreferences.ts`](src/shared/editingPreferences.ts) solely owns descriptor-safe setters plus `editingPreferencesData`, exact booleans, frozen host snapshots, safe/equal revision behavior, and validation before transport, configuration, publication, revision, or UI effects | Strong bounded protocol slice |
| URL-content acquisition workflow | [`HostUrlContentApplicationHandler`](src/host/urlContentApplicationHandler.ts) owns URL validation, redirect-following fetch, timeout/abort, byte caps, classification/sniffing, truncation, response publication, and disposal; `QueryEditorProvider` only composes transport | Strong bounded host-application boundary |
| Control-command syntax lookup | [`HostControlCommandSyntaxApplicationHandler`](src/host/controlCommandSyntaxApplicationHandler.ts) owns the 24-hour cache, Microsoft Learn fetch/normalization, syntax/entity extraction, `with(...)` parsing, response publication, and disposal; `QueryEditorProvider` only composes transport | Strong bounded host-application boundary |
| Markdown resource URI resolution | [`HostResourceUriApplicationHandler`](src/host/resourceUriApplicationHandler.ts) retains the main cache/base/workspace algorithm while [`MdCompatEditorProvider`](src/host/mdCompatEditorProvider.ts) retains the plain-Markdown document-directory algorithm; both use the shared validated request/result channel | Strong bounded application adapters behind one protocol |
| Copilot content opening | [`HostCopilotContentOpenApplicationHandler`](src/host/copilotContentOpenApplicationHandler.ts) owns untitled plaintext tool-result display, beside-column presentation, local Markdown preview dispatch, exact failures, and disposal; `QueryEditorProvider` only composes routing | Strong bounded host-application boundary |
| Host information notifications | [`HostInformationNotificationApplicationHandler`](src/host/informationNotificationApplicationHandler.ts) owns exact `showInfo` text forwarding, fire-and-forget native invocation, synchronous failures, and disposal; `QueryEditorProvider` only composes routing | Strong bounded host-application boundary |
| Cached Values opening | [`HostCachedValuesOpenApplicationHandler`](src/host/cachedValuesOpenApplicationHandler.ts) owns awaited zero-argument `kusto.seeCachedValues` dispatch, exact rejection, response-free behavior, and disposal; command registration and `CachedValuesViewerV2` retain first-launch and viewer lifecycle ownership | Strong bounded host-application boundary |
| Copilot Agent opening | [`HostCopilotAgentOpenApplicationHandler`](src/host/copilotAgentOpenApplicationHandler.ts) owns awaited zero-argument delegation to the unchanged shared Agent Chat helper, exact rejection, response-free behavior, and disposal; extension, dashboard, and Copilot helper callers remain independent | Strong bounded host-application boundary |
| Editor cursor status | [`HostEditorCursorStatusApplicationHandler`](src/host/editorCursorStatusApplicationHandler.ts) owns cursor update/snapshot routing, provider-scoped owner identity, panel visibility/disposal clearing, development snapshot transport, and disposal; `EditorCursorStatusBar` retains shared item/format/accessibility/stale-owner ownership | Strong bounded host-application boundary |
| Editing preferences and first launch | [`HostEditingPreferencesApplicationHandler`](src/host/editingPreferencesApplicationHandler.ts) owns the three provider mutation routes and global/current-panel publication choice; [`editingPreferences.ts`](src/host/editingPreferences.ts) retains application persistence, revisions, snapshots, and migration; transactional profile setup remains separate | Strong bounded host-application boundary |
| Kusto connection intake | [`HostKustoConnectionIntakeApplicationHandler`](src/host/kustoConnectionIntakeApplicationHandler.ts) owns discovered-cluster and Kusto Explorer XML intake plus exact post-mutation refresh ordering; [`ConnectionManager`](src/host/connectionManager.ts) retains persistence and [`HostKustoConnectionsProjectionApplicationHandler`](src/host/kustoConnectionsProjectionApplicationHandler.ts) is the revisioned refresh effect | Strong bounded host-application boundary |
| Kusto single-connection onboarding | [`HostKustoConnectionOnboardingApplicationHandler`](src/host/kustoConnectionOnboardingApplicationHandler.ts) owns prompt/add/test routing, exact validation/persistence/selection/refresh ordering, account-aware draft testing, tracing, and terminals; profile services and the Kusto projection handler remain injected owners | Strong bounded host-application boundary |
| SQL single-connection onboarding | [`HostSqlConnectionOnboardingApplicationHandler`](src/host/sqlConnectionOnboardingApplicationHandler.ts) owns native prompt and direct-form intake, AAD/SQL Login credential shaping, selection persistence, acknowledgement publication, and disposal; [`SqlConnectionManager`](src/host/sqlConnectionManager.ts) retains persistence/secrets and the projection handler retains revisioned policy/principal-aware publication | Strong bounded host-application boundary |
| Query Editor SQL favorites | [`HostSqlFavoritesApplicationHandler`](src/host/sqlFavoritesApplicationHandler.ts) owns exact naming UX, sanitized mutation/persistence, response publication/failure containment, projection reads, and disposal; Connection Manager routes and [`HostSqlConnectionsProjectionApplicationHandler`](src/host/sqlConnectionsProjectionApplicationHandler.ts) remain separate | Strong bounded host-application boundary |
| Query Editor Kusto favorites | [`HostKustoFavoritesApplicationHandler`](src/host/kustoFavoritesApplicationHandler.ts) owns migration, principal filtering, mutation/persistence, cross-panel/listener publication, confirmation, projection reads, activation, and disposal; Connection Manager routes remain an independent workflow | Strong bounded host-application boundary |
| Query Editor SQL database discovery | [`HostSqlDatabaseDiscoveryApplicationHandler`](src/host/sqlDatabaseDiscoveryApplicationHandler.ts) owns target adoption, passive/refresh acquisition, canonical target/principal/privacy admission, exact terminals, protected STS cleanup, cache sequencing, and disposal; lifecycle/workbench/cache/client owners remain separate | Strong bounded host-application boundary |
| Query Editor KQL language requests | [`HostKqlLanguageRequestApplicationHandler`](src/host/kqlLanguageRequestApplicationHandler.ts) owns validated request admission, language-host delegation, typed success/fixed-failure responses, logging, and disposal; language host/analyzer and webview resolver owners remain separate | Strong bounded host-application boundary |
| Canonical KQL source analysis | [`analyzeKqlSource`](src/host/kqlLanguageService/sourceAnalysis.ts) owns exact-layout masking, statements/management spans, lexical scopes, and physical table references across diagnostics, reference lookup, schema matching, and fail-closed Fully Qualify ranges | Strong pure analyzer boundary |
| Query Editor SQL last selection | [`HostSqlLastSelectionApplicationHandler`](src/host/sqlLastSelectionApplicationHandler.ts) owns trimmed connection admission, ordered last-connection/optional last-database application-state writes, exact rejection, response-free behavior, and disposal; emitter/onboarding/projection owners remain separate | Strong bounded host-application boundary |
| Copilot development-note mutation | [`HostDevelopmentNoteMutationApplicationHandler`](src/host/developmentNoteMutationApplicationHandler.ts) owns request IDs, five-second deadlines, delivery failures, matching response settlement, and disposal; Copilot semantics, webview mutation/persistence, and unclaimed tool responses remain with their existing owners | Strong bounded host-application boundary |
| Development-note mutation protocol | [`developmentNoteMutationProtocol.ts`](src/shared/developmentNoteMutationProtocol.ts) solely owns descriptor-safe exact add/supersede/remove requests plus correlated response admission across Copilot, agent, main-webview, native/companion, and both host waiters | Strong bounded protocol slice |
| Tool-state snapshot protocol | [`toolStateSnapshotProtocol.ts`](src/shared/toolStateSnapshotProtocol.ts) solely owns bounded descriptor-safe `requestToolState` and `toolStateResponse` construction/admission before snapshot, startup reentrancy, waiter, timer, SQL reconciliation, or settlement effects | Strong bounded protocol slice |
| Kusto execution-start handshake protocol | [`kustoExecutionStartProtocol.ts`](src/shared/kustoExecutionStartProtocol.ts) solely owns descriptor-safe Copilot/lineage-complete comparison start construction plus exact acknowledgement admission before startup, section, comparison-binding, waiter, timer, or physical-dispatch effects | Strong bounded protocol slice |
| Power BI publish application handshake protocol | [`powerBiPublishProtocol.ts`](src/shared/powerBiPublishProtocol.ts) solely owns exact descriptor-safe result/acknowledgement construction and admission before startup, standalone transport, HTML metadata/document/dialog effects, outbound transport, or HST-1 lease/timer/cleanup effects | Strong bounded protocol slice |
| Copilot inline-completion admission | [`HostCopilotInlineCompletionApplicationHandler`](src/host/copilotInlineCompletionApplicationHandler.ts) owns Kusto/SQL route admission, SQL owner-token assertion/delegation, exact empty SQL fallback, and disposal; Copilot model behavior, SQL lifecycle/privacy, and webview resolution/application remain with their existing owners | Strong bounded host-application boundary |
| Copilot inline-completion protocol | [`copilotInlineCompletionProtocol.ts`](src/shared/copilotInlineCompletionProtocol.ts) solely owns descriptor-safe request/result types and runtime admission before transport, SQL/model effects, both host result producers, dispatcher settlement, and direct SQL owner admission | Strong bounded protocol slice |
| Copilot availability admission | [`HostCopilotAvailabilityApplicationHandler`](src/host/copilotAvailabilityApplicationHandler.ts) owns exact route admission, awaited Copilot capability delegation, rejection, and disposal; `CopilotService` retains model discovery, failure-to-false, and response publication | Strong bounded host-application boundary |
| Copilot write-query preparation admission | [`HostCopilotWriteQueryPreparationApplicationHandler`](src/host/copilotWriteQueryPreparationApplicationHandler.ts) owns exact Kusto/SQL route admission, awaited capability delegation, rejection, and disposal; `CopilotService` retains box normalization, model/tool selection, and options/status publication | Strong bounded host-application boundary |
| Copilot conversation-clear admission | [`HostCopilotConversationClearApplicationHandler`](src/host/copilotConversationClearApplicationHandler.ts) owns exact Kusto/SQL route admission, Kusto request-identity validation, awaited capability delegation, rejection, and disposal; `CopilotService` retains box normalization, state deletion, exact Kusto owner matching, and owner deletion | Strong bounded host-application boundary |
| Copilot history-removal admission | [`HostCopilotHistoryRemovalApplicationHandler`](src/host/copilotHistoryRemovalApplicationHandler.ts) owns exact route admission, awaited box/entry capability delegation, rejection, and disposal; `CopilotService` retains normalization, history lookup, eligible-entry decisions, and mutation while the shared Kusto/SQL UI retains immediate removal and emission | Strong bounded host-application boundary |
| Copilot Chat first-time workflow | [`HostCopilotChatFirstTimeApplicationHandler`](src/host/copilotChatFirstTimeApplicationHandler.ts) owns exact route admission, durable-state ordering, exact modal UX, Agent Chat delegation, result publication, and disposal; initial projection, shared callers, result application, and helper behavior remain separate | Strong bounded host-application boundary |
| Workbench panel tool session | [`HostWorkbenchToolSessionApplicationHandler`](src/host/workbenchToolSessionApplicationHandler.ts) owns orchestrator connection/reactivation/disconnect, five-second state correlation, schema-refresh target projection, SQL callbacks, all three tool routes, and disposal; [`toolStateSnapshotProtocol.ts`](src/shared/toolStateSnapshotProtocol.ts) guards the state channel while orchestrator, schema, SQL lifecycle, and HST-23 response ownership remain separate | Strong bounded host-application boundary |
| Kusto connection browsing and selection | [`HostKustoConnectionBrowsingApplicationHandler`](src/host/kustoConnectionBrowsingApplicationHandler.ts) owns all four list/browse/refresh/select routes, exact database request shaping, selection ordering, diagnostics containment, and disposal; the Kusto projection handler and `ConnectionService` remain canonical capabilities | Strong bounded host-application boundary |
| Copilot query generation and optimization workflow | [`HostCopilotQueryWorkflowApplicationHandler`](src/host/copilotQueryWorkflowApplicationHandler.ts) owns write-query start/cancel and Optimize prepare/cancel/run admission, exact SQL owner-preflight races/terminals, Kusto cancellation identity, lazy adapter delegation, and disposal; Copilot/SQL services remain canonical capabilities | Strong bounded host-application boundary |
| Kusto section execution and publication workflow | [`HostKustoSectionExecutionApplicationHandler`](src/host/kustoSectionExecutionApplicationHandler.ts) owns section open/target/close, exact start/publication acknowledgement ledgers and deadlines, manual execution admission, exact cancellation, physical/policy result admission, and disposal; coordinator/client/connection/Copilot services remain canonical capabilities | Strong bounded host-application boundary |
| Cross-engine comparison preparation workflow | [`HostComparisonPreparationApplicationHandler`](src/host/comparisonPreparationApplicationHandler.ts) owns pending requests, exact Kusto/SQL preparation identity, all five SQL acknowledgement phases, rollback/retry, target/policy revalidation, removal cancellation, and disposal; SQL/Kusto/Copilot services remain canonical capabilities | Strong bounded host-application boundary |
| Manual SQL section execution workflow | [`HostSqlSectionExecutionApplicationHandler`](src/host/sqlSectionExecutionApplicationHandler.ts) owns exact execute/cancel admission, synchronous preflight, owner/protection and tool-owner validation, query shaping, physical start, exact terminals, protected logging, cleanup, and disposal; SQL lifecycle/broker/workbench/client services remain canonical capabilities | Strong bounded host-application boundary |
| Comparison summary presentation | [`displayComparisonSummary`](src/webview/sections/query-execution.controller.ts) computes and renders from immutable artifact lineage; no host summary mirror, waiter, route, callback, or protocol message remains | Strong local presentation boundary |
| Native document-view lifecycle protocol | [`documentViewProtocol.ts`](src/shared/documentViewProtocol.ts), one host-created panel UUID, runtime parsing, exactly-once initial projection, and session-fenced commands/results/Save barriers | Strong initial protocol slice |
| Main-webview startup | [`MainWebviewStartupGateway`](src/host/mainWebviewStartupGateway.ts) is the shared panel-scoped transport for native KQLX plus KQL and SQL compatibility, with listener-first installation, explicit dispatcher readiness, ordered startup queues, correlated reply reentrancy, and stable close retirement | Strong startup boundary |
| Preload Add takeover | [`installRuntimeAddSectionBridges()`](src/webview/core/persistence.ts) atomically adopts and clears the preload Add buffer before replacing stubs; one runtime queue drains once per applied projection before default creation | Strong bounded startup handoff |
| Browser read-only composition | [`BrowserViewerRoot`](browser-ext/src/browser-viewer-root.ts) owns generation-fenced payload adoption and one immutable typed projection; the direct browser presentation adapter declares least-privileged host capabilities without synthetic VS Code startup replay | Strong initial browser boundary |
| Native document configuration ownership | [`MarkdownDocumentAggregate`](src/shared/markdownDocumentAggregate.ts) plus seven section definitions own ordered Markdown, URL, Python, Chart, Transformation, HTML, and hidden Development Notes state; the optimistic client, document-view channel, URI queue, acknowledged projection, and Save barrier remain shared | Strong transitional document owner |
| Unmigrated section serialization | Kusto/SQL and remaining adapter-heavy kinds still participate in broad restore/persistence switches; all other native persisted section state, including hidden Development Notes, no longer comes from component or passthrough serialization | Useful transitional boundary |

These parts should normally be wrapped behind golden-outcome ports, not rewritten. Their adversarial lifecycle tests are migration assets.

## Priority Model

Product direction is the eligibility gate. A gap explicitly deferred by the product owner remains documented against the golden outcome but cannot preempt eligible product work, regardless of its raw score. Every eligible gap receives six scores from 1 to 5. The register scores architecture themes; the active iteration is selected separately and must state its own feasibility. A theme cannot borrow the feasibility of a narrow slice or make that slice appear to close the whole theme.

| Dimension | Weight | A score of 5 means |
| --- | ---: | --- |
| `H` - Harm | 3 | The gap can lose/cross-contaminate data, violate privacy, wedge a core workflow, or publish stale work. |
| `A` - Change amplification | 2 | Ordinary feature changes require coordinated edits across many owners or duplicated paths. |
| `R` - Reach | 2 | The gap affects most features, document types, or hosts. |
| `L` - Leverage | 2 | Closing it unlocks several later golden-outcome boundaries. |
| `F` - Feasibility | 1 | A narrow vertical slice can be shipped and reversed safely. |
| `E` - Evidence | 1 | Runtime failures, source-confirmed defects, or strong test gaps demonstrate the problem. |

The score is:

```text
priority = 3H + 2A + 2R + 2L + F + E
```

The maximum is 55. The score orders eligible gaps; it does not override dependency constraints. A correctness, security, or privacy finding preempts the queue only when product direction or release policy classifies it as release-blocking.

## Gap Register

| Rank | ID | Gap | H/A/R/L/F/E | Score | State |
| ---: | --- | --- | ----------- | ----: | ----- |
| 1 | `PRO` | Runtime-validated protocol, view sessions, and deterministic startup | 3/3/4/3/1/5 | 35 | Closed through PRO-25; SQL comparison admission defects are runtime-proven, but canonical SQL section identity spans multiple owners and is not a bounded protocol slice |
| 2 | `DOC` | Document actor and section-definition registry | 3/4/3/4/1/2 | 34 | DOC-1 through DOC-7 closed; Kusto/SQL complete-record migration remains not narrow |
| 3 | `EXA` | Exact execution and immutable artifact spine | 3/3/2/4/1/4 | 32 | Closed through EXA-2; SQL terminal delivery/application acknowledgement is evidenced but lacks a complete cross-producer contract |
| 4 | `BRW` | Real browser read-only composition root | 3/2/2/3/2/5 | 30 | BRW-1 closed; failed-presentation adoption is evidenced but root/boot/standalone pending-generation ownership is not specified |
| 5 | `CMP` | Compatibility-provider composition around the shared sidecar core | 3/3/2/2/2/4 | 29 | CMP-1 through CMP-3 closed; residual Save/partial-failure recovery lacks a complete durable recovery contract |
| - | `COD` | Lossless versioned codecs and one document-kind capability matrix | 1/1/2/2/1/1 | 15 | Closed through COD-2; no eligible residual ownership gap |
| - | `HST` | Host application composition; retire `QueryEditorProvider` as an application shell | 1/1/2/3/4/1 | 20 | Closed through HST-42; `QueryEditorProvider` has zero direct application switch cases |
| - | `DSH` | Dashboard compiler IR separated from VS Code/Fabric adapters | 1/1/1/1/1/1 | 11 | Closed through DSH-2; no eligible residual ownership gap |
| 6 | `KLS` | Custom KQL analyzer decomposition behind a language-analysis port | 1/2/2/2/2/4 | 21 | Closed through KLS-1; joined dynamic-column facts are lost, but recursive inference has no typed fact contract |

Bundle headroom is a release constraint and must remain gated, but it is not itself an ownership architecture. Large domain algorithms are not automatically gaps when they have one owner and focused contracts.

## Gap Details

### `COD` - Lossless Codecs And Document Capabilities

**Golden outcome:** runtime-validated codecs preserve unknown root/state fields, known-section extension fields, opaque unknown sections, and order. One capability matrix validates `.kqlx`, `.sqlx`, and `.mdx` without silently filtering incompatible content.

**Status:** closed through COD-2.

**Current alignment:**

- `kqlxOverlay.ts` now preserves unknown root/state fields, known-section extensions, nested future fields, opaque sections, and relative order across notebook and sidecar persistence.
- Known field omission remains authoritative, legacy/id-less sections receive stable projected identities, and privacy sanitation removes modeled row-bearing fields without gaining authority over future extensions.
- `documentSectionCapabilities.ts` is the sole matrix for known/canonical kinds, legacy `copilotQuery`, hidden `devnotes`, persisted compatibility, visible add kinds, and defaults.
- The parser requires the URI-derived document kind, reports known incompatibility with document kind plus section index/ID/type, and leaves opaque future kinds outside incompatibility classification.
- Host projection, KQL/SQL/Markdown upgrade destinations, webview controls/defaults/restore, actual tool dispatch, and browser read-only parsing use that owner. An explicit empty host capability set remains empty.
- MDX silent sanitation and provider-local arrays are deleted. A static source guard rejects their return, while the cross-layer table tests every known matrix cell and the opaque-future control.

The remaining distributed section parse/reduce/serialize/view knowledge belongs to `DOC`, not COD.

### `EXA` - Exact Execution And Immutable Artifacts

**Golden outcome:** one transport-neutral execution coordinator owns reservation, replacement, cancellation, terminal settlement, and artifact publication. Every terminal carries exact operation identity. Every result becomes an immutable artifact revision with lineage and permissions.

**Current alignment:**

- EXA-1 introduced `KustoExecutionCoordinator` as the sole Kusto section terminal authority. It reserves before awaits, performs exact replacement/cancellation, captures authenticated dispatch identity, and settles success/error/cancellation/supersession once.
- Manual, Run Function, Copilot-final, performance-comparison, and agent-tool section publication use the coordinator. Copilot/comparison dispatch waits for exact webview start acknowledgement; tools acknowledge the exact owner and can cancel before or after acknowledgement.
- Section incarnation, target generation, connection/database, producer, reservation sequence, execution ID, physical attempt, account partition, client activity ID, connection revision/identity, and Leave No Trace revision are correlated through the real controller/provider/window-dispatcher boundary.
- Connection, true account rotation, removed/recreated sessions, authority, target, policy, section, and panel lifecycle changes revoke affected reservations. Only first automatic account establishment preserves its exact run. Comparison retarget clears result, persistence, summary, Optimize, and conversation state before target mutation.
- Kusto Copilot output and comparison preparation carry exact request/lifecycle identity; standalone Optimize has a parallel exact owner. Delegated tools capture exact request owners and use pre-start cancellation tombstones, panel disposal cancels all exact Kusto Copilot/Optimize owners, and recreated sections reject queued box-only or stale-generation output.
- Kusto Leave No Trace uses an atomically committed, file-locked, watched cross-window policy with per-cluster generations. Dispatch, result admission, table preview, and direct Copilot output/history mutation execute under the shared lock before watcher delivery, including awaited asynchronous delivery. Restoration waits for policy readiness and policy transitions purge queued, stored, shared, and rendered protected data. Unrecoverable post-migration state dominates older unblocked versions and stays globally fail closed for persistence and publication.
- Host-issued physical connection stamps fence reservation, pre-start admission, schema lifecycle, and dispatch, so reusing a saved connection ID cannot redirect stale execution or metadata work. Authentication-session generations fence recreated same-account sessions.
- Metadata viewers and direct agent tools carry exact physical/auth/policy/cache owners. Sensitive deliveries use stage/commit/revoke plus application acknowledgement, and persisted search rows are restored from per-row proofs rather than whole-profile invalidation.
- Mixed Kusto/SQL snapshots and persistence sanitation use one-attempt SQL owner locks and retry the complete Kusto-then-SQL acquisition without holding Kusto privacy admission during SQL contention.
- The webview retains cancelling and retired identities until exact terminal admission and rejects unstamped Kusto section terminals.
- `ResultArtifactStore` now snapshots every accepted result into a deep-immutable per-source revision while `results-state.ts` preserves the mutable latest-result facade for compatibility. Current pointers, explicit consumer bindings, source-wide revocation, and reference-based pruning have one owner.
- Admitted Kusto successes publish exact reservation, dispatch, target, principal, policy, and optional comparison lineage. Bounded `resultJson` persistence can carry a row-free artifact descriptor; owner/policy admission precedes descriptor installation, restored identity remains stable, and later revisions remain monotonic.
- Charts, transformations, comparisons, Diff, HTML dashboards, Power BI metadata, model/tool responses, Share Results, CSV export, and governed table-local copy now consume immutable artifacts or exact bindings. URL CSV sections publish immutable source artifacts while retaining their separate generic local copy/save workflow.
- Transformations bind primary and join-right revisions independently, compute from those immutable inputs, and publish derived artifacts with direct lineage roles plus flattened leaf-source policies. Formula edits stay pinned; source changes and dependent refresh explicitly rebind.
- Artifact retention now follows lineage references. Source clear revokes affected revisions and downstream bindings transitively while preserving a retargeted current derived revision whose lineage is independent of the cleared source.
- Kusto comparison source and output executions carry one exact run identity. Manual and standalone Optimize sequence on admitted source success; Copilot retries retain the same source execution. Output and summaries use the pinned source artifact and fail closed on target or policy mismatch.
- HTML dashboard previews bind an immutable provenance fact artifact and require an explicit active-content exposure decision. Refresh rebinds intentionally; revoke/source change hides and blanks iframe data synchronously; restore revalidates exposure after owner admission.
- Model/tool responses, Share Results, and result-table CSV export use independent policy-bearing bindings. CSV tables carry an exact artifact ID plus table-generation token; native save uses a host nonce challenge after the asynchronous picker, and browser viewers emulate the same challenge locally.
- Persistence deletion and host privacy sanitation remove rows and descriptors atomically. A descriptor is never row authority, cannot restore without admitted `resultJson`, and is rejected when its source or policy claims disagree with the current owner.
- Static terminal-construction/cancellation guards, protocol inventory, focused state-machine tests, full sequential tests, and authenticated normal/rerun/retarget/cancellation E2E are green.

**Remaining divergence:**

- Mutable latest-result state remains as a presentation and presence compatibility facade. The EXA-2 audit found no active row-bearing cross-section or egress consumer that still treats it as lineage authority.
- SQL results publish exact query/connection/database ownership for migrated consumers, but transport/principal parity with Kusto remains intentionally incomplete.
- Manual SQL terminal publication can ignore a `false` panel-transport result, and webview SQL settlement precedes artifact publication. A complete fix requires one application-acknowledged terminal contract shared by manual and Copilot producers; transport rejection alone does not define whether to retry, cancel, retain the owner, or restore an artifact.
- Kusto and SQL retain transport-specific execution coordinators over the shared low-level run registry; a transport-neutral artifact publication contract has not yet proven which semantics should be shared above transport.

**Failure modes:**

- A future row consumer can regress if it bypasses artifact binding, declared-column projection, generation liveness, or synchronous revocation contracts.
- SQL restoration and persistence can preserve rows without the same first-class immutable producer, target, principal, policy, and lineage record as Kusto.
- Prematurely merging Kusto and SQL execution abstractions could erase transport-specific cancellation and privacy semantics before the artifact contract is proven.

**Migration theme:** EXA-2 is closed. Future transport-neutral coordinator work requires a separately selected iteration and must preserve the proven transport-specific cancellation and privacy contracts.

### `DOC` - Document Actor And Section Registry

**Golden outcome:** one serial document actor owns application content revisions, ordered domain state, and section commands while a VS Code `CustomDocument` adapter owns native dirty/undo/save/revert/backup/close integration. Each section kind has one definition for parse, migration, serialization, validation, dependencies, and policy. Views do not serialize the DOM.

**Current divergence:**

- Native Markdown, URL, Python, Chart, Transformation, HTML, and hidden Development Notes persisted state are migrated: one host aggregate, one command client, one URI queue, and per-kind definitions own ordered state and revisions; all six visual Lit components are views, Development Notes have no DOM component, and native Save consults only acknowledged host projections.
- Native Development Notes add, supersede, and remove derive from the optimistic full projection and settle from the exact command entry. A later rejected command cannot retroactively fail an earlier committed note. Sidecar-enabled KQL/SQL companions retain one explicit metadata-free adapter; plain compatibility and read-only hosts remain rejected.
- [`persistence.ts`](src/webview/core/persistence.ts) imports section construction/removal and centrally hydrates every section type.
- [`section-factory.ts`](src/webview/core/section-factory.ts) imports persistence, creating a cycle between mutation and storage.
- Creation, restoration, removal, tool configuration, dependency refresh, and privacy sanitation for Kusto/SQL and remaining adapter-heavy paths are distributed across large switches and per-type arrays.
- Unmigrated components still supply serialization without being canonical domain state. Restore behavior still knows component-private methods and timing.
- Adding a section requires edits across format, component, factory, persistence, tool removal, startup imports, and privacy handling.

**Migration theme:** DOC-1 through DOC-7 prove the transport-neutral aggregate/definition pattern with Markdown, URL, Python, Chart, Transformation, HTML, and Development Notes while preserving runtime-heavy and compatibility adapters. Kusto and SQL serializers still combine primary text, target hints, presentation, execution settings, persisted artifacts, privacy identity, and comparison metadata; migrating only configuration would create dual authority, while migrating either complete record is not yet a narrow reversible iteration. No further DOC slice is selected.

### `PRO` - Protocol, View Sessions, And Startup

**Golden outcome:** one runtime-validated protocol, explicit view-session identity, capability negotiation, plugin-ready handshake, one initial projection, and revisioned deltas.

**Current divergence:**

- `documentViewProtocol.ts` now owns runtime schemas and discriminated types for native `documentData`, `documentReloadResult`, host-owned section commands/results, and Save barriers. `KqlxEditorProvider` stamps one host-created UUID per panel; both directions reject malformed or cross-session envelopes before side effects, and the webview applies the initial projection exactly once.
- `MainWebviewStartupGateway` now owns listener-first installation, explicit dispatcher readiness, ordered inbound/outbound startup buffering, correlated waiter reentrancy, and retirement for native KQLX plus KQL and SQL compatibility panels. Embedded tutorial traffic uses the same transport. `index.ts` starts the dispatcher explicitly after registration; readiness is consumed as control traffic before domain routing.
- `compatibilityPersistenceProtocol.ts` owns strict KQL/SQL request/projection, persistence, final-persist, acknowledgement, and reload schemas. Providers create one UUID per panel, validate at gateway admission, and stamp outbound lifecycle traffic. The webview uses bounded request/replay ledgers, rejects stale generations, re-acknowledges identical retries without rematerializing, and resends newer local state.
- `kustoSchemaProtocol.ts` owns exact runtime schemas and discriminated types for Kusto `prefetchSchema`, `requestCrossClusterSchema`, `schemaData`, `schemaError`, `crossClusterSchemaData`, and `crossClusterSchemaError`. Requests validate before webview transport and again at the host handler; `SchemaService` publishes only the typed delivery union; recognized host deliveries validate at dispatcher entry before runtime gates, tracing, primary/synthetic/legacy routing, catalogs, supplemental coordination, or worker effects. Raw schemas remain opaque and every existing inner identity remains authoritative.
- `kustoDatabaseDiscoveryProtocol.ts` owns exact runtime schemas and discriminated types for Kusto `getDatabases`, `refreshDatabases`, `databasesData`, and `databasesError`. Requests validate before webview transport and again at the browsing handler; `ConnectionService` publishes only the typed delivery union; recognized host deliveries validate before dispatcher or bundled waiter effects. Blank, partial, tokenless, synthetic, account-partition, provenance, and dense-empty compatibility remains with existing owners.
- `sqlDatabaseDiscoveryProtocol.ts` owns exact runtime schemas and discriminated types for SQL `getSqlDatabases`, `refreshSqlDatabases`, `sqlDatabasesLoading`, `sqlDatabasesData`, and `sqlDatabasesError`. Requests validate before webview transport and again at the discovery handler; the handler publishes only the typed delivery union; recognized host deliveries validate before dispatcher or direct-router request-ledger and UI effects. Exact identity and dense empty arrays remain unchanged while array containers, sparse or inherited-index arrays, and non-string entries fail closed.
- `sqlSchemaProtocol.ts` owns exact runtime schemas and discriminated types for SQL `prefetchSqlSchema` and `sqlSchemaData`. Requests validate before webview transport and again at the schema handler; the handler publishes only the typed delivery; recognized host deliveries validate before dispatcher or direct-router catalog and schema-info effects. Complete identity, strict success/error metadata, dense table/view arrays, nested string column maps, stored procedures, valid empty errors, and exact object identity remain compatible while malformed containers and mixed metadata fail closed.
- `kqlLanguageProtocol.ts` owns exact runtime schemas and discriminated types for `kqlLanguageRequest` and `kqlLanguageResponse`. Requests validate before transport and host analysis; handler delivery is typed; recognized responses validate exact IDs, branch shape, dense diagnostics/references, and pending method before resolver settlement. Analysis, the 1,500 ms deadline, and Fully Qualify range admission remain independent inner owners.
- `controlCommandSyntaxProtocol.ts` owns exact runtime schemas and discriminated types for `fetchControlCommandSyntax` and `controlCommandSyntaxResult`. Requests validate before pending-ledger insertion, transport, and host cache/lookup effects; handler delivery is typed; recognized results validate exact values, dense string arguments, and canonical failures before exact `(commandLower, requestId)` settlement. Learn acquisition, the 24-hour cache, caret-doc deduplication/presentation, valid empty syntax, and no timeout remain independent owners.
- `resourceUriProtocol.ts` owns exact runtime schemas and discriminated types for `resolveResourceUri` and `resolveResourceUriResult` across main Query Editor and plain `.md` compatibility. Requests validate before transport and host path/stat/cache effects; both webviews validate results before resolver lookup, deletion, timer cancellation, promise settlement, or rendering. Exact valid strings, optional/empty `baseUri`, empty paths, both host algorithms, two-second deadlines, stale/late handling, and browser no-op behavior remain independent owners.
- `urlContentProtocol.ts` owns exact runtime schemas and discriminated types for `fetchUrl`, `urlContent`, and `urlError`. Requests atomically classify their discriminator and snapshot own enumerable data fields before E2E capture/transport and again before host trimming or fetch effects; handler delivery is typed. The central dispatcher and independent URL component listener perform the same atomic admission before correlation, request retirement, state mutation, artifact revocation/publication, or rendering. Required scalar identity, 2xx successes, empty text bodies, exclusive image/text branches, canonical errors, and hostile callable/prototype/proxy/accessor traffic fail closed without invoking getters.
- `pythonExecutionProtocol.ts` owns exact runtime schemas and discriminated types for `executePython`, `pythonResult`, and `pythonError`. The live component and outbound wrapper atomically classify and snapshot before reservation, running state, E2E capture, or transport; the host repeats admission before working-directory/process effects and publishes typed terminals. Dispatcher and leaf parsing precede reservation consumption, state, persistence, and rendering. Exact strings, empty code/output, safe-integer-or-null exit codes, exclusive terminal branches, independent 200 KB UTF-8 caps, and hostile callable/prototype/proxy/accessor traffic fail closed without invoking getters.
- `artifactCsvSaveProtocol.ts` owns exact runtime schemas and discriminated types for `requestArtifactCsvSave`, `artifactCsvSaveData`, `cancelArtifactCsvSaveIntent`, `requestArtifactCsvSaveData`, and `cancelArtifactCsvSave`. The outbound wrapper, startup gateway, host handler, central dispatcher, webview leaf, and bundled browser shim atomically classify and snapshot before E2E/transport, queue/reentrancy, picker/intent/nonce/tombstone/file, pending-export/timer/artifact-binding, response, or download effects. Exact export, nonce, box, and artifact identities remain unnormalized; malformed, padded, mismatched, callable, prototype, proxy, and accessor traffic cannot consume current work.
- `querySharingProtocol.ts` owns exact runtime schemas and discriminated types for `copyAdeLink`, `shareToClipboard`, and `shareContentReady`. Requests descriptor-capture before E2E/transport and host lookup/formatting; handler delivery is typed and revalidated; the dispatcher admits before rich or plain clipboard effects. Exact valid scalars, dense string columns/rows, empty-content UX, ADX encoding, Kusto/SQL formatting, row caps, artifact policy, disposal, and browser behavior remain unchanged. Host `showInfo` duplication is retired while the native notification remains.
- `editingPreferences.ts` owns exact runtime schemas and discriminated types for the three editing-preference setters and `editingPreferencesData`. Requests descriptor-capture before E2E/transport and configuration writes; host snapshots and all standalone publication paths validate to frozen values; dispatcher and leaf admission precede revision or UI effects. Exact booleans, safe revisions, equal replay, MAX saturation, first-launch/configuration refresh, optimistic controls, and PRO-17 embedded fields retain their existing owners and behavior.
- `copilotInlineCompletionProtocol.ts` owns exact runtime schemas and discriminated types for `requestCopilotInlineCompletion` and `copilotInlineCompletionResult`. Requests descriptor-capture before E2E/transport and HST-24 SQL/model effects; both host producers publish typed revalidated results; dispatcher and direct SQL routing admit before owner/resolver/timer/UI effects. Exact strings, optional fields, dense empty/populated completions, stale/duplicate behavior, and the eight/ten-second timing owners remain unchanged while hostile descriptor/prototype/proxy traffic fails closed.
- `kustoPublicationProtocol.ts` owns exact runtime schemas and discriminated types for `kustoPublicationStage`, `kustoPublicationAck`, `kustoPublicationCommit`, and `kustoPublicationRevoke` across Query Editor, Connection Manager, and Cached Values. Host senders capture before transport, host receivers admit before exact waiter/deadline settlement, and webviews admit before stage, timer, payload, revoke, completed-ledger, or acknowledgement effects. The main envelope contributes one original descriptor snapshot so custom prototypes and hidden/accessor fields cannot be normalized into valid traffic. Staged payloads remain opaque outer records and every existing nested terminal/projection/search/schema/snapshot/privacy validator retains authority.
- `developmentNoteMutationProtocol.ts` owns exact descriptor-safe add/supersede/remove `updateDevNotes` requests and only their correlated `toolResponse` subset. Copilot and agent producers admit before transport; capture-first main-webview ingress retains the original descriptor snapshot; one frozen complete response admission is shared across startup reentrancy, provider routing, and both correlation owners before native/companion mutation, generic routing, waiter lookup, timer cancellation, or settlement. Generic tool responses remain outside the subset.
- `toolStateSnapshotProtocol.ts` owns exact descriptor-safe `requestToolState` and `toolStateResponse` construction/admission. It captures bounded immutable Kusto/SQL section snapshots, rejects malformed or oversized matching traffic before startup reentrancy, waiter lookup, timer cancellation, SQL comparison-owner reconciliation, or settlement, and preserves HST-30 purpose/target/schema behavior plus direct provider forwarding.
- `kustoExecutionStartProtocol.ts` owns exact descriptor-safe `kustoExecutionStarted` and `kustoExecutionStartedAck` construction/admission. It accepts only Copilot starts without comparison lineage or lineage-complete comparison starts, validates before host waiter/timer/transport and startup/standalone routing, validates before webview section/predecessor/comparison-binding effects, constructs typed acknowledgements, and validates before host ledger lookup or timer cancellation. Tentative comparison rebinds retain and restore the prior artifact transactionally when the exact section claim rejects.
- `powerBiPublishProtocol.ts` owns exact descriptor-safe `publishToPowerBIResult` and `publishToPowerBIAck` construction/admission. Host results validate before publication; startup and standalone paths consume the original descriptor snapshot before reentrancy/routing or transport; HTML section and dialog admission precede correlation, metadata, document, persistence, and dialog effects; acknowledgements use a scalar constructor and validate before outbound transport or HST-1 lease/timer/cleanup effects. Exact success/failure branches and optional DirectQuery refresh/workspace fields remain compatible.
- SQL comparison admission has no shared runtime owner. Matching non-boolean acknowledgements can consume the current HST-35 phase waiter as rejection, malformed host decisions can roll back a provisional comparison, and trimmed phase/proof lookups can alias padded identities. Temporary discriminating probes proved both traffic directions, then disproved a protocol-only boundary: `parseKqlxText()` validates trimmed IDs while preserving the original persisted strings, and SQL registries, lifecycle, execution, artifacts, routing, and restore paths normalize those identities independently. A future slice must first establish one canonical SQL section-identity contract and migration boundary across those owners.
- `sqlStsEditorLanguageProtocol.ts` owns exact runtime schemas and discriminated types for `stsRequest`, `stsDidOpen`, `stsDidChange`, `stsDidClose`, `stsConnect`, `stsResponse`, `stsDiagnostics`, and `stsConnectionState`. Local request/document/connect construction, outbound transport, host lifecycle admission, typed coordinator delivery, central dispatch, and direct SQL routing all validate before reservation, lifecycle, resolver, diagnostics, readiness, owner, or UI effects. Response results and marker records remain opaque; dense marker containers and complete owner/expected-owner identity fail closed without consuming current work.
- `sqlConnectionsProjectionProtocol.ts` owns exact runtime schemas and discriminated types for `getSqlConnections` and `sqlConnectionsData`. Requests validate and descriptor-capture before E2E/transport and again before HST-38 effects; handler publication is typed and validated. Host deliveries validate and descriptor-capture before revision or any SQL projection effect. Revisioned snapshots are complete, unrevisioned legacy snapshots remain intentionally partial, prototype-sensitive IDs are ordinary own keys, and the watermark commits only after complete application.
- Existing source generations, command IDs, revisions, per-URI queues, Save leases, sidecar sessions, and close coordination remain inner owners. Markdown compatibility and browser/legacy behavior is unchanged.
- `queryEditor.js` now owns only the preload Add count buffer. `installRuntimeAddSectionBridges()` synchronously merges supported counts into `pState.queryEditorPendingAdds`, clears the preload object, and replaces every stub in one call stack. The capability-aware finalizer drains that sole runtime queue once per applied projection before creating a document default.
- Remaining unrelated host/webview domains still use duplicated unions, `unknown` host output, and `any` dispatcher input. Their runtime contracts require separately selected bounded slices.
- [`message-handler.ts`](src/webview/core/message-handler.ts) is a central switch while some temporary and component listeners observe the same transport independently.
- [`window-bridges.d.ts`](src/webview/window-bridges.d.ts) remains a large ambient contract, and state is mirrored between module bindings and `window`.
- Browser startup implements a different buffering/acknowledgement protocol.

**Migration theme:** PRO-1 through PRO-25 close one complete protocol domain at a time, most recently the bounded Power BI publish application handshake across result construction/publication, startup and standalone transport, HTML/dialog effects, typed acknowledgement transport, and HST-1 lease settlement. The shallow runtime envelope capture remains a one-shot descriptor source rather than a schema claim for unrelated payloads. SQL comparison admission now has concrete runtime reds, but a local protocol would promise identity semantics that the document, registry, lifecycle, execution, artifact, routing, and restore owners do not yet share. No PRO-26 is selected until that canonical identity boundary is specified. Do not turn the proven narrow contracts into a global rewrite. Ambient bridges are removed only after their owning contract exists.

### `HST` - Host Application Composition

**Golden outcome:** the VS Code provider is a transport/composition adapter. Application workflows live in use-case handlers and actors.

**Current divergence:**

- HST-1 moved dashboard prompts, export, workspace/existence lookup, publish request lifetimes, first-external-commit admission, document metadata application/compensation leases, and cleanup into one injected `HostDashboardApplicationHandler`. The provider has no dashboard workflow maps, Fabric/export imports, discriminator switch, or publish transition authority.
- HST-2 moved governed result-table CSV picker admission, one-use nonce challenges, cancellation, deadlines, replay tombstones, exact correlation, and file publication into one injected `HostArtifactCsvSaveApplicationHandler`. The provider has no artifact CSV maps, discriminator cases, deadlines, tombstones, or write transitions.
- HST-3 moved Python interpreter fallback, process/stdin/stdout/stderr lifecycle, independent 200 KB UTF-8 output caps, 15-second timeout/kill behavior, exactly-once terminal publication, and disposal into one injected `HostPythonExecutionApplicationHandler`. The provider has no Python process creation, discriminator case, output accumulation, timeout transition, or terminal construction.
- HST-4 moved imported URL-section CSV empty-data UX, native picker admission, URI-preserving extension handling, exact UTF-8 publication, saved-file actions, failures, and disposal into one injected `HostImportedCsvSaveApplicationHandler`. The provider has no imported CSV picker/write/notification code or discriminator case.
- HST-5 moved Kusto ADX-link generation, Kusto/SQL rich and plain-text share formatting, host clipboard effects, `shareContentReady`, notifications, and disposal into one injected `HostQuerySharingApplicationHandler`. The provider has no sharing discriminator cases, zlib/link generation, formatting, clipboard effects, or sharing publication.
- HST-6 moved URL validation, redirect-following fetch, timeout/abort, text/image byte caps, truncation, content classification/sniffing, response shaping, and disposal into one injected `HostUrlContentApplicationHandler`. The provider has no URL acquisition discriminator case, fetch/abort transition, cap/classification decision, or URL terminal construction.
- HST-7 moved the 24-hour control-command syntax cache, Microsoft Learn URL normalization/fetch, HTML/entity syntax extraction, `with(...)` argument parsing, failure shaping, `controlCommandSyntaxResult` publication, and disposal into one injected `HostControlCommandSyntaxApplicationHandler`. The provider has no syntax cache, parsing helper, Learn fetch, discriminator case, or syntax-result construction.
- HST-8 moved local resource request shaping, passthrough/local-base decisions, Windows/POSIX/workspace path normalization, stat and exact base/path caching, webview URI conversion, failure shaping, `resolveResourceUriResult` publication, and disposal into one injected `HostResourceUriApplicationHandler`. The provider has no resource cache, path import/normalization, stat, discriminator case, failure strings, or result construction.
- HST-9 moved exact `openToolResultInEditor` and `openMarkdownPreview` native presentation effects, content coercion, untitled plaintext/beside display, local Markdown preview dispatch, failure notifications, and disposal into one injected `HostCopilotContentOpenApplicationHandler`. The provider has neither discriminator case, method, nor native content-open decision.
- HST-10 moved exact `showInfo` native information-message invocation, fire-and-forget return handling, synchronous failure propagation, and disposal into one injected `HostInformationNotificationApplicationHandler`. The provider has neither the discriminator case nor a direct information-notification call.
- HST-11 moved exact awaited `kusto.seeCachedValues` command dispatch, resolved-value discard, exact rejection, response-free behavior, and disposal into one injected `HostCachedValuesOpenApplicationHandler`. The provider has neither the discriminator case nor knowledge of the command ID; first-launch registration and viewer lifecycle remain with `extension.ts` and `CachedValuesViewerV2`.
- HST-12 moved exact awaited zero-argument `openKustoWorkbenchAgentChat()` delegation, resolved-boolean discard, exact rejection, response-free behavior, and disposal into one injected `HostCopilotAgentOpenApplicationHandler`. The provider has neither the `openCopilotAgent` discriminator case nor a shared-helper import; helper behavior and extension/dashboard/Copilot callers remain unchanged.
- HST-13 moved `editorCursorPositionChanged`, development-only `getEditorCursorStatusSnapshot`, panel-hidden clearing, panel-disposal clearing, provider-scoped owner IDs, and snapshot failure containment into one injected `HostEditorCursorStatusApplicationHandler`. The provider has no cursor discriminator cases, owner-prefix state, status update, snapshot shaping, or prefix-clear decisions; `EditorCursorStatusBar` retains shared item/format/accessibility/stale-owner/global-disposal authority.
- HST-14 moved `setCaretDocsEnabled`, `setAutoTriggerAutocompleteEnabled`, `setCopilotInlineCompletionsEnabled`, exact `STORAGE_KEYS` mapping, the then-current truthiness handling, revisioned mutation delegation, and global-versus-current-panel publication into one injected `HostEditingPreferencesApplicationHandler`. PRO-19 later replaced that coercive boundary with shared exact-boolean admission. The provider has no preference discriminator cases, storage-key mapping, mutation call, or publication decision; `editingPreferences.ts` and `extension.ts` retain persistence/revision/migration and configuration-listener ownership, while HST-40 owns the initial `connectionsData` read.
- HST-15 moved `addConnectionsForClusters`, `promptImportConnectionsXml`, `importConnectionsFromXml`, exact discovered/XML intake semantics, native XML selection/read, import notifications, and post-mutation refresh ordering into one injected `HostKustoConnectionIntakeApplicationHandler`. The provider has none of the three cases or intake decisions, and `ConnectionService` has none of the three methods. `ConnectionManager` retains persistence, HST-40 owns the injected revisioned `connectionsData` refresh effect, and webview XML parsing/message shapes remain unchanged.
- HST-16 moved `promptAddConnection`, `addConnection`, and `testKustoConnection`, their validation/persistence/selection/refresh ordering, account-aware draft testing, database normalization, tracing, and exact terminals into one injected `HostKustoConnectionOnboardingApplicationHandler`. The provider has none of the three cases and `ConnectionService` has none of the three displaced methods. `ConnectionManager`, `KustoAuthPreferenceService`, `KustoQueryClient`, selection storage, HST-40 revisioned `connectionsData`, and webview controls/message shapes remain unchanged owners.
- HST-17 moved `promptAddSqlConnection`, `addSqlConnectionFromWebview`, exact native prompt/cancellation behavior, credential-bearing manager input, `sql.lastConnectionId` ordering, `sqlConnectionAdded` construction, and disposal into one injected `HostSqlConnectionOnboardingApplicationHandler`. The provider has neither case nor method. `SqlConnectionManager` retains persistent connection/secret ownership; HST-38's projection handler retains policy/principal-aware `sqlConnectionsData`; SQL workbench/lifecycle services, controls, response application, and message shapes remain unchanged owners.
- HST-18 moved `requestAddSqlFavorite`, `removeSqlFavorite`, exact naming UX, sanitized keying/replacement/removal, `sql.favorites` persistence, `sqlFavoritesData` publication and warning containment, projection reads, and disposal into one injected `HostSqlFavoritesApplicationHandler`. The provider has neither case and `ConnectionService` has none of the displaced SQL-favorites methods. HST-38's projection handler consumes the sanitized favorites capability; Connection Manager SQL-favorites routes, controls, response application, emitters, and message shapes remain unchanged owners.
- HST-19 moved `requestAddFavorite`, `removeFavorite`, `confirmRemoveFavorite`, Kusto favorite migration/filtering/mutation/persistence, the live-handler/listener registry, exact confirmation, projection reads, activation, and disposal into one injected `HostKustoFavoritesApplicationHandler`. The provider has none of the three cases and `ConnectionService` has none of the displaced Kusto-favorites state or methods. HST-40 `connectionsData` and unchanged schema inference consume the handler's read-only projection; Connection Manager routes, controls, response application, emitters, and message shapes remain unchanged owners.
- HST-20 moved `getSqlDatabases`, `refreshSqlDatabases`, synchronous target adoption, database acquisition/publication, discovery-specific owner protection, cache orchestration, notifications, logging, and disposal into one injected `HostSqlDatabaseDiscoveryApplicationHandler`. The provider has neither case nor discovery method/helper. `SqlEditorLifecycleCoordinator` retains monotonic tickets/currentness/exact completion, `SqlWorkbenchService` retains canonical principal/privacy admission, `sqlDatabaseCache.ts` retains cache ownership/CAS, and SQL connection/client/STS adapters retain physical acquisition. Connection snapshots, favorites, schema, execution, comparisons, persistence, controls, emitters, response routing, and message shapes remain unchanged owners.
- HST-21 moved `kqlLanguageRequest`, both `KqlLanguageServiceHost` calls, response construction, logging, and disposal into one injected `HostKqlLanguageRequestApplicationHandler`. The provider has no language-host import/field, discriminator case, request method, terminal construction, or request logging. PRO-9 later made request admission and response delivery runtime-validated and fail-closed while preserving `KqlLanguageServiceHost`, `KqlLanguageService`, independent text-editor diagnostics, and the webview timeout/resolver ledger.
- HST-22 moved `saveSqlLastSelection`, trimmed connection admission, ordered `sql.lastConnectionId` then optional `sql.lastDatabase` writes, exact undefined/empty database behavior, rejection, and disposal into one injected `HostSqlLastSelectionApplicationHandler`. The provider has no discriminator case or direct selection writes. `SqlSectionSessionController` and SQL onboarding retain writes; HST-38's projection handler retains the two `sqlConnectionsData` reads; SQL lifecycle and message shapes remain unchanged owners.
- HST-23 moved `webviewMutationResponseResolvers`, development-note request IDs, the five-second timeout, outbound delivery failures, exact matching `toolResponse` settlement, and disposal cleanup into one injected `HostDevelopmentNoteMutationApplicationHandler`. The provider retains only a thin `CopilotServiceHost` delegate and unclaimed `toolResponse` fallthrough. `CopilotService`, webview mutation/persistence, agent tools, document ownership, execution, and message shapes remain unchanged owners.
- HST-24 moved exact `requestCopilotInlineCompletion` route admission, Kusto/SQL flavor selection, SQL owner-token assertion, exact issued-owner/token delegation, the existing empty SQL fallback, and disposal into one injected `HostCopilotInlineCompletionApplicationHandler`. The provider has no discriminator case, flavor branch, direct owner assertion, direct Copilot completion invocation, or fallback construction. `CopilotService`, SQL lifecycle/workbench, webview resolver/spinner/application, editing preferences, emitters, and message shapes remain unchanged owners.
- HST-25 moved exact `checkCopilotAvailability` route admission, awaited delegation, exact rejection, and disposal into one injected `HostCopilotAvailabilityApplicationHandler`. The provider has no discriminator case, direct availability invocation, model-discovery decision, or availability publication. `CopilotService`, global/Kusto/SQL callers, response application, and message shapes remain unchanged owners.
- HST-26 moved exact Kusto/SQL `prepareCopilotWriteQuery` route admission, awaited exact-object delegation, exact rejection, and disposal into one injected `HostCopilotWriteQueryPreparationApplicationHandler`. The provider has no discriminator case, direct preparation invocation, model/tool decision, or options/status publication. `CopilotService`, Kusto/SQL chat-manager callers, response application, and message shapes remain unchanged owners.
- HST-27 moved exact Kusto/SQL `clearCopilotConversation` route admission, flavor branching, complete Kusto request-identity validation, awaited delegation, exact rejection, and disposal into one injected `HostCopilotConversationClearApplicationHandler`. The provider has no discriminator case, flavor/identity branch, or direct clear invocation. `CopilotService`, Kusto/SQL chat-manager callers, conversation creation/history, start/cancel execution, response routing, and message shapes remain unchanged owners.
- HST-28 moved exact Kusto/SQL-originated `removeFromCopilotHistory` route admission, awaited box/entry delegation, exact rejection, and disposal into one injected `HostCopilotHistoryRemovalApplicationHandler`. The provider has no discriminator case or direct history-removal invocation. `CopilotService`, `kw-copilot-chat`, the shared Kusto/SQL chat manager, conversation workflows, response routing, and message shapes remain unchanged owners.
- HST-29 moved the complete `copilotChatFirstTimeCheck` transaction, including state read/write ordering, exact modal UX, Agent Chat delegation, three result outcomes, current transport settlement, and disposal into one injected `HostCopilotChatFirstTimeApplicationHandler`. The provider has no discriminator case or workflow decisions, and `CopilotService` has neither the complete method nor the Agent Chat helper import. `STORAGE_KEYS`, HST-40 initial projection, shared Kusto/SQL caller, webview result application, helper behavior, conversations, execution, optimization, and message shapes remain unchanged owners.
- HST-30 moved the complete panel tool session into one injected `HostWorkbenchToolSessionApplicationHandler`: orchestrator token/connect/reactivate/disconnect, the five-second state resolver ledger, request publication and settlement, schema-refresh section filtering and exact Kusto resolution, SQL callbacks, and all three tool routes. The provider has no tool token, resolver map, lifecycle helpers, schema target projection, SQL callbacks, or tool discriminator cases; its state request is a thin delegate, persistence calls SQL lifecycle directly, and HST-23 retains matching-response priority.
- HST-31 moved the complete Kusto connection list-to-browse-to-refresh-to-select route surface into one injected `HostKustoConnectionBrowsingApplicationHandler`: exact `getConnections`, `getDatabases`, `refreshDatabases`, and `saveLastSelection` admission; passive/interactive request shaping; blank selection behavior; ordered persistence; and contained post-selection diagnostics refresh. The provider has none of the four cases or direct route effects. HST-40 `connectionsData`, `ConnectionService`, diagnostics registration, and plain-file pre-forward cache/inference remain separate canonical owners.
- HST-32 retired the unused comparison-summary host bridge instead of extracting dead state: provider maps, waiter, routes, Copilot API/calls, SQL lifecycle callback/calls, webview sends, and both protocol types are gone. Immutable artifact-backed local rendering, diff links, banner teardown, Kusto/SQL owner cancellation, execution, lineage, Copilot narrative summaries, persistence, and browser behavior remain unchanged owners.
- HST-33 moved the complete five-route Copilot query workflow into one injected `HostCopilotQueryWorkflowApplicationHandler`: exact write-query start/cancel and Optimize prepare/cancel/run admission, SQL preflight reservation and owner-token assertion, cancellation-wins races and exact terminals, complete Kusto cancellation identity, synchronous cancel versus awaited start/prepare/run delegation, lazy SQL adapter resolution, and disposal. The provider has none of the five cases, preflight constant, owner-token branch, terminal construction, or direct delegation. `CopilotService`, `SqlExecutionBroker`, `SqlEditorLifecycleCoordinator`, SQL adapters, callers, response routing, execution, artifacts, persistence, browser behavior, and message shapes remain unchanged owners.
- HST-34 moved the complete seven-route Kusto section execution/publication workflow into one injected `HostKustoSectionExecutionApplicationHandler`: section open/target/close, exact start/publication acknowledgement ledgers and five-second deadlines, manual execution admission, query shaping, selection ordering, physical/account/Leave No Trace admission, exact cancellation, terminal/error/refresh behavior, Copilot close cleanup, late suppression, and disposal. The provider has none of the seven cases, acknowledgement maps, manual execution method, or physical-dispatch decision; only thin `CopilotServiceHost` delegates remain. `KustoExecutionCoordinator`, `KustoQueryClient`, `ConnectionService`, `ConnectionManager`, `CopilotService`, callers, messages, artifacts, persistence, schema, SQL, comparisons, and browser behavior remain unchanged owners.
- HST-35 moved the complete cross-engine comparison preparation transaction into one injected `HostComparisonPreparationApplicationHandler`: exact `sqlComparisonAdmissionAck`, `comparisonBoxEnsured`, and `sqlComparisonRemoved` admission; pending request/Kusto owner maps; the 20-second deadline; SQL staged/committed/finalized/completed/rolledBack correlation; rollback retry; Kusto/SQL target and SQL policy revalidation; removal cancellation; lifecycle rejection; and disposal settlement. The provider has none of the three cases, maps, helpers, or transaction decisions and retains only a thin preparation delegate. `SqlEditorLifecycleCoordinator`, `SqlEditorSessionRegistry`, `SqlExecutionBroker`, `KustoExecutionCoordinator`, `CopilotService`, callers, messages, artifacts, persistence, and browser behavior remain unchanged owners.
- HST-36 moved the complete manual SQL reservation-to-terminal workflow into one injected `HostSqlSectionExecutionApplicationHandler`: exact `executeSqlQuery` and `cancelSqlQuery` admission, synchronous preflight, owner-token/protection and tool-owner validation, query-mode shaping, physical start, ordinary/protected terminals, detail-free protected logging, exact cleanup, and disposal. The provider has neither case, no private execution method or terminal helper; at HST-36 closure it had exactly 13 live discriminator cases. `SqlEditorLifecycleCoordinator`, `SqlEditorSessionRegistry`, `SqlExecutionBroker`, `SqlWorkbenchService`, `SqlQueryClient`, Copilot SQL execution, callers, messages, artifacts, persistence, and browser behavior remain unchanged owners.
- HST-37 moved `prefetchSqlSchema`, synchronous per-section reservation/supersession, lifecycle-owner capture, acquisition, cache/result/error admission, logging, publication, and disposal into `HostSqlSchemaRequestApplicationHandler`. Both webview and lifecycle-triggered refreshes carry exact section instance, target generation, connection, database, and force-refresh identity through that owner. `SqlSchemaService` revalidates request currentness inside disk CAS and before memory/return admission; cache-generation fail-closed cleanup and Cached Values generation/Kusto/SQL/application acknowledgement prevent stale reuse or display.
- HST-38 moved exact `getSqlConnections`, every lifecycle/execution/Copilot refresh trigger, revision/tail sequencing, connection/application-state/cache/favorites reads, canonical policy/principal joining, exact `sqlConnectionsData` construction/publication, and disposal into `HostSqlConnectionsProjectionApplicationHandler`. The provider has no projection state, payload, sender, or discriminator branch. Global SQL policy recovery publishes no favorites, including orphaned entries, and does not read the favorites projection.
- HST-39 moved synchronous, fresh, fail-closed, and lock-held publication sanitation; legacy result removal; Kusto and SQL target/principal/account/revocation admission; comparison and orphaned artifact/fingerprint cleanup; exact Kusto-then-SQL lock ordering; both persistence invalidation events; and disposal into `HostPersistedResultSanitizationApplicationHandler`. The provider preserves the four public sanitation methods and two event properties only as exact delegates for native KQLX and KQL/SQL compatibility callers.
- HST-40 moved every Kusto connection snapshot trigger, revision/tail sequencing, editing-preference and first-time-state reads, account enumeration, physical connection identity/incarnation projection, selection/cache/favorites reads, canonical policy-lock admission, exact `connectionsData` construction, correlated retry, application-acknowledged settlement, development isolation, and disposal into `HostKustoConnectionsProjectionApplicationHandler`. HST-31 remains the exact `getConnections` route owner; `ConnectionService.sendConnectionsData()` and provider projection state are deleted.
- HST-41 moved exact `sqlSectionOpen`, `retireSqlTarget`, both development-auth routes, and all five STS request/document/connect routes into `HostSqlEditorLifecycleApplicationHandler`. Production gating and account-map-before-token ordering remain exact, while `SqlEditorLifecycleCoordinator` and existing auth helpers retain every algorithm.
- HST-42 moved exact `prefetchSchema` and `requestCrossClusterSchema` admission, argument shaping, settlement, and disposal into `HostKustoSchemaRequestApplicationHandler`. `SchemaService` retains schema/cache/request/physical-owner/publication algorithms and all message construction.
- [`QueryEditorProvider`](src/host/queryEditorProvider.ts) now has zero direct application switch cases. It composes handlers and service capabilities, reference-identically offers messages, awaits claimed work, and retains intentional transport plus external delegate surfaces.

**Migration theme:** HST is closed through HST-42. Future provider work belongs to a separately selected protocol, document, or compatibility boundary rather than another host-application switch extraction.

### `BRW` - Browser Composition Root

**Golden outcome:** browser rendering uses shared codecs, projections, and section views with an explicit read-only capability set.

**Status:** closed through BRW-1.

**Current alignment:**

- `BrowserViewerRoot` consumes the immutable `LoadedBrowserFile` snapshot, rejects stale and duplicate load generations, delegates native and companion parsing to the shared codecs and native validation, and publishes one deep-frozen typed projection.
- Browser presentation defaults are applied only to a cloned state. The parsed document preserves unknown root/state fields, known-section extensions, opaque sections, and order.
- `browserViewerProjection.ts` runtime-validates explicit host capabilities: read and admitted derived download are available; editing, persistence, authentication, Kusto/SQL execution, and Copilot are absent. Authored HTML and script behavior is trusted and unchanged.
- `browser-viewer-presentation.ts` restores through the existing section and immutable-artifact paths without entering the main VS Code dispatcher. Restored rows gain only CSV export permission.
- `viewer-boot.js` owns loading/banner/error/height presentation only. Synthetic `persistenceMode`, `connectionsData`, `copilotAvailability`, and `documentData` replay, persistence-global patching, and delayed read-only repair are gone.
- Shared section chrome and Monaco writability honor the read-only capability. URL sources render statically without host fetch, Python has no browser execution shortcut/admission, and browser CSV nonce/replay/download behavior remains adapter-owned.
- Inline and standalone payloads carry the content-script load generation. Duplicate standalone retries acknowledge without replaying or changing accepted UI; stale deliveries neither acknowledge nor alter presentation.

**Remaining divergence:**

- `BrowserViewerRoot` records a generation before its one-way `present()` callback is asynchronously acknowledged. `viewer-boot.js` owns the later applied event and parent load acknowledgement, so a failed presentation can leave the generation permanently duplicate while standalone retries stop. A future slice must first define one pending/adopted generation owner, failure retry, stale-newer ordering, and parent acknowledgement semantics across root, boot, inline, and standalone paths.
- The browser presentation adapter still composes the shared legacy section runtime and a narrow browser event handshake rather than a fully host-neutral plugin projection port.
- `vscode-shim.js` remains a browser CSV adapter, although it no longer impersonates unsupported host responses.

**Migration theme:** BRW-1 is closed. Do not fork section implementations without a separately selected slice.

### `CMP` - Compatibility Provider Composition

**Golden outcome:** KQL, SQL, and Markdown compatibility providers are thin primary-text and UX adapters around one document/session composition.

**Current divergence:**

- Shared sidecar format/store/session mechanics, projection/reload coordination, and panel close orchestration are strong.
- `CompatSidecarCloseCoordinator` now solely owns KQL/SQL retained close admission, exactly-once startup, gateway/session/store ordering, Save/Discard and exact recovery, disposal, and settlement. Providers inject only language-specific effects.
- `CompatSidecarProjectionCoordinator` now solely owns projection generation reservation, generation-zero baseline compatibility, active/pending source identity, same-source pending supersession, bounded initial recovery/coalescing, reload acknowledgement currentness, persist-generation admission, source-reload authority, and rollback-failure fencing. Providers inject projection construction and physical source effects.
- `CompatSidecarPersistCoordinator` now solely owns the complete post-protocol `persistDocument` application transaction: final-request admission, every terminal, two-phase upgrade validation, session/projection admission, revision timing, sanitation/materialization, source verification and projection-owned rollback, retryable drafts, state/materialized commits, one live acknowledgement attempt, beforeunload, and exactly-once final settlement. Providers expose typed physical effects and contain no direct queue, generation admission, rollback, acknowledgement, or final-waiter sites.
- `compatibilityPersistenceProtocol.ts` now runtime-validates and session-fences the duplicated KQL/SQL lifecycle routes before effects.
- Provider shells still duplicate native `onWillSaveTextDocument` / `onDidSaveTextDocument` wiring and physical sidecar publication, while a primary-save/sidecar partial failure has no durable recovery record. A future CMP slice must own the complete Save/partial-failure transition rather than extract event callbacks alone.

**Migration theme:** CMP-1 through CMP-3 close the bounded close, projection/reload, and persist-snapshot application transitions. Preserve those owners. Residual Save/partial-failure coordination scores below PRO-21 and is not ready until a durable recovery-record schema, restart admission, repair action, and close-recovery interaction are defined. Do not extract callback shells, merge providers, or move language-specific primary state, KQL inference, SQL lifecycle, storage/CAS/recovery, Save, or close behavior without that complete contract and a separately selected red test.

### `DSH` - Dashboard Compiler Boundary

**Golden outcome:** one portable dashboard specification and compiler IR drives shared bindings in preview and standalone HTML plus PBIR/TMDL/DAX and publishing adapters. An active-content source extension preserves trusted preview and raw standalone HTML behavior that is not portable to Power BI.

**Current alignment:**

- `portableDashboardCompiler.ts` is the sole pure owner for provenance v1 parsing, browser-created target discovery, replacement-stability admission, scalar/table/repeated-table/pivot/chart/tooltip validation, source-schema admission, typed diagnostics, and normalized portable IR. It uses `parse5` tree construction, exact source ranges, full-tree masked equivalence, and parser-state guards rather than textual target matching.
- `htmlDashboardUpgrade.ts` is a compatibility-notice adapter over the compiler. Preview scalar/chart/table/repeated-table bindings and slicers consume admitted provenance; `bindHtml()` remains explicitly preview-only.
- Agent validation returns compiler diagnostics. Local export and publish preparation throw `PortableDashboardAdmissionError` with the same diagnostics before filesystem, authentication, or Fabric effects. Local export recompiles transformed HTML, precomputes the complete measure before the first write, and rewrites only compiler-owned source ranges; every admitted binding is rewritten exactly once and dynamic text is HTML/XML escaped.
- Malformed binding arrays and source schemas fail closed. The old host missing-column validator, host provenance parser, and shared compatibility target/display interpreter are deleted and blocked by ownership tests.
- `powerBiProjectArtifacts.ts` compiles every `.pbip`/PBIR/TMDL path and byte into one ordinally sorted in-memory manifest after portable admission. It rejects Windows-equivalent file and directory collisions, exposes fresh byte copies, and accepts an explicit identifier source so complete output goldens are deterministic.
- `powerBiProjectWriter.ts` is the sole Power BI project filesystem adapter. Fabric publish converts report/model artifacts directly to sorted inline-base64 definition parts, excludes local `.platform` files, and performs no temporary filesystem read, write, or cleanup.

**Remaining divergence:** none within the selected compiler boundary. Raw standalone HTML remains an intentional trusted active-content export outside portable IR.

**Migration theme:** DSH is closed through DSH-2. Do not conflate the portable project manifest with raw standalone active-content export.

### `KLS` - KQL Analyzer Boundary

**Golden outcome:** language analysis is a replaceable adapter behind a typed request/result contract.

**Status:** closed through KLS-1.

**Current alignment:**

- `sourceAnalysis.ts` owns one immutable exact-layout result for comments/strings, statement and management-command spans, lexical `let`/parameter/tabular scopes, and physical table references.
- `findTableReferences()`, unknown-table diagnostics, schema matching, and Fully Qualify consume that result. The schema-inference scanner and webview qualifier fallback are deleted.
- Fully Qualify validates standalone exact UTF-16 ranges, ordering, uniqueness, and non-overlap before replacing; malformed, empty, or failed responses leave source text unchanged.
- Focused regressions cover aliases, wrappers, functions, scalar-query wrappers, union forms, management commands, lexical shadowing, wildcard/qualified/function exclusions, CRLF offsets, strings/comments, and hostile ranges.

**Remaining divergence:** full semantic column-flow analysis remains a best-effort diagnostics concern in `service.ts`; Monaco-Kusto worker/schema behavior remains a separate mature adapter. Joined or looked-up right-hand dynamic columns currently enter the name set without their dynamic type fact because recursive tabular inference returns only `Set<string>`. A future slice must define one typed recursive column-fact result across table, let, join, lookup, and union inference rather than add a member-access exemption. Neither residual is a second physical-source owner.

## Completed Iteration Detail

All nonzero `QueryEditorProvider` case counts in completed iteration records below describe the provider at that iteration's closure. They are historical evidence, not current requirements; HST-42 establishes the current authoritative count as zero direct application switch cases.

### Iteration `EXA-1`: Exact Kusto Execution Ownership

**Status:** complete and qualified.

**Why now:** query execution is the product's central workflow, and the current Kusto path has a source-confirmed cross-layer contradiction. The webview creates an exact `executionId` and rejects terminals that do not match it, while the host drops that identity when registering the run and publishing success, error, and cancellation. Cancellation also has two logical owners, and downstream data is mutable latest-result state keyed only by section ID. This affects normal runs, rapid reruns, cancellation, comparisons, Copilot/tool execution, derived sections, persistence, and future provider decomposition.

**Falsifiable hypothesis:** if one host-owned coordinator reserves each Kusto execution synchronously, finalizes its actual dispatch identity after authentication, and remains the sole logical terminal owner, then the current run settles exactly once while stale runs, stale cancellations, retargeted results, and account/policy-revoked results cannot render or clear newer state.

**Cheapest discriminating check:** start through the real Kusto execution controller, capture its outbound `executionId`, invoke the registered provider message path, and deliver the resulting terminal through the real webview dispatcher. Before EXA-1 the host terminal omitted that ID; the cross-layer test now proves one identity through request, dispatch, terminal, rendering, persistence admission, and controller cleanup. Native immediate-rerun and retarget scenarios prove stale work cannot settle the current section.

**Scope:**

1. Define a synchronous `KustoExecutionReservation` containing document/view scope where available, section incarnation, target intent/generation, reservation sequence, and `executionId`.
2. Reserve before `saveLastSelection` or any other `await`. Missing connection/database, selection-write failure, synchronous client failure, cancellation, and replacement each settle through one correlated terminal path.
3. Finalize an immutable `KustoDispatchLease` immediately before each physical query dispatch using the actual connection revision, endpoint/authority/database identity, authenticated account partition/principal, and applicable policy revision. Preflight failures use the reservation and do not invent a dispatch principal.
4. Make one coordinator the sole logical terminal owner. The webview enters `cancelling` and waits for the exact host terminal instead of clearing the active ID and rendering cancellation independently. Physical SDK cancellation remains best-effort.
5. Make replacement and explicit cancellation conditional on the exact reservation. Retarget, account/authority change, Leave No Trace change, section removal, reload, and disposal revoke affected reservations or dispatch leases before publication.
6. Echo exact identity in every section-publishing Kusto success, error, cancellation, and supersession terminal. Cover manual/run-function execution, integrated Copilot final execution, performance comparisons, and agent-tool execution. Model-context-only queries may remain separate only when they never publish generic section terminals.
7. Give agent-tool execution immediate request-to-reservation acknowledgement, propagate invocation cancellation to the exact run, and align host/webview terminal deadlines.
8. Add cross-layer and coordinator tests for reversed preflight ordering, rapid replacement, stale cancellation, retarget, account change, policy revocation, success, error, cancellation, disposal, and exactly-once cleanup.
9. Add protocol and architecture guards that reject uncorrelated Kusto section terminals and box-only cancellation paths.

**Non-goals:**

- Do not redesign the result payload or persisted notebook format yet.
- Do not merge Kusto and SQL transport implementations; reuse SQL ownership concepts only where they form a transport-neutral contract.
- Do not redesign Copilot prompts or conversations; only section-publishing execution enters this owner.
- Do not broadly split `QueryEditorProvider`, `message-handler.ts`, or remove window bridges.

**Completion evidence:**

- The real controller-to-provider-to-window-dispatcher test fails before the change and passes after it with one identity throughout.
- Current and stale execution/cancellation/retarget/account/policy cases are deterministic under deliberately reversed ordering.
- Every section-publishing Kusto terminal carries a complete reservation and, when dispatched, its immutable dispatch identity.
- Every admitted reservation receives exactly one logical terminal; cancellation and supersession cannot clear or replace a newer run.
- No result produced under an old target/account/policy renders or persists under the current one.
- Table preview, direct Copilot delivery, and persisted result restoration use the same policy generation/admission boundary; protected transitions clear queued and retained data.
- Comparison retarget clears old rows, persisted JSON, summaries, Optimize, and target-bound conversation state before adopting the new target.
- Copilot, standalone Optimize, and delegated cancellation are exact request owners; delayed cancellation cannot affect a newer same-box request.
- Only absent-to-account establishment is preserved; an automatic A-to-B rotation revokes affected execution and retained state.
- Manual, run-function, Copilot-final, comparison, and agent-tool paths use the same owner when they publish into a section.
- Focused host, coordinator, dispatcher, protocol, Copilot/tool, and query-section tests pass.
- Native run, cancel-and-rerun, and retarget-during-run E2E scenarios pass.
- Full sequential tests, production package, browser build, and bundle gates pass.
- Displaced box-only cancellation and uncorrelated terminal paths are deleted or blocked by architecture tests.

**Final validation (2026-07-28):**

- Mandatory pessimistic review returned `VERDICT: ACCEPTABLE FOR EXA-1 COMPLETION` after all seven blocker classes were fixed.
- Focused EXA gate: 27 files, 1,270/1,270 tests with one worker.
- Full sequential Vitest: 195 files, 5,098/5,098 tests.
- Host/webview TypeScript and integration-test compilation: pass. Production package: pass with 0 lint errors and 6 style warnings. Browser production build: pass. Integrated and standalone bundle gates: pass.
- VS Code integration suite: 113/113 tests on VS Code 1.130.0.
- Authenticated native `kusto-execution-contract`: unchanged rerun passed 3/3 scenarios for normal execution, immediate exact replacement, and database retarget; the preceding first-scenario timeout remains in the flake ledger. The identity JSON and all three passing-run screenshots were reviewed.
- Authenticated native `query-cancel`: 2/2 scenarios for physical long-run cancellation, fast cancellation race, and recovery; all six screenshots were reviewed.

**Iteration feasibility:** realized at 3/5. The migration required every section-publishing producer plus target/account/policy/disposal fencing, but the old Kusto terminal authority and box-only cancellation path are now removed.

## Completed Iteration

### Iteration `EXA-2`: Immutable Result Artifacts And Lineage

**Status:** closed on 2026-07-30. Final pessimistic review returned `VERDICT: CLOSE EXA-2`.

**Why next:** EXA-1 makes the producer execution exact, but admitted rows still collapse into mutable section-keyed state. This leaves the highest-risk remaining break between exact production and downstream consumption.

**Falsifiable hypothesis:** if each admitted success creates an immutable artifact revision carrying producer reservation/dispatch identity, target/principal/policy metadata, and source lineage, then a derived consumer can bind to one revision while a later rerun replaces the section's current pointer without changing what that consumer reads.

**Cheapest discriminating check:** execute a source section, bind one low-risk derived consumer to the resulting revision, rerun the source, and prove the consumer retains its original revision until an explicit rebind while current result rendering still shows the new revision.

**Initial boundary:** introduce immutable runtime and persisted artifact records behind existing result APIs, then migrate one low-risk derived consumer. Preserve current file compatibility and rendering.

**Initial evidence:** every accepted result now creates an immutable revision; exact Kusto successes add producer/policy metadata; bounded Kusto persistence restores stable descriptor identity only after owner admission; and charts render a bound revision. The discriminating tests prove source revision A remains readable after current advances to B, explicit rebind selects B, and source clear synchronously revokes both current and bound artifacts. Pessimistic review returned `VERDICT: ACCEPTABLE FOR INITIAL EXA-2 SLICE`.

**Initial qualification:** the 12-file focused ring passed 527 tests; full sequential Vitest passed 196 files and 5,116 tests; production extension/browser builds, integration compilation, and bundle gates passed; the extension-host suite passed all 113 tests with five-second Mocha headroom for its existing three-second bounded-close case; and native `default/chart-regressions` passed both scenarios on VS Code 1.130.0, including the A-to-B artifact contract.

**Transformation evidence:** primary and join-right inputs now have independent bindings; outputs retain direct revisions and flattened leaf-source policies; unchanged-source tool edits remain pinned; dependent refresh explicitly rebinds; join-right changes trigger recomputation; removal releases inputs and output; and source revocation follows lineage transitively without clearing a retargeted independent current revision. Pessimistic review returned `VERDICT: ACCEPTABLE FOR TRANSFORMATION ARTIFACT SLICE`.

**Transformation qualification:** the 14-file focused ring passed 570 tests; full sequential Vitest passed 196 files and 5,123 tests; host/webview type checks, production extension/browser builds, integration compilation, lint with zero errors, and both bundle gates passed; the extension-host suite passed all 113 tests with the established five-second Mocha headroom; and native `default/transformation-artifacts` passed on its first run on VS Code 1.130.0.

**Comparison evidence:** source and comparison requests/reservations/terminals now carry one exact `KustoComparisonRunIdentity`; manual Compare and standalone Optimize no longer race on a timer; direct Run with a previously cached source delegates to the same source-first pair; Copilot retries reuse the admitted source execution; output publication requires the bound source artifact plus matching physical target and all five policy stamps; summaries follow output lineage; and every failure/removal/retarget path releases temporary pins. Pessimistic review returned `VERDICT: ACCEPTABLE FOR EXACT COMPARISON ARTIFACT SLICE`.

**Comparison qualification:** the 16-file focused EXA ring passed 707 tests; full sequential Vitest passed 196 files and 5,135 tests; host/webview type checks, production extension/browser builds, integration compilation, lint with zero errors, and both bundle gates passed; the extension-host suite passed all 113 tests with established Mocha headroom; and native `default/comparison-artifacts` passed both runs on VS Code 1.130.0.

**Dashboard bridge evidence:** provenance fact sources now bind immutable artifact revisions; same-source HTML edits remain pinned and dependent refresh explicitly rebinds; missing/mixed exposure decisions deny iframe data; source change, revoke, removal, and reorder reconnect cannot retain stale bridge rows; Kusto/SQL admission and derived leaf policies make exposure explicit; and persisted metadata cannot self-authorize without matching the locally recomputed decision.

**Dashboard bridge qualification:** the 18-file focused EXA/dashboard ring passed 844 tests; full sequential Vitest passed 196 files and 5,144 tests; host/webview type checks, production extension/browser builds, integration compilation, lint with zero errors, and both bundle gates passed; the extension-host suite passed all 113 tests with established Mocha headroom; and native `default/html-artifact-bridge` passed on its first run on VS Code 1.130.0.

**Model-result evidence:** configure-and-execute previews and delegated Kusto Copilot responses now bind `model:<requestId>:result` to the artifact for the exact producer execution and require `sendToModel === true` independently of active-content exposure. Mutable current and terminal-row fallbacks are gone. Delegated starts carry the host-captured query, row caps remain stable, and every success, denial, exception, cancellation ordering, timeout, failed start, owner invalidation, and cleanup path releases request state exactly once. Policyless leaves remain explicit; positive persisted capabilities require local admission; and restored Kusto comparisons must match locally reconstructed source lineage and leaf policies. Legacy exposure-only descriptors do not gain model permission, and cyclic comparison restores are discarded. Final pessimistic review found no blocking issue.

**Model-result qualification:** the 21-file focused EXA/model ring passed 1,140 tests; full sequential Vitest passed 196 files and 5,163 tests; host/webview type checks, production extension/browser builds, integration compilation, ESLint with zero errors, and all existing bundle gates passed without threshold changes; the extension-host suite passed all 113 tests with established Mocha headroom; and native `default/model-result-artifacts` passed on its first run on VS Code 1.130.0.

**Clipboard-share evidence:** Share Results now binds `share:clipboard:result` and requires the independent `shareToClipboard === true` decision. Query, connection, database, and rows come from one exact artifact; an open modal stays on A through rerun, close releases A, and reopen selects B. Denial, revocation, owner/document teardown, and forged or mismatched restored provenance fail closed without blocking title/query-only sharing. Kusto and owner-admitted SQL manual/tool/Copilot/comparison producers carry exact query/target metadata. SQL external reruns preserve only already-bound A, comparison replacement retires stale persisted/current/rendered rows, Copilot comparison starts are admitted before transport, and direct/cold/reordered SQL restores revalidate live owner/query/database. SQL formatting omits the ADX link. Final pessimistic review found no blocking issue.

**Clipboard-share qualification:** the 12-file focused EXA/share ring passed 610 tests; full sequential Vitest passed 197 files and 5,182 tests; host/webview type checks, production extension/browser builds, integration compilation, ESLint with zero errors, and all final bundle gates passed; the intentional webview baseline increased by 10 KB only; the extension-host suite passed all 113 tests on VS Code 1.130.0 and 1.131.0; and native `default/share-result-artifacts` passed both runs on VS Code 1.130.0.

**CSV-export evidence:** Kusto, SQL, restored, comparison, and transformation results carry the independent `exportToCsv` decision; derived outputs promote it only when every leaf permits export. Each governed table registers one exact artifact plus a table-generation token, hides Save when admission fails, and serializes its current sorted/filtered projection using only declared columns. Replacement, transitive revocation, removal, every applied document transition (including malformed replacement), and stale same-ID cleanup synchronously release only the owned generation and hide Save. Picker cancellation or transfer timeout cancels only that export attempt, leaving a still-valid table retryable. Webview projections are one-per-table and capped globally. The host opens the native picker before requesting bytes, issues a one-use nonce, validates nonce/box/artifact, retains cardinality- and TTL-bounded intent tombstones against replay, and writes only the correlated response. Concurrent, delayed, replayed, mismatched, remote-URI, and disposal paths fail closed. URL-imported CSV and Connection Manager previews remain separate direct-data workflows. Read-only browser viewers restore persisted Kusto/SQL rows and SQL-derived comparisons into fresh runtime artifacts carrying only `exportToCsv`; they never trust persisted model, clipboard, or active-content claims. Inline and standalone browser hosts emulate the same challenge, cancel timed-out projections, restrict forwarding to the owned iframe, and were exercised with real persisted payloads and exact downloaded bytes.

**CSV-export qualification:** the 14-file focused EXA/CSV ring passed 586 tests; full sequential Vitest passed 199 files and 5,224 tests. Repeated first-attempt proper-lockfile/SQL credential lock failures were unrelated; each affected file passed unchanged and every complete rerun passed. Host/webview type checks, production extension/browser builds, integration compilation, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Final production sizes are 1,762.9 KB for `extension.js` and 2,660.1 KB for `webview.bundle.js`; synchronized baselines moved only from 1,711 to 1,713 KB and 2,605 to 2,611 KB. The extension-host suite passed all 113 tests on VS Code 1.131.0. Native `default/csv-result-artifacts` proved denied Save is absent, allowed Save is present, and the Windows picker wrote exactly 26 bytes (`Name,Score\nalpha,1\nbravo,2`); both screenshots and the JSON byte artifact were reviewed. Built browser inline Kusto, standalone Kusto, SQL, and SQL-derived comparison fixtures in both section orders restored without live connections and downloaded exact CSV bytes.

**Closure evidence:** governed Kusto, SQL, transformation, and Diff tables bind exact artifacts and synchronously purge rows, table/search models, viewers, dialogs, chart controls, and copy authority on revocation. Declared-column projection occurs before presentation, search, model/share/HTML egress, artifact publication, and persistence. Kusto and SQL comparisons retain exact source execution lineage through runtime cancellation and source-first/comparison-first restore. Restored producer claims are grounded in live ownership. URL CSV sections publish immutable artifacts for chart/transformation consumers while request IDs, requested URLs, component-incarnation uniqueness, debounce cancellation, and box-authoritative cleanup prevent stale-response or replacement races. Ungoverned URL and Connection Manager tables retain their direct local workflows.

**Closure qualification:** the final 26-file acceptance ring passed 1,046 tests and final review returned `VERDICT: CLOSE EXA-2`. Full sequential Vitest passed 200 files and 5,264 tests on rerun; the first run had one unrelated SQL credential-lock timing failure, and that file passed unchanged at 52/52 before the complete rerun. Host/webview type checks, production extension build, browser-extension build, integration compilation, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Final production sizes are 1,763.0 KB for `extension.js` and 2,673.8 KB for `webview.bundle.js`; intentional EXA-2 runtime growth moved only the synchronized webview baseline from 2,611 to 2,624 KB with the existing 50 KB buffer unchanged. The extension-host suite passed 113/113 with the documented five-second Mocha headroom; two default two-second attempts reached 112/113 because the existing bounded-close test itself waits up to three seconds. The previously reviewed native `default/csv-result-artifacts` exact-byte evidence remains applicable because the host nonce/challenge/write contract did not change in the closure audit; no new native capture was required.

**Post-closure schema/autocomplete reliability qualification:** EXA-2 remains closed. Same-target metadata requests are brokered without coalescing owner-gated dispatch, SDK clients stay leased through concurrent operations, empty or malformed schema cannot poison cache state, JSON-to-tabular fallback stays within one authenticated attempt, disposal/currentness fences every physical schema command, compact cache entries are upgraded to worker-ready `.show schema` JSON, and queued primary worker intent no longer invalidates an executing primary transaction. The focused nine-file recovery ring passed 414 tests; full sequential Vitest passed 200 files and 5,297 tests. Host/webview type checks, production extension and browser builds, integration compilation, ESLint with zero errors and the same five pre-existing warnings, and both bundle gates passed. Deterministic production sizes are 1,774.1 KB for `extension.js` and 2,681.0 KB for `webview.bundle.js`; synchronized baselines moved from 1,713 to 1,725 KB and from 2,624 to 2,632 KB with the 50 KB buffer unchanged. The extension-host suite passed 113/113 with the established five-second Mocha headroom. Native VS Code 1.131.0 qualification passed `default/kusto-schema-replacement` and authenticated `kusto-auth/kusto-restored-startup`; the latter restored five same-target sections, kept four unfocused sections deferred, and rendered the exact section-three worker table in 917 ms with word suggestions and the auxiliary provider disabled. A preceding `--no-build` launch that opened no webview remains runner-state evidence rather than a product result.

**Subsequent boundary:** DOC-4 is closed. PRO-1 is selected below; a fifth section migration and Kusto/SQL coordinator convergence remain excluded.

## Completed Codec Iterations

### Iteration `COD-1`: Lossless Codec Preservation

**Status:** closed on 2026-08-01; final pessimistic review returned `VERDICT: CLOSE COD-1`.

**Boundary:** introduce one lossless overlay codec path that preserves unknown root fields, unknown state fields, extension fields on known sections, opaque unknown sections, and section order while editing one known field. Keep current document actors and UI restore paths in place.

**Cheapest discriminating check:** open a golden fixture containing future root/state fields, a future field on a known section, and an unknown section; edit one supported known field; save; prove every unknown value and the original order survive byte-semantic round-trip.

**Exclusions:** no document actor migration, capability-policy expansion, or execution-coordinator convergence.

**Completion evidence:** `kqlxOverlay.ts` now provides one recursive overlay codec with compile-time exhaustive host-known section schemas. Root/state extensions, known-section extensions, nested future fields, opaque sections, and order survive known edits. Known omissions remain deletions; stable nested identities cannot inherit metadata from replacements, while explicitly renameable transformation items retain extensions only when correlation is unambiguous. Required/typed known leaves, arrays, records, and nested identities fail read-only when malformed; hostile JSON keys remain data properties. Exact JSON-semantic persistence comparison prevents omitted empty/default fields from being falsely acknowledged. KQLX/SQLX/MDX privacy publication, KQL/SQL sidecar write/repair/recovery, id-less projections, legacy `copilotQuery`, devnotes, chart restoration, and linked-query ownership use the same preservation contract. Linked targets are single, plain-query, non-self, source-generation-fenced, and published through one exact content lease. Session, linked-query, and sidecar writes retain the physical identity accepted at load or locked creation/adoption, with independent durable/buffer rollback and exact Save/Close ownership.

**Qualification:** the expanded focused Vitest ring passed 484 tests, and the final complete sequential suite passed 202 files and 5,404 tests. Host/webview type checks, integration compilation, production extension build, browser-extension build, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. The complete extension-host suite passed 166/166 with the established five-second Mocha headroom. Native VS Code 1.131.0 `default/codec-lossless-roundtrip` passed on its first run after editing one known query field; its reviewed semantic artifact preserved future root/state fields, a known-section extension, the opaque payload, and exact section order, and its reviewed screenshot showed the expected clean editor state. Deterministic production sizes are 1,818.4 KB for `extension.js` and 2,683.9 KB for `webview.bundle.js`; synchronized baselines moved from 1,744 to 1,769 KB and from 2,632 to 2,634 KB while the 50 KB buffer remained unchanged.

### Iteration `COD-2`: One Document-Kind Capability Matrix

**Status:** closed on 2026-08-02; the definitive pessimistic review returned `VERDICT: CLOSE COD-2`.

**Boundary:** replace distributed `.kqlx` / `.sqlx` / `.mdx` section-kind filters and creation rules with one shared capability matrix used by parsing/validation, host projection, webview add controls, tools, and browser/read-only composition. Invalid known kinds must be reported rather than silently filtered; opaque future sections remain preserved by COD-1.

**Cheapest discriminating check:** enumerate every known section kind against each document kind and prove parser validation, host `allowedSectionKinds`, webview add controls, and tool admission return the same decision, including an incompatible known section that currently disappears from MDX projection.

**Exclusions:** no document actor, section-definition registry, protocol redesign, or execution-coordinator convergence.

**Completion evidence:** `documentSectionCapabilities.ts` owns the typed matrix, canonical aliases, persisted/addable distinction, hidden `devnotes`, defaults, compatibility classification, and actionable validation. `parseKqlxText()` validates the actual URI-bound kind before projection. Provider-local arrays, SQL compatibility's advertised Kusto query, MDX sanitation/filtering, the overlay's MDX passthrough sets, compatibility primary-kind alias lists, and ad hoc `copilotQuery` decision branches are gone from capability surfaces. Host projections and KQL/SQL/Markdown upgrades derive from the destination kind. Compatibility sidecars validate the destination row before parse, hydration, repair, build, adoption, or write; malformed SQL linkage, multiple linked owners, and other invalid KQL/SQL companions project read-only and cannot reach an overwrite prompt. The webview intersects host declarations with the matrix before controls, insert zones, default creation, restore, and tool dispatch. `createSectionWithCapabilities()` owns every non-restore creation: tools, Kusto Copilot auto-create, Copilot Insert, and Kusto/SQL comparison preparation cannot bypass admission or silently lose compatibility-file sections. Empty capability projections stay empty, and default creation consults the persisted section array so visual, hidden, and opaque-only documents remain unchanged. SQL optimization comparisons persist as `sql` with `comparisonSourceBoxId`; comparison removal clears webview/host owner state and source backlinks before recreation; tool-created SQLX files use the matrix default. Every browser provider recognizes MDX, the production manifest makes raw-file routing reachable, and the viewer delegates structural and capability validation to the host parser, including hostile IDs, duplicate IDs, known shapes, linked owners, and invalid companions. Browser companion hydration additionally requires first-section ownership of the exact raw URL. Legacy preload APIs queue canonical query adds rather than maintaining a second alias kind. The ownership guard prevents removed lists, raw automation factories, alias branches, empty-set fallback, and the browser parser copy from returning.

**Final closure hardening:** SQL comparison preparation now uses an application-acknowledged `staged -> committed -> finalized -> completed` CAS transaction over the complete live descriptor, query/result revisions, persisted rows/artifact, and active execution identity. The host retains the prior owner until completed acknowledgement, retries acknowledged rollback before completion, releases terminal proofs explicitly, revalidates both exact targets afterward, and rejects nested sources before mutation. Admission fences Monaco and execution; restore reconstructs exact SQL lineage. Browser standalone handoff is UUID-tokened, one-shot, and TTL-bounded. Plain-file sidecars retain fixed primary identities and validate a complete candidate before source mutation. The webview starts runtime-inactive, activates only after successful materialization, and turns malformed retained DOM into an inert visual snapshot while retiring executable owners, rows, tools, and pending work.

**Qualification:** the final blocker ring passed 10 files and 584 tests; complete sequential Vitest passed 211 files and 5,543 tests. Host/webview and browser TypeScript checks, integration compilation, production extension and browser builds, ESLint with zero errors and the same five pre-existing warnings, and both bundle gates passed. The complete extension-host suite passed 174/174 with `--timeout 5000` on VS Code 1.131.0. The definitive native `default/document-capabilities` run `20260802-103341` passed 4/4: three reviewed MDX screenshots proved actionable byte-preserving rejection, exact allowed controls, and no opaque-only default; exact DOM/file assertions proved real host SQL comparison create/save/reopen, nested rejection, remove/recreate with a fresh ID, and final reopen with one valid SQL comparison and lineage. The definitive pessimistic review returned `VERDICT: CLOSE COD-2`. Final production sizes are 1,835.2 KB for `extension.js` and 2,704.9 KB for `webview.bundle.js`; synchronized baselines are 1,786 KB and 2,655 KB with the 50 KB buffer unchanged.

## Completed Document Iterations

### Iteration `DOC-1`: Host-Owned Markdown Section Lifecycle

**Status:** closed on 2026-08-03. The definitive pessimistic review returned `VERDICT: CLOSE DOC-1`.

**Why this slice:** COD was closed, so DOC's prerequisite was satisfied. Markdown was the lowest-risk section capable of proving host-owned ordered state, revisioned commands, view recreation, and lossless Save through the real custom-editor boundary.

**Boundary:** introduce the smallest transport-neutral document aggregate and section-definition contract needed to own one low-risk Markdown section's add, patch, remove, validation, and snapshot transitions behind the existing lossless codec and VS Code adapter. Keep the current protocol and view as adapters. Do not migrate Kusto/SQL lifecycle, execution, compatibility orchestration, or other section kinds in this slice.

**Falsifiable hypothesis:** if a serial host document owner and one Markdown section definition are authoritative, then a Markdown add/edit/remove survives view teardown and recreation and produces the same lossless snapshot without reading component or DOM serialization state.

**Cheapest discriminating check:** load a fixture containing Markdown plus opaque future data, execute add/patch/remove commands against the host owner, destroy and recreate the view projection, then save and prove the exact command result and opaque data persist while a deliberately stale or throwing DOM `serialize()` implementation is never consulted.

**Exclusions:** no protocol redesign, Kusto/SQL execution convergence, broad provider split, or registry migration for a second section kind until the Markdown path deletes its displaced persistence authority.

**Completion evidence:** `markdownSectionDefinition.ts` owns Markdown persisted validation and patch semantics. `MarkdownDocumentAggregate` owns ordered application state, document and section revisions, and add/patch/remove transitions. Native Markdown components emit commands and render complete projections; native snapshot creation reads the host projection and never calls component `serialize()`, while plain `.md` compatibility remains primary-text-owned. Every command has a terminal result. Projection candidates activate only after a live, latest-generation, source-current application acknowledgement; rejected, expired, or drifted acknowledgements preserve the prior owner. Each normalized URI has one queue before ownership exists and across transient panels, commands, adapters, rollback, Save, close, and reopen. Save barriers become commit-scoped leases, rejected barriers abort, overlapping Saves serialize, internal canonical Saves cannot release user leases, and disposal settles accepted work. Live panel ownership is separate from retained aggregate state; canonical handoff preserves revision history through ABA-shaped text restoration, and close cleanup rechecks reopen state atomically before deleting the exact owner/queue. Final persistence overlays host Markdown onto adapter state through the lossless codec, preserving future root/state data, known-section extensions, opaque sections, and relative order.

**Qualification:** the focused Vitest ring passed 7 files and 444 tests; the expanded provider authority/multi-panel/Save/reopen matrix passed 12/12, and the sidecar suite passed 117/117 with `--timeout 5000`. Complete sequential Vitest passed 213 files and 5,565 tests. The complete VS Code 1.131.0 extension-host suite passed 187/187 with `--timeout 5000`. Native run `20260803-054923` passed `default/host-owned-markdown-lifecycle`; the reviewed screenshot showed `Host owned`, `after`, Preview mode, no error UI, and a clean editor. Reviewed JSON artifacts proved zero DOM serialization calls, stale-command rejection, exact `markdown_doc1|future_doc1` order, future root/state preservation, the Markdown extension, opaque payload, and `dirty:false` after Save and recreation. Host/webview and browser TypeScript checks, integration compilation, production extension and browser builds, ESLint with zero errors and the same five pre-existing warnings, and both bundle gates passed. Final production sizes are 1,854.4 KB for `extension.js` and 2,712.5 KB for `webview.bundle.js`; synchronized baselines are 1,805 KB and 2,663 KB with the 50 KB buffer unchanged.

### Iteration `DOC-2`: Host-Owned URL Section State

**Status:** closed on 2026-08-03. The definitive pessimistic review returned `VERDICT: CLOSE DOC-2`.

**Why next:** DOC remains the highest eligible theme at 50. The Markdown slice proves the aggregate, revision, queue, acknowledgement, Save, panel-handoff, and lossless overlay contracts. URL is the next small persisted-state shape that can expand the section-definition registry without entering Kusto/SQL execution or dependency-heavy chart/transformation ownership.

**Boundary:** add one URL section definition and route URL add/patch/remove plus persisted presentation state through the existing host document aggregate and command protocol. Keep fetch, sanitization, and iframe rendering in their current adapters. Do not migrate a third section kind.

**Falsifiable hypothesis:** if URL persisted state joins the proven host aggregate, then URL add/edit/remove and view recreation can use host revisions and lossless snapshots while a stale or throwing URL component serializer cannot affect native Save.

**Cheapest discriminating check:** load URL plus Markdown and opaque future data, execute URL add/patch/remove against the host owner, recreate the view, then Save and prove exact URL state, Markdown state, order, and future data survive while URL `serialize()` is never consulted.

**Exclusions:** no URL fetch/network redesign, HTML/Markdown resource-policy redesign, Kusto/SQL lifecycle migration, protocol rewrite, or migration of another section kind.

**Completion evidence:** `urlSectionDefinition.ts` owns URL persisted validation and patch semantics. URL joins Markdown inside the existing `MarkdownDocumentAggregate`, optimistic webview projection ledger, command protocol, save barrier, and per-URI physical queue. Commands reserve the queue before source reads, and successful terminals must match the complete predicted owned projection, including every Markdown/URL section revision and exact mixed order. Rejected terminals reconcile both kinds and use one stable whole-container ordering pass. Native snapshots substitute acknowledged URL state and never call `kw-url-section.serialize()`.

The component retains fetch/render ownership. Authored URL and resolved redirect identity are distinct; only authored URL plus persisted presentation state enters the aggregate. Same-document projections retain unchanged URL elements and their debounce, CSV artifact/table/observer, and iframe identity. Synchronous DOM moves are not disposal; genuine removal defers teardown until disconnection is confirmed, and detached or replaced same-ID instances cannot emit stale persistence callbacks. Fetch request identity, content, loading/error state, DOMPurify, iframe rendering, CSV artifacts, and autosizing otherwise remain unchanged.

**Qualification:** the definitive focused ring passed 11 files and 513 tests. Complete sequential Vitest passed 215 files and 5,592 tests. The provider lifecycle matrix passed 13/13, including two session URL commands whose source reads were deliberately reversed at admission. The complete VS Code 1.131.0 extension-host suite passed 188/188 with `--timeout 5000`. Both TypeScript projects, integration compilation, production extension/browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Native run `20260803-104840` passed the extended `default/host-owned-markdown-lifecycle` scenario. Its reviewed screenshot showed restored `Host owned` Markdown and collapsed `Host owned URL` sections with no error UI; reviewed JSON artifacts proved zero Markdown/URL serializer calls, stale-command rejection, exact `markdown_doc1|url_doc1|future_doc1` order, future root/state/Markdown/URL/opaque preservation, and `dirty:false`. A preceding native run failed only because its test artifact returned an `undefined` optional field; the strict-JSON assertion was corrected without product changes. Final production sizes are 1,857.1 KB for `extension.js` and 2,724.2 KB for `webview.bundle.js`; synchronized baselines are 1,808 KB and 2,675 KB with the 50 KB buffer unchanged.

### Iteration `DOC-3`: Host-Owned Python Section State

**Status:** closed on 2026-08-03. The definitive post-qualification review returned `VERDICT: CLOSE DOC-3`.

**Why next:** DOC remains the highest eligible theme at 50. Python is the next bounded persisted-state shape after Markdown and URL. It can expand the proven aggregate and definition contract without entering Kusto/SQL target lifecycle or chart/transformation dependency scheduling.

**Boundary:** add one Python section definition and route Python add/patch/remove plus persisted `name`, `code`, `output`, `expanded`, and `editorHeightPx` through the existing host aggregate and command queue. Keep Monaco instances, execution request/process ownership, stdout/stderr acquisition, and local-code policy in their current adapters. Do not migrate a fourth section kind.

**Falsifiable hypothesis:** if Python persisted state joins the same host aggregate, then source/output/presentation edits, stale rejection, view recreation, and lossless Save remain correct while a stale or throwing Python component serializer cannot affect native persistence.

**Cheapest discriminating check:** load Python plus host-owned Markdown/URL and opaque future data, execute Python add/patch/remove against the host owner, reject a stale command, recreate the view, poison Python `serialize()`, then Save and prove exact Python/Markdown/URL state, order, and future data survive.

**Exclusions:** no Python execution/process redesign, Monaco lifecycle rewrite, protocol rewrite, Kusto/SQL migration, or chart/transformation/HTML or fourth section kind.

**Completion evidence:** `pythonSectionDefinition.ts` owns Python persisted validation and patch semantics. Python joins Markdown and URL inside the existing aggregate, optimistic full-projection ledger, command protocol, Save barrier, and per-URI physical queue. Native snapshots substitute acknowledged Python state and never call `kw-python-section.serialize()`. Add, code/output/name/presentation patch, remove, tool mutation, stale rejection, rejected-result reconciliation, Save, panel handoff, close, and recreation all use the same owner.

The component retains Monaco and execution adapters. Same-document projection and reorder preserve the existing Monaco instance; genuine removal disposes it; detached or replaced instances cannot persist. A webview-local admission registry fences the legacy box-ID-only execution protocol to one outstanding exact component/code owner, retires publication on projection/removal/invalidation, consumes inactive terminals without output, and blocks rerun until retired work settles. The host's 15-second deadline publishes one terminal independently of process close, so a non-settling child cannot strand the admission tombstone. Only an admitted terminal patches persisted `output`; transient `Running...` never enters the aggregate. Metadata-free browser/legacy behavior and local-code policy remain unchanged.

**Qualification:** the final focused ring passed 12 files and 539 tests. Complete sequential Vitest passed 216 files and 5,615 tests. The provider lifecycle matrix passed 14/14, including Python-only Save before projection acknowledgement. The complete VS Code 1.131.0 extension-host suite passed 189/189 with `--timeout 5000`. Host/webview and browser TypeScript checks, integration compilation, production extension/browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Native run `20260803-124847` passed the extended `default/host-owned-markdown-lifecycle` scenario; the reviewed screenshot showed restored Markdown plus collapsed URL and Python sections without error UI. Three reviewed JSON artifacts proved zero Markdown/URL/Python serializer calls, stale-command rejection, exact `markdown_doc1|url_doc1|python_doc1|future_doc1` order, all five Python fields, future root/state/known-section/opaque preservation, and `dirty:false`. The definitive post-qualification review returned `VERDICT: CLOSE DOC-3`. Final production sizes are 1,859.0 KB for `extension.js` and 2,731.4 KB for `webview.bundle.js`; synchronized baselines are 1,810 KB and 2,682 KB with the 50 KB buffer unchanged.

### Iteration `DOC-4`: Host-Owned Chart Configuration State

**Status:** closed on 2026-08-03. The definitive post-fix closure review returned `VERDICT: CLOSE DOC-4`.

**Why next:** DOC remains the highest eligible theme at 50. Chart is the next configuration-bearing section that can broaden the definition contract while retaining mature immutable artifact bindings and ECharts rendering in their current adapters.

**Boundary:** add one chart section definition and route chart add/patch/remove plus the existing persisted chart configuration through the aggregate and shared URI queue. Keep ECharts instances, immutable artifact bindings, source refresh, rendering, and dependency scheduling in current adapters. Do not migrate a fifth section kind.

**Falsifiable hypothesis:** if chart configuration joins the host aggregate, chart source/configuration/presentation edits, stale rejection, view recreation, and lossless Save remain correct while chart rendering and bound artifact identity survive host projection application and a stale or throwing chart serializer cannot affect native persistence.

**Cheapest discriminating check:** load Chart plus host-owned Markdown/URL/Python and opaque future data, execute chart add/patch/remove, reject a stale command, recreate the view, poison chart `serialize()`, then Save and prove exact chart configuration, owned-section order, future data, and retained runtime binding behavior.

**Exclusions:** no artifact-store or ECharts redesign, transformation scheduling migration, HTML/Kusto/SQL lifecycle, protocol redesign, or fifth section kind.

**Completion evidence:** `chartSectionDefinition.ts` owns the complete persisted Chart configuration, including nested axis, legend, heatmap, color-record, title, validation, and null-delete patch semantics. Chart joins the existing aggregate, optimistic full-projection ledger, command envelope, URI queue, acknowledgement, Save lease, handoff, and lossless overlay path. Native snapshots substitute acknowledged Chart state and never call `kw-chart-section.serialize()`; metadata-free browser/legacy hosts keep the serializer.

The component and renderer remain adapters. Equal projections retain the exact element, global renderer state, ECharts instance, zoom state, and immutable artifact binding without rendering or command emission. Genuine source changes synchronously transition the binding even while collapsed; normal dependent refresh still owns explicit revision rebind. Automatic first-render layout does not persist defaults. Genuine removal disposes ECharts and releases its binding. Detached or replaced same-ID elements, delayed first updates, and delayed projection callbacks cannot command, render into, or remove the current owner. Transformation rename propagation uses component type rather than ID prefix and commits arbitrary-ID Charts through the same command client. Add/configure/remove/collapse tools wait for exact command settlement.

**Qualification:** the expanded focused ring passed 17 files and 744 tests. Complete sequential Vitest passed 216 files and 5,626 tests. The provider lifecycle matrix passed 15/15, including Chart-only Save before projection acknowledgement and a command arriving after a barrier response but before lease reservation. The complete VS Code 1.131.0 extension-host suite passed 190/190 with `--timeout 5000`. Host/webview and browser TypeScript checks, integration compilation, production extension and browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Native run `20260803-194348` passed both `default/host-owned-markdown-lifecycle` scenarios with a clean extension-host log. Reviewed screenshots showed clean restored Markdown/URL/Python and all four collapsed owned sections including arbitrary-ID `Host owned Chart`. Eleven reviewed JSON artifacts proved four successful exact Chart terminals with no reload, an explicitly accepted Save barrier, zero serializer calls, zero equal-projection commands, retained runtime identity, stale rejection, exact five-section order, complete nested configuration, future root/state/known-section/nested-setting/opaque preservation, and `dirty:false` immediately after Save and after reopen. Full-projection comparison follows JSON transport semantics, so omitted and `undefined` object fields cannot cause a false reload. Final production sizes are 1,863.5 KB for `extension.js` and 2,744.2 KB for `webview.bundle.js`; intentional growth moved synchronized baselines from 1,810 to 1,814 KB and from 2,682 to 2,695 KB with the 50 KB buffer unchanged. An initial closure review blocked on a rejected Save barrier in superseded run `20260803-185050`; the barrier catch-up and JSON-semantic projection fixes are covered by provider/client regressions and the clean replacement run. The definitive post-fix review found no remaining blocker and returned `VERDICT: CLOSE DOC-4`.

### Iteration `DOC-5`: Host-Owned Transformation Configuration State

**Status:** closed on 2026-08-04. The definitive blocker-only review returned `VERDICT: CLOSE DOC-5`.

**Boundary:** `transformationSectionDefinition.ts` owns persisted Transformation validation, deep cloning, nested arrays, and null-delete patches. Transformation joins the existing aggregate, optimistic full-projection ledger, runtime-validated document-view channel, URI queue, Save lease, handoff, and lossless overlay path. Native snapshots substitute acknowledged Transformation state and never call `kw-transformation-section.serialize()`; metadata-free browser/legacy behavior retains it.

**Adapter boundary:** primary/join-right immutable artifact bindings, expression evaluation, derived publication and lineage, dependency scheduling, refresh cascades, table/CSV rendering, warnings, and automatic layout remain in `kw-transformation-section` and artifact adapters. Equal projections retain the exact element, pins, lineage, and output. Configuration-only edits recompute from existing pins; source/type edits synchronously rebind only affected roles and publish while collapsed; dependent refresh explicitly advances pins. Renderer-only height cannot enter a host patch, explicit fit/resize persists, and detached/replaced instances cannot command, publish, unbind, or remove a current owner.

**Displaced authority and guards:** native Transformation DOM serialization is no longer persistence authority. Add/patch/remove/collapse/configure tools wait for the shared command terminal. Arbitrary-ID downstream rename propagation uses component type. A real provider regression proves stale rejection, view recreation, poisoned serialization, exact mixed order, and nested future metadata. Pure owner/client/protocol tests cover nested validation, optimistic equality, and ambiguity rejection; adapter tests cover equal/config/source projection behavior, collapsed primary/right retargets, renderer-height isolation, explicit refresh, lineage, and stale same-ID fencing.

**Qualification:** the final focused ring passed 19 files and 729 tests. Complete sequential Vitest and the official sequential coverage gate passed 217 files and 5,657 tests; statement coverage is 48.11% against the unchanged 27.67% threshold. Provider lifecycle passed 20/20, compatibility sidecars passed 124/124, and the complete VS Code 1.131.0 extension-host suite passed 200/200 with `--timeout 5000`. Final hardening covers pending-authority rejection before adapter lease acquisition, real-webview retry acknowledgements, URI-queue privacy durability, canonical restoration, overlapping Saves, and inode-aware rejection of dirty open physical aliases before linked durable publication. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Final native run `20260804-144628` passed all four `host-owned-markdown-lifecycle` scenarios. Four reviewed foreground `1280x1000` screenshots were clean, and all 25 strict JSON artifacts were reviewed. DOC-5 evidence proves one exact successful Transformation terminal, stale rejection, accepted stamped Save, zero serializer/equal-projection commands, A pins/lineage retained through configuration while source currents advanced to B, explicit refresh rebinding both roles to B, exact recreation at 460px, clean Save, exact mixed order, nested future metadata, and opaque preservation. Inherited DOC-3/DOC-4/PRO-1 artifacts remained clean. Logs contained only existing Git/CSP/Mermaid noise, with no DOC-5 product error. Final sizes are 1,886.5 KB for `extension.js` and 2,773.4 KB for `webview.bundle.js`; synchronized baselines are 1,837 KB and 2,724 KB with the 50 KB buffer unchanged. The definitive blocker-only reviewer found no closure blocker and returned `VERDICT: CLOSE DOC-5`.

## Completed Protocol Iteration

### Iteration `PRO-1`: Runtime-Validated Document View Session

**Status:** closed on 2026-08-03. The definitive blocker-only review returned `VERDICT: CLOSE PRO-1`.

**Boundary:** `documentViewProtocol.ts` owns one versioned, runtime-validated channel for only native `documentData`, `documentReloadResult`, host-owned section commands/results, and Save barrier traffic. `KqlxEditorProvider` creates one UUID per concrete panel incarnation and stamps every in-scope host envelope. Host and webview ingress parse before side effects and reject malformed, retired, or cross-session traffic. The webview adopts the session from its first valid projection, permanently tombstones that initial request, and keeps later duplicate detection bounded. Existing source generations, command IDs, revisions, URI queues, file formats, and Save leases remain unchanged inner authorities. Compatibility and browser/legacy messages remain metadata-free.

**Displaced authority:** native document lifecycle messages no longer cross provider/webview boundaries as unchecked `unknown`/`any` objects or duplicated local message shapes. A disposed panel cannot acknowledge a successor projection, reserve a successor command, settle a successor terminal, or release a successor Save barrier merely by carrying otherwise current generations, request IDs, or revisions.

**Guards:** the required provider regression establishes a session, replaces it, and injects predecessor acknowledgement, command, and barrier traffic using successor-valid correlation fields; none affects the successor, while current-session traffic completes exactly once and the accepted Save lease remains held until `didSave`. Webview tests reject malformed and duplicate initial projections before adoption and reject prior-session results/barriers before client admission. Pure protocol tests inventory and validate all six discriminators and nested projections, the real outbound wrapper tests stamping/drop behavior, and the static message inventory follows the shared protocol source.

**Qualification:** the finalized focused ring passed 7 files and 375 tests. Complete sequential Vitest and sequential coverage passed 217 files and 5,635 tests; statement coverage is 47.78% against the unchanged 27.67% threshold. Provider lifecycle passed 16/16, compatibility sidecars passed 117/117, and the complete VS Code 1.131.0 extension-host suite passed 191/191 with `--timeout 5000`. Host/webview/browser typechecks, production extension and browser builds, ESLint with zero errors and five pre-existing warnings, and both synchronized bundle gates passed. Native run `20260803-213448` passed all three `host-owned-markdown-lifecycle` scenarios. Three reviewed `1280x1000` screenshots were clean and correctly foreground-captured; reviewed JSON proved distinct session UUIDs, one initial request per session, exactly one stamped command/result per session, no terminal or mutation from the predecessor command, accepted stamped Save barriers, and `dirty:false` persisted state. Inherited DOC-3/DOC-4 artifacts and logs remained clean; logs contained only existing VS Code Git/CSP warnings and the unrelated Mermaid proposed-API error. Final production sizes are 1,872.6 KB for `extension.js` and 2,752.8 KB for `webview.bundle.js`; intentional PRO-1 growth moved synchronized baselines from 1,814 to 1,823 KB and from 2,695 to 2,703 KB with the 50 KB buffer unchanged.

## Completed Document Iteration

### Iteration `DOC-6`: Host-Owned HTML Configuration State

**Status:** closed on 2026-08-05. Post-fix blocker reviews returned `VERDICT: NO DOC-6 BLOCKER`; the closure decision is `VERDICT: CLOSE DOC-6`.

**Selection rationale:** DOC was the highest eligible gap at 48. HTML was the next bounded persisted-configuration section after Transformation. Its durable shape was already explicit in `kqlxFormat.ts` and the lossless overlay, while Monaco, preview iframe, provenance/data bridge, immutable artifact binding, slicers, validation, export, and publishing could remain adapter-owned. Kusto and SQL combined primary text, target, language, execution, privacy, and result ownership and were materially riskier candidates.

**Boundary:** add one HTML section definition and route existing persisted `name`, `code`, `mode`, `expanded`, editor/preview heights, `previewHeightUserSet`, `dataSourceIds`, `pbiPublishInfo`, and `powerBiUpgradeNotice` through the existing aggregate, optimistic client, document-view channel, and URI queue. Keep Monaco instances, preview iframe/scripts, provenance parsing, data bridge, immutable artifact binding, slicers, renderer state, validation, Power BI export, and Fabric publishing in existing adapters. Do not migrate Kusto, SQL, dashboard compiler ownership, or another section kind.

**Falsifiable hypothesis:** if HTML persisted configuration joins the host document owner while preview/data/export behavior remains adapter-owned, then code/mode/height/publish-metadata edits, stale rejection, view recreation, and lossless Save use host revisions while an equal projection retains the exact Monaco/iframe/artifact binding and a stale or throwing HTML serializer cannot affect native persistence.

**Cheapest discriminating check:** load native Markdown/URL/Python/Chart/Transformation plus one HTML section bound to an immutable fact artifact, nested future publish/notice metadata, and an opaque section. Apply one HTML configuration edit and reject one stale edit through the host command path, recreate the view, poison `kw-html-section.serialize()`, then Save and prove exact configuration/order/future data while an equal projection retains the same Monaco, preview iframe, and fact binding without rerender or command emission.

**Exclusions:** no dashboard compiler or provenance redesign, artifact-store redesign, Power BI/Fabric workflow migration, Kusto/SQL ownership, protocol expansion beyond the existing document-view channel, or seventh section kind.

**Boundary:** `htmlSectionDefinition.ts` owns persisted HTML validation, cloning, nested publish/notice metadata, and null-delete patches. HTML joins the existing aggregate, optimistic full-projection ledger, runtime-validated document-view channel, physical URI queue, Save lease, handoff, and lossless overlay. Native Save substitutes acknowledged HTML and never calls `kw-html-section.serialize()`; metadata-free browser/legacy behavior retains serialization.

**Adapter boundary:** Monaco, sandboxed iframe/scripts, provenance parsing, data bridge, immutable fact binding, slicers, validation, Power BI export, and Fabric publishing remain adapter-owned. Equal projection retains the exact element/editor/iframe/binding without rerender or commands. Same-ID/detached instances, delayed measurement, stale responses, and no-op DOM ordering cannot mutate or reload the current runtime.

**Publish transaction hardening:** dashboard requests carry exact identities and cancellation. Fabric mutation uses one account and final Leave No Trace admission, UUID staging, exact paginated recovery, and partial-create compensation. Returned IDs enter the document through one-field correlated HTML commands. Apply/compensate binds exact section, complete metadata, document/section revisions, and authoritative previous value. Concurrent ordinary HTML patches carry a provisional new tuple; failures reconcile from host projection. Queue-stable cleanup retains referenced or uncertain items and deletes only an absent exact tuple.

**Qualification:** final focused ring passed 20 files and 661 tests. Complete sequential Vitest and coverage passed 220 files and 5,703 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. Provider lifecycle passed 21/21, compatibility sidecars passed 124/124, and the complete VS Code 1.131.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, and both synchronized bundle gates passed. Final sizes are 1,905.5 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; baselines are 1,856 KB and 2,752 KB with the 50 KB buffer unchanged. Definitive native run `20260805-043534` passed all five `host-owned-markdown-lifecycle` scenarios after one harness file-not-found rerun. Five reviewed foreground `1280x1000` screenshots and eight DOC-6 JSON artifacts proved exact successful/stale terminals, accepted Save, zero serializer/equal-projection commands, exact editor/iframe/binding retention, clean recreation, exact mixed order, nested future publish/notice metadata, and opaque preservation. Logs contained only existing Git/CSP/Mermaid noise. No live authenticated Fabric tenant run was performed.

## Completed Host Iterations

### Iteration `HST-1`: Dashboard Workflow Application Handler

**Status:** closed on 2026-08-05. The definitive blocker-only review returned `VERDICT: NO HST-1 BLOCKER`.

**Boundary:** `HostDashboardApplicationHandler` is the injected owner for all nine dashboard request/cancel/ack routes, request abort controllers, user prompts, HTML/PBIP export, Fabric workspace/item lookup, first-external-commit Leave No Trace admission, publish application/compensation leases, stale retirement, and cleanup finalization. `QueryEditorProvider` constructs or accepts the handler, offers typed inbound messages, preserves synchronous admission for unrelated Kusto/SQL traffic, and disposes it. `KqlxEditorProvider` supplies only the explicit publish apply/settle/cleanup hooks backed by the existing URI queue.

**Displaced authority:** `QueryEditorProvider` no longer owns dashboard workflow maps, publish-ack maps, cleanup admission, dashboard discriminator cases, Power BI/Fabric adapter imports, or publish state transitions. `powerBiExport.ts`, `powerBiPublish.ts`, the document aggregate/URI queue, `kw-html-section`, `kw-publish-pbi-dialog`, provenance v1, generated PBIR/TMDL/DAX, message shapes, and product behavior are unchanged.

**Guards:** the requested real-provider test was written red first, then proves exact forwarding of every dashboard message to a compile-time-complete fake handler. A source guard prevents workflow maps, native publish hooks, and Power BI/Fabric adapter calls from returning to the provider. Direct handler tests cover synchronous decline, prompts, correlated workspace/existence responses, successful publish response/ack, exact cancellation, stale retirement, first-commit policy, apply/compensate interleavings, same-ID replacement, exact old-metadata restoration, and queue-stable cleanup. Protocol extraction inventories handler-emitted messages.

**Qualification:** the final focused ring passed 10 files and 466 tests. Complete sequential Vitest and the official coverage gate passed 221 files and 5,708 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.131.0 extension-host suite passed 201/201 with `--timeout 5000`, and the provider lifecycle passed 21/21. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates passed. Final sizes are 1,905.8 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`, within unchanged limits. No live authenticated Fabric tenant run was performed.

### Iteration `HST-2`: Artifact CSV Save Application Handler

**Status:** closed on 2026-08-05. The definitive blocker-only review returned `VERDICT: NO HST-2 BLOCKER`.

**Boundary:** `HostArtifactCsvSaveApplicationHandler` is the injected owner for `requestArtifactCsvSave`, `artifactCsvSaveData`, and `cancelArtifactCsvSaveIntent`. It owns native picker admission, eight active intents, one-use UUID nonce challenges, intent and transfer cancellation, 60-second deadlines, bounded 10-minute replay tombstones, exact box/artifact correlation, URI-preserving `.csv` publication, saved-file notifications, and disposal. `QueryEditorProvider` only constructs or accepts the handler, offers typed messages synchronously, supplies panel transport, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns artifact CSV intent/save/completed maps, timeout/tombstone constants, discriminator cases, picker transitions, nonce issuance/admission, or governed CSV file writes. `artifact-csv-export.ts`, immutable result-artifact bindings, table-generation tokens, `kw-data-table`, browser shim/download behavior, message shapes, imported CSV, and Connection Manager preview export remain unchanged.

**Guards:** the requested real-provider injection/forwarding test was written red first and failed with zero handler calls, then passed for all three exact typed messages. A static displaced-authority guard prevents maps, methods, and discriminator cases from returning to the provider. Direct handler tests cover synchronous decline, exact UTF-8 bytes, active/completed bounds, concurrent reverse delivery, mismatched and replayed nonces, picker cancellation, cancellation during picker and transfer, deadlines, disposal, and remote URI authority. Protocol extraction includes the handler and explicitly requires both outbound CSV messages.

**Qualification:** the final focused ring passed 11 files and 310 tests. Complete sequential Vitest and the official coverage gate passed 223 files and 5,713 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.131.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, `git diff --check`, and both bundle gates passed. Final production sizes are 1,906.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved from 1,856 to 1,857 KB with the 50 KB buffer unchanged. Native run `20260805-164304` exercised denied/allowed Save admission, the real Windows picker, exact intent/export/nonce/box/artifact correlation, zero cancellation, host write completion, and the exact 26-byte file `Name,Score\nalpha,1\nbravo,2`. The permanent scenario's fresh screenshots were blocked before picker execution by the framework's foreground-HWND validation; previously reviewed clean screenshots remain the visual baseline.

### Iteration `HST-3`: Python Execution Application Handler

**Status:** closed on 2026-08-05. The definitive blocker-only review returned `VERDICT: NO HST-3 BLOCKER`.

**Boundary:** `HostPythonExecutionApplicationHandler` is the injected owner for `executePython`, interpreter fallback (`python`, `python3`, `py`), child-process/stdin/stdout/stderr lifecycle, independent 200 KB UTF-8 output caps, the 15-second timeout/kill terminal, `pythonResult` / `pythonError` publication, and disposal. Timeout/disposal establish terminal authority before process termination; expected teardown stream errors remain non-terminal, unexpected stream errors settle once and kill best-effort, and late child/stdio events are consumed. `QueryEditorProvider` only constructs or accepts the handler, offers typed messages synchronously, supplies panel transport, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer imports `spawn`, creates Python processes, owns the `executePython` discriminator branch, accumulates stdout/stderr, schedules Python deadlines, or constructs Python terminals. `kw-python-section`, `python-execution-admission.ts`, host-owned Python persisted state, local-code policy, Monaco/runtime rendering, message shapes, and the document-view protocol remain unchanged.

**Guards:** the requested real-provider injection test was written red first and failed with zero fake-handler calls, then passed with exact unchanged forwarding. A static displaced-authority guard prevents Python process/terminal code from returning to the provider. Direct handler tests cover synchronous decline, success, synchronous and event-based fallback, all interpreters missing, non-ENOENT failure, expected and unexpected stdio errors, synchronous/late close/error races, timeout, 200 KB UTF-8 boundaries, and disposal. Protocol extraction inventories both handler terminals.

**Qualification:** the final focused ring passed 10 files and 502 tests. Complete sequential Vitest and the official coverage gate passed 224 files and 5,728 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, `git diff --check`, and both bundle gates passed. Final production sizes are 1,908.2 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved from 1,857 to 1,859 KB with the 50 KB buffer unchanged. Final isolated native run `20260805-194937` passed first attempt against the reviewed build: the real Run button executed built-in Python, rendered exact `HST3:24`, saved, closed/reopened, and restored raw Windows stdout `HST3:24\r\n` with normalized output `HST3:24` and `dirty:false`. Its foreground-valid `1280x1000` screenshot and two JSON artifacts were reviewed clean; logs contained only existing Git, missing-CSP, and Mermaid proposed-API noise.

### Iteration `HST-4`: Imported CSV Save Application Handler

**Status:** closed on 2026-08-05. The definitive blocker-only review returned `VERDICT: NO HST-4 BLOCKER`.

**Boundary:** `HostImportedCsvSaveApplicationHandler` is the injected owner for `saveImportedCsv`, empty-data UX, native picker admission, workspace/home default selection, URI-preserving `.csv` extension handling, exact UTF-8 publication, Open File / Show in Folder actions, notification failure containment, write-failure messaging, and disposal. `QueryEditorProvider` only constructs or accepts the handler, offers typed inbound messages synchronously, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer imports imported-CSV file helpers, owns `saveImportedCsvFromWebview`, handles the discriminator case, opens the picker, writes bytes, or publishes save notifications. URL fetch identity/content acquisition, `kw-url-section`, browser download behavior, governed artifact CSV saving, Connection Manager preview export, and message shapes remain unchanged.

**Guards:** the requested real-provider injection test was written red first and failed with zero fake-handler calls, then passed unchanged. A static displaced-authority guard prevents the provider method, discriminator case, and file helpers from returning. Direct handler coverage proves synchronous decline, empty-data UX, picker cancellation, exact local/remote URI publication, UTF-8 bytes, extension handling, Open File / Show in Folder actions, notification/action failure containment, write-failure messaging, and disposal while the picker is open. The provider lifecycle test proves panel disposal retires the handler.

**Qualification:** the focused eight-file HST ring passed 128 tests and the provider lifecycle suite passed 101 tests. Complete sequential Vitest and the official coverage gate passed 225 files and 5,736 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`; a preceding npm-wrapper attempt passed the timeout as a positional argument and hit the known two-second close-bound test at 200/201. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,908.5 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines and the 50 KB buffer are unchanged. Native run `20260805-211100` passed 1/1 against the reviewed build with the real imported-table Save button and Windows picker, an exact 36-byte UTF-8 artifact, and a foreground-valid `1280x1000` screenshot showing the saved notification plus Open File / Show in Folder. Two earlier harness attempts produced correct product output but failed on literal Unicode-escape matching and the documented controller notification-capture limitation; the final scenario uses exact bytes plus visual evidence. Logs contain only existing Git/CSP/Mermaid/Node noise.

### Iteration `HST-5`: Query Sharing Application Handler

**Status:** closed on 2026-08-05. The definitive blocker-only review returned `VERDICT: NO HST-5 BLOCKER`.

**Boundary:** `HostQuerySharingApplicationHandler` is the injected owner for `copyAdeLink` and `shareToClipboard`, including input validation, Kusto gzip/base64 ADX-link generation, SQL shares without an ADX-link promise, exact HTML/plain-text formatting and escaping, host clipboard writes, `shareContentReady`, notifications, and disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers typed messages, supplies connection lookup and panel transport, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer imports zlib or ADX-link export helpers, owns either sharing discriminator case/method, formats clipboard content, writes the host clipboard, publishes `shareContentReady`, or emits sharing notifications. The webview share modal, singleton `share:clipboard:result` binding and policy admission, browser clipboard write, row caps, Kusto/SQL execution and connection ownership, message shapes, and protocol remain unchanged.

**Guards:** the requested real-provider forwarding test was written red first and failed with zero fake-handler calls, then passed unchanged for both exact typed messages. A static guard prevents the methods, cases, zlib/link helper, and response publication from returning to the provider. Direct handler tests cover synchronous decline, missing/malformed inputs, exact gzip/base64 decoding, SQL no-link output, HTML/plain escaping, row summaries, empty selection/content, clipboard failure, notifications, and disposal/late completion. Protocol extraction inventories both handler responses.

**Qualification:** the expanded focused ring passed 12 files and 454 tests. Complete sequential Vitest and the official coverage gate passed 227 files and 5,749 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000` on unchanged rerun; the first run had one unrelated sidecar repair timeout plus a cascading setup failure, and both cases passed alone before the clean rerun. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, and both bundle gates passed. Final sizes are 1,909.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; only the synchronized extension baseline moved 1,859 -> 1,860 KB and the 50 KB buffer is unchanged. Definitive native run `20260805-231107` passed 2/2 `share-result-artifacts` scenarios: exact rich clipboard JSON retained B and excluded denied/revoked rows, the real host clipboard contained the expected ADX prefix, and a reviewed foreground-valid `1280x1000` screenshot showed the exact success toast. A preceding identity-checklist attempt stopped before clipboard on unrelated Connection Manager behavior; a later controller notification assertion missed the visibly present toast after exact clipboard success.

### Iteration `HST-6`: URL Content Acquisition Application Handler

**Status:** closed on 2026-08-05. The definitive blocker-only review returned `VERDICT: NO HST-6 BLOCKER`.

**Why next:** HST remains the highest eligible gap. `fetchUrl` is one cohesive provider-owned acquisition workflow: URL validation, redirect-following fetch, timeout/abort, byte limits, content classification, UTF-8/image shaping, and exact `urlContent` / `urlError` publication. The URL component already owns request identity, stale-response rejection, rendering, CSV artifact publication, debounce, and teardown.

**Boundary:** extract only `fetchUrl` orchestration into one injected application handler. Preserve the existing 15-second timeout, 100 MB text/CSV and 5 MB image caps, 200,000-character truncation, redirect URL, content sniffing/classification, exact response identity, and message shapes. Keep `kw-url-section`, request admission/stale rejection, rendering, URL artifacts, imported CSV saving, browser behavior, `resolveResourceUri`, and control-command syntax lookup unchanged.

**Falsifiable hypothesis:** if one URL-content handler owns fetch validation, deadlines, byte acquisition/classification, and response publication, `QueryEditorProvider` can synchronously offer `fetchUrl` without retaining URL-content decisions while the current request-correlated webview behavior remains unchanged.

**Cheapest discriminating check:** construct the real provider with a fake URL-content handler and prove exact forwarding of `fetchUrl`; then drive the handler through invalid/non-HTTP URLs, redirects, HTTP failures, CSV/HTML/text/image classification, body sniffing, truncation, byte caps, timeout/abort, fetch failure, unrelated-message decline, and disposal/late completion.

**Exclusions:** no URL-section or artifact redesign, imported CSV changes, browser-host changes, `resolveResourceUri` or control-command migration, or generic handler framework.

**Boundary:** `HostUrlContentApplicationHandler` is the injected owner for `fetchUrl`, including HTTP/HTTPS validation, redirect-following fetch, the 15-second timeout and abort lifecycle, 100 MB text/CSV and 5 MB image caps, 200,000-character truncation, CSV/HTML/text/image classification and HTML body sniffing, exact original/resolved URL identity, `urlContent` / `urlError` publication, and disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers typed messages, supplies panel transport, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns the `fetchUrl` discriminator case or method, creates URL fetch abort controllers, applies byte/truncation limits, classifies content, or constructs URL terminals. `kw-url-section` request identity, stale rejection, rendering, immutable CSV artifacts, debounce, autosizing, teardown, imported CSV saving, browser behavior, `resolveResourceUri`, control-command syntax lookup, and message shapes remain unchanged.

**Guards:** the requested real-provider forwarding test was written red first and failed with zero fake-handler calls, then passed unchanged after injection. A static guard prevents URL fetch, abort, cap, classification, and terminal authority from returning to the provider. Direct tests cover synchronous decline, invalid/non-HTTP input, exact original/resolved identity, redirects, HTTP and transport failures, CSV/HTML/text/image classification, HTML body sniffing, image data URIs, truncation, both caps, timeout/abort, disposal with signal-ignoring fetch, disposal abort rejection, and late-publication suppression. Protocol extraction attributes both URL terminals to the handler.

**Qualification:** the definitive seven-file URL ring passed 223 tests. Complete sequential Vitest passed 229 files and 5,767 tests before the review-added disposal regression; the official sequential coverage gate passed the final 229 files and 5,768 tests at 48.18% statements against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,910.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; only the synchronized extension baseline moved 1,860 -> 1,861 KB and the 50 KB buffer is unchanged. Native E2E was not required for this host-only extraction because the webview runtime and message shapes did not change. The blocker-only reviewer returned `VERDICT: NO HST-6 BLOCKER`; both non-blocking follow-ups were addressed.

### Iteration `HST-7`: Control-Command Syntax Lookup Application Handler

**Status:** closed on 2026-08-06. The definitive blocker-only review returned `VERDICT: NO HST-7 BLOCKER`.

**Boundary:** `HostControlCommandSyntaxApplicationHandler` is the injected owner for `fetchControlCommandSyntax`, including the exact 24-hour cache boundary, Microsoft Learn URL normalization/fetch, Syntax-section and first-`pre` fallback extraction, HTML entity decoding, case-insensitive `with(...)` argument de-duplication, existing failure-cache behavior, exact request/command identity, `controlCommandSyntaxResult` publication, and disposal/late-settlement suppression. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers typed messages, supplies panel transport, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns `controlCommandSyntaxCache`, `CONTROL_COMMAND_SYNTAX_CACHE_TTL_MS`, the `fetchControlCommandSyntax` discriminator case, parsing helpers, Learn fetch, failure shaping, or syntax-result construction. Caret-docs request de-duplication, presentation caching/rendering, Kusto control-command grammar/detection/execution, generated command data, browser behavior, `resolveResourceUri`, URL content acquisition, and message shapes remain unchanged.

**Guards:** the requested real-provider forwarding test was written red first, failed with zero fake-handler calls while the old provider attempted a Learn fetch, and passed unchanged after injection. A static source guard prevents every displaced cache/parser/fetch/result authority from returning. Direct tests cover synchronous decline, invalid request shaping, relative and absolute Learn URL normalization, existing `view` replacement, Syntax-section preference, first-`pre` fallback, entity decoding, `with(...)` parsing/de-duplication, exact cache hit/expiry and failure-cache behavior, fetch failure, disposal, and late fetch/text resolve/reject suppression. Protocol extraction attributes `controlCommandSyntaxResult` to the handler, and provider lifecycle coverage requires disposal.

**Qualification:** the definitive five-file HST-7 ring passed 321 tests, and the expanded 17-file host-application ring passed 424 tests. Complete sequential Vitest and the official coverage gate passed 231 files and 5,782 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, `git diff --check`, and the bundle gate passed. Final production sizes are 1,910.9 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`, within the existing 1,911/2,802 KB limits and unchanged 50 KB buffer. Native E2E was not required because this was a host-only ownership extraction and the caret-docs runtime/message shapes remained unchanged.

### Iteration `HST-8`: Local Resource URI Resolution Application Handler

**Status:** closed on 2026-08-06. The definitive blocker-only review returned `VERDICT: NO HST-8 BLOCKER`.

**Boundary:** `HostResourceUriApplicationHandler` is the injected owner for `resolveResourceUri`, including exact request shaping, HTTP/HTTPS/data/blob/webview passthrough, local-file-only base validation, Markdown backslash normalization, workspace-root and relative/absolute path construction, file stat, exact `${baseUri.toString()}::${rawPath}` caching, live-panel webview URI conversion, exact failure strings, `resolveResourceUriResult` publication, and disposal/late-settlement suppression. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers typed messages, supplies panel transport plus the live conversion capability, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns `resolvedResourceUriCache`, the `resolveResourceUri` discriminator case or method, the `path` import, path/base/stat/cache decisions, failure strings, or result construction. Markdown request de-duplication, timeout and rendering, browser behavior, URL acquisition, navigation, linked queries, and message shapes remain unchanged.

**Guards:** the requested real-provider forwarding test was written red first, failed with zero fake-handler calls, and passed unchanged after injection. A static source guard prevents every displaced cache/path/stat/result authority from returning. Direct tests cover synchronous decline, missing identity, exact empty/base/path/conversion failures, every passthrough scheme, workspace-root and relative paths, Windows drive/UNC and POSIX behavior, stat-before-panel ordering, exact raw-path cache identity and cache hits, response-transport containment, disposal/cache clearing, and late stat/conversion resolve/reject suppression. Protocol extraction attributes `resolveResourceUriResult` to the handler, and provider lifecycle coverage requires disposal.

**Qualification:** the expanded six-file HST-8/Markdown ring passed 378 tests. Complete sequential Vitest and the official coverage gate passed 233 files and 5,816 tests; statement coverage is 48.18% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,911.9 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; only the synchronized extension baseline moved 1,861 -> 1,862 KB and the 50 KB buffer is unchanged. Native E2E was not required because this was a host-only ownership extraction and Markdown/browser runtime plus message shapes remained unchanged.

### Iteration `HST-9`: Copilot Content Open Application Handler

**Status:** closed on 2026-08-06. The definitive documentation-aware blocker review returned `VERDICT: NO HST-9 BLOCKER`.

**Boundary:** `HostCopilotContentOpenApplicationHandler` is the injected owner for `openToolResultInEditor` and `openMarkdownPreview`, including existing tool/content coercion, untitled `plaintext` document creation, `preview: true` and `ViewColumn.Beside`, exact local Markdown file URI construction, `markdown.showPreview`, user-facing failure prefixes, and disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers each original typed message object, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns either discriminator case or method and cannot create/show the untitled document, construct the Markdown URI, execute the preview command, or emit either failure notification. Copilot generation, conversations, tools, controller presentation intent, browser behavior, generic navigation, message shapes, and Kusto/SQL execution remain unchanged.

**Guards:** the requested real-provider forwarding test was written red first and failed with zero fake-handler calls, then passed unchanged after injection and now proves reference-identical forwarding while every provider-native open API remains untouched. A static source guard prevents all displaced effects from returning. Direct tests cover exact coercion and ignored metadata, untitled plaintext/beside options, exact URI/command dispatch, both failure prefixes, safe non-Error conversion, synchronous Kusto/SQL decline, idempotent disposal, suppression of later requests, and accepted success/failure effects across disposal. The unchanged Copilot controller emitters and response-free protocol inventory are explicit tests, and provider lifecycle coverage requires handler disposal.

**Qualification:** the final five-file HST-9 ring passed 154 tests. Complete sequential Vitest and the official coverage gate passed 235 files and 5,834 tests; statement coverage is 48.19% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,912.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; only the synchronized extension baseline moved 1,862 -> 1,863 KB and the 50 KB buffer is unchanged. Native E2E was not required because this was a host-only ownership extraction and both message shapes plus controller emitters remained unchanged. The definitive documentation-aware reviewer returned `VERDICT: NO HST-9 BLOCKER`.

### Iteration `HST-10`: Host Information Notification Application Handler

**Status:** closed on 2026-08-06. The implementation blocker review returned `VERDICT: NO HST-10 IMPLEMENTATION BLOCKER`.

**Boundary:** `HostInformationNotificationApplicationHandler` is the injected response-free owner for `showInfo`. It receives the original typed message, calls `vscode.window.showInformationMessage(message.message)` exactly once with exact whitespace, discards the returned thenable, preserves synchronous native throws, declines unrelated traffic synchronously, and owns idempotent disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the original object, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns the `showInfo` discriminator case or directly invokes `showInformationMessage`. Cached Values, Agent Chat, every notification emitter, message shapes, browser behavior, and Kusto/SQL lifecycle remain unchanged.

**Guards:** the requested real-provider test was written red first and failed with zero fake-handler calls, then passed unchanged after injection and proves reference-identical forwarding while the provider-native notification API remains untouched. Direct tests cover synchronous Kusto/SQL decline, exact whitespace, exactly-once invocation, a never-settling returned thenable, synchronous native throws, accepted notification behavior across disposal, idempotent disposal, and suppression of later requests. Static displaced-authority, response-free protocol inventory, and provider lifecycle disposal are explicit guards.

**Qualification:** the final four-file HST-10 ring passed 146 tests. Complete sequential Vitest and the official coverage gate passed 237 files and 5,844 tests; statement coverage is 48.19% against the unchanged 27.67% threshold. The first full run had one unrelated `proper-lockfile` release race in Kusto Leave No Trace recovery; that file passed unchanged at 8/8 before the clean complete rerun. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,912.6 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; existing synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because this was a host-only ownership extraction with unchanged emitters and message shapes.

### Iteration `HST-11`: Cached Values Open Application Handler

**Status:** closed on 2026-08-06. The blocker-only implementation review returned `VERDICT: NO HST-11 IMPLEMENTATION BLOCKER`.

**Boundary:** `HostCachedValuesOpenApplicationHandler` is the injected response-free owner for `seeCachedValues`. It receives the original typed object, invokes exactly `vscode.commands.executeCommand('kusto.seeCachedValues')` with zero arguments, awaits settlement, discards the resolved value, propagates the exact rejection, declines unrelated traffic synchronously, and owns idempotent disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the original message, awaits claimed work, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns the `seeCachedValues` discriminator or knows the command ID. `extension.ts` remains the first-launch command-registration owner and `CachedValuesViewerV2` remains the viewer singleton/lifecycle owner. The handler does not import or open the viewer. Cache/schema/auth/privacy, Agent Chat, editing preferences, emitters, message shapes, protocol, browser, document, and Kusto/SQL lifecycle remain unchanged.

**Guards:** the requested real-provider test was written red first without importing a future handler module and failed exactly with zero fake-handler calls, then passed unchanged after injection with reference-identical forwarding and no provider-native command invocation. Direct tests cover synchronous Kusto/SQL decline, exactly one zero-argument dispatch, pending completion, resolved-value discard, exact asynchronous and synchronous failure propagation, accepted resolve/reject across disposal, idempotent disposal, and suppression of later requests. Static displaced-authority, command-registration/viewer ownership, response-free protocol inventory, and provider lifecycle disposal are explicit guards.

**Qualification:** the final four-file focused ring passed 149 tests and the adjacent first-launch/viewer ring passed 208 tests. Complete sequential Vitest and the official coverage gate passed 239 files and 5,856 tests; statement coverage is 48.19% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,913.0 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,863/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because this was a host-only ownership extraction with unchanged command registration, viewer behavior, emitter, and message shape.

### Iteration `HST-12`: Copilot Agent Open Application Handler

**Status:** closed on 2026-08-06. The definitive blocker-only implementation review returned `VERDICT: NO HST-12 IMPLEMENTATION BLOCKER`.

**Boundary:** `HostCopilotAgentOpenApplicationHandler` is the injected response-free owner for `openCopilotAgent`. It receives the original typed object, invokes `openKustoWorkbenchAgentChat()` exactly once with zero arguments, awaits settlement, discards the resolved boolean, propagates the exact rejection, declines unrelated traffic synchronously, and owns idempotent disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the original message, awaits claimed work, and disposes it.

**Displaced authority:** `QueryEditorProvider` no longer owns the `openCopilotAgent` discriminator or imports/invokes `openKustoWorkbenchAgentChat`. The shared helper retains its first `workbench.action.chat.open`, 150 ms Kusto Workbench mode reapplication, optional query/submit shaping, and fallback behavior. Existing extension, dashboard, and Copilot callers remain unchanged. Copilot generation, conversations, tools, first-time UX, emitters, message shapes, browser behavior, document lifecycle, and Kusto/SQL lifecycle remain outside this handler.

**Guards:** the required real-provider test was written red first without importing a future handler module and failed exactly with zero fake-handler calls, then passed unchanged after injection with reference-identical forwarding and no direct shared-helper call. Strengthened provider tests prove the final constructor slot, awaited deferred settlement, and exact rejection adoption. Direct tests cover synchronous Kusto/SQL decline, exactly one zero-argument helper call, resolved-value discard, exact asynchronous and synchronous failure propagation, accepted resolve/reject across disposal, idempotent disposal, and suppression of later requests. Behavioral helper tests pin both native commands, exact 150 ms reapplication, mode, query/submit shape, and fallback. Static displaced-authority/caller/emitter, response-free protocol inventory, and provider lifecycle disposal are explicit guards.

**Qualification:** the final six-file focused/adjacent ring passed 226 tests. Complete sequential Vitest and the official coverage gate passed 242 files and 5,873 tests; statement coverage is 48.19% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and both bundle gates passed. Final production sizes are 1,913.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,863 -> 1,864 KB while the 50 KB buffer remained unchanged. Native E2E was not required because this was a host-only ownership extraction with unchanged helper behavior, helper callers, emitter, and message shape.

### Iteration `HST-13`: Editor Cursor Status Application Handler

**Status:** closed on 2026-08-06. The definitive blocker review returned `VERDICT: NO HST-13 BLOCKER`.

**Boundary:** `HostEditorCursorStatusApplicationHandler` is the injected owner for `editorCursorPositionChanged`, development-only `getEditorCursorStatusSnapshot`, panel-hidden clearing, panel-disposal clearing, one unique provider owner prefix, trimmed box/editor identity, visible-panel admission, exact snapshot correlation, hidden fallback, failure-contained awaited transport, and idempotent disposal. `QueryEditorProvider` only injects, forwards exact objects and visibility, awaits snapshot work, and disposes the handler. `EditorCursorStatusBar` retains shared item, formatting, accessibility, stale-owner, and global-disposal ownership.

**Guards:** the required real-provider test was written red first and failed with zero fake-handler calls, then passed unchanged with both exact objects forwarded while the status adapter and provider transport remained untouched. Direct tests cover synchronous decline, hidden/visible updates, trimmed and fallback owner IDs, unique prefixes, stale-owner protection, development/Production snapshots, exact request/snapshot identity, pending transport, thrown/rejected transport, hidden fallback, hide/disposal clearing, idempotent disposal, and no shared-item disposal. Static displaced-authority, unchanged emitter/protocol, sender-inventory, and provider-lifecycle guards are explicit.

**Qualification:** the seven-file focused ring passed 169 tests. Complete sequential Vitest and the official coverage gate passed 244 files and 5,883 tests; statement coverage is 48.19% against the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the blocker review passed. Final production sizes are 1,914.0 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,864 -> 1,865 KB with the 50 KB buffer unchanged. Native E2E was not required because status formatting, shared item ownership, emitters, and message shapes remained unchanged.

### Iteration `HST-14`: Editing Preferences Application Handler

**Status:** closed on 2026-08-06. The definitive blocker review returned `VERDICT: NO HST-14 BLOCKER`.

**Boundary at HST-14 closure:** `HostEditingPreferencesApplicationHandler` became the injected owner for `setCaretDocsEnabled`, `setAutoTriggerAutocompleteEnabled`, and `setCopilotInlineCompletionsEnabled`, exact discriminator-to-`STORAGE_KEYS` mapping, one awaited `setEditingPreference`, authoritative-object publication, live all-webview broadcast with awaited current-panel fallback, exact rejection, synchronous decline, and idempotent disposal. PRO-19 subsequently replaced truthiness normalization and duplicated wire shapes with shared exact-boolean runtime admission while preserving this application owner. `QueryEditorProvider` only injects, forwards each original typed object, awaits claimed work, and disposes the handler. Accepted work settles across disposal; later requests are claimed and suppressed.

**Preserved owners:** `editingPreferences.ts` remains the application-configuration persistence, revision, snapshot, and migration owner. `extension.ts` retains configuration-change refresh, first-launch remains transactional and separate, and initial preferences still project through `connectionsData`. Existing emitters, message unions, toolbar optimistic state, application scope, revision admission, and cross-window behavior are unchanged.

**Guards:** the required real-provider test was written red first and failed with zero fake-handler calls, then passed unchanged with all three reference-identical messages while mutation, orchestrator broadcast, and panel transport remained untouched. Direct tests originally covered key mapping and normalization; PRO-19 now covers malformed-request rejection, exact booleans, exactly-one mutation, validated authoritative payload identity, live publisher lookup, current-panel fallback, pending settlement, exact failures, synchronous decline, disposal-crossing settlement, suppression, and idempotence. Static guards pin displaced authority, shared protocol ownership, unchanged persistence/configuration/initial-projection owners, and provider disposal. The protocol extractor discovers option-backed handler transports plus every `postToAllWebviews` call and inventories both preference publication sites.

**Qualification:** the final 10-file focused ring passed 553 tests and the adjacent five-file first-launch/preference ring passed 42 tests. Complete sequential Vitest and the official coverage gate passed 246 files and 5,893 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,914.4 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because application scope, configuration listeners, initial projection, emitters, webview application, and message shapes remained unchanged.

### Iteration `HST-15`: Kusto Connection Intake Application Handler

**Status:** closed on 2026-08-06. The blocker review returned `VERDICT: NO HST-15 BLOCKER`.

**Boundary:** `HostKustoConnectionIntakeApplicationHandler` is the injected owner for `addConnectionsForClusters`, `promptImportConnectionsXml`, and `importConnectionsFromXml`, including exact short-cluster and cluster-plus-authority deduplication, HTTPS/default-name/authority normalization, blank/malformed/failing-entry behavior, sequential additions, `%LOCALAPPDATA%\Kusto.Explorer` picker defaults and options, UTF-8 delivery, exact import notifications, post-mutation refresh ordering, synchronous decline, and disposal. Accepted add/import requests await one refresh after intake settles, including no-op input; rejected discovered-cluster mutation does not refresh, and picker traffic never refreshes. Accepted settlement survives disposal and later related traffic is suppressed.

**Preserved owners:** `ConnectionManager` remains the persistent connection owner. HST-40 retains the revisioned `connectionsData` snapshot/tail as the injected refresh effect. Webview XML parsing, all message shapes, favorites, single-connection onboarding/testing, auth lifecycle, databases/schemas, selection persistence, SQL, browser, and document behavior remain unchanged.

**Guards and qualification:** the required real-provider test was red first with zero fake-handler calls, then passed reference-identical forwarding and exact settlement for all three messages while legacy intake, provider refresh, and provider transport remained untouched. Direct and adjacent tests cover deduplication, normalization, sequential additions/imports, no-op and rejected refresh behavior, picker defaults/options/cancellation, UTF-8 text/error objects, fire-and-forget notifications, disposal crossing, unchanged parser/emitters, protocol sender inventory, displaced authority, provider disposal, and synchronized bundle baselines. The final post-documentation focused/adjacent ring passed 20 files and 703 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate passed 248 files and 5,909 tests at 48.19% statements. Host/webview typechecks, integration compilation, the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,915.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,865 -> 1,866 KB while the 50 KB buffer remained unchanged. Native E2E was not required because picker options, response objects, XML parsing, emitters, and message shapes remained unchanged.

### Iteration `HST-16`: Kusto Single-Connection Onboarding Application Handler

**Status:** closed on 2026-08-07. The blocker review returned `VERDICT: NO HST-16 BLOCKER`.

**Boundary:** `HostKustoConnectionOnboardingApplicationHandler` is the injected owner for `promptAddConnection`, `addConnection`, and `testKustoConnection`, including exact dialog identity, add validation and normalization, explicit-account resolution/fallback and persistence, last-selection and revisioned-refresh ordering, draft identity, transient authentication preferences, interactive non-persisting database discovery, database normalization, tracing, exact terminals, synchronous decline, and disposal. Accepted settlement survives disposal and later related traffic is suppressed.

**Preserved owners:** `ConnectionManager` retains persistent connections, `KustoAuthPreferenceService` retains account preferences, `KustoQueryClient` retains draft authentication and database discovery, `ConnectionService` retains selection storage, and HST-40 retains revisioned `connectionsData` publication as an injected effect. Webview dialog controls, response application, emitters, message shapes, favorites, bulk/XML intake, browsing, execution, SQL, browser, and document behavior remain unchanged.

**Guards and qualification:** the required real-provider test was red first and failed with zero fake-handler calls, then passed unchanged with reference-identical forwarding, no direct service/refresh/transport/auth/client effects, deferred settlement, and exact rejection. Direct tests cover prompt identity, every add validation/account/persistence/selection/refresh branch and ordering, all draft-test terminals, transient and fallback paths, trace lifecycle, normalized databases, accepted success/failure settlement across disposal, later suppression, and idempotence. Static guards prevent the three cases and methods from returning, preserve external owners and emitters, inventory all handler responses, and require provider disposal. The definitive focused and adjacent ring passed 10 files and 519 tests. Complete sequential Vitest and the official coverage gate passed 250 files and 5,931 tests at 48.18% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, and both bundle gates passed. Final sizes are 1,916.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,866 -> 1,867 KB while the 50 KB buffer remained unchanged. Native E2E was not required because dialog controls, message shapes, response application, selection storage, auth/client adapters, and tracing were unchanged.

### Iteration `HST-17`: SQL Single-Connection Onboarding Application Handler

**Status:** closed on 2026-08-07. The blocker review returned `VERDICT: NO HST-17 BLOCKER`.

**Boundary:** `HostSqlConnectionOnboardingApplicationHandler` is the injected owner for `promptAddSqlConnection` and `addSqlConnection`, including exact SQL Server address/authentication/username/password/optional-name prompts and cancellation, AAD and SQL Login shaping, trimming/defaults, direct-form optional fields, separate password delivery, manager persistence, `sql.lastConnectionId` ordering, exact `sqlConnectionAdded`, synchronous decline, and disposal. Accepted settlement survives disposal and later related traffic is suppressed.

**Preserved owners:** `SqlConnectionManager` retains persistent connections and SecretStorage. `HostSqlConnectionsProjectionApplicationHandler` retains revisioned policy/principal-aware `sqlConnectionsData`; `SqlWorkbenchService`, `SqlEditorLifecycleCoordinator`, webview controls, response application, emitters, and message shapes remain unchanged.

**Guards and qualification:** the requested real-provider test was red first, initially exposed and corrected a missing test-shim `showQuickPick`, then recorded the intended zero fake-handler calls before implementation and passed unchanged after extraction. Direct tests cover every cancellation branch, AAD/SQL Login prompts, credential separation, direct-form defaults, manager-selection-acknowledgement ordering, exact failures, fire-and-forget transport settlement, disposal crossing, later suppression, and idempotence. Static guards delete both provider cases/methods, preserve external owners and emitters, inventory `sqlConnectionAdded`, and require provider disposal. The final focused and adjacent ring passed 6 files and 414 tests. Complete sequential Vitest and the official coverage gate passed 252 files and 5,951 tests at 48.18% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final sizes are 1,916.7 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,867 -> 1,868 KB while the 50 KB buffer remained unchanged. Native E2E was not required because prompt controls, emitters, response application, manager/SecretStorage behavior, and message shapes were unchanged.

### Iteration `HST-18`: SQL Favorites Application Handler

**Status:** closed on 2026-08-07.

**Boundary:** `HostSqlFavoritesApplicationHandler` is the injected owner for `requestAddSqlFavorite` and `removeSqlFavorite`, including exact prompt/default/cancellation behavior, sanitized `sql.favorites` reads, case-insensitive database identity, replacement order and submitted spelling, append/removal persistence, storage-before-publication ordering, add-only originating `boxId`, exact contained warning behavior, synchronous decline, projection reads, and disposal. Accepted settlement survives disposal and later related traffic is suppressed.

**Preserved owners:** Query Editor controls, emitters, response application, and message shapes remain unchanged. `HostSqlConnectionsProjectionApplicationHandler` retains revisioned policy/principal-aware `sqlConnectionsData` and filters the handler's sanitized projection under the existing owner snapshot. Connection Manager SQL-favorites routes retain their own UI workflow over the shared pure helpers. Kusto favorites remain a separate workflow.

**Guards and qualification:** the requested real-provider test was red first with exactly zero fake-handler calls, then passed unchanged with ordered reference-identical forwarding and exact settlement. Direct tests cover malformed storage, every prompt fallback and cancellation, case-insensitive duplicate replacement with submitted database spelling, append/removal order, unchanged-removal persistence, storage/publication ordering, add-only `boxId`, exact failures, warning/logger containment, disposal crossing, and later suppression. Static guards delete both provider cases and every displaced `ConnectionService` SQL-favorites method, preserve provider projection, Connection Manager routes, shared helpers, emitters, response handling, sender inventory, and provider disposal. The final focused ring passed 7 files and 403 tests. Complete sequential Vitest and the official coverage gate passed 254 files and 5,975 tests at 48.23% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final sizes are 1,916.6 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because prompt controls, storage/message shapes, emitters, response application, and Connection Manager behavior were unchanged.

### Iteration `HST-19`: Kusto Favorites Application Handler

**Status:** closed on 2026-08-07.

**Boundary:** `HostKustoFavoritesApplicationHandler` is the injected owner for `requestAddFavorite`, `removeFavorite`, and `confirmRemoveFavorite`, including exact prompt/confirmation UX, normalized inputs, migration and active-principal filtering, per-connection partition-resolution isolation, connection-presence and account-partition revalidation, exact trimmed connection-ID plus case-insensitive database replacement/removal, hidden-principal merge, unresolved-entry fail-closed persistence, shared-storage live-handler/listener broadcasts, add-only originating `boxId`, exact contained failures, activation, and disposal. Accepted work settles across disposal; later recognized traffic is claimed and suppressed until reactivation.

**Preserved owners:** HST-40 retains revisioned Kusto `connectionsData` orchestration and reads favorites through the handler. `ConnectionService` retains selection, cached databases, database discovery, and schema-inference orchestration, with favorite weighting supplied by the same read-only handler projection. Connection Manager Kusto-favorites routes remain an independent unchanged workflow over the shared pure helpers. SQL favorites, connection listing/selection, schemas, execution, Copilot, webview controls, emitters, response handling, and message shapes remain unchanged.

**Guards and qualification:** the requested real-provider test was written red first without importing the future module and recorded exactly zero fake-handler calls before production changes. It then passed unchanged with ordered reference-identical forwarding, deferred settlement, and exact rejection. Direct tests cover migration, malformed/unresolved storage, principal filtering, fallback and failing partition resolution, every prompt and confirmation branch, prompt-time and pre-write owner revalidation, exact connection-ID and case-insensitive database keying/order/casing, hidden-principal merge, storage failures, all-live-panel and listener broadcasts, add-only originating `boxId`, exact warning containment, reactivation, disposal crossing, and the complete 34-case cross-file/provider permutation matrix. Static guards delete all three provider cases and every displaced `ConnectionService` authority while preserving projection/inference consumers, external callers, Connection Manager routes, emitters, response handlers, message shapes, sender inventory, and provider lifecycle.

The final focused ring passed 9 files and 596 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate passed 256 files and 6,013 tests; statement coverage is 48.20%. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, and both bundle gates passed. Final production sizes are 1,918.0 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because prompt controls, emitters, response application, message shapes, and Connection Manager routes were unchanged.

**Explicit residual boundary:** Connection Manager remains a separate pre-existing writer with its unchanged migration/reorder/delete behavior. HST-19 does not claim to migrate or harden those routes; broadening into them would violate the selected slice. Query Editor mutations fail closed when their own migration has unresolved entries.

### Iteration `HST-20`: SQL Database Discovery Application Handler

**Status:** closed on 2026-08-07. The definitive blocker review returned `VERDICT: NO HST-20 BLOCKER`.

**Boundary:** `HostSqlDatabaseDiscoveryApplicationHandler` is the injected owner for `getSqlDatabases` and `refreshSqlDatabases`, including synchronous target adoption, passive/refresh mode, exact loading/data/error sequencing, durable terminal retry and ticket completion, canonical target/principal/privacy admission, protected one-operation STS acquisition/cleanup, sorted results, cache CAS/fallback, notifications, detail-free protected logging, and disposal. `QueryEditorProvider` only constructs/injects, offers exact typed objects, supplies transport, and disposes the handler.

**Preserved owners:** `SqlEditorLifecycleCoordinator` retains section identities, targets, generations, monotonic database request tickets, currentness, and exact completion. `SqlWorkbenchService` retains canonical principal and Leave No Trace dispatch; `sqlDatabaseCache.ts` retains cross-window cache/CAS; `SqlConnectionManager`, `SqlQueryClient`, `StsQueryService`, and protected `StsRuntime` retain physical acquisition and cleanup. Provider SQL connection snapshots/favorites, schema, execution, comparisons, STS language lifecycle, persistence, controls, emitters, response routing, and message shapes remain unchanged.

**Guards and qualification:** the required real-provider test recorded the intended zero fake-handler calls before production edits, then passed unchanged with reference-identical forwarding, deferred settlement, and exact rejection. Direct coverage proves target adoption, passive/refresh behavior, exact ticket/terminal identity, overlapping and concurrent sections, first-AAD establishment and rotation, canonical-file versus stale-mirror ownership, Leave No Trace transitions, protected cleanup and detail-free diagnostics, cache CAS/read/write/fallback, transport/admission retry, fixed data-free retirement, disposal, and every owner replacement race found by pessimistic review. Static guards delete both provider cases and all displaced discovery methods/helpers while preserving lifecycle/workbench/cache/client, connection snapshot/favorites/schema/execution/persistence/comparison, emitter/router/message-shape, sender, constructor-slot, and disposal owners.

The definitive focused ring passed 15 files and 405 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 258 files and 6,060 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,928.9 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,868 -> 1,879 KB with the 50 KB buffer unchanged. Native E2E was not required because controls, emitters, response routing, and message shapes were unchanged.

### Iteration `HST-21`: KQL Language Request Application Handler

**Status:** closed on 2026-08-07. The definitive blocker review returned `VERDICT: NO HST-21 BLOCKER`.

**Boundary at HST-21 closure:** `HostKqlLanguageRequestApplicationHandler` became the injected owner for `kqlLanguageRequest`, both language-host methods, response construction, existing fire-and-forget transport behavior, failure logging, and disposal. `QueryEditorProvider` only constructs/injects, reference-identically offers typed objects, awaits claimed work, supplies transport/logger capabilities, and disposes the handler. PRO-9 subsequently replaced permissive/coercive request and response admission with the shared fail-closed `kqlLanguageProtocol.ts` contract.

**Preserved owners:** `KqlLanguageServiceHost` retains selection fallback, cached-schema/auth-partition resolution, and analysis delegation. `KqlLanguageService` retains diagnostics/reference algorithms; `extension.ts` retains independent text-editor diagnostics; and the webview retains request timeout/resolver ownership and response application. Message shapes and the KQL language protocol are unchanged. Accepted analysis settles across disposal while late publication/logging and later recognized requests are suppressed.

**Guards and qualification at HST-21 closure:** the required test was written red first without importing the future module and recorded exactly zero fake-handler calls plus legacy resolved rejection behavior before production changes. It then passed unchanged with reference-identical forwarding, deferred settlement, exact rejection, and zero direct provider language-host/transport/logger effects. The then-current direct tests covered both methods, permissive runtime admission, response construction, transport/logger behavior, disposal crossing, and late suppression. PRO-9 replaced those permissive-admission assertions with shared-parser and malformed-traffic guards while retaining the HST-21 provider ownership boundary.

The final focused ring passed 4 files and 159 tests; the expanded ring passed 6 files and 388 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 260 files and 6,076 tests at 48.20% statements. The definitive VS Code 1.132.0 extension-host rerun passed 201/201 with `--timeout 5000`; the first run had two unrelated unchanged sidecar timing/lock failures, both passed alone before the clean rerun. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,929.4 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,879 -> 1,880 KB with the 50 KB buffer unchanged. Native E2E was not required because analyzer/schema/auth behavior, emitters, webview response routing, and message shapes were unchanged.

### Iteration `HST-22`: SQL Last-Selection Application Handler

**Status:** closed on 2026-08-08. The definitive blocker-only review returned `VERDICT: NO HST-22 BLOCKER`.

**Boundary:** `HostSqlLastSelectionApplicationHandler` is the injected response-free owner for `saveSqlLastSelection`, trimmed non-empty connection admission, ordered `sql.lastConnectionId` then optional `sql.lastDatabase` writes, exact `undefined` versus empty-database behavior, exact rejection, and disposal. `QueryEditorProvider` only constructs/injects it immediately after KQL language requests, reference-identically offers exact typed objects, awaits claimed work, and disposes it.

**Preserved owners:** `SqlSectionSessionController` retains target transitions and unchanged message emission. `HostSqlConnectionOnboardingApplicationHandler` retains its independent post-add connection-selection write. `HostSqlConnectionsProjectionApplicationHandler` retains revisioned policy/principal-aware `sqlConnectionsData` projection and reads both application-state keys. SQL lifecycle, connections/secrets, discovery, schema, execution, comparisons, persistence, Copilot, controls, response routing, and message shapes remain unchanged.

**Guards and qualification:** the required provider test was written red first without importing the future module and recorded the intended zero fake-handler calls plus legacy resolved rejection behavior before production edits. It then passed unchanged with reference-identical forwarding, deferred settlement, exact rejection, injected-slot identity, and zero direct provider `globalState.update` effects. Direct tests cover trimming/blank admission, strict write ordering, `undefined`/empty/exact database values, both rejection points, synchronous decline, disposal-crossing settlement, later suppression, and idempotence. Static guards delete the provider case/writes while preserving emitters, onboarding, projection reads, message shape, response-free sender inventory, constructor slot, and provider disposal.

The focused ring passed 4 files and 158 tests; the expanded SQL-owner ring passed 10 files and 279 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 262 files and 6,090 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and both bundle gates passed. Final sizes are 1,929.8 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines remain 1,880/2,752 KB with the 50 KB buffer unchanged. Native E2E was not required because selection controls, emitters, application-state keys, message shapes, projection reads, SQL lifecycle, and connection behavior were unchanged. The definitive blocker-only reviewer returned `VERDICT: NO HST-22 BLOCKER`.

### Iteration `HST-23`: Copilot Development-Note Mutation Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-23 BLOCKER`.

**Boundary:** `HostDevelopmentNoteMutationApplicationHandler` is the injected owner for `webviewMutationResponseResolvers`, development-note request-ID generation, the exact five-second timeout, `updateDevNotes` transport, unavailable/rejected/failed delivery results, matching `toolResponse` settlement, and disposal settlement. `QueryEditorProvider.updateDevelopmentNotes()` is only a thin delegate, matching responses are offered reference-identically, and every unclaimed `toolResponse` continues unchanged to `toolOrchestrator`.

**Preserved owners:** `CopilotService` retains add/supersede/remove semantics, note IDs/content/category shaping, tool-result copy, and conversation history. The webview retains `updateDevNotes` validation, compatibility/read-only admission, document mutation, persistence, and response construction. Agent tools retain their independent request/response path. Document ownership, Kusto/SQL execution, Copilot model lifecycle, comparison summaries, inline completion, protocol/browser behavior, and message shapes remain unchanged.

**Guards and qualification:** the requested real-provider test was written red first without importing the future module and failed with exactly zero fake-handler calls. It then passed unchanged with exact mutation delegation/settlement, reference-identical claimed-response offering, zero provider resolver-map effects, and unchanged unclaimed orchestrator delivery. Direct tests cover exact request/outbound identity, unavailable and all delivery failures, claimed success/error/false results, the exact five-second boundary, late responses, concurrent disposal settlement, later suppression, and idempotence. Static guards delete provider correlation authority while preserving Copilot/webview/tool/message owners; sender inventory and provider lifecycle require the handler.

The focused ring passed 4 files and 156 tests; the expanded Copilot/webview/codec ring passed 16 files and 950 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 264 files and 6,101 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, protocol sender inventory, and both bundle gates passed. Final sizes are 1,930.5 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,880 -> 1,881 KB while the 50 KB buffer remained unchanged. Native E2E was not required because note semantics, validation, persistence, emitters, response shapes, and tool orchestration were unchanged.

### Iteration `HST-24`: Copilot Inline-Completion Admission Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-24 BLOCKER`.

**Boundary:** `HostCopilotInlineCompletionApplicationHandler` is the injected owner for exact original-object `requestCopilotInlineCompletion` admission, Kusto versus SQL routing, SQL owner-token assertion through the existing lifecycle capability, SQL delegation with the exact issued owner/token, the existing empty `copilotInlineCompletionResult` fallback after SQL assertion or delegation failure, and disposal. `QueryEditorProvider` only constructs/injects it immediately after development-note mutation, reference-identically offers requests, awaits claimed work, supplies narrow callbacks, and disposes it.

**Preserved owners:** `CopilotService` retains request normalization, model selection/cache, KQL/T-SQL prompts, context trimming, eight-second cancellation, replacement, streaming, owner revalidation, cleanup, and normal result construction. SQL lifecycle/workbench services retain owner-token issuance/assertion semantics, canonical principal/privacy admission, and dispatch. The webview retains request IDs, the ten-second resolver, Monaco spinner/application, cancellation policy, editing-preference admission, emitters, response routing, and message shapes.

**Guards and qualification:** the requested real-provider test was written before production edits without importing the future module and recorded 2/2 intended failures with zero fake-handler calls. Implementation validation exposed one extra positional placeholder in the new harness; removing it placed the structural fake in the required constructor slot without changing the production boundary being tested. The passing contract proves reference-identical Kusto/SQL forwarding, deferred settlement, exact rejection, injected-slot identity, and zero direct provider owner-token/Copilot/fallback effects. Direct tests cover synchronous decline, exact Kusto delegation/rejection, SQL assertion ordering, exact issued owner/token identity, both fallback causes, unawaited fallback transport, disposal-crossing settlement, later suppression, and idempotence. Static guards preserve Copilot model/cache/prompt/cancellation/result ownership, SQL token authority, webview resolver/spinner/preferences/message shapes, sender inventory, and provider disposal.

The focused ring passed 4 files and 158 tests; the expanded Copilot/SQL/webview ring passed 13 files and 614 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 266 files and 6,114 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, protocol sender inventory, both bundle gates, and the blocker-only review passed. Final sizes are 1,931.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,881 -> 1,882 KB while the 50 KB buffer remained unchanged. Native E2E was not required because model behavior, SQL owner/privacy semantics, webview resolution/application, preferences, emitters, and message shapes were unchanged.

### Iteration `HST-25`: Copilot Availability Admission Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-25 BLOCKER`.

**Boundary:** `HostCopilotAvailabilityApplicationHandler` is the injected response-free owner for exact original-object `checkCopilotAvailability` admission, awaited delegation of the exact `boxId` to the existing Copilot capability, exact rejection, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. `QueryEditorProvider` only constructs/injects it immediately after inline completion, reference-identically offers requests, awaits claimed work, and disposes it.

**Preserved owners:** `CopilotService` retains `vscode.lm.selectChatModels({ vendor: 'copilot' })`, model-present/no-model/failure-to-false behavior, and exact `copilotAvailability` construction/publication. Global startup plus Kusto and SQL section callers, response routing/application, model lifecycle, conversations, inline completion, editing preferences, and message shapes remain unchanged.

**Guards and qualification:** the requested provider test was written red first without importing the future module and recorded 2/2 intended failures with zero fake-handler calls plus legacy resolved rejection behavior. It then passed unchanged with reference-identical forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct provider Copilot/transport effects. Direct tests cover exact delegation, asynchronous/synchronous rejection, synchronous decline, disposal-crossing resolve/reject settlement, later suppression, and idempotence. Copilot-service regressions pin model discovery and exact true/false publication; static guards preserve all three callers, response application, message shapes, response-free sender inventory, and provider disposal.

The focused ring passed 4 files and 156 tests; the expanded ring passed 8 files and 505 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 268 files and 6,126 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, protocol sender inventory, both bundle gates, and blocker-only review passed. Final sizes are 1,931.6 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,882/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because model discovery, caller timing, response routing/application, and message shapes were unchanged.

### Iteration `HST-26`: Copilot Write-Query Preparation Admission Application Handler

**Status:** closed on 2026-08-08. The documentation-aware blocker review returned `VERDICT: NO HST-26 BLOCKER`.

**Boundary:** `HostCopilotWriteQueryPreparationApplicationHandler` is the injected response-free owner for exact original-object Kusto/SQL `prepareCopilotWriteQuery` admission, awaited delegation to the existing Copilot capability, exact rejection, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. `QueryEditorProvider` only constructs/injects it immediately after Copilot availability, reference-identically offers requests, awaits claimed work, and disposes it.

**Preserved owners:** `CopilotService` retains box-ID normalization, `vscode.lm.selectChatModels({ vendor: 'copilot' })`, ID filtering, model-label sorting, persisted preferred/default selection, Kusto/SQL local-tool selection, unavailable status, and exact `copilotWriteQueryOptions` / `copilotWriteQueryStatus` construction/publication. Kusto and SQL chat-manager callers, response routing/application, model execution, conversations/history, first-time UX, and message shapes remain unchanged.

**Guards and qualification:** the requested provider test was written red first without importing the future module and recorded 2/2 intended failures with zero structural-handler calls plus legacy resolved rejection behavior. It then passed unchanged with reference-identical Kusto/SQL forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct provider Copilot/transport effects. Direct tests cover exact-object delegation, asynchronous/synchronous rejection, synchronous decline, disposal-crossing resolve/reject settlement, later suppression, and idempotence. Copilot-service regressions pin normalization, filtering/sorting/labels, persisted and preferred-default selection, Kusto/SQL tools, unavailable status, and exact options/status publication; static guards preserve both callers, response application, message shapes, response-free sender inventory, and provider disposal.

The focused ring passed 4 files and 157 tests; the expanded ring passed 8 files and 503 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 270 files and 6,140 tests at 48.20% statements. The first full run had one unrelated unchanged SQL account-map watcher timing failure; that file passed 17/17 alone and the complete rerun passed unchanged. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, protocol sender inventory, both bundle gates, and the documentation-aware blocker review passed. Final sizes are 1,932.2 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; intentional host growth moved only the synchronized extension baseline from 1,882 to 1,883 KB with the 50 KB buffer unchanged. Native E2E was not required because preparation semantics, caller timing, response routing/application, and message shapes were unchanged.

### Iteration `HST-27`: Copilot Conversation-Clear Admission Application Handler

**Status:** closed on 2026-08-08. The documentation-aware blocker review returned `VERDICT: NO HST-27 BLOCKER`.

**Boundary:** `HostCopilotConversationClearApplicationHandler` is the injected response-free owner for exact original-object Kusto/SQL `clearCopilotConversation` admission, Kusto-versus-SQL branching, complete Kusto request-identity validation, awaitable delegation to the existing clear capabilities, exact rejection, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. `QueryEditorProvider` only constructs/injects it immediately after write-query preparation, reference-identically offers requests, awaits claimed work, and disposes it.

**Preserved owners:** `CopilotService.clearCopilotConversation()` retains box-ID normalization and deletion of general-rules, development-note, and conversation-history state. `CopilotService.clearKustoCopilotConversation()` retains exact active Kusto conversation-owner matching and owner deletion. Kusto and SQL chat-manager callers, conversation creation/history semantics, start/cancel execution, response routing, and message shapes remain unchanged.

**Guards and qualification:** the requested provider test was written red first without importing the future module and recorded 2/2 intended failures with zero structural-handler calls plus the legacy direct Kusto-path rejection. It then passed unchanged with reference-identical Kusto/SQL forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct provider Copilot/transport effects. Direct tests cover complete Kusto identity validation, exact Kusto-object and SQL-box delegation, metadata-free SQL compatibility, asynchronous/synchronous rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotence. Copilot-service regressions pin trimmed box normalization, deletion of general-rules/development-note/history state, neighboring-state retention, exact active Kusto owner matching, and owner deletion. Static guards preserve all five caller sites, start/cancel flow, message shapes, response-free sender inventory, and provider disposal.

The focused ownership ring passed 4 files and 160 tests; the expanded Copilot/provider/protocol/webview ring passed 10 files and 671 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 272 files and 6,153 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, response-free sender inventory, and both bundle gates passed. Final sizes are 1,932.8 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines remain 1,883/2,752 KB with the 50 KB buffer unchanged. Native E2E was not required because conversation-state semantics, callers, response routing, and message shapes were unchanged. The definitive reviewer found no correctness, ownership, identity, privacy, regression, test, or documentation blocker and returned `VERDICT: NO HST-27 BLOCKER`.

### Iteration `HST-28`: Copilot History-Removal Admission Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-28 BLOCKER`.

**Boundary:** `HostCopilotHistoryRemovalApplicationHandler` is the injected response-free owner for exact original-object `removeFromCopilotHistory` admission, awaited delegation of the exact unnormalized box/entry scope, exact rejection, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. `QueryEditorProvider` only constructs/injects it immediately after conversation clear, reference-identically offers requests, awaits claimed work, and disposes it.

**Preserved owners:** `CopilotService.removeFromCopilotHistory()` retains box/entry normalization, history lookup, eligible `tool-call` / `general-rules` entry decisions, and the `removed` mutation. `kw-copilot-chat` retains immediate local removal, and the shared Kusto/SQL chat manager retains exact box/entry emission. Conversation creation, clear semantics, first-time UX, start/cancel execution, optimization, response routing, and message shapes remain unchanged.

**Guards and qualification:** the requested provider test was written red first without importing the future module and recorded 2/2 intended failures with zero structural-handler calls plus the exact legacy direct-route rejection. It then passed unchanged with reference-identical Kusto/SQL-originated forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct provider Copilot/transport effects. Direct tests cover exact box/entry delegation, asynchronous/synchronous rejection, synchronous decline, disposal-crossing resolve/reject settlement, later suppression, and idempotence. Service/static guards pin normalization, eligible-entry mutation, neighboring-state retention, immediate local UI removal, one shared manager emitter, both flavor declarations, unchanged message shapes/workflows, response-free sender inventory, and provider disposal.

The focused ring passed 2 files and 9 tests; the expanded ring passed 10 files and 581 tests after its first run exposed only the line-sensitive reviewed sender allowlist; the final post-documentation/baseline ring passed 11 files and 590 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 274 files and 6,164 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, response-free sender inventory, and both bundle gates passed. Final sizes are 1,933.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; intentional host growth moved only the synchronized extension baseline from 1,883 to 1,884 KB with the 50 KB buffer unchanged. Native E2E was not required because service semantics, shared UI behavior, emitters, response routing, and message shapes were unchanged.

### Iteration `HST-29`: Copilot Chat First-Time Workflow Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-29 BLOCKER`.

**Boundary:** `HostCopilotChatFirstTimeApplicationHandler` is the injected owner for exact original-object `copilotChatFirstTimeCheck` admission, `copilotChatFirstTimeDismissed` read/write ordering, the exact modal copy/options, zero-argument Agent Chat helper delegation, exact `proceed` / `openedAgent` / `dismissed` result publication, current fire-and-forget transport settlement, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. `QueryEditorProvider` only constructs/injects it immediately after history removal, reference-identically offers requests, awaits claimed work, and disposes it.

**Preserved owners:** `STORAGE_KEYS` retains key definition; HST-40 `connectionsData` retains the initial read; the shared Kusto/SQL chat manager retains request emission; the webview retains local-flag and `proceed` application; and `copilotChatOpenUtils.ts` retains command, 150 ms timing, mode, query/submit shaping, and fallback behavior. Conversations, history, clear/removal, start/cancel execution, optimization, model behavior, and message shapes remain unchanged. `CopilotService` no longer imports the helper or owns the complete first-time workflow.

**Guards and qualification:** the requested provider test was red first without importing the future module and recorded 2/2 intended failures with zero structural-handler calls plus the exact legacy workflow rejection. It then passed with reference-identical forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct provider state/modal/helper/transport effects. Direct tests cover already-seen state, write-before-modal ordering, exact copy/options, all three choices, helper settlement/failure, result shaping, fire-and-forget transport, synchronous decline, and disposal. The focused ownership/protocol ring passed 5 files and 74 tests; the expanded complete journey ring passed 12 files and 508 tests with `--maxWorkers=1`. The first full attempt exposed only a missing HST-29 disposable in a prototype lifecycle harness and was terminated after its failed callback left timers live; that file passed 99/99 after repair, and the clean full and official coverage runs passed 276 files and 6,180 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, and both bundle gates passed. Final sizes are 1,933.7 KB host and 2,801.9 KB webview; synchronized 1,884/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because modal/state/helper/caller/router behavior and message shapes were preserved and directly covered.

### Iteration `HST-30`: Workbench Tool Session Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-30 BLOCKER`.

**Boundary:** `HostWorkbenchToolSessionApplicationHandler` is the injected owner for the panel orchestrator token, initial connection and visible-panel reactivation, guarded disconnect, exact five-second state correlation, state publication/settlement, schema-refresh query-section filtering and exact Kusto resolution, SQL connection/ready-owner callbacks, exact `toolExecutionStarted`, unclaimed `toolResponse`, and `toolStateResponse` routing, and disposal. `QueryEditorProvider` only constructs/injects immediately after HST-29, activates/disposes, reference-identically offers messages, and exposes a thin state delegate; persistence calls SQL lifecycle directly.

**Preserved owners:** `KustoWorkbenchToolOrchestrator` retains extension-scoped command targeting, command correlation, cancellation, and multi-editor selection. `SchemaService` retains schema acquisition/cache/publication. `SqlEditorLifecycleCoordinator` retains SQL section/comparison ownership and ready tool owners. `HostDevelopmentNoteMutationApplicationHandler` retains first claim on matching responses. Webview messages, execution, comparisons, persistence, connection/STS, protocol, and browser behavior remain unchanged.

**Guards and qualification:** the requested provider test was red first without importing the future module and recorded 2/2 intended failures with zero structural-handler calls while the legacy state map settled its own response. It then passed constructor-slot identity, reference-identical forwarding for all three routes, deferred settlement, HST-23-first claiming, thin state delegation, and zero legacy/global effects. Direct tests cover connect/reactivate, exact state requests, reversed concurrent correlation, timeout/error/publication settlement, late responses, schema targets, exact Kusto resolution, SQL callbacks, all routes, and disposal. Static guards delete displaced authority and preserve orchestrator/schema/SQL owners; sender inventory and provider lifecycle require the handler.

The final focused ring passed 2 files and 11 tests; the expanded owner/message-path ring passed 9 files and 538 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 278 files and 6,192 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, sender inventory, and both bundle gates passed. The first production gate correctly rejected the exact 1,935.2 KB host bundle against the prior 1,934 KB limit; synchronized host baselines moved 1,884 -> 1,886 KB while the 50 KB buffer stayed unchanged. Final webview size is 2,801.9 KB. Native E2E was not required because orchestrator command semantics, schema acquisition, SQL ownership, webview routing, and message shapes were unchanged.

### Iteration `HST-31`: Kusto Connection Browsing Application Handler

**Status:** closed on 2026-08-08. The blocker-only review returned `VERDICT: NO HST-31 BLOCKER`.

**Boundary:** `HostKustoConnectionBrowsingApplicationHandler` is the injected response-free owner for exact original-object `getConnections`, `getDatabases`, `refreshDatabases`, and `saveLastSelection` admission; passive versus interactive database request shaping; exact request/lifecycle field preservation; blank discovery delegation; trimmed blank-selection rejection; ordered selection persistence; `undefined` versus empty database preservation; best-effort diagnostics refresh after successful persistence; exact rejection; synchronous decline; accepted settlement across disposal; later suppression; and idempotent disposal. `QueryEditorProvider` only constructs/injects it immediately after HST-30, reference-identically offers requests, awaits claimed work, supplies narrow projection/service/command capabilities, and disposes it.

**Preserved owners:** `ConnectionService` retains selection persistence, database cache, authentication, discovery, tracing, recovery, terminals, and schema inference. HST-40 retains the revisioned privacy- and principal-aware `connectionsData` projection as an injected callback. `extension.ts` retains diagnostics command registration. Plain `.kql` compatibility retains its file-specific cache and inferred-selection update before forwarding `saveLastSelection`. Webview lifecycle identity, message shapes, schema, execution, Copilot, comparisons, persistence, and browser behavior are unchanged.

**Guards and qualification:** the required real-provider test was red first without importing the future module and recorded 2/2 intended failures: zero structural-handler calls and a legacy-resolved injected rejection. It then passed constructor-slot identity, reference-identical four-route forwarding, deferred settlement, exact rejection, and zero direct provider projection, `ConnectionService`, or diagnostics-command effects. Direct tests cover projection delegation, passive/interactive shaping, blank inputs, all lifecycle fields, `undefined`/empty database values, persistence and diagnostics failures, disposal crossing, later suppression, and idempotence. Static guards preserve `ConnectionService`, diagnostics registration, plain-file cache/inference ordering, message shapes, response-free sender inventory, and provider disposal. The focused ring passed 2 files and 14 tests; the expanded owner/caller ring passed 11 files and 315 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 280 files and 6,205 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, both bundle gates, and the blocker-only review passed. Final sizes are 1,936.0 KB host and 2,801.9 KB webview; intentional host growth moved the synchronized extension baseline from 1,886 to 1,887 KB with the 50 KB buffer unchanged. Native E2E was not required because request/response shapes, provider projection, `ConnectionService`, compatibility caching, diagnostics registration, and webview behavior were preserved and directly covered. The reviewer returned `VERDICT: NO HST-31 BLOCKER`.

### Iteration `HST-32`: Comparison Summary Host Bridge Retirement

**Status:** closed on 2026-08-08. The definitive documentation-aware review returned `VERDICT: NO HST-32 BLOCKER`.

**Boundary:** remove the unused provider summary maps, 20-second waiter, `comparisonSummary` / `clearComparisonSummary` cases, `CopilotServiceHost` wait/delete members, Copilot delete calls, SQL lifecycle delete effect/calls, both webview sends, and both host/webview message types. `displayComparisonSummary()` remains the sole presentation owner and computes from an immutable comparison artifact plus exact `comparison-source` lineage. Query target retirement removes the banner locally.

**Preserved owners:** Kusto and SQL comparison execution, exact cancellation, section/comparison owners, immutable artifacts and lineage, diff links, local banner controls, Copilot-generated narrative summaries, persistence, protocol-unrelated messages, and browser rendering remain unchanged. No generic handler or replacement state was introduced.

**Guards and qualification:** the real webview/controller boundary was red first: the two exact legacy sends failed while 74 neighboring tests passed. After retirement, the same pair passed 76/76 while retaining banner rendering, exact lineage, diff links, row/persistence clearing, banner removal, and Copilot cancellation. A static source guard rejects any host map, waiter, route, callback, or message-type return while requiring local artifact rendering and teardown. The focused ring passed 9 files and 505 tests; the final expanded comparison-preservation ring passed 13 files and 846 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 281 files and 6,206 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, and both bundle gates passed. Final sizes are 1,933.8 KB host and 2,801.6 KB webview; synchronized 1,887/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because visible local rendering and teardown were directly covered while execution, artifacts, lineage, persistence, and browser composition remained unchanged. The implementation review returned `VERDICT: NO HST-32 IMPLEMENTATION BLOCKER`, and the definitive documentation-aware review returned `VERDICT: NO HST-32 BLOCKER`.

### Iteration `HST-33`: Copilot Query Workflow Application Handler

**Status:** closed on 2026-08-08. The definitive documentation-aware review returned `VERDICT: NO HST-33 BLOCKER`.

**Boundary:** `HostCopilotQueryWorkflowApplicationHandler` is the injected owner for exact original-object `startCopilotWriteQuery`, `cancelCopilotWriteQuery`, `prepareOptimizeQuery`, `cancelOptimizeQuery`, and `optimizeQuery` admission. It reserves SQL preflight before asynchronous owner-token assertion, preserves exact safe failure and cancellation terminals, lets cancellation win resolving or rejecting assertion, passes complete Kusto cancellation identity, invokes cancellation synchronously, awaits start/prepare/run delegation, resolves SQL manager/schema/client lazily only at accepted delegation, and owns disposal. `QueryEditorProvider` only constructs/injects immediately after Kusto browsing, reference-identically offers messages, awaits claimed work, supplies existing capabilities, and disposes the handler.

**Preserved owners:** `CopilotService` retains models, prompts, tools, conversation/history, query and Optimize execution, exact Kusto owners, statuses/options/output, and ordinary terminals. `SqlExecutionBroker` retains preflight generations/currentness and exact cancellation; `SqlEditorLifecycleCoordinator` retains owner-token authority; SQL manager/schema/client adapters retain their algorithms and state. Kusto/SQL callers, response application, execution, artifacts, persistence, browser behavior, and message shapes are unchanged.

**Guards and qualification:** the required real-provider test was red first without importing the future module and failed exactly 2/2 with zero structural-handler calls plus legacy resolved rejection behavior. It then passed constructor-slot identity, reference-identical five-route forwarding, deferred settlement, exact rejection, and zero direct Copilot/preflight/token/transport effects. Direct tests cover Kusto start/exact cancel, SQL preflight success/failure and both cancel races, Optimize prepare/cancel/run, lazy adapter timing, disposal crossing, later suppression, and idempotence. A static guard requires five handler cases, exactly 25 provider cases, canonical service ownership, message shapes, sender inventory, and provider disposal. The corrected focused ring passed 3 files and 111 tests; the expanded and final post-documentation owner/caller/router ring passed 13 files and 738 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 283 files and 6,219 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, sender inventory, and both bundle gates passed. Final sizes are 1,934.7 KB host and 2,801.6 KB webview; synchronized 1,887/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because the extraction leaves model, prompt, execution, identity, caller, response-application, artifact, persistence, and browser behavior unchanged and directly covers the SQL/Kusto ordering contracts. The definitive review returned `VERDICT: NO HST-33 BLOCKER`.

### Iteration `HST-34`: Kusto Section Execution And Publication Application Handler

**Status:** closed on 2026-08-08. The definitive documentation-aware reviewer returned `VERDICT: NO HST-34 BLOCKER`.

**Boundary:** `HostKustoSectionExecutionApplicationHandler` is the injected owner for exact original-object `kustoSectionOpen`, `kustoSectionTarget`, `kustoSectionClose`, `kustoExecutionStartedAck`, `kustoPublicationAck`, `executeQuery`, and `cancelQuery` admission. It owns both acknowledgement ledgers and five-second deadlines, section lifecycle routing, manual execution admission and query shaping, selection ordering, physical target/account/Leave No Trace admission, exact cancellation, refresh/error/terminal behavior, narrow Copilot section-close cleanup, accepted execution settlement across disposal, late acknowledgement suppression, later-request suppression, and idempotent disposal. `QueryEditorProvider` only constructs/injects immediately after the Copilot query workflow, reference-identically offers messages, exposes thin `CopilotServiceHost` delegates, and disposes the handler.

**Preserved owners:** `KustoExecutionCoordinator` retains reservation, replacement, dispatch capture, cancellation, and exactly-once terminal algorithms. `KustoQueryClient` retains authenticated transport and physical cancellation. `ConnectionService`, `ConnectionManager`, and `CopilotService` retain selection, profile/policy, and Copilot state. Callers, message shapes, artifacts, persistence, schema, SQL, comparisons, and browser behavior are unchanged.

**Guards and qualification:** the required real-provider test was red first without importing the future module and failed exactly 2/2 with zero structural-handler calls plus legacy resolved rejection behavior. It then passed constructor-slot identity, reference-identical seven-route forwarding, deferred settlement, exact rejection, and zero direct coordinator/client/selection/transport/acknowledgement-map/Copilot-close effects. Direct coverage proves section lifecycle, exact start/publication acknowledgements and deadlines, manual success/failure/cancellation/replacement, disposal crossing, late suppression, later suppression, and idempotence. Static guards require seven handler cases, exactly 18 provider cases, no displaced maps/methods, canonical owner preservation, sender inventory, and provider disposal. The final expanded Kusto execution/Copilot/tool/webview/persistence/schema/protocol ring passed 15 files and 806 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 285 files and 6,231 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, and both bundle gates passed. Final sizes are 1,936.9 KB host and 2,801.6 KB webview; synchronized 1,887/2,752 KB baselines and the 50 KB buffer remain unchanged. An initial root-level browser command targeted the wrong package and failed before compilation; the correct `browser-ext` build passed. Native E2E was not required because coordinator/client algorithms, callers, message shapes, terminal application, artifacts, persistence, schema, SQL, comparisons, and browser behavior were unchanged and exact cross-layer identity is directly covered. The implementation and definitive documentation-aware reviews returned `VERDICT: NO HST-34 IMPLEMENTATION BLOCKER` and `VERDICT: NO HST-34 BLOCKER`.

### Iteration `HST-35`: Cross-Engine Comparison Preparation Application Handler

**Status:** closed on 2026-08-08. The definitive documentation-aware review returned `VERDICT: NO HST-35 BLOCKER`.

**Boundary:** `HostComparisonPreparationApplicationHandler` is the injected owner for exact original-object `sqlComparisonAdmissionAck`, `comparisonBoxEnsured`, and `sqlComparisonRemoved` admission; pending comparison request and Kusto comparison-owner maps; the 20-second deadline; SQL `staged`, `committed`, `finalized`, `completed`, and `rolledBack` acknowledgement correlation; rollback retry; exact Kusto/SQL target and SQL policy revalidation; removal cancellation; lifecycle rejection; and disposal settlement. `QueryEditorProvider` only constructs/injects immediately after Kusto section execution, reference-identically offers exact messages, keeps `ensureComparisonBoxInWebview()` as a thin delegate, and disposes the handler.

**Preserved owners:** `SqlEditorLifecycleCoordinator` retains section target and lifecycle sequencing; `SqlEditorSessionRegistry` retains SQL comparison/result owners and owner tokens; `SqlExecutionBroker` retains exact SQL cancellation; `KustoExecutionCoordinator` retains Kusto section execution; and `CopilotService` retains model/conversation cancellation. The existing webview transaction gains only the finalized-removal local retirement path; callers, message shapes, artifacts, persistence formats, and browser behavior remain unchanged.

**Guards and qualification:** the required real-provider structural test was red first without importing the future module and failed exactly 2/2 with zero fake calls and legacy resolved routing. It then passed constructor-slot identity, reference-identical forwarding of all three routes plus preparation, deferred settlement, exact rejection, and zero direct map/lifecycle/broker/coordinator/Copilot/transport effects. Direct handler coverage passed 14 tests for Kusto/SQL preparation, all five SQL acknowledgement phases, rollback retry, target/policy races, removal cancellation before and after finalization, atomic finalization/removal ordering, lifecycle rejection, deadlines, late suppression, and disposal. The focused owner/service/caller/router/artifact/persistence/protocol ring passed 16 files and 832 tests, and the definitive post-review ring passed 19 files and 844 tests with `--maxWorkers=1`. The first full run passed 286/287 files and 6,244/6,245 tests, failing only a stale source guard that still expected the engine choice in the provider; the pre-review full and coverage runs passed 287 files and 6,245 tests at 48.19% statements. The documentation-aware review then found an inherited finalized-removal race. Pre-finalization removal remains on exact acknowledged rollback, while atomically accepted finalization lets removal terminally settle the host and use a webview-local leaf port to clear request/box correlation, persistence snapshot, admission attribute, and read-only state before ownership detaches. Late completion cannot install ownership and retry admission remains available. The definitive post-fix full and official coverage runs passed 287 files and 6,249 tests at 48.21% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, sender inventory, and both bundle gates passed. Task-owned files had clean diagnostics; VS Code still reports 73 pre-existing ambient `Window` declaration diagnostics in `section-factory.ts` even though strict webview TypeScript passes. `git diff --check` found no whitespace errors and reported only existing LF-to-CRLF conversion warnings. The first production package failed only the prior 1,937 KB host limit; intentional 1.6 KB growth moved synchronized host baselines 1,887 -> 1,889 KB with the 50 KB buffer unchanged. Final sizes are 1,938.5 KB host and 2,802.0 KB webview. Native E2E was not required because canonical algorithms, callers, messages, artifacts, persistence formats, and browser behavior remain unchanged and exact cross-layer identity plus the complete host/webview transaction are directly covered. The implementation review returned `VERDICT: NO HST-35 IMPLEMENTATION BLOCKER`; the first documentation-aware review found the finalized-removal blocker; the fixed two-sided path and atomic phase transition were separately approved; and the definitive review returned `VERDICT: NO HST-35 BLOCKER`.

### Iteration `HST-36`: SQL Section Execution Application Handler

**Status:** closed on 2026-08-09. The definitive documentation-aware review returned `VERDICT: NO HST-36 BLOCKER`.

**Boundary:** `HostSqlSectionExecutionApplicationHandler` is the injected owner for exact original-object `executeSqlQuery` and `cancelSqlQuery` admission, synchronous preflight, SQL owner/protection and tool-owner validation, query-mode shaping, physical execution start, result/error/cancellation terminals, detail-free protected logging, exact cleanup, accepted settlement across disposal, later suppression, and idempotence. `QueryEditorProvider` only constructs/injects immediately after comparison preparation, reference-identically offers messages, supplies narrow capabilities, and disposes it.

**Preserved owners:** `SqlEditorLifecycleCoordinator` retains section identity and lifecycle sequencing; `SqlEditorSessionRegistry` retains targets, principals, revocation generations, result owners, and owner tokens; `SqlExecutionBroker` retains preflight/admission/currentness/cancellation/lease algorithms; `SqlWorkbenchService` retains connection/principal/Leave No Trace locking and protected runtime selection; `SqlQueryClient` and STS retain physical execution and cancellation. Copilot SQL execution, controls, response application, messages, artifacts, persistence, and browser behavior are unchanged.

**Guards and qualification:** the real-provider structural test was red first at 2/2 intended failures with zero fake calls and legacy resolved routing, then passed constructor identity, reference-identical execute/cancel forwarding, deferred settlement, exact rejection, and zero direct provider lifecycle/broker/workbench/client/transport effects. Direct coverage passes 23 tests. The definitive focused owner/service/caller/router/artifact/persistence/protocol ring passes 28 files and 1,053 tests. Full and official coverage Vitest pass 289 files and 6,275 tests at 48.21% statements; the first full run failed only a stale terminal-owner allowlist and the complete rerun passed. VS Code 1.132.0 extension-host passed 201/201 with `--timeout 5000` on the complete rerun after one unchanged bounded-close test timed out in the first 200/201 run and passed alone in 912 ms. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, sender inventory, and both bundle gates pass. Final sizes are 1,939.5 KB host and 2,802.0 KB webview; synchronized host baseline moved 1,889 -> 1,890 KB with the 50 KB buffer unchanged. Native E2E was not required because external SQL and webview behavior is unchanged and the complete host workflow is directly covered. Reviews returned `VERDICT: NO HST-36 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO HST-36 BLOCKER`.

## Completed Protocol Iteration

### Iteration `PRO-2`: Deterministic Main-Webview Startup Gateway

**Status:** closed on 2026-08-09. The definitive documentation-aware review returned `VERDICT: NO PRO-2 BLOCKER`.

**Boundary:** `MainWebviewStartupGateway` is the shared panel-scoped transport for native KQLX, KQL compatibility, and SQL compatibility composition. It installs inbound and disposal listeners synchronously, buffers ordinary inbound traffic until handler construction, buffers all outbound traffic until explicit dispatcher readiness, drains each direction once in order, permits only identity-shaped waiter replies to re-enter a blocked handler, and retires the panel on disposal. `index.ts` explicitly starts the webview dispatcher after registrations; startup drains the preload buffer before publishing `mainWebviewDispatcherReady` exactly once. `QueryEditorProvider` consumes readiness before application routing and supplies one injectable transport used by application handlers and embedded tutorials.

**Retirement contract:** ordinary and outbound traffic is rejected after retirement. Eligibility is captured when close-critical traffic is admitted and is not re-evaluated against an expired grace window. One serialized retired chain contains retained and post-disposal admissions, isolates individual failures, rejects new traffic after atomic admission closure, and is awaited to a stable tail before native or compatibility session cleanup. Native listener installation also precedes linked-file physical validation; panel disposal cancels that startup wait and releases same-URI close tracking without waiting for filesystem completion.

**Preserved owners:** PRO-1 native panel UUID envelopes, source generations, command/reload/request identities, document revisions, URI queues, Save leases, compatibility sidecar sessions, document aggregates, Kusto/SQL lifecycle, application handlers, and exact 13-case provider domain routing remain unchanged authorities. Correlated reentrancy is only transport scheduling; canonical handlers still validate exact request, source, execution, publication, phase, and owner identities. At PRO-2 closure, compatibility message shapes remained metadata-free; PRO-3 later migrated only their persistence lifecycle.

**Guards:** the three real host modes prove listener-first inbound and readiness-gated outbound traffic, exact ordering, one initial projection, and retired-panel rejection. Direct gateway tests cover duplicate readiness, ordinary versus correlated reentrancy, synchronous reentry, handler rejection, retirement before handler installation, immutable close admission, stable-tail quiescence, and successor isolation. Native and sidecar lifecycle tests cover disposal inside linked validation, pre-handler and blocked-handler close, expired final-persist rejection, correlated Save replies, multiple retained messages beyond the grace window, and exact final persistence. Protocol extraction inventories the gateway plus embedded tutorial senders/listeners and keeps readiness outside the 13 application routes.

**Qualification:** the final focused gateway/tutorial/protocol/provider/session ring passes 11 files and 414 tests; direct gateway coverage passes 12/12 and protocol inventory passes 58/58. The complete compatibility sidecar matrix passes 128/128 and native KQLX lifecycle passes 23/23. Full sequential and official coverage Vitest pass 291 files and 6,293 tests at 48.25% statements. The complete VS Code 1.132.0 extension-host suite passes 209/209 with `--timeout 5000`; its first run exposed five invalid/read-only fixtures whose no-listener assumption conflicted with listener-first validation, and the corrected active-subscription assertions plus complete rerun passed. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, protocol sender inventory, and both bundle gates pass. Final production sizes are 1,946.2 KB host and 2,802.1 KB webview; intentional growth moved synchronized baselines from 1,890/2,752 KB to 1,897/2,753 KB with the 50 KB buffer unchanged. No native E2E was required because external editor behavior and rendering are unchanged while the startup, persistence, retirement, and real-provider boundaries are exercised in the extension host. The definitive documentation-aware review returned `VERDICT: NO PRO-2 BLOCKER`.

**Exclusions:** no migration of the 13 provider domain cases; no SQL/Kusto execution, schema, language, connection, document, sidecar, artifact, persistence, browser-root, global protocol, ambient-window, dashboard/compiler, or analyzer redesign.

## Completed Browser Iteration

### Iteration `BRW-1`: Typed Read-Only Browser Projection Root

**Status:** closed on 2026-08-09. The definitive documentation-aware review returned `VERDICT: NO BRW-1 BLOCKER`.

**Boundary:** `BrowserViewerRoot` is the sole browser payload-adoption and projection owner. It consumes `LoadedBrowserFile`, preserves the content-script generation, rejects stale and duplicate adoption, delegates native and KQL-companion parsing to `viewer-document.ts` and shared codecs, deep-freezes the parsed document, applies browser defaults to a separate deep clone, and presents exactly one typed projection. `browserViewerProjection.ts` owns the runtime-validated cross-bundle contract and explicit host capabilities.

**Displaced authority:** `viewer-boot.js` no longer parses documents, mutates codec state, emits synthetic `persistenceMode`, `connectionsData`, `copilotAvailability`, or `documentData`, patches persistence globals, or schedules repeated read-only repair. It owns only payload admission plus loading, banner, error, background, spacing, and height presentation. `vscode-shim.js` owns only browser CSV nonce/replay/download behavior and supplies no synthetic response for unsupported host work.

**Read-only contract:** editing, persistence, authentication, Kusto execution, SQL execution, and Copilot are explicitly unavailable. Shared section chrome removes rename/reorder/remove/edit/authentication controls; Monaco creation and writable guards retain `readOnly`/`domReadOnly`; browser light-DOM host controls are absent. URL sections render the authored URL statically without host fetch, and Kusto, SQL, and Python register no browser Run shortcuts and reject execution before reservation/publication. Static source, section view controls, result inspection, and CSV Save remain usable. Authored HTML and script behavior remains trusted and unchanged.

**Guards:** the red-first root suite drives native KQLX and KQL-plus-companion payloads through the real owner, proves deep immutability, cloned defaults, opaque preservation, explicit capabilities, and stale/duplicate rejection, and statically forbids the four synthetic startup messages and timer/global patches. Its bundled real-boot regression proves duplicate standalone retries acknowledge without replaying or changing accepted UI while stale deliveries do neither. Adjacent regressions prove capability-owned Monaco read-only behavior, least-privileged section chrome, static URL rendering, absent Python execution, artifact restoration, and CSV behavior.

**Preserved owners and exclusions:** browser source providers, `BrowserFileLoadCoordinator`, shared codecs/native validation/document-kind matrix, section views, immutable artifact restoration, CSV download, content-script iframe ownership, standalone token/acknowledgement/resize handling, PRO-2 startup, and all 13 provider routes remain intact. No native document ownership, Kusto/SQL execution, compatibility convergence, global protocol, dashboard/compiler, or ambient-window redesign is included.

**Qualification:** the definitive focused BRW ring passes 16 files and 460 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 292 files and 6,303 tests at 48.23% statements. Strict host/shared, webview, and browser TypeScript checks, integration compilation, task-owned diagnostics, and `git diff --check` pass. The complete VS Code 1.132.0 extension-host suite passes 209/209 with `--timeout 5000`. Production extension and strict browser builds pass; ESLint reports zero errors and five existing warnings. Both synchronized bundle gates pass at 1,946.2 KB host and 2,807.1 KB webview; baselines are 1,897/2,758 KB with the 50 KB buffer unchanged. Built-browser checks prove mixed Kusto/SQL/Markdown/Python/URL/Chart views, read-only editors, static URL, exact restored rows and CSV bytes, duplicate acknowledgement without replay, stale rejection, zero host execution/synthetic startup messages, and no `390x844` horizontal overflow. The desktop screenshot is clean; the integrated capture surface reset during mobile screenshot capture, so mobile evidence is the exact Playwright DOM/layout and accessibility result. Reviewer-driven regressions cover duplicate/stale retry UI, trusted authored-content preservation, static URL rendering, and Kusto/SQL/Python execution denial. Reviews returned `VERDICT: NO BRW-1 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO BRW-1 BLOCKER`.

## Completed Compatibility Iteration

### Iteration `CMP-1`: Shared Compatibility Close Coordinator

**Status:** closed on 2026-08-09. The definitive documentation-aware review returned `VERDICT: NO CMP-1 BLOCKER`.

**Boundary:** one panel-scoped `CompatSidecarCloseCoordinator` now surrounds the existing `CompatSidecarSession`, `CompatSidecarStore`, and `MainWebviewStartupGateway` for both KQL and SQL compatibility providers. It owns delayed-beforeunload and correlated-final-snapshot retired admission, exactly-once close start, final-persist/beforeunload ordering, stable gateway-admission closure, session close and persist/upgrade drain, subscription disposal, dirty Save/Discard, exact attempted-draft recovery, final repair, exact store drain, rejected-initialization cleanup, and terminal settlement. Only explicit Discard abandons a dirty draft; prompt rejection or dismissal recovers it.

**Displaced authority:** both providers no longer own delayed-beforeunload admission flags, final-persist reply predicates, close-start flags, final-persist/beforeunload sequencing, stable gateway closure, session close/settlement, subscription-disposal loops, Save/Discard choice, recovery selection, or final repair/store drain. They retain primary-text edits, KQL inference/cache, SQL lifecycle integration, privacy sanitation, sidecar create/adopt UX, diff UI, protocol names, copy, and injected write/recovery/repair/notification state updates.

**Guards and qualification:** the real-provider test was red first with zero coordinator creations while the legacy transactions still ran. It now parameterizes KQL and SQL across Save and forced-recovery modes, dirties a real sidecar, delays a correlated final request, disposes, delivers beforeunload then ordinary then correlated-final traffic, and proves one coordinator, newest sanitized draft identity, ordinary-retired rejection, exact recovery, repair, store drain, and settlement. Rejected initialization before and after panel disposal, prompt rejection/dismissal, Discard, persist/upgrade tails, notification failures, repair failure, duplicate disposal, nested-provider cleanup, hidden-to-visible startup resynchronization, and displaced provider authority have direct regressions. The final focused owner/session/store/gateway/protocol/nested-lifecycle ring passes 7 files and 227 tests; the compatibility matrix passes 134/134 with `--timeout 5000`. A preceding 133-test complete matrix attempt reached 129/133 on four timing-sensitive unchanged cases; all four passed together in isolation and the complete unchanged rerun passed. Full sequential and official coverage Vitest pass 294 files and 6,338 tests at 48.33% statements. The complete VS Code 1.132.0 extension-host suite passes 213/213 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates pass. Final production sizes are 1,948.7 KB host and 2,816.9 KB webview; synchronized baselines are 1,899/2,767 KB with the 50 KB buffer unchanged. CMP-1 accounts for the host increase; the webview baseline includes disjoint externally committed autocomplete work at `747199c07b74d17de16d939094aafacf32c72250`. At qualification time CMP-1 remained uncommitted and unpushed; it was later committed locally at `ff178e9025c803a1923ab19654670e1bb73f7e40` and is now included in the synchronized pushed PRO-25 tree at `4787cd11e7d5c5d3b0ccc06bc50dd10963cf0462`. The first full Vitest run failed only a stale PRO-2 source guard at 6,334/6,335; the guard was updated to assert the new owner and every complete rerun passed. No native E2E was required because external controls, rendering, primary-text ownership, sidecar formats, and message shapes are unchanged.

**Preserved owners:** `TextDocument` remains primary-text and native edit-history authority. `CompatSidecarFormat`, `CompatSidecarStore`, and `CompatSidecarSession` retain linkage/hydration, lock/CAS/repair/recovery, revisions/persist queues/final snapshot/reload primitives. `MainWebviewStartupGateway` retains transport retirement. KQL inference/cache, SQL lifecycle, privacy sanitation, sidecar create/adopt UX, message shapes, and `QueryEditorProvider` domain routing remain unchanged.

**Exclusions:** no projection/reload migration, provider merge, Markdown compatibility migration, sidecar format/store/session rewrite, primary-text ownership change, document actor/protocol redesign, Kusto/SQL execution or schema change, or dashboard/compiler work.

**Residual:** a permanently non-settling external `QueryEditorProvider.initializeWebviewPanel()` promise still requires upstream cancellation or a startup deadline. Rejected initialization and all admitted close work settle through the coordinator; the residual is not a second close authority.

## Completed Protocol Iteration

### Iteration `PRO-3`: Runtime-Validated Compatibility Persistence Channel

**Status:** complete on 2026-08-10. The definitive blocker review returned `VERDICT: NO PRO-3 IMPLEMENTATION BLOCKER`.

**Boundary:** `compatibilityPersistenceProtocol.ts` owns one versioned runtime schema for KQL/SQL request/projection correlation, `persistDocument`, final-persist responses, `persistDocumentAck`, and `documentReloadResult`. Each provider creates one UUID, injects it before preload startup, parses at gateway admission, and stamps outbound lifecycle traffic. The webview parses before DOM or persistence effects and owns bounded pending/applied request ledgers.

**Displaced authority and guards:** compatibility lifecycle traffic no longer crosses provider/webview boundaries through metadata-free casts. Malformed or predecessor-session traffic is rejected before revision adoption, persist reservation, sanitation, primary-text mutation, sidecar draft/recovery, acknowledgement, final waiter settlement, or reload settlement. The shared parser checks structural state and primary type; the existing sidecar codec remains the complete identity, linkage, destination-kind, known-field, and lossless validator. Identical lost-ack retries re-acknowledge without rematerialization, lower generations fail, newer local state is resent, and changed current-generation retries may apply. Multi-section snapshots revalidate after the upgrade barrier, so cancelled or failed companion creation cannot acknowledge or poison later plain-file revisions.

**Preserved owners and exclusions:** `CompatSidecarCloseCoordinator`, `CompatSidecarSession`, `CompatSidecarStore`, `CompatSidecarFormat`, `MainWebviewStartupGateway`, primary `TextDocument`, KQL inference/cache, SQL lifecycle, privacy sanitation, sidecar create/adopt UX, provider projection/reload ownership, and all non-compatibility messages remain intact. No provider merge, Markdown compatibility migration, sidecar-format rewrite, global protocol rewrite, or execution/schema/dashboard/browser composition is included.

**Qualification:** the final focused ring passes 12 files and 499 tests with `--maxWorkers=1`; the parameterized compatibility extension-host matrix passes 134/134 with `--timeout 5000`. Full sequential and official coverage Vitest pass 295 files and 6,352 tests at 48.38% statements. The complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and both synchronized bundle gates pass. Final production sizes are 1,957.7 KB host and 2,826.6 KB webview; baselines are 1,908/2,777 KB with the 50 KB buffer unchanged. The first full Vitest run had one unchanged ten-second Kusto identity timeout and a dependent delegated-Copilot miss; both passed alone, and the complete verbose rerun passed unchanged. A subsequent worker process orphaned before reporting progress and was terminated; it produced no test failure. Native E2E was not required because controls, rendering, file formats, primary-text behavior, and provider ownership remain unchanged.

## Completed Dashboard Iteration

### Iteration `DSH-1`: Portable Dashboard Admission IR

**Status:** closed on 2026-08-10. The definitive blocker-only review returned `VERDICT: NO DSH-1 BLOCKER`.

**Boundary:** `portableDashboardCompiler.ts` is the sole pure owner for provenance v1 parsing, exactly-one browser-created `data-kw-bind` target admission, scalar/table/repeated-table/pivot/chart/tooltip and slicer validation, source-schema validation, typed diagnostics, and normalized portable IR. Standards-based `parse5` tree construction plus source locations reject source-less browser clones, inert/hidden contexts, parser-state form boundaries, overlapping/reparented ranges, and replacements that alter DOM outside the target. One shared aggregate vocabulary is validated and normalized across every aggregate-bearing binding position. `htmlDashboardUpgrade.ts` is a typed compatibility-notice adapter. Preview scalar/chart/table/repeated-table paths consume admitted provenance, while `bindHtml()` remains explicitly preview-only.

**Cross-path admission:** preview preflight, agent validation, local export, and publish preparation pass the same authored HTML and fact schema to the compiler. A structurally valid scalar `SUM(MissingMetric)` returns the same `missing-column` diagnostic object across all four paths. Local export and publish reject every compiler diagnostic or null IR with `PortableDashboardAdmissionError` before filesystem, authentication, or Fabric effects. Malformed provenance, binding arrays, dimensions, source schemas, unavailable fact sources, unsupported aggregates, duplicate/overlapping targets, browser tree corrections, unstable replacement contexts, and empty binding IDs fail closed with typed diagnostics rather than throwing or falling back. Provenance-only zero-binding data bridges remain valid consistently across all adapters.

**Displaced authority:** the complete host source-column validator, host provenance parser/target scanner, compatibility analyzer's target/display interpreter, Power BI binding regex/rewrite scanner, host string-only export/publish preflights, and obsolete `showPowerBiUnsupportedVisualHelp` route are deleted. `HostDashboardApplicationHandler` now owns nine dashboard routes and validates before mode or Leave No Trace effects. `powerBiExport.ts` recompiles the transformed HTML, precomputes its measure before the first write, and consumes exact compiler-owned source ranges; direct renderer-unit APIs retain structural IR only as an algorithm test adapter. Static ownership and protocol guards prevent duplicate admission or the retired route from returning.

**Preserved owners and exclusions at DSH-1 closure:** `HostDashboardApplicationHandler`, HTML Monaco/iframe/artifact binding, provenance v1, preview renderer algorithms, DAX/TMDL/PBIR output, Fabric authentication/create/update/recovery/cleanup, temporary PBIP publishing staging, document persistence, and raw standalone HTML remained intact. DSH-2 later removed only the temporary staging boundary.

**Qualification:** the final focused ring passes 14 files and 556 tests with `--maxWorkers=1`. Full sequential and official coverage Vitest pass 297 files and 6,457 tests; statement coverage is 48.35% versus the unchanged 27.67% threshold. The complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, current generated-route scans, and both synchronized bundle gates pass. Final production sizes are 2,146.2 KB host and 3,020.2 KB webview; synchronized baselines are 2,147/3,021 KB with the 50 KB buffer unchanged. Native E2E run `20260810-185619` passes all four `html-powerbi-upgrade` scenarios; six foreground-valid `1300x950` screenshots and three JSON artifacts were reviewed, proving restored warning visibility, dismissal behavior, clean valid previews, exact compiler reasons, and the active section-specific upgrade chat. No live authenticated Fabric tenant publish was performed; transaction/effect ordering remains covered by focused mocked REST, filesystem, authentication, cancellation, and cleanup tests.

### Iteration `DSH-2`: Pure Power BI Project Artifacts

**Status:** closed on 2026-08-10. The definitive documentation-aware review returned `VERDICT: NO DSH-2 BLOCKER`.

**Selection rationale:** HST and DOC remained higher-scoring themes but had no coherent eligible bounded slice. PRO likewise had no selected bounded route. DSH-2 had one concrete reversible boundary: remove filesystem staging without changing renderer output or Fabric transactions.

**Boundary:** `powerBiProjectArtifacts.ts` is the pure owner of one sorted project manifest from admitted portable IR and export input. `powerBiProjectWriter.ts` writes that manifest through VS Code filesystem APIs for local `.pbip` export. `powerBiPublish.ts` consumes the same report/model bytes directly as Fabric definition parts. Existing DAX/TMDL/PBIR renderers remain in `powerBiExport.ts` with an injectable identifier source and unchanged production defaults.

**Displaced authority and guards:** publish temporary URI allocation, local export invocation, recursive filesystem collection, and best-effort staging cleanup are deleted. Complete path/SHA-256 goldens pin all 17 representative project artifacts. Deterministic local/publish parity proves byte identity. Manifest bytes are copy-on-read, filesystem-equivalent path collisions fail before effects, local cancellation rejects before directory creation, and static guards keep VS Code filesystem access in the writer and out of the compiler/publish adapter.

**Preserved owners and exclusions:** Fabric REST/auth/create/update/rename/recovery/compensation/refresh, first-external-commit admission, publish cancellation, data modes, generated formats and renderer algorithms, provenance v1, portable admission IR, preview, document/protocol ownership, and raw standalone HTML remain unchanged.

**Qualification:** the red-first contract failed on the missing manifest modules. The final focused ring passes 3 files and 330 tests; the adjacent dashboard/protocol/compensation ring passes 9 files and 457 tests with `--maxWorkers=1`. Full sequential and official coverage Vitest pass 297 files and 6,465 tests at 48.35% statements. The complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,147.4 KB host and 3,020.2 KB webview; synchronized 2,147/3,021 KB baselines and the 50 KB buffer remain unchanged. The first blocker review found mutable bytes, Windows path collisions, and insufficient exact-output proof; all three were fixed before the clean review. Native E2E was not required because controls, messages, renderer behavior, and external transaction semantics are unchanged; no live authenticated Fabric tenant publish was performed.

## Completed KQL Analyzer Iteration

### Iteration `KLS-1`: Canonical KQL Source Analysis

**Status:** closed on 2026-08-10. The definitive review returned `VERDICT: NO KLS-1 BLOCKER`.

**Boundary:** `sourceAnalysis.ts` is the pure immutable owner of exact UTF-16 source layout, ordinary/hidden/verbatim/triple-backtick strings, comments, statement and management-command spans, lexical `let`/query/scalar/tabular parameter scopes, and physical table references. `KqlLanguageService.findTableReferences()`, unknown-table and lexical column diagnostics, `extractKqlSchemaMatchTokens()`, and Fully Qualify consume that result. Column propagation remains a separate best-effort stage and Monaco worker/schema behavior is unchanged.

**Displaced authority and guards:** the service's duplicate comment and table scanners, schema inference's string/comment scanner plus service re-entry, and the webview qualification fallback lexer are deleted. Fully Qualify now preserves exact CRLF text and admits only sorted, unique, non-overlapping standalone identifier ranges before replacement. Static ownership coverage prevents the removed scanners from returning. Adversarial regressions cover alias chains and declaration order, lexical shadowing, wrapper/grouped/qualified/source-form expressions, function bodies and parameters, scalar query wrappers, union operands and bounds, wildcard patterns, management commands, strings/comments, and hostile ranges.

**Red-first evidence:** the required query initially returned only `StringOnly` and `CommentOnly` from both references and schema matching while omitting `TableA`. The unchanged assertions now return exactly `TableA` and no `KW_UNKNOWN_TABLE` diagnostic.

**Qualification:** the definitive focused and adjacent ring passes 8 files and 131 tests with `--maxWorkers=1`. Full sequential and official coverage Vitest pass 299 files and 6,505 tests at 48.07% statements. The complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, and the synchronized bundle gate pass. Final production sizes are 2,147.5 KB host and 3,020.0 KB webview within unchanged baselines and the 50 KB buffer. Native E2E was not required because controls, message shapes, Monaco worker/schema lifecycle, and external effects are unchanged.

## Completed Protocol Iteration

### Iteration `PRO-4`: Exactly-Once Preload Add Handoff

**Status:** closed on 2026-08-10. The definitive blocker review returned `VERDICT: NO PRO-4 BLOCKER`.

**Boundary and displaced authority:** `queryEditor.js` retains a preload-only Add count object. `installRuntimeAddSectionBridges()` snapshots supported positive safe-integer counts, merges them with `pState.queryEditorPendingAdds`, clears the preload object, and only then replaces every Add stub. The sole runtime queue retains the existing capability-aware post-acknowledgement drain. `documentDefaultsFinalizedApplyCount` makes Add/default finalization exactly once for each applied projection, so a repeated finalizer cannot create a default after consuming an early request. No startup gateway, protocol, browser loader, compatibility provider, section behavior, or ambient bridge was redesigned.

**Red-first guard:** the real preload source queues one Markdown request before takeover. Before implementation, the regression produced zero Markdown sections while the other 122 persistence tests passed. It now proves one Markdown, zero default Query sections, both queues cleared, no duplication after repeated takeover, and no duplicate/default after repeated finalization.

**Qualification:** the final focused preload/persistence/dispatcher/capability/protocol/startup ring passes 6 files and 410 tests with `--maxWorkers=1`. Full sequential and official coverage Vitest pass 299 files and 6,506 tests at 48.13% statements. The complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and both synchronized bundle gates pass. Final production sizes are 2,147.5 KB host and 3,020.6 KB webview within unchanged baselines and the 50 KB buffer. Native E2E was not required because controls, message shapes, startup transport, and visible section behavior are unchanged.

## Completed Host Iteration

### Iteration `HST-37`: Exact SQL Schema Refresh Ownership

**Status:** closed on 2026-08-10. The definitive documentation-aware review returned `VERDICT: NO HST-37 BLOCKER`.

**Boundary:** `HostSqlSchemaRequestApplicationHandler` is the sole Query Editor owner of `prefetchSqlSchema`. It synchronously reserves by section, aborts and supersedes prior same-section work, captures the exact section instance, target generation, `SqlResultOwner`, and physical connection, drives `SqlSchemaService`, and admits cache/result/error publication only while both request and owner remain current. Webview and lifecycle-triggered refreshes route through the same request object. `QueryEditorProvider` only constructs/injects, reference-identically forwards, supplies capabilities, and disposes the handler; at HST-37 closure its switch had 12 cases and no schema request branch or response construction.

**Cache and publication hardening:** request currentness participates in physical disk CAS, memory publication, and return admission. If stale post-rename cleanup cannot delete the canonical file, the shared cache generation advances under lock; readers reject both fresh and explicitly stale captured generations. Cached Values SQL schema and snapshot success uses generation → Kusto policy → SQL owner lock order and staged/applied acknowledgement. SQL Clear All retires pending snapshots, invalidates schema generation even when database-cache clearing fails, emits one host-owned fresh snapshot, and settles pending UI state after bounded acknowledgement failure.

**Red-first evidence:** two same-target requests entered through the real provider, B resolved before A, and both terminals were routed through the real SQL schema router. Before implementation, cache publication was `['SchemaB', 'SchemaA']`; after implementation only B reaches cache and UI. Reviewer-driven regressions cover supersession during disk CAS, stale-file deletion failure, stale captured generations, delayed/rejected application acknowledgements under all three locks, pending snapshot versus Clear All, partial clear failure, and the real SQL Clear All button.

**Qualification:** the final focused ring passes 13 files and 621 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 301 files and 6,526 tests at 48.24% statements. The first complete run failed only four stale 13-case provider inventory guards; their focused 12-test ring passed after the intentional update and the complete unchanged rerun passed. The complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean task diagnostics, clean `git diff --check`, and integrated plus standalone bundle gates pass. Final production sizes are 2,151.5 KB host and 3,020.6 KB webview within synchronized baselines and the fixed 50 KB buffer. Native E2E was not required because editor controls, external schema messages, STS behavior, and SQL section rendering remain unchanged; the only Cached Values control change removes a duplicate snapshot request and is covered through the real Lit button plus host application-acknowledgement path. The definitive documentation-aware reviewer found no remaining blocker and returned `VERDICT: NO HST-37 BLOCKER`.

**Exclusions preserved:** no protocol/startup redesign, STS language extraction, Kusto schema change, cache-format change, compatibility/browser redesign, document-actor migration, generic handler framework, or dashboard work.

## Completed Host Iteration

### Iteration `HST-38`: SQL Connections Projection Application Owner

**Status:** closed on 2026-08-11. The definitive implementation blocker review returned `VERDICT: NO HST-38 IMPLEMENTATION BLOCKER`.

**Boundary:** `HostSqlConnectionsProjectionApplicationHandler` is the sole Query Editor owner of exact `getSqlConnections` admission; webview, lifecycle, manual-execution, and Copilot refresh triggers; revision/tail ordering; captured connection, application-state, database-cache, and favorites reads; canonical SQL policy/principal snapshot joining; owner and Leave No Trace filtering; exact `sqlConnectionsData` construction/publication; and disposal. `QueryEditorProvider` only constructs/injects, reference-identically forwards, delegates refreshes, supplies profile capabilities and transport, and disposes the handler. At HST-38 closure its main switch had exactly 11 cases.

**Privacy and failure hardening:** cache entries are admitted only when both canonical target and principal match. Selectively protected connections lose principal fingerprints, cache rows, and matching favorites. A globally blocked SQL policy publishes no favorites, including orphaned entries, and avoids the favorites read entirely. Snapshot revisions remain serialized across overlapping work and prior failure; every caller receives its exact delivery result. Disposal suppresses work before canonical owner admission/publication and claims later requests.

**Red-first and guards:** the real provider test failed with zero structural-handler calls before production changes, then passed unchanged with reference-identical forwarding, held settlement, exact awaiting, and zero connection-manager, application-state, owner-snapshot, favorites, cache, or transport effects. Direct tests cover complete payload identity, target drift, AAD-principal rotation, selective and global policy filtering, overlapping and failed tails, and disposal during cache acquisition. Static guards remove the provider branch, state, reads, payload, and sender; protocol inventory attributes `sqlConnectionsData` to the handler.

**Qualification:** the final focused SQL owner/trigger/cache/lifecycle/protocol/webview ring passes 22 files and 778 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 302 files and 6,534 tests at 48.24% statements. The first complete run failed only three stale 12-case provider inventory guards; their 9-test focused ring and the unchanged complete rerun passed. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, and both integrated and standalone bundle gates pass. An initial root-level `npm run build` targeted a nonexistent script and failed before compilation; the correct unchanged browser-package build passed. Final production sizes are 2,152.1 KB host and 3,020.6 KB webview within unchanged synchronized limits. Native E2E was not required because controls, external message shapes, webview application, STS behavior, favorites mutation, cache formats, and persistence behavior are unchanged.

**Exclusions preserved:** no STS lifecycle algorithm, HST-37 schema/cache, Kusto schema, SQL favorites/onboarding mutation, persistence-format, protocol/startup, compatibility/browser, DOC, or dashboard change.

## Completed Host Iteration

### Iteration `HST-39`: Persisted Result Sanitation Application Owner

**Status:** closed on 2026-08-11. The definitive blocker review returned `VERDICT: NO HST-39 BLOCKER`.

**Boundary:** `HostPersistedResultSanitizationApplicationHandler` is the sole cross-language owner of synchronous, fresh, fail-closed, and lock-held publication sanitation; helper transformations; legacy row-bearing payload removal; Kusto and SQL target/principal/account/revocation admission; comparison-source handling; orphaned result-artifact and SQL-principal cleanup; exact Kusto-then-SQL lock ordering; both persistence invalidation events; and disposal. `QueryEditorProvider` constructs or accepts the handler, supplies connection/policy/lifecycle capabilities, routes SQL and Kusto invalidation to it, and preserves its four public sanitation methods plus two events only as exact delegates for unchanged native and compatibility callers.

**Privacy and lifecycle contract:** fresh sanitation retries complete ordered Kusto-then-SQL acquisition and falls back by stripping all Kusto and SQL rows when acquisition fails. Lock-held publication awaits the exact callback before releasing either canonical owner. Disposing the handler retires invalidation delivery but deliberately leaves all four sanitation paths available for admitted close-time persistence. File and sidecar persistence mechanics, policy stores, lifecycle algorithms, artifact shapes, and public file formats remain separate unchanged owners.

**Red-first and guards:** the real provider test failed with zero structural-handler calls before production changes. It now proves reference-identical state and callback forwarding, held settlement, exact event identity, both invalidation routes, and zero direct lifecycle, connection, policy, owner-snapshot, or publication effects. Direct tests cover exact admitted owners, fail-closed fresh fallback, lock lifetime and ordering, events, idempotent disposal, and every sanitation path after disposal. The existing 100-test provider sanitation matrix runs through the real handler, while static guards forbid helper transformations, locks, and emitters from returning to the provider.

**Qualification:** the final focused sanitation/privacy/persistence/artifact/lifecycle/protocol ring passes 14 files and 843 tests with `--maxWorkers=1`. The adjacent native KQLX and KQL/SQL compatibility extension-host ring passes 159/159 with `--timeout 5000`; its first run had four timing-sensitive failures, each passed unchanged alone in 104–369 ms, and the complete unchanged rerun passed. Full sequential and official coverage Vitest pass 304 files and 6,543 tests at 48.24% statements. The first full run failed only one stale HST-30 ownership guard; its focused 4-test check and the complete unchanged rerun passed after assigning reconciliation to the new owner. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and integrated plus standalone bundle gates pass. Final production sizes are 2,153.7 KB host and 3,020.6 KB webview within unchanged synchronized limits. Native E2E was not required because callers, persistence mechanics, formats, policy/lifecycle algorithms, messages, controls, and rendering are unchanged.

**Exclusions preserved:** no file-format, document actor, sidecar store/session/close, compatibility-provider merge, policy-store algorithm, SQL/Kusto lifecycle, artifact model, provider switch-route, schema/cache, HST-38, dashboard, browser, or protocol change.

## Completed Host Iteration

### Iteration `HST-40`: Kusto Connections Projection Application Owner

**Status:** closed on 2026-08-11. The definitive documentation-aware review returned `VERDICT: NO HST-40 BLOCKER`.

**Boundary:** `HostKustoConnectionsProjectionApplicationHandler` is the sole Query Editor owner of every Kusto connection snapshot trigger; trigger-admission revision/tail ordering; editing-preference and Copilot first-time reads; account enumeration; current physical connection identity/incarnation, selection, database-cache, and favorites projection; canonical Leave No Trace lock admission; exact `connectionsData` construction; correlated `policyRequestId` retry; application-acknowledged publication settlement; development isolation; and disposal. `QueryEditorProvider` constructs/injects, delegates public/lifecycle refreshes, supplies capabilities/transport, and disposes only. `ConnectionService.sendConnectionsData()` and the provider revision/tail are deleted.

**Preserved owners:** HST-31 remains the sole route owner for `getConnections`, `getDatabases`, `refreshDatabases`, and `saveLastSelection`. `ConnectionManager`, `KustoAuthPreferenceService`, `KustoQueryClient`, `HostKustoFavoritesApplicationHandler`, `ConnectionService` selection/cache/discovery/tracing/recovery/terminal/inference algorithms, Kusto publication acknowledgement, webview application, and message shapes remain unchanged capabilities.

**Red-first and guards:** the real provider initially called the structural projection handler zero times. The unchanged final test routes an exact `getConnections` object with `policyRequestId` through HST-31, holds settlement, and proves exact forwarding/awaiting while provider revision/tail state, editing preferences, application state, legacy service publication, accounts, connections, favorites, caches, policy locks, and transport remain untouched. Direct tests cover complete payload identity, lock-held application acknowledgement, fresh correlated retry, delayed-account physical capture, global recovery, overlapping and failed tails, preference fallback, development isolation, and disposal before policy admission. Static guards remove provider/service authority and protocol inventory attributes `connectionsData` to the handler.

**Qualification:** the final focused/adjacent Kusto projection, browsing, lifecycle, preference, first-time, favorites, connection, policy, execution, protocol, and webview ring passes 20 files and 654 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate pass 306 files and 6,550 tests at 48.22% statements. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean task diagnostics, clean `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,155.0 KB host and 3,020.6 KB webview within unchanged synchronized baselines and the fixed 50 KB buffer. Native E2E was not required because controls, messages, external algorithms, and rendering are unchanged.

**Exclusions preserved:** no HST-39/HST-38 change; no HST-31 route redesign; no connection, authentication, cache, favorites, policy-store, Kusto execution/publication-ledger, persistence, file-format, compatibility-provider, protocol, DOC, browser, SQL, or dashboard change.

## Completed Host Iteration

### Iteration `HST-41`: SQL Editor And Language Lifecycle Forwarding Application Handler

**Status:** closed on 2026-08-11. The definitive documentation-aware review returned `VERDICT: NO HST-41 BLOCKER`.

**Boundary:** `HostSqlEditorLifecycleApplicationHandler` is the sole provider-route owner of exact `sqlSectionOpen`, `retireSqlTarget`, `testSetSqlAuthOverride`, `testClearSqlAuthOverride`, `stsRequest`, `stsDidOpen`, `stsDidChange`, `stsDidClose`, and `stsConnect` admission and settlement. It preserves Production suppression, ordered account-map then token writes, exact lifecycle argument forwarding, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. At HST-41 closure, `QueryEditorProvider` only constructed/injected, reference-identically offered, awaited, and disposed the handler while its residual switch still contained the two Kusto schema cases later closed by HST-42.

**Preserved owners:** `SqlEditorLifecycleCoordinator` retains section/target identity, STS documents/requests/connect/replay, principal/policy/runtime invalidation, and every lifecycle algorithm. `sqlAuthState.ts` and `sqlServerAccountMapStore.ts` retain authentication normalization, canonical-map, secret, and token behavior. STS services, SQL registry/broker/workbench/client, schema, execution, Copilot, persistence, compatibility, protocol, webview routing, browser behavior, and message shapes are unchanged.

**Red-first and guards:** the real provider initially called the structural handler zero times and invoked `sqlLifecycle.didChange` directly. The unchanged final test forwards the exact `stsDidChange` object, holds settlement, proves provider awaiting and constructor-slot identity, and observes zero direct lifecycle, auth-helper, or transport effects. Direct tests cover every route and exact field, reference-identical nested params/owners, Production gating, account-map-before-token ordering, failed first write, exact rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotence. Static guards remove all nine provider branches/auth imports, retain the two Kusto schema cases, preserve canonical lifecycle/auth owners, inventory the response-free handler, and require disposal.

**Qualification:** the definitive focused/adjacent lifecycle, STS language/runtime/query, development-auth, protocol, webview-routing, provider-inventory, and disposal ring passes 18 files and 442 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 308 files and 6,563 tests at 48.22% statements. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, clean `git diff --check`, and integrated/standalone bundle gates pass. Final sizes are 2,155.8 KB host and 3,020.6 KB webview within unchanged synchronized baselines and the 50 KB buffer. Native E2E was not required because controls, message shapes, lifecycle/STS/auth algorithms, webview routing, and rendering are unchanged.

**Exclusions preserved:** no SQL lifecycle, registry, broker, STS, connection, authentication-helper, schema, execution, Copilot, persistence, protocol, compatibility, browser, Kusto, dashboard, DOC, or CMP algorithm change; the two Kusto schema routes were intentionally left for HST-42.

## Completed Host Iteration

### Iteration `HST-42`: Kusto Schema Forwarding Application Handler

**Status:** complete on 2026-08-11. The implementation blocker review returned `VERDICT: NO HST-42 IMPLEMENTATION BLOCKER`.

**Boundary:** `HostKustoSchemaRequestApplicationHandler` is the sole provider-route owner of exact `prefetchSchema` and `requestCrossClusterSchema` admission, existing refresh/cache/silent/reason/lifecycle argument shaping, exact settlement, synchronous decline, accepted settlement across disposal, later suppression, and idempotent disposal. `QueryEditorProvider` constructs/injects, reference-identically offers, awaits, and disposes the handler; it has zero direct application switch cases and no direct calls to either schema-service route.

**Preserved owners:** `SchemaService` retains all primary and supplemental schema acquisition, cache, request, physical-connection, tracing, response construction, and publication algorithms. `KustoEditorSchemaCoordinator`, the primary schema router, `SyntheticRequestBroker`, `KustoSupplementalSchemaCoordinator`, Monaco worker mutation ownership, callers, messages, persistence, protocol, browser, and SQL behavior are unchanged.

**Red-first and guards:** the real provider initially called the structural handler zero times and invoked `SchemaService` directly. The unchanged final test forwards the exact `requestCrossClusterSchema` object, holds settlement, proves constructor-slot identity and provider awaiting, and observes zero direct schema or transport effects. Direct tests cover both routes, boolean defaults, complete-versus-partial lifecycle shaping, exact rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotence. Static guards require a zero-case provider, preserve both `SchemaService` methods, inventory the response-free handler, and require provider disposal.

**Qualification:** the focused/adjacent handler, provider, `SchemaService`, primary/supplemental ownership/router, synthetic-request, protocol, webview-routing, persistence/loading, zero-case, and disposal ring passes 18 files and 669 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 310 files and 6,573 tests at 48.22% statements. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean task diagnostics, clean `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,156.3 KB host and 3,020.6 KB webview within unchanged synchronized limits. Native E2E was not required because controls, message shapes, schema/cache/coordinator/worker algorithms, routers, and rendering are unchanged.

**Exclusions preserved:** no `SchemaService`, Kusto coordinator/worker/cache/request, connection/authentication, execution, Copilot, persistence, protocol, compatibility, browser, SQL, dashboard, DOC, or CMP algorithm change.

## Completed Protocol Iteration

### Iteration `PRO-5`: Runtime-Validated Kusto Schema Request And Delivery Channel

**Status:** complete on 2026-08-11. The definitive blocker review returned `VERDICT: NO PRO-5 IMPLEMENTATION BLOCKER`.

**Boundary:** `src/shared/kustoSchemaProtocol.ts` is the sole discriminated type and runtime-parser owner for exactly `prefetchSchema`, `requestCrossClusterSchema`, `schemaData`, `schemaError`, `crossClusterSchemaData`, and `crossClusterSchemaError`. Host and webview unions reference the shared request type. `postMessageToHost()` validates recognized requests before transport; `HostKustoSchemaRequestApplicationHandler` validates them again before `SchemaService`; and `SchemaServiceHost.postMessage()` accepts only the four typed deliveries. The webview dispatcher parses recognized host deliveries at entry before document-runtime gates, traces, primary routing, synthetic settlement, catalogs, supplemental coordination, or worker effects.

**Preserved owners and compatibility:** `SchemaService` retains every cache, fetch, authentication, physical-owner, tracing, response, and publication algorithm. `KustoEditorSchemaCoordinator`, `SyntheticRequestBroker`, `KustoSupplementalSchemaCoordinator`, schema catalogs, preparation, and `KustoWorkerMutationPort` retain all inner state and identity. Editor, synthetic, legacy, cache, stale/fresh supplemental, and partial-lifecycle flows keep their existing identities. Primary and supplemental raw show-schema payloads remain opaque; shallow primary containers and cache/refresh metadata are validated. Finite clock-skewed cache ages and legacy string function entries remain accepted. No panel-session envelope or second lifecycle authority was introduced.

**Red-first and guards:** a current-token `crossClusterSchemaData` carrying `requestSource: 'manual'` or `cacheAgeMs: 'fresh'` passed the real dispatcher and called the supplemental delivery owner before implementation; the new test failed exactly 2/209 while 207 neighboring tests passed. Final regressions prove malformed recognized requests reach no service effect, malformed current primary delivery reaches no trace/catalog/worker effect, and malformed current supplemental delivery reaches no supplemental effect. Pure protocol tests inventory all six discriminators, exact object/raw-payload identity, optional lifecycle compatibility, metadata enums, finite numeric ages, legacy functions, and malformed fields. Static guards require the shared owner, both receiver parsers before effects, typed service delivery, and absence of duplicated request shapes.

**Qualification:** the definitive focused/adjacent protocol, handler, provider, service, message inventory/transport, primary router/coordinator, synthetic broker, supplemental coordinator, loading, persistence, tracking, and disposal ring passes 18 files and 693 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 311 files and 6,605 tests at 48.24% statements. VS Code 1.132.1 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, and integrated/standalone bundle gates pass. Final production sizes are 2,157.9 KB host and 3,026.8 KB webview within unchanged synchronized limits and the fixed 50 KB buffer. Reviewer-found enum-coercion, clock-skew, and untyped-metadata blockers were fixed before the clean review. Native E2E was not required because controls, wire shapes, schema/cache/fetch/auth/coordinator/worker algorithms, and rendering remain unchanged while both real ingress boundaries are directly covered.

**Exclusions preserved:** no global protocol rewrite, panel-session handshake, database discovery, SQL schema, cache/fetch/auth algorithm, coordinator/worker mutation, deep raw-schema validation, message-handler decomposition, DOC, CMP, or browser change. PRO-5 was uncommitted and unpushed at qualification time and is now included in the synchronized pushed PRO-25 tree at `4787cd11e7d5c5d3b0ccc06bc50dd10963cf0462`.

## Completed Protocol Iteration

### Iteration `PRO-6`: Runtime-Validated Kusto Database Discovery Request And Delivery Channel

**Status:** complete on 2026-08-11. The definitive implementation review returned `VERDICT: NO PRO-6 IMPLEMENTATION BLOCKER`.

**Boundary:** `src/shared/kustoDatabaseDiscoveryProtocol.ts` is the sole discriminated type and runtime-parser owner for exactly `getDatabases`, `refreshDatabases`, `databasesData`, and `databasesError`. Host and webview unions reference the shared request union. `postMessageToHost()` validates recognized requests before E2E capture or VS Code transport; `HostKustoConnectionBrowsingApplicationHandler` validates again before `ConnectionService`; and `ConnectionServiceHost.postMessage()` accepts only the two typed deliveries. The main dispatcher parses recognized deliveries before runtime gates or database routing, and the bundled E2E waiter parses before clearing its timer/listener or resolving.

**Preserved owners and compatibility:** `HostKustoConnectionBrowsingApplicationHandler` retains route shaping and `ConnectionService` retains cache, fetch, authentication, tracing, zero-result recovery, fallback, terminal, and selection algorithms. `KustoEditorSchemaCoordinator`, `SyntheticRequestBroker`, the legacy token mirror, account-partition checks, and `QueryConnectionController` retain all inner identities and semantics. Blank connection IDs, explicit or omitted optional values, partial lifecycle identity, legacy tokenless traffic, synthetic owners, optional partitions, dense empty database arrays, and `authoritative` / `fallback` provenance remain valid. Parsing returns the original object without normalization. Known discriminator arrays and sparse/non-string database arrays are claimed and rejected before effects.

**Red-first and guards:** a current editor lifecycle and request token received otherwise-current `databasesData` with `authoritative: 'yes'`; before implementation the real dispatcher called `updateDatabaseSelect` and the new test failed exactly 1/211 while 210 neighbors passed. Final regressions cover both request ingresses, typed service deliveries, malformed current delivery, object identity, blank/partial/legacy shapes, array-shaped known discriminators, sparse arrays, and the real bundled waiter ignoring malformed matching traffic until a later valid response. Static inventory requires the shared owner, absence of duplicated request declarations, and parser-before-service/router/waiter ordering. The first blocker review found array-container and sparse-array bypasses; both were fixed before the clean implementation verdict.

**Qualification:** the definitive focused/adjacent protocol, browsing handler, provider, service, message inventory/transport, dispatcher, editor router/coordinator, synthetic broker, loading, persistence, schema ownership, and bundled-waiter ring passes 14 files and 575 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate pass 313 files and 6,620 tests at 48.34% statements. The first final ordinary run timed out in one unchanged exact Kusto execution identity test and then failed one later delegated-Copilot assertion; both passed together 2/2, and the complete unchanged rerun passed 313/313 files and 6,620/6,620 tests. The first coverage attempt failed only one concurrent unchanged Transformation reorder assertion; that file passed 123/123 under identical instrumentation and the complete unchanged rerun passed. VS Code 1.132.1 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, task-owned `git diff --check`, and integrated/standalone bundle gates pass. The combined worktree retains one unrelated concurrent blank-line-at-EOF finding in `kql-companion-sidecar.feature`, which PRO-6 did not modify. Final settled production sizes are 2,159.8 KB host and 3,030.9 KB webview within unchanged synchronized limits and the fixed 50 KB buffer. Concurrent non-PRO-6 worktree edits were preserved; broad gates ran against the settled combined worktree while the focused ring isolates PRO-6. Native E2E was not required because controls, wire shapes, and database/cache/fetch/auth/recovery/rendering algorithms are unchanged while every in-scope ingress is directly covered.

**Exclusions preserved:** no `getConnections` / `connectionsData`, `saveLastSelection`, cache, fetch, authentication, zero-result recovery, tracing, connection projection, persistence, coordinator/broker, schema/worker, SQL discovery, startup/session envelope, global protocol, message-handler decomposition, browser, DOC, or CMP change.

## Completed Protocol Iteration

### Iteration `PRO-7`: Runtime-Validated SQL Database Discovery Request And Delivery Channel

**Status:** complete on 2026-08-11. The definitive blocker review returned `VERDICT: NO PRO-7 BLOCKER`.

**Boundary:** `src/shared/sqlDatabaseDiscoveryProtocol.ts` is the sole discriminated type and runtime-parser owner for exactly `getSqlDatabases`, `refreshSqlDatabases`, `sqlDatabasesLoading`, `sqlDatabasesData`, and `sqlDatabasesError`. Host and webview unions reference the shared request union. `postMessageToHost()` validates recognized requests before E2E capture or VS Code transport; `HostSqlDatabaseDiscoveryApplicationHandler` validates again before target adoption; and every handler delivery is typed. The main dispatcher and direct SQL router parse recognized deliveries before request-ledger admission/completion or database UI effects.

**Preserved owners and compatibility:** `HostSqlDatabaseDiscoveryApplicationHandler` retains route shaping, lifecycle ticketing, owner admission, protected one-operation STS, cache/CAS, retry, terminal reconciliation, logging, notifications, and disposal. `SqlEditorLifecycleCoordinator`, `SqlWorkbenchService`, `SqlConnectionManager`, `SqlQueryClient`, `StsQueryService`, `StsRuntime`, and `sqlDatabaseCache.ts` retain all inner state and algorithms. Requests and deliveries preserve exact object identity, strings, generations, and dense empty arrays without normalization. Known-discriminator arrays, ordinary sparse arrays, inherited-index sparse arrays, and non-string database entries are claimed and rejected.

**Red-first and guards:** a valid current loading delivery followed by otherwise-current `sqlDatabasesData` containing `['CurrentDb', 42]` reached the real dispatcher, called `updateSqlDatabaseSelect`, and completed the request before implementation; the new test failed exactly 1/214 while 213 neighbors passed. Final tests cover both request ingresses, typed deliveries, direct and dispatcher routing, stale and reentrant lifecycle behavior, exact identity, dense empty arrays, malformed containers/fields, and pending-request preservation. The first blocker review found an inherited-index sparse-array bypass; a second red run failed exactly 1/10, the parser added own-index validation, and the final review returned `VERDICT: NO PRO-7 BLOCKER`.

**Qualification:** the definitive focused/adjacent protocol, handler, provider, transport, dispatcher/router, lifecycle/session, loading, cache, client, STS, and message-inventory ring passes 14 files and 582 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate pass 316 files and 6,639 tests at 48.35% statements. VS Code 1.132.1 extension-host passes 215/215 with `--timeout 5000` and `--fail-zero`; an earlier CLI launch without the explicit `.vscode-test.mjs` config ran zero tests and is not evidence. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, clean `git diff --check`, and the synchronized bundle gate pass. Final production sizes are 2,160.7 KB host and 3,033.0 KB webview within unchanged limits and the fixed 50 KB buffer. Concurrent browser, remote-file, E2E, and regression-plan edits were preserved; broad gates ran against the settled combined worktree while the focused ring isolates PRO-7. Native E2E was not required because controls, wire shapes, and SQL discovery/lifecycle/cache/auth/privacy/protected-runtime/rendering algorithms are unchanged while every in-scope ingress is directly covered.

**Exclusions preserved:** no PRO-6 rework; no SQL discovery/cache/auth/privacy/protected-runtime algorithm change; no SQL schema or STS language migration; no connection projection, persistence, startup/session envelope, global protocol, message-handler decomposition, browser, DOC, or CMP change.

## Completed Protocol Iteration

### Iteration `PRO-8`: Runtime-Validated SQL Schema Request And Delivery Channel

**Status:** closed on 2026-08-11. The definitive documentation-aware review returned `VERDICT: NO PRO-8 BLOCKER`.

**Boundary:** `src/shared/sqlSchemaProtocol.ts` is the sole discriminated type and runtime-parser owner for exactly `prefetchSqlSchema` and `sqlSchemaData`. Host and webview unions reference the shared request type. `postMessageToHost()` validates recognized requests before E2E capture or VS Code transport; `HostSqlSchemaRequestApplicationHandler` validates again before target adoption; and its delivery transport is typed. The main dispatcher and direct SQL router parse recognized deliveries before catalog or schema-info effects.

**Preserved owners and compatibility:** HST-37 reservation, same-section supersession, lifecycle owner admission, `SqlSchemaService` cache/currentness, cache generation, Cached Values, and STS/lifecycle algorithms remain unchanged. Requests preserve exact identity and optional `forceRefresh`. Valid deliveries preserve exact object and nested-object identity; complete target identity, strict branch-specific success/error metadata, dense string table/view arrays, nested string-valued column maps, and stored-procedure fields are validated. Dense empty collections and empty error messages remain valid; the existing `Schema failed` presentation fallback is preserved. Known-discriminator arrays, sparse or inherited-index arrays, mixed metadata, and malformed schema entries fail closed.

**Red-first and guards:** with a valid current SQL target and prior catalog/status, otherwise-current `sqlSchemaData` carrying `tables: ['Events', 42]` reached the real dispatcher and replaced the catalog before implementation; the red run failed exactly 1/215 while 214 neighboring tests passed. Final regressions cover pure parser identity and branch metadata, both request ingresses, typed handler delivery, direct and dispatcher routing, malformed current success, mixed error metadata, valid empty errors, and sole-owner/parser-order inventory. Two pessimistic reviews found mixed success/error metadata and the empty-error fallback regression; both were fixed before the definitive `VERDICT: NO PRO-8 BLOCKER`.

**Qualification:** the final focused protocol/transport/handler/router/dispatcher/inventory ring passes 6 files and 322 tests with `--maxWorkers=1`; the final adjacent HST-37 lifecycle, cache-generation, Cached Values, session, loading, and reverse-completion ring passes 15 files and 661 tests. The latest complete ordinary and official coverage runs pass 317 files and 6,655 tests; statement coverage is 48.42%. The first complete run reached 315/317 files and 6,651/6,653 tests, failing only an unchanged exact-identity timeout and account-map watcher timing case; both passed together at 232/232 before the clean complete reruns. One intermediate full run was stopped after documentation and concurrent edits made it non-final and is not evidence. VS Code 1.132.1 extension-host passes 215/215 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser TypeScript, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, and the synchronized bundle gate pass. Final production sizes are 2,161.7 KB host and 3,037.4 KB webview within unchanged limits and the fixed 50 KB buffer. Concurrent SQL result-height, test-helper, and E2E edits were preserved; broad gates cover the settled combined tree while the focused ring isolates PRO-8. Native E2E was not required because controls, external wire shapes, HST-37/cache/lifecycle algorithms, and SQL rendering remain unchanged while both real ingress boundaries and direct routing are covered.

**Exclusions preserved:** no SQL schema acquisition/cache/currentness algorithm change; no STS language protocol migration; no SQL database discovery, connection projection, Cached Values transaction redesign, persistence, startup/session envelope, global protocol, browser, DOC, or CMP change.

## Completed Protocol Iteration

### Iteration `PRO-9`: Runtime-Validated KQL Language Request And Response Channel

**Status:** complete on 2026-08-11. The final implementation review returned `VERDICT: NO PRO-9 IMPLEMENTATION BLOCKER`.

**Boundary:** `src/shared/kqlLanguageProtocol.ts` is the sole discriminated type/runtime-parser owner for exactly `kqlLanguageRequest` and `kqlLanguageResponse`. Host and webview unions reference its request type; `kqlLanguageService/protocol.ts` is a compatibility re-export. `postMessageToHost()` validates recognized requests before E2E capture or VS Code transport; `HostKqlLanguageRequestApplicationHandler` validates again before analysis and publishes only the typed response union. The dispatcher parses recognized responses and verifies the successful result branch against the pending method before resolver settlement or deletion.

**Preserved owners and compatibility:** `KqlLanguageServiceHost` retains schema/auth-context resolution and exact analysis delegation; `KqlLanguageService` and `sourceAnalysis.ts` retain diagnostics and physical-reference algorithms; the webview retains its request ledger and exact 1,500 ms timeout; and `kql-table-reference-ranges.ts` retains fail-closed Fully Qualify admission. Valid nonblank request IDs, params, results, and nested values preserve exact identity without normalization. Dense empty arrays and fixed user-safe analysis failures remain valid. Blank IDs, unknown methods, malformed/mixed branches, invalid diagnostic/reference fields, sparse or inherited-index arrays, and wrong-method successes fail closed.

**Red-first and guards:** the requested real dispatcher test started `window.__kustoRequestKqlTableReferences()`, sent a matching success with `endOffset: '11'`, then sent a valid response. Before implementation the malformed result settled and deleted the resolver, so the test failed exactly 1/216 while 215 neighbors passed. Final routing coverage adds blank-ID and wrong-method non-settlement, exact valid-result identity, and the 1,499/1,500 ms timeout boundary. Pure protocol, both request ingresses, typed delivery, provider ownership, diagnostics, source analysis, schema inference, Fully Qualify, and parser-order inventory are guarded. Reviews found and closed blank-ID transport and trimmed-correlation-ID blockers.

**Qualification:** the final focused protocol/transport/handler/provider/dispatcher/diagnostics/Fully Qualify/inventory ring passes 8 files and 363 tests with `--maxWorkers=1`; the expanded source-analysis/schema-inference/Monaco/toolbar/schema-ownership/provider-lifecycle ring passes 14 files and 913 tests. Complete sequential and official coverage Vitest pass 318 files and 6,669 tests at 48.43% statements. VS Code 1.132.1 extension-host passes 215/215 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, and the bundle gate pass. Final combined-tree sizes are 2,162.6 KB host and 3,045.7 KB webview within unchanged limits and the fixed 50 KB buffer. Concurrent Kusto UX/E2E hardening was preserved; broad gates cover the combined tree while the focused rings isolate PRO-9. Native E2E was not required because controls, external valid wire shapes, analyzer behavior, resolver timing, and Fully Qualify behavior remain unchanged while every ingress and settlement boundary is directly covered.

**Exclusions preserved:** no analyzer or diagnostics algorithm change; no Fully Qualify rewrite; no PRO-8 rework; no global protocol/session envelope, message-handler decomposition, DOC, CMP, browser, or KLS change.

## Completed Protocol Iteration

### Iteration `PRO-10`: Runtime-Validated Control-Command Syntax Request And Result Channel

**Status:** complete on 2026-08-11. The definitive blocker review returned `VERDICT: NO PRO-10 BLOCKER`.

**Boundary:** `src/shared/controlCommandSyntaxProtocol.ts` is the sole discriminated type/runtime-parser owner for exactly `fetchControlCommandSyntax` and `controlCommandSyntaxResult`. Host and webview unions reference its request type. The caret-doc scheduler validates before pending-ledger insertion, `postMessageToHost()` validates before E2E capture or VS Code transport, and `HostControlCommandSyntaxApplicationHandler` validates again before cache lookup or Learn acquisition. Handler delivery is typed. The main dispatcher parses recognized results before effects and requires exact `(commandLower, requestId)` pending identity before cache writes, pending deletion, or active-caret-doc refresh.

**Preserved owners and compatibility:** `HostControlCommandSyntaxApplicationHandler` retains the exact 24-hour cache boundary, Microsoft Learn URL normalization/fetch, Syntax-section/first-`pre` extraction, entity decoding, case-insensitive `with(...)` argument de-duplication, existing failure-cache behavior, and disposal suppression. Caret docs retain command detection, request deduplication, presentation caching, rendering, and the deliberate absence of a timeout. Valid request/command/href/syntax/argument values preserve exact identity; valid empty syntax and dense empty `withArgs` remain accepted. Failed results use the existing canonical empty payload. No browser, analyzer, resource, session-envelope, or presentation behavior moved into the protocol.

**Red-first and guards:** the required real dispatcher regression seeded one current request, delivered a matching result with `withArgs: ['HotCache', 42]`, then a valid stale request ID, then the valid current result. Before implementation, the malformed result cached `['HotCache', '42']`, deleted current pending work, and refreshed; the red run failed exactly 1/218 while 217 neighbors passed. Final coverage proves malformed and stale results are side-effect free, only exact current identity settles, canonical empty failures cannot be poisoned by nonempty payloads, malformed scheduler metadata creates no permanent pending entry, dense empty arrays remain valid, sparse/inherited arrays fail closed, and valid request/cache behavior is unchanged. Static inventory prevents duplicate request shapes, untyped delivery, parser-after-effect ordering, or command-only settlement from returning.

**Review hardening:** the first pessimistic review found that a forged nonempty `ok: false` payload could settle and poison the cache. The shared compile-time union and runtime parser now require `syntax: ''` and `withArgs: []` on failures, with direct parser and dispatcher regressions. A later documentation-aware review found scheduler metadata was still string-coerced before parsing and contributor guidance overclaimed main resource-URI ownership. The scheduler now parses raw metadata before deriving cache identity or pending state, hostile numeric/array/sparse/inherited metadata is covered, and plain `.md` resource ownership is explicit. The final review found no correctness, ownership, compatibility, test, or documentation blocker and returned `VERDICT: NO PRO-10 BLOCKER`.

**Qualification:** the final focused protocol/handler/provider/transport/dispatcher/caret-doc/inventory ring passes 7 files and 497 tests with `--maxWorkers=1`; the expanded adjacent KQL analysis, prior-protocol, Monaco, toolbar, schema-ownership, and provider-lifecycle ring passed 18 files and 778 tests before final hardening, and the final complete ordinary and official coverage runs pass 321 files and 6,694 tests at 48.75% statements. VS Code 1.132.1 extension-host passes 215/215 with the exact `npx vscode-test --config .vscode-test.mjs --timeout 5000 --fail-zero` command. One preceding shell invocation failed before test launch when `npx` transiently did not resolve. A later complete attempt reached 213 passing before an unchanged schema-cache timeout, its cleanup cascade, and an unrelated sidecar follow-on; both primary cases passed alone at 1/1, and the complete unchanged rerun passed 215/215. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, `git diff --check`, and the synchronized bundle gate pass. Final stable production sizes are 2,163.3 KB host, 3,052.5 KB main webview, and 289.1 KB Markdown webview within unchanged limits and the fixed 50 KB buffer. The development esbuild watcher was paused only while recording stable production sizes and then restored. Concurrent user-owned resize/E2E changes were preserved; broad gates cover the combined tree while focused tests isolate PRO-10. Native E2E was not required because valid external shapes, Learn acquisition/cache algorithms, controls, and caret-doc presentation remain unchanged while every in-scope ingress and settlement effect is directly covered.

**Exclusions preserved:** no Learn fetch/parser/cache algorithm, timeout, caret-doc presentation/deduplication, href behavior, browser, analyzer, PRO-8/PRO-9, global protocol/session envelope, dispatcher decomposition, DOC, CMP, or KLS change.

## Completed Protocol Iteration

### Iteration `PRO-11`: Runtime-Validated Resource URI Request And Result Channel

**Status:** complete on 2026-08-12. The pessimistic implementation review returned `VERDICT: NO PRO-11 IMPLEMENTATION BLOCKER`.

**Boundary:** `src/shared/resourceUriProtocol.ts` is the sole discriminated type/runtime-parser owner for exactly `resolveResourceUri` and `resolveResourceUriResult`. Host and webview unions reference its request type. `postMessageToHost()` validates recognized requests before E2E capture or VS Code transport. `HostResourceUriApplicationHandler` and `MdCompatEditorProvider` validate again before path, workspace, stat, cache, or conversion effects and publish typed result branches. The main dispatcher and plain-Markdown listener parse recognized results before resolver lookup, deletion, timer cancellation, promise settlement, or rendering effects.

**Preserved owners and compatibility:** `HostResourceUriApplicationHandler` retains HTTP/HTTPS/data/blob/webview passthrough, local-file base validation, Windows/POSIX/workspace path normalization, exact base/path cache, stat, conversion, and disposal algorithms. Plain `.md` retains its independent document-directory resolution algorithm. Both webviews retain their own request ledger, exact two-second deadline, stale/late behavior, and Markdown rendering. Exact valid strings, nonblank but unnormalized request IDs, optional or empty `baseUri`, empty-path failure behavior, request deduplication, browser no-op behavior, URL acquisition, navigation, and linked queries remain unchanged.

**Red-first and guards:** the required real-dispatcher regression parameterized main and plain-Markdown paths, seeded current work, delivered matching `{ ok: 'yes', uri: 'forged' }`, then a valid stale ID, then the valid current result. Before implementation the malformed matching result canceled the current timer; the red run failed exactly 1/220 while 219 neighbors passed. Final coverage proves malformed and stale results leave the promise/render value and timer untouched, only the exact valid current result settles once, duplicate and late results are inert, and both deadlines settle at 1,999/2,000 ms. Pure parser tests cover exact object identity, optional/empty values, canonical success/failure branches, arrays, malformed and mixed branches, and unrelated traffic. Main and plain-Markdown host tests prove malformed requests cause no coercion/path/stat/cache/conversion/publication effect; the real `.md` provider additionally proves exact empty, passthrough, and relative-file behavior. Static inventory requires sole shared ownership, typed delivery, and parser-before-effect ordering in all four adapters.

**Qualification:** the focused/adjacent resource protocol, transport, dispatcher, host handler/provider, Markdown, capability, and message-inventory ring passes 9 files and 356 tests with `--maxWorkers=1`. The complete plain-Markdown extension-host suite passes 3/3. Complete sequential Vitest passes 322 files and 6,707 tests; the official coverage gate passes 322 files and 6,709 tests at 48.89% statements. VS Code 1.132.1 extension-host passes 216/216 with exact `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, and synchronized bundle gates pass. Final production sizes are 2,164.3 KB host, 3,055.8 KB main webview, and 290.7 KB Markdown webview within unchanged limits and the fixed 50 KB buffer. Concurrent user-owned Connection Manager, data-table focus, and E2E edits were preserved; broad gates cover the combined tree while focused rings isolate PRO-11. Native E2E was not required because valid wire shapes, both resolution algorithms, rendering, controls, and browser behavior are unchanged while every in-scope ingress and settlement effect plus real plain-Markdown file resolution is directly covered.

**Exclusions preserved:** no path, cache, stat, workspace-root, or URI-conversion algorithm change; no timeout or Markdown rendering redesign; no browser capability simulation; no ambient bridge removal, global protocol/session envelope, dispatcher decomposition, DOC, CMP, KLS, or unrelated protocol rework.

## Completed Protocol Iteration

### Iteration `PRO-12`: Runtime-Validated URL Content Request And Delivery Channel

**Status:** complete on 2026-08-12 with `VERDICT: NO PRO-12 BLOCKER`.

**Boundary:** `src/shared/urlContentProtocol.ts` is the sole type/runtime-parser owner for exactly `fetchUrl`, `urlContent`, and `urlError`. Host and webview request unions reference it. `postMessageToHost()` atomically classifies and snapshots recognized requests before E2E capture or VS Code transport; `HostUrlContentApplicationHandler` does the same before trimming, URL construction, working fetch state, or network effects and publishes only the typed delivery union. The central dispatcher and independent `kw-url-section` listener both atomically admit recognized deliveries before correlation, `_activeFetchRequest` retirement, loading/render state mutation, artifact revocation/publication, or rendering.

**Canonical shapes and hostile input:** required fields are own enumerable data properties and successful parsers return plain descriptor-captured messages with exact scalar values. Content requires a 2xx status, nonblank requested/resolved URLs, finite safe-integer status and byte length, and one exclusive branch: image with nonblank `contentType`/`dataUri`, or CSV/HTML/text with `body` and `truncated`. Empty text bodies remain valid. Errors preserve an optionally empty requested URL but require a nonblank canonical message and contain no success fields. Callable, inherited, non-enumerable, accessor, mixed, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters or escaping the parser. Native `DOMException` AbortError accessors remain explicitly supported so the existing timeout terminal is preserved.

**Red-first and guards:** the required component regression created one pending URL request, delivered matching malformed `kind: ['csv']`, then a canonical stale request ID, then the canonical current result. Before implementation, the malformed matching result cleared `_activeFetchRequest`; the red run failed exactly 1/53 while 52 neighboring component tests passed. Final coverage proves malformed matching, stale, wrong-requested-URL, inherited-field, and hostile-proxy traffic leaves the exact pending request, loading/render state, and artifact untouched; only the valid current delivery settles and publishes rows. Pure parser, host request, outbound capture, central dispatcher, component listener, static sole-owner/parser-order, redirect, empty-body, canonical-error, and native timeout tests prevent the old coercive path from returning.

**Preserved behavior:** `HostUrlContentApplicationHandler` retains HTTP/HTTPS validation, redirect-following fetch, the exact 15-second timeout/abort lifecycle, 100 MB text/CSV and 5 MB image caps, 200,000-character truncation, classification and HTML sniffing, normalized request identity, original/resolved URL publication, and disposal. URL sections retain debounce, same-document runtime retention, stale/recreated-section rejection, image/CSV/HTML/text rendering, immutable CSV artifact publication/revocation, autosizing, imported CSV saving, and browser static/no-fetch behavior. Direct chart/layout/imported-CSV native fixtures now use the same canonical correlated delivery shape.

**Qualification:** the definitive focused protocol, handler, provider, transport, dispatcher, URL component, artifact, chart/Transformation, persistence, imported-CSV, browser-static, and inventory ring passes 13 files and 620 tests with `--maxWorkers=1`. Complete sequential Vitest passes 323 files and 6,742 tests; the official coverage gate passes 323 files and 6,744 tests at 49.00% statements. One complete attempt exposed a concurrent PRO-11 static guard that incorrectly forbade unrelated `path.posix.basename()` title formatting; the guard was narrowed to path-resolution operations, passed 2/2 alone, and the complete unchanged rerun passed. Coverage first timed an unchanged cross-layer identity case at 10,018 ms; it passed unchanged alone in 8.04 seconds. A later coverage attempt loaded an intermediate concurrent owner-enrichment test edit; the settled case passed unchanged alone in 24 ms, and the complete settled coverage rerun passed. VS Code 1.133.0 extension-host passes 216/216. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, `git diff --check`, and the integrated and standalone bundle gates pass. Final production sizes are 2,170.6 KB host, 3,065.5 KB main webview, and 292.8 KB Markdown webview within unchanged baselines; total `dist/` is 18,956.0 KB. Native E2E was not required because fetch, rendering, artifact, imported-CSV, and browser behavior remain unchanged and are covered by the focused ring. The definitive code and documentation-aware blocker reviews returned `VERDICT: NO PRO-12 BLOCKER`.

**Exclusions preserved:** no fetch, rendering, artifact-policy, timeout, cancellation, browser, resource-URI, session-envelope, dispatcher decomposition, provider composition, or DOC/HST change.

## Completed Protocol Iteration

### Iteration `PRO-13`: Runtime-Validated Python Execution Request And Terminal Channel

**Status:** complete on 2026-08-12 and committed/pushed as part of the qualified combined baseline at `83041109a7f287b017d5c57fa0368858f02c1725` with `VERDICT: NO PRO-13 BLOCKER`.

**Boundary:** `src/shared/pythonExecutionProtocol.ts` is the sole type/runtime-parser owner for exactly `executePython`, `pythonResult`, and `pythonError`. Host and webview unions reference its request type. The live Lit component parses before box-only reservation or `Running...`, sends through `postMessageToHost()`, and that wrapper atomically re-admits before E2E capture or transport. `HostPythonExecutionApplicationHandler` re-admits before workspace-directory/process effects and publishes only the typed terminal union. The central dispatcher and leaf terminal functions re-admit before reservation consumption, running/output mutation, host-owned persistence, or legacy rendering.

**Canonical shapes and exact settlement:** required fields are own enumerable data properties and successful parsers return plain descriptor-captured messages with exact scalar values. Requests preserve empty code; results preserve empty stdout/stderr, require a safe-integer-or-null exit code, and enforce independent 200 KB UTF-8 caps; errors preserve exact strings and are exclusive from result fields. Callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters. The existing box-only identity and one-outstanding-run contract remain unchanged; terminal consumption no longer string-coerces identity, and no execution ID was added.

**Red-first and guards:** the required component regression reserved one current run, delivered matching malformed `pythonResult` with `boxId: ['py_test_1']`, then the canonical terminal. Before implementation the malformed message consumed the exact reservation, so the red run failed exactly 1/17 while 16 neighboring component tests passed. Final coverage proves malformed traffic leaves reservation, disabled/running UI, persisted output, and host command stream untouched; only the canonical terminal settles and persists. Pure descriptor/proxy/cap tests, request-before-reservation and host-effect tests, dispatcher admission, exact admission-registry tests, typed delivery, sole-owner/parser-order inventory, and direct browser denial prevent the old path from returning. The uncalled `runPythonBox()` residual was deleted.

**Preserved behavior:** interpreter fallback remains `python`, `python3`, then `py`; workspace working directory, stdin execution, stdout/stderr composition, exact 15-second timeout and kill ordering, independent UTF-8 caps, disposal, projection/removal/replacement retirement, inactive-terminal consumption, same-document Monaco retention, empty-code behavior, host-owned output persistence, and browser/read-only denial remain unchanged. Native E2E was not repeated because these behaviors retain the HST-3 native exact-output baseline and are covered by the focused host/component/persistence/browser ring.

**Qualification:** the definitive focused protocol, admission, transport, host handler, real provider, dispatcher, component, document aggregate/client, persistence, capability, browser-read-only, lifecycle, and inventory ring passes 14 files and 619 tests with `--maxWorkers=1`. The final complete sequential Vitest run passes 325 files and 6,769 tests after concurrent tests settled. The official coverage gate passes the preceding 325-file tree at 6,766 tests and 49.02% statements. Its first attempt timed an unchanged exact-identity case, exposed an unrelated stale SQL event expectation after concurrent provenance work, and missed one first-launch ordering case; the two unchanged files passed 247/247 under identical coverage after the expectation was aligned, and the complete settled rerun passed. VS Code extension-host passes 216/216. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, diff checks, integrated/standalone bundle gates, and definitive blocker reviews pass. Final production sizes are 2,172.9 KB host, 3,070.5 KB main webview, 294.8 KB Markdown webview, and 18,965.5 KB total `dist/`, within unchanged limits. The implementation review returned `VERDICT: NO PRO-13 IMPLEMENTATION BLOCKER`; the final documentation-aware verdict is `VERDICT: NO PRO-13 BLOCKER`.

**Exclusions preserved:** no execution ID, cancellation or process-management redesign, artifact CSV migration, global protocol/session envelope, dispatcher decomposition, DOC/HST work, browser rework, or unrelated refactor.

## Completed Protocol Iteration

### Iteration `PRO-14`: Runtime-Validated Artifact CSV Save Intent And Transfer Channel

**Status:** complete on 2026-08-12 with definitive `VERDICT: NO PRO-14 BLOCKER`. The initial implementation was committed and pushed at `167453aeba2318a67861b3f7a5b6d5078451ee6d`; reviewer-driven exact-correlation hardening and closure documentation were committed and pushed at `2fb571f3ef602f1d801db5ee67c52a264530849a`.

**Boundary:** `src/shared/artifactCsvSaveProtocol.ts` is the sole type/runtime-admission owner for webview messages `requestArtifactCsvSave`, `artifactCsvSaveData`, and `cancelArtifactCsvSaveIntent` plus host deliveries `requestArtifactCsvSaveData` and `cancelArtifactCsvSave`. Host and webview unions reference it. `postMessageToHost()`, `MainWebviewStartupGateway`, and `HostArtifactCsvSaveApplicationHandler` atomically classify and snapshot before E2E capture, startup queue/reentrancy, picker/intent/nonce/tombstone, file, or notification effects. Handler output is typed and validated before transport. The central dispatcher, `artifact-csv-export.ts`, and the bundled browser shim repeat host-delivery admission before pending-export lookup/consumption, timer cancellation, artifact binding, response publication, or download.

**Canonical shapes and exact settlement:** required fields are own enumerable data properties and successful parsers return plain descriptor-captured snapshots. Valid nonblank IDs retain exact whitespace; optional filename and empty CSV behavior remain unchanged. Accepted and rejected transfer branches are exclusive. Callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters. Host nonce settlement compares exact request, box, and artifact identity. Webview challenge settlement peeks by exact export identity and compares exact box/artifact ownership before consuming the pending entry or timer. Padded, mismatched, stale, replayed, and malformed traffic is inert.

**Red-first and review hardening:** the required host and webview characterization run failed exactly 2/24 while 22 neighboring tests passed: array `boxId` consumed the host nonce and produced forged webview bytes. The shared parser made both sequences canonical-only. The blocker reviews then found two remaining exact-correlation coercions. Padded export/cancel traffic failed exactly 1/10 by publishing under the padded nonce; padded box/artifact traffic failed exactly 1/10 by emitting `accepted:false` and consuming current work. Exact lookup plus comparison-before-consumption fixed both, and source-order guards prevent regression. The final reviewer returned `VERDICT: NO PRO-14 BLOCKER`.

**Preserved behavior:** picker-first transfer, eight active intents, one-use UUID nonces, exact 60-second transfer deadline, ten-minute/256-intent replay tombstones, reverse-order concurrency, cancellation/retry/disposal, local and remote URI authority, exact UTF-8 publication, immutable artifact/table-generation/export-policy admission, startup reply reentrancy, and browser-local downloads remain unchanged. The browser build now bundles the same shared parser into its existing least-privileged shim. CSV serialization, artifact lineage/policy, imported CSV, picker/filesystem UX, global envelopes, STS, DOC, CMP, HST, and BRW composition remain outside PRO-14.

**Qualification:** the definitive focused protocol/application/provider/startup/dispatcher/artifact/table/Kusto/SQL/Transformation/persistence/notification/browser ring passes 18 files and 789 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate pass 326 files and 6,787 tests at 49.05% statements. VS Code extension-host passes 216/216 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, `git diff --check` with only existing line-ending warnings, and both integrated/standalone bundle gates pass. Final sizes are 2,177.4 KB host, 3,075.6 KB main webview, 298.4 KB Markdown webview, and 18,978.5 KB total `dist/`; synchronized main/Markdown baselines moved `3021→3026` and `245→249` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid picker/write/download behavior is unchanged, retains the HST-2/EXA-2 exact-byte baseline, and is directly covered across native host and built-browser adapters.

## Completed Protocol Iteration

### Iteration `PRO-15`: Runtime-Validated SQL Tools Service Editor-Language Channel

**Status:** complete on 2026-08-12 with definitive `VERDICT: NO PRO-15 BLOCKER`; committed at `af64b74751da6fe3432f9e7ca23f10b739c1d3cb` and now pushed as the parent of PRO-16 commit `72abcf96153e78a6e56015da2004bcadcbae77a2`.

**Boundary:** `src/shared/sqlStsEditorLanguageProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for webview requests `stsRequest`, `stsDidOpen`, `stsDidChange`, `stsDidClose`, and `stsConnect` plus host deliveries `stsResponse`, `stsDiagnostics`, and `stsConnectionState`. Host and webview unions reference it. Monaco providers validate before request reservation; `kw-sql-section` validates before document-open and connect-pending state; `postMessageToHost()` validates before E2E capture or transport; `HostSqlEditorLifecycleApplicationHandler` revalidates before lifecycle effects; and `SqlEditorLifecycleCoordinator` constructs typed, parsed deliveries. The central dispatcher and direct SQL router both validate before resolver, diagnostics, readiness, owner, or UI effects.

**Canonical shapes and exact settlement:** required identity fields are own enumerable data properties. `stsResponse.result` remains opaque, dense diagnostic arrays are validated while marker records remain opaque, and `expectedOwner` is either absent or a complete exact proof. Empty SQL text, empty diagnostics, null responses, generation zero, empty connection errors, and generation-less connection errors remain valid. Callable, inherited, non-enumerable, accessor, array-shaped, proxy-trapped, cyclic, sparse, inherited-index, and malformed branch traffic fails closed. A canonical well-formed wrong owner still reaches `SqlStsRequestCoordinator` and intentionally settles `null`; malformed ownership never consumes the pending resolver or timer.

**Red-first and guards:** one current request owned by `{ ownerToken: 'owner-a', targetGeneration: 1 }` received a matching `stsResponse` with `ownerToken: ['owner-a']`, followed by the canonical response. Before implementation the router returned `handled`, converted the malformed owner to `undefined`, and consumed the request as `null`; the red run failed exactly 1/13. Final coverage proves the malformed response leaves the exact resolver and timer untouched and only the canonical response settles with its original result. Separate regressions reject malformed diagnostics and connection state before state effects, reject invalid Monaco positions before reservation, reject malformed requests before E2E and host lifecycle effects, and inventory the shared owner plus parser-before-effect ordering.

**Preserved behavior:** `HostSqlEditorLifecycleApplicationHandler` retains all nine routes and development account-map-before-token ordering. `SqlEditorLifecycleCoordinator`, `SqlEditorSessionRegistry`, `SqlStsRequestCoordinator`, `StsLanguageService`, runtime replacement/replay, owner rotation, principal/privacy invalidation, Leave No Trace protected execution, request deadlines, SQL controls, and the host-internal JSON-RPC/data-plane protocol remain unchanged owners.

**Qualification:** the definitive focused and adjacent ring passes 21 files and 729 tests with `--maxWorkers=1` (12 files/509 core plus 9 files/220 adjacent). Complete sequential Vitest and the official coverage gate pass 328 files and 6,800 tests at 49.10% statements. VS Code 1.133.0 extension-host passes 216/216 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, `git diff --check` with only existing line-ending notices, and integrated/standalone bundle gates pass. Stable final sizes are 2,185.9 KB host, 3,084.2 KB main webview, 303.2 KB Markdown webview, and 19,000.6 KB total `dist/`; synchronized main/Markdown baselines moved `3026→3035` and `249→254` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid wire shapes, controls, lifecycle/STS algorithms, replay, and rendering are unchanged while every in-scope admission and settlement boundary is directly covered.

**Exclusions preserved:** no STS JSON-RPC/data-plane, downloader, process, runtime, SQL execution/schema/database, global-envelope, DOC, CMP, HST, or browser redesign.

## Completed Protocol Iteration

### Iteration `PRO-16`: Runtime-Validated SQL Connections Snapshot Channel

**Status:** complete on 2026-08-13 with definitive `VERDICT: NO PRO-16 BLOCKER`; implementation committed and pushed at `72abcf96153e78a6e56015da2004bcadcbae77a2` (`feat(sql-connections): implement SQL connections projection protocol with validation and parsing`). At that closure point, closure documentation and synchronized bundle baselines remained uncommitted; they are now included in the synchronized pushed PRO-25 tree at `4787cd11e7d5c5d3b0ccc06bc50dd10963cf0462`.

**Boundary:** `src/shared/sqlConnectionsProjectionProtocol.ts` is the sole type/runtime-admission owner for webview request `getSqlConnections` and host delivery `sqlConnectionsData`. Host and webview unions reference it. The outbound wrapper validates and descriptor-captures before E2E capture or transport; `HostSqlConnectionsProjectionApplicationHandler` validates before profile/cache/policy effects and publishes only the typed, validated host union. The dispatcher validates and descriptor-captures the complete delivery before revision comparison, connections, cache, selection, favorites, Leave No Trace, selectors, pending restoration, or UI effects.

**Canonical and compatibility contract:** revisioned snapshots require complete state versions, canonical connections, selection, cache, favorites, and Leave No Trace fields. Intentionally supported unrevisioned snapshots retain minimal `id`/`serverUrl` connections and optional projection fields. Ordinary valid parser results retain exact object identity; transport/application capture creates plain own-data values. Dense empty collections, finite legacy-compatible ports, optional SQL fields, revision zero, equal replay, and explicit `revision: undefined` legacy traffic remain valid. Lower present revisions are stale, legacy traffic does not advance the positive watermark, and the watermark commits only after every application effect succeeds. Operational array overrides, sparse/inherited entries, accessors, unstable proxies, malformed nested values, and custom iterators fail closed without execution. SQL cached databases use a null-prototype own-data record, and host policy/account reads use own descriptors, so `__proto__` and `constructor` remain ordinary connection IDs.

**Red-first and review hardening:** the required dispatcher regression failed exactly 1/224 while 223 neighbors passed: malformed `revision: Number.MAX_SAFE_INTEGER` with object-shaped containers cleared current SQL connections before canonical revision 21. Final coverage proves malformed object and iterator-hostile MAX snapshots leave every connection/cache/favorite/selection/Leave No Trace/selector/restore/UI effect and the watermark untouched, then revision 21 applies completely. Reviewer-driven tests additionally cover failed valid-MAX application followed by lower recovery, zero/equal/lower/unrevisioned ordering, prototype-sensitive host publication and cache removal, descriptor/proxy traps, exact parser identity versus captured application values, and large-array-safe nonvariadic connection replacement.

**Preserved behavior:** HST-38 remains the sole trigger/revision-tail, canonical principal/privacy join, cache/favorite filter, global-block, and publication owner. SQL onboarding, favorites mutation, database discovery/cache, last selection, lifecycle, STS, schema, execution, Copilot refresh, selectors, result restoration, compatibility files, and `sqlLeaveNoTraceData` remain separate unchanged owners. No global envelope, mutation workflow, DOC, CMP, HST, or browser redesign was included.

**Qualification:** the definitive core ring passes 5 files and 345 tests; the expanded adjacent SQL projection/lifecycle/UI/persistence ring passes 25 files and 934 tests with `--maxWorkers=1`. Complete sequential Vitest and the official coverage gate pass 329 files and 6,821 tests at 49.11% statements. VS Code 1.133.0 extension-host passes 216/216 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, ESLint with zero errors and five existing warnings, production extension and browser builds, clean task diagnostics, `git diff --check` with only an unrelated existing line-ending notice, and integrated/standalone bundle gates pass. Final sizes are 2,191.5 KB host, 3,093.4 KB main webview, 304.9 KB Markdown webview, and 19,016.9 KB total `dist/`; synchronized main/Markdown baselines moved `3035→3044` and `254→256` with the fixed 50 KB buffer unchanged. The initial production gate correctly rejected the old limits, and one root-level browser command targeted a nonexistent script before the correct browser-package build passed. Native E2E was not repeated because valid controls, snapshot semantics, HST-38 algorithms, selectors, restoration, and compatibility behavior remain unchanged and every in-scope effect is directly covered. The definitive reviewer returned `VERDICT: NO PRO-16 BLOCKER`.

## Completed Protocol Iteration

### Iteration `PRO-17`: Runtime-Validated Kusto Connections Snapshot Channel

**Status:** closed on 2026-08-13. The definitive blocker review returned `VERDICT: NO PRO-17 BLOCKER`. No commit or push was requested or performed.

**Boundary:** `src/shared/kustoConnectionsProjectionProtocol.ts` is the sole type/runtime-admission owner for webview request `getConnections` and host delivery `connectionsData`. Host and webview unions reference it. `postMessageToHost()` captures one stable top-level envelope, validates and captures recognized requests before E2E/transport, and `HostKustoConnectionBrowsingApplicationHandler` repeats admission before HST-40 effects. `HostKustoConnectionsProjectionApplicationHandler` constructs a typed canonical snapshot, publishes absent selections as `null`, descriptor-captures complete application values, and preserves its existing physical-identity reads, policy lock, correlated retry, application acknowledgement, development isolation, revision tail, and disposal.

**Canonical and compatibility contract:** revisioned snapshots require complete connections, accounts, JSON-stable selections, cache, favorites, editing preferences, Copilot first-time state, Leave No Trace state, and development-note state. Intentionally supported unrevisioned deliveries retain partial legacy connections and optional projection fields. Ordinary parser results preserve object identity; capture creates plain own-data arrays and null-prototype records. Dense empty collections, revision zero, equal replay, stale lower revisions, explicit unrevisioned traffic, finite values, and prototype-sensitive keys remain valid. Sparse/inherited arrays, operational iterators, accessors, trapped or descriptor-varying proxies, malformed nested values, and canonical `undefined` selections fail closed without invoking getters.

**Staging, transaction, and revision contract:** the generic Kusto publication envelope remains separate. Stage and commit each capture their own stable payload; recognized Kusto snapshots validate before storage/application, and commit attaches `publicationId` through data descriptors rather than an unchecked spread. One in-flight application fence rejects synchronous acknowledgement reentrancy. Core projection, preference revision, policy state, deferred restore jobs, persisted rows/artifacts/owners, stored-result signatures, immutable artifact bindings/revisions, rendered result state, and clean persistence signature have explicit snapshots. Leave No Trace computes a purge plan while application is reversible; it emits no row deletion, artifact revocation, consumer event, workflow cancellation, UI clear, deferred settlement, or persistence before commit. Commit performs those privacy effects, then selector reconciliation runs outside the rollback window with per-section failure containment. Applied acknowledgement occurs before revision-watermark movement. A pre-commit failure restores the exact prior projection and accepts a lower recovery snapshot.

**Red-first and review hardening:** the required malformed-MAX regression first reproduced state/watermark poisoning. Final coverage stages revision 300, rejects malformed `Number.MAX_SAFE_INTEGER` with `connections: {}` in both acknowledgement phases with zero projection/privacy/preference/selector effect, and applies revision 301. Reviewer-driven regressions cover JSON transport of fresh-profile null selections; failed valid MAX followed by lower recovery; synchronous lower-revision dispatch during applied acknowledgement; request, delivery, and staged unknown-to-known discriminator proxies with one descriptor read and zero getters/effects; prototype-sensitive records; exact artifact/owner/signature rollback; no pre-commit revocation or persistence; revocation/persistence at commit; and real two-section selector failure containment. `runtimeMessageEnvelope.ts` now captures each main-webview envelope once and the outbound type whitelist is mechanically checked against the typed union. This shallow guard does not validate unrelated domain payloads.

**Preserved/excluded:** HST-40 remains trigger/revision-tail, physical account/connection, canonical policy-lock/retry, and publication owner; HST-31 remains request-route owner. Connection/authentication, database cache/discovery, favorites mutation, onboarding/intake, execution, schema invalidation/prewarm, compatibility files, generic Kusto publication envelopes, SQL traffic including `sqlLeaveNoTraceData`, DOC, CMP, HST, and browser algorithms remain separate. No control, valid external wire shape, file format, or visual layout changed.

**Qualification:** the definitive focused reviewer ring passes 624 tests. Complete sequential Vitest and the official coverage gate pass 331 files and 6,861 tests at 49.27% statements. The first complete run passed 330/331 files and 6,860/6,861 tests, failing only a stale HST-21 source guard that still required KQL language parsing from raw `msg`; the corrected guard passed 3/3 and the unchanged complete rerun passed. VS Code 1.133.0 extension-host passes 216/216 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, focused `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,202.5 KB host, 3,114.2 KB main webview, 310.6 KB Markdown webview, and 19,054.5 KB total `dist/`; synchronized host/main/Markdown baselines moved `2147→2153`, `3044→3065`, and `256→261` with the fixed 50 KB buffer unchanged. The first production build correctly rejected the old limits. An initial Vitest invocation used removed `--minWorkers` syntax and ran no tests; final sequential evidence uses `--maxWorkers=1 --no-file-parallelism`. Native E2E was not repeated because controls, valid external message shapes, file formats, and rendering are unchanged, while protocol, transaction, persistence, selector, extension-host, and browser boundaries are directly covered. The definitive reviewer returned `VERDICT: NO PRO-17 BLOCKER`.

## Completed Compatibility Iteration

### Iteration `CMP-2`: Shared Compatibility Projection And Reload Coordinator

**Status:** closed on 2026-08-13. The definitive blocker review returned `VERDICT: NO CMP-2 BLOCKER`. No commit or push was requested or performed.

**Boundary:** one panel-scoped `CompatSidecarProjectionCoordinator` now owns monotonic projection generation reservation, the generation-zero pre-initial baseline, active/pending source identity, same-source pending-projection supersession, bounded initial retry/coalescing plus one requested follow-up, `documentReloadResult` currentness, `persistDocument` source-generation admission, source-reload authority, and terminal rollback-failure fencing for both KQL and SQL compatibility providers. Reload waiters remain `CompatSidecarSession` primitives and are reserved lazily after language-specific preparation, immediately before transport, so sanitation time does not consume their five-second deadline.

**Displaced and preserved authority:** `postDocumentGeneration`, `activeSourceGeneration`, `pendingSourceGeneration`, `pendingProjectionEditRevision`, `initialProjectionRecovery`, `initialProjectionRestartRequested`, `sourceReloadAuthority`, and `sourceRollbackFailedCandidate` are absent from both providers. `MainWebviewStartupGateway` retains transport; `compatibilityPersistenceProtocol.ts` retains schemas/session fencing; `CompatSidecarSession` retains revisions, persist/upgrade queues, final/reload waiters, and close primitives; `CompatSidecarCloseCoordinator` retains Save/Discard/recovery/cleanup. Providers retain projection payload construction, privacy sanitation, exact primary-text edits, EOL-normalized rollback effects, sidecar materialization/storage/CAS/repair/recovery, KQL inference/cache, SQL lifecycle integration, upgrades, Save, close, diff UX, and user-facing copy.

**Race and compatibility contract:** the real parameterized KQL/SQL test holds projection A's acknowledgement, changes source to B, starts projection B, releases A late, then sends a newer-edit A-generation snapshot. A cannot advance the session revision, mutate B text, publish A sidecar state, or receive an acknowledgement; B alone activates and persists. Projection source currentness remains byte-exact while rollback comparison remains EOL-normalized. A current failed initialized reload demotes initialization so the next request re-enters bounded recovery; a superseded failure cannot demote a successful successor. Generation zero remains valid before the first acknowledgement. Timeout, callback throw before/after reservation, duplicate acknowledgement, and disposal-after-reservation paths retire their waiter without stale adoption.

**Qualification:** the final focused coordinator/session/store/format/close/protocol/provider-guard ring passes 8 files and 175 tests. The complete parameterized compatibility extension-host matrix passes 135/135 with `--timeout 5000`, including the required A/B race and reviewer-driven recovery/CRLF/close cases. Complete sequential and official coverage Vitest pass 332 files and 6,875 tests at 49.27% statements. The clean complete VS Code 1.133.0 extension-host rerun passes 217/217 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`; the first run had one unchanged linked-notebook Save timing failure, which passed alone before rerun. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean task-owned diagnostics and `git diff --check`, and synchronized bundle gates pass. Final sizes are 2,206.7 KB host, 3,114.2 KB main webview, 310.6 KB Markdown webview, and 19,058.7 KB total `dist/`; only the host baseline moves `2153→2157`, with main/Markdown baselines unchanged at `3065/261` and the fixed 50 KB buffer preserved. Native E2E was not required because controls, valid message shapes, file formats, primary-text behavior, and visible rendering remain unchanged while the real provider/session/store boundary is directly covered.

## Completed Compatibility Iteration

### Iteration `CMP-3`: Shared Compatibility Persist Snapshot Application Coordinator

**Status:** closed on 2026-08-13 with definitive `VERDICT: NO CMP-3 BLOCKER`; committed and pushed in synchronized combined commit `396446f58e77eee1ca7e89116c2a0e6cab0e1392`.

**Boundary:** one panel-scoped `CompatSidecarPersistCoordinator` now owns the complete post-protocol KQL/SQL `persistDocument` application transition. It directly composes the existing `CompatSidecarSession` and `CompatSidecarProjectionCoordinator` for final-request admission, load/unavailable/noop/closing/stale and failure terminals, two-phase validation across upgrades, generation/revision/queue admission, sanitation/materialization sequencing, EOL and blank-file policy, source verification, projection-owned rollback, retryable source-matching draft retention, state/materialized commit ordering, one non-awaited live `persistDocumentAck` attempt, beforeunload observation, and exactly-once final settlement. It adds no queue, revision store, waiter map, rollback authority, durable store call, or VS Code dependency.

**Displaced and preserved authority:** both provider `persistDocument` cases are one coordinator call. Their direct `queuePersist`, `completeFinalPersist`, `admitPersist`, `rollbackSupersededSourceEdit`, `persistDocumentAck`, and `markBeforeUnload` sites are deleted and statically forbidden. Providers retain typed physical validation/build, privacy sanitation, sidecar materialization, `WorkspaceEdit` construction/tracking, source reads, in-memory state adoption, diff publication, error UX, KQL inference/cache, SQL lifecycle, upgrades, sidecar create/adopt UX, and existing `didSave`/close storage effects. `CompatSidecarSession` remains the queue/revision/final-waiter owner; `CompatSidecarProjectionCoordinator` remains generation/epoch/rollback owner; `CompatSidecarFormat` remains validation/build owner; and store/close owners retain CAS, repair, recovery, Save/Discard, and drain.

**Red-first and terminal matrix:** the required real-provider test failed exactly 1 case while 217 extension-host tests passed. Before coordinator release, the legacy KQL branch created zero coordinators but performed one source edit, three session commits, one diff publication, one acknowledgement, and one final settlement; durable sidecar bytes correctly remained unchanged. The same unchanged test now parameterizes KQL and SQL, creates a real final waiter, holds the real coordinator, proves zero pre-release effects, then proves one coordinator call, one live acknowledgement attempt, one final settlement, and no durable sidecar write until the existing `didSave` path. The direct 17-test matrix covers unknown/expired finals, load/unavailable/noop/closing/stale/generation terminals, two-phase upgrade revalidation, sanitation/materialization failure, retryable draft retention, source reject/drift/supersession/rollback failure, CRLF and blank-file policy, acknowledgement false/reject/pending/throw, disposal-retained finals, success ordering, and ownership guards. Host-message inventory attributes `persistDocumentAck` to the coordinator.

**Qualification:** the focused coordinator/session/projection/close/store/format/protocol/provider/inventory ring passes 9 files and 195 tests. Complete sequential Vitest and the official coverage gate pass 335 files and 6,923 tests at 49.27% statements. The complete explicit VS Code 1.133.0 extension-host suite passes 218/218 with `--timeout 5000` and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, `git diff --check`, zero production source maps, and integrated/standalone bundle gates pass. The first deterministic production gate correctly rejected intentional host growth by 1 KB; CMP-3 moves the host baseline `2168→2169`, while a preserved concurrent main-webview change independently moves the synchronized main baseline `3074→3076`. The 50 KB buffer is unchanged. Final sizes are 2,218.7 KB host, 3,125.2 KB main webview, 317.3 KB Markdown webview, and 19,088.4 KB total `dist/`. Native E2E was not required because controls, valid message shapes, file formats, primary-text semantics, storage/CAS, and rendering remain unchanged while every migrated terminal and both real provider paths are directly covered.

## Completed Protocol Iteration

### Iteration `PRO-18`: Runtime-Validated Query Sharing Channel

**Status:** closed on 2026-08-13 with definitive `VERDICT: NO PRO-18 BLOCKER`. The initial implementation review returned `VERDICT: NO PRO-18 IMPLEMENTATION BLOCKER`; the first documentation-aware review found and drove canonical-array hardening plus isolated bundle measurement, and the definitive re-review found no remaining blocker. No commit or push was requested or performed.

**Boundary:** `src/shared/querySharingProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for webview requests `copyAdeLink` and `shareToClipboard` plus host delivery `shareContentReady`. Host and webview unions reference the shared request union. `postMessageToHost()` descriptor-captures before E2E capture or VS Code transport; `HostQuerySharingApplicationHandler` repeats admission before connection lookup, gzip/base64 ADX encoding, or Kusto/SQL formatting and publishes only a typed, revalidated delivery. The central dispatcher admits that delivery before `ClipboardItem`, rich clipboard, or plain-text fallback effects.

**Canonical and hostile-input contract:** required scalar fields are own enumerable data properties and retain exact valid values, including empty strings handled by existing UX. `totalRows` is a non-negative safe integer. `columns` and every `rowsData` row are dense string-only descriptor snapshots that neither execute iterators nor invoke getters. Callable, inherited, non-enumerable, accessor, sparse, operational-iterator, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed. The unconsumed handler-emitted webview `showInfo` duplicate is deleted while the exact native success notification remains.

**Red-first and preserved behavior:** the required dispatcher test failed exactly 1/239 while 238 neighbors passed because non-string malformed content caused two clipboard writes; accessor traffic invoked no getter. Final behavior leaves malformed traffic inert and performs exactly one canonical write. HST-5 gzip/base64 encoding, ADX URL escaping, Kusto/SQL rich and plain formatting, immutable `share:clipboard:result` binding, `shareToClipboard` policy, row caps, native clipboard fallback, disposal, browser behavior, valid wire values, and notification wording remain unchanged. No request correlation, artifact-policy, execution, persistence, provider, general clipboard, or browser-composition work was included.

**Qualification:** the core protocol/transport/handler/dispatcher/inventory ring passes 5 files and 359 tests; the adjacent provider/toolbar/artifact/persistence/SQL ring passes 10 files and 578 tests. Complete sequential Vitest and the official coverage gate pass 333 files and 6,887 tests at 49.29% statements. VS Code 1.133.0 extension-host passes 217/217 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, clean `git diff --check`, and integrated/standalone bundle gates pass. Watchers were paused for the definitive production measurement, which contained zero source maps. Final sizes are 2,211.5 KB host, 3,119.5 KB main webview, 315.2 KB Markdown webview, and 19,073.4 KB total `dist/`; synchronized host/main/Markdown baselines move `2157→2162`, `3065→3070`, and `261→266` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid query-sharing behavior and browser composition are unchanged, the HST-5 exact clipboard baseline remains applicable, and every migrated admission/effect boundary is directly covered.

## Completed Protocol Iteration

### Iteration `PRO-19`: Runtime-Validated Editing Preferences Channel

**Status:** closed on 2026-08-13 with definitive `VERDICT: NO PRO-19 BLOCKER`. Implementation re-review returned `VERDICT: NO PRO-19 IMPLEMENTATION BLOCKER`; the documentation-aware closure review found no remaining blocker.

**Boundary:** `src/shared/editingPreferences.ts` is the sole descriptor-safe type/runtime owner for webview requests `setCaretDocsEnabled`, `setAutoTriggerAutocompleteEnabled`, and `setCopilotInlineCompletionsEnabled` plus host delivery `editingPreferencesData`. Host and webview unions reference its request union. `postMessageToHost()` captures before E2E/transport; HST-14 re-admits before configuration writes; host snapshot creation, HST-14 output, first launch, and configuration refresh validate before publication; dispatcher and leaf admission precede revision, controls, user-set flags, documentation, or watermark effects.

**Canonical and revision contract:** required fields are own enumerable data properties. Setters preserve exact booleans. Host deliveries require six exact booleans and one non-negative safe-integer revision and become frozen descriptor snapshots; revalidation preserves canonical snapshot identity. Lower revisions are stale and equal revisions intentionally reapply so cross-window updates can converge after revision saturation. Host growth remains timestamp-monotonic until it saturates at `Number.MAX_SAFE_INTEGER`. Callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters.

**Red-first and review hardening:** the required malformed revision 999 followed by canonical revision 1 and malformed-setter characterization failed exactly 3 tests while 248 neighbors passed: malformed delivery reached dispatcher and leaf effects, and all three malformed setters wrote configuration. Final coverage proves zero malformed control, user-set, documentation, watermark, configuration, publication, or revision effects and exactly one canonical application. The first pessimistic review found safe-integer overflow at `Number.MAX_SAFE_INTEGER`; revision growth now saturates, equal-MAX snapshots reapply, and behavioral tests replace the brittle source assertion.

**Preserved behavior:** HST-14 remains mutation/publication owner. Application-scoped configuration, migration, first-launch transactions, configuration listeners, all-webview publication with current-panel fallback, accepted work across disposal, optimistic Kusto/SQL controls, Smart documentation presentation, PRO-17 embedded preference fields, persistence signatures, and browser-local defaults remain unchanged. No request correlation, panel-session/global envelope, PRO-17 projection, first-launch, dispatcher, browser, or persistence/provider redesign was included.

**Qualification:** the final core ring passes 5 files and 265 tests; the expanded adjacent HST-14/PRO-17/persistence/first-launch/provider/protocol ring passes 15 files and 569 tests. Complete sequential Vitest and the official coverage gate pass 334 files and 6,903 tests at 49.30% statements. VS Code 1.133.0 extension-host passes 217/217 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, zero production source maps, and both synchronized bundle gates pass. Final watcher-free sizes are 2,217.0 KB host, 3,123.2 KB main webview, 317.3 KB Markdown webview, and 19,084.8 KB total `dist/`; synchronized host/main/Markdown baselines move `2162→2168`, `3070→3074`, and `266→268` with the fixed 50 KB buffer unchanged. Watch tasks were restored healthy. Native E2E was not repeated because visible controls, first-launch UX, valid wire values, and PRO-17 projection behavior remain unchanged while every migrated admission/effect boundary is directly covered.

## Completed Protocol Iteration

### Iteration `PRO-20`: Runtime-Validated Copilot Inline-Completion Channel

**Status:** closed on 2026-08-13 with definitive `VERDICT: NO PRO-20 BLOCKER`. The initial implementation review returned `VERDICT: NO PRO-20 IMPLEMENTATION BLOCKER`; the documentation-aware re-review found no remaining blocker.

**Boundary:** `src/shared/copilotInlineCompletionProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for webview `requestCopilotInlineCompletion` and host `copilotInlineCompletionResult`. Host and webview unions reference the shared request type. `postMessageToHost()` captures before E2E/transport; HST-24 captures before SQL owner or model effects; both host result producers construct typed results and parse before transport; dispatcher and direct SQL routing capture before owner/resolver/timer/UI effects. The narrow Monaco runtime owns exact resolver consumption while request creation, spinner, ten-second timeout, and completion application remain in `monaco.ts`.

**Canonical and hostile-input contract:** exact request, box, text-before, text-after, optional flavor/owner, completion text, and optional error values are preserved without trimming or coercion. Dense empty and populated completion arrays are valid. Required and present optional fields are own enumerable data properties; completion entries are canonical plain/null-prototype records. Callable, inherited, non-enumerable, accessor, sparse, inherited-index, operational-iterator, descriptor-trapping, revoked, cyclic, unbounded, custom-prototype, and malformed recognized traffic fails closed without invoking getters. Malformed matching, canonical stale, and duplicate results cannot consume current work; only a canonical current result settles once.

**Preserved owners and exclusions:** HST-24 retains route/flavor, SQL owner assertion/delegation, empty fallback, and disposal. `CopilotService` retains model selection/cache, prompts, context trimming, eight-second cancellation, replacement, streaming, SQL owner revalidation, cleanup, and completion construction. SQL lifecycle/privacy retains owner-token authority. Monaco retains request/spinner/application and the ten-second deadline; editing preferences remain unchanged. No correlation ID, cancellation, prompt/model, ambient bridge, global protocol, browser, CMP, DOC, or HST redesign was included.

**Red-first, guards, and qualification:** the required characterization failed exactly two tests while 254 neighbors passed: malformed non-string `insertText` deleted the live resolver, and an array owner token reached SQL owned-message admission. Final regressions add malformed/stale/current/duplicate resolver ordering, malformed owner containers, exact model output, outbound and HST request admission, both typed producers, hostile parser cases, sole-owner/source-order guards, and sender inventory. The expanded ring passes 13 files and 729 tests. Complete sequential and official coverage Vitest pass 336 files and 6,940 tests at 49.27% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task diagnostics, clean `git diff --check`, zero production source maps, and integrated/standalone bundle gates pass. Watcher-free final sizes are 2,225.2 KB host, 3,132.2 KB main webview, 320.7 KB Markdown webview, and 19,105.3 KB total `dist/`; synchronized baselines move `2171→2176`, `3077→3083`, and `268→271` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid controls, request/application behavior, model prompts, SQL ownership/privacy, timing, and rendering are unchanged while every migrated boundary is directly covered.

## Completed Protocol Iteration

### Iteration `PRO-21`: Runtime-Validated Kusto Publication Transaction Channel

**Status:** closed on 2026-08-14 with definitive `VERDICT: NO PRO-21 BLOCKER`. The post-fix implementation review returned `VERDICT: NO PRO-21 IMPLEMENTATION BLOCKER`.

**Boundary:** `src/shared/kustoPublicationProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for `kustoPublicationStage`, `kustoPublicationAck`, `kustoPublicationCommit`, and `kustoPublicationRevoke` across the main Query Editor, Connection Manager, and Cached Values. Host and webview unions reference its acknowledgement type. All three host senders capture canonical stage/commit/revoke messages before transport, all three host receivers admit acknowledgements before waiter lookup or deadline settlement, and all three webviews admit before stage, commit, revoke, timer, payload, completed-ledger, or acknowledgement effects.

**Canonical and hostile-input contract:** exact nonblank publication IDs, `staged`/`applied` phases, booleans, and finite deadlines survive without coercion. Required fields are own enumerable data properties and canonical messages contain no extra fields. Stage payloads become frozen descriptor-captured outer records while nested terminal, connection projection, search, schema, snapshot, privacy, and application validators retain semantics. Callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, custom/unbounded-prototype, and malformed recognized traffic fails closed without invoking getters or consuming work. `runtimeMessageEnvelope.ts` preserves the original one-shot descriptor snapshot so domain admission precedes normalization without weakening the existing exactly-once unknown-to-known proxy guard.

**Red-first and review hardening:** the required parameterized Query Editor, Connection Manager, and Cached Values malformed-commit sequence plus the host waiter regression failed exactly four tests while 249 neighbors passed: malformed array IDs consumed the exact stage or deadline. Final tests require exact stage, payload, timer, host waiter, and deadline identity to survive malformed traffic; only the canonical commit/acknowledgement applies once. The first pessimistic review found generic envelope normalization plus startup/provider routing before domain admission; five boundary probes failed while 285 neighbors passed. Raw descriptor classification now precedes E2E capture, startup correlation/queueing, outbound preparation, provider tracing/routing, and every leaf effect while unrelated discriminator proxies remain one-read inert.

**Preserved owners and exclusions:** `HostKustoSectionExecutionApplicationHandler`, Connection Manager, and Cached Values retain their separate waiter/stage/completed ledgers, five-second deadlines, ten-second completed entries, revoke reconciliation, privacy locks, application acknowledgements, nested payload validators, retry behavior, and disposal. Main startup reentrancy and the bundled identity-checklist listener use the shared parser without becoming new authorities. No global/session envelope, execution algorithm, payload-domain migration, ledger centralization, browser behavior, DOC, CMP, HST, or source-authentication policy changed.

**Qualification:** the definitive expanded ring passes 18 files and 938 tests. Complete sequential and official coverage Vitest pass 337 files and 6,957 tests at 49.33% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean `git diff --check` apart from two existing line-ending notices, zero root/browser production source maps, and both synchronized bundle gates pass. Final sizes are 2,230.9 KB host, 3,137.9 KB main webview, 324.2 KB Markdown webview, and 19,120.2 KB total `dist/`; synchronized baselines move `2176→2181`, `3083→3088`, and `271→275` with the fixed 50 KB buffer unchanged. The first production build correctly rejected the old limits. Native E2E was not repeated because valid transaction shapes, deadlines, privacy locks, controls, and rendering are unchanged while every live ingress, waiter, ledger, startup, and bundled-listener boundary is directly covered.

## Completed Document Iteration

### Iteration `DOC-7`: Host-Owned Development-Note State

**Status:** closed on 2026-08-14 with definitive `VERDICT: NO DOC-7 BLOCKER`. The final implementation review returned `VERDICT: NO DOC-7 IMPLEMENTATION BLOCKER`. No commit or push was requested or performed.

**Boundary:** `developmentNoteSectionDefinition.ts` owns persisted Development Note section and entry validation, deep cloning, and entries patches. Hidden `devnotes` joins Markdown, URL, Python, Chart, Transformation, and HTML inside the existing `MarkdownDocumentAggregate`, optimistic full-projection client, version-1 document-view channel, physical URI queue, acknowledged projection, and Save barrier. Native restore, panel recreation, state reads, and Save derive notes from the acknowledged host projection; the old native passthrough field and direct `schedulePersist('devnotes-update')` authority are absent.

**Mutation and compatibility contract:** native add, supersede, and remove plan against the optimistic projection, preserve multiple sections and unique note matching, generate collision-free hidden section IDs, and settle only from the exact pending command's verified full projection. The global command waiter remains Save-barrier-only. Sidecar-enabled `.kql` and `.sql` companions intentionally retain one renamed `metadataFreeDevelopmentNoteSections` adapter and existing sidecar persistence. Plain compatibility, Markdown compatibility, browser, invalid, and read-only hosts remain rejected. HST-23 retains Copilot request IDs, the exact five-second deadline, delivery/failure/response/disposal settlement, and unclaimed tool-response fallthrough. Copilot/agent note IDs, categories, content, sources, history, target resolution, the show-notes command, and hidden/non-addable policy remain unchanged.

**Lossless and lifecycle contract:** COD-1 overlay retains future Development Note section and stable-entry metadata, exact mixed order, root/state extensions, and opaque sections; changing a replacement note ID does not inherit the old entry's future metadata. Stale adapter notes cannot replace acknowledged state during provider rebase, recreation, final persistence, or Save. Rejected command projections reconcile hidden notes without visible UI work.

**Red-first and review hardening:** the required aggregate regression failed exactly 1/12 while 11 neighbors passed because patching `devnotes_owner` returned `missing-section`. The first blocker review found global queue settlement could report committed mutation A as failed when later mutation B rejected; exact per-command settlement inside the existing pending ledger fixed it, with an overlapping A-success/B-failure regression. The second review found sidecar-enabled `.kql`/`.sql` compatibility would be rejected; an explicit metadata-free companion branch restored add/supersede/remove and persistence for both while keeping native authority singular. Documentation-aware reviews found reorder still inferred hidden ownership from the `devnotes_` prefix and compared normalized companion ownership to unnormalized reorder IDs; exact projected hidden IDs plus normalized hidden-only comparison now admit arbitrary note IDs, whitespace-bearing companion IDs, and visible prefix-sharing IDs, with static guards against both regressions.

**Qualification:** the final expanded focused ring passes 15 files and 974 tests with `--maxWorkers=1 --no-file-parallelism`. Complete sequential Vitest and the official coverage gate pass 337 files and 6,970 tests at 49.48% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`; the real provider lifecycle proves authoritative section revision 1 survives a stale adapter note, recreation, and lossless Save. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, clean `git diff --check`, and integrated/standalone bundle gates pass. Final sizes are 2,233.5 KB host, 3,144.6 KB main webview, 326.7 KB Markdown webview, and 19,132.0 KB total `dist/`; synchronized baselines move `2181→2184`, `3088→3095`, and `275→277` with the fixed 50 KB buffer unchanged. Native UI E2E was not repeated because Development Notes remain hidden and introduce no control or rendering change; real provider lifecycle evidence covers the changed native behavior.

## Completed Protocol Iteration

### Iteration `PRO-22`: Runtime-Validated Development-Note Mutation Channel

**Status:** closed on 2026-08-14 with definitive `VERDICT: NO PRO-22 BLOCKER`. The earlier implementation review returned `VERDICT: NO PRO-22 IMPLEMENTATION BLOCKER`. No commit or push was requested or performed.

**Boundary:** `src/shared/developmentNoteMutationProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for exact add, supersede, and remove `updateDevNotes` requests plus only their correlated `toolResponse` subset. Both Copilot and agent producers construct canonical requests before waiter, timer, or transport effects, so caller payloads cannot overwrite generated type or correlation identity. Main-webview ingress validates the original descriptor snapshot before document-runtime, native aggregate, or KQL/SQL companion effects. Every mutation terminal is built through one typed response constructor.

**Host correlation and hostile-input contract:** `MainWebviewStartupGateway` and standalone panel ingress capture original descriptors plus direct prototype identity as their first operation and route only an inert own-data clone. Strict Kusto publication, artifact CSV, and Development Note protocols retain raw-shape authority through that snapshot. The mutation protocol derives and caches one frozen complete admission by captured-envelope identity, so startup reentrancy, provider tracing/routing, Copilot, and agent correlation consume the same decision. Nested entry/result/related-section values reject self-aliases and Node-host proxy/wrapper objects before reflection; Kusto publication applies the same rule to its opaque outer payload. Matching malformed traffic is claimed before waiter-map lookup, timer cancellation, or settlement. Canonical inherited or non-enumerable data-property correlation is identified through descriptors without invoking getters; accessor-backed correlation is quarantined only while Development Note work is pending. The agent's generic resolver categorically refuses marked mutation IDs unless reached from the canonical admitted path. Callable, array-shaped, custom-prototype, inherited/accessor/non-enumerable wire fields, sparse or operational arrays, descriptor traps, revoked/cyclic/unbounded proxies, aliases, extra fields, identity aliases, transparent wrappers, malformed entry fields, non-boolean success, and top-level or nested descriptor/prototype variation fail closed. Canonical unmatched tool responses retain generic fallthrough.

**Preserved owners and behavior:** DOC-7 retains aggregate/client/physical URI queue/acknowledged projection/Save authority and exact command-scoped settlement. HST-23 retains Copilot request generation, exact five-second deadline, delivery failure/disposal behavior, and matching-response priority. Agent target resolution, note IDs/category/content/source/history, KQL/SQL companion admission and persistence, hidden/non-addable policy, show-notes behavior, generic tool orchestration, Kusto/SQL execution, and all file formats remain unchanged.

**Red-first and review hardening:** the required host sequence failed exactly 1/10 because malformed matching `{ success: 'yes' }` consumed the live waiter. Early reviews retained fixes for revoked nested arrays and inherited/accessor generic fallthrough. Documentation-aware reviews then successively exposed startup request-ID and type getters, top-level and nested descriptor/prototype variation, standalone-panel repeated reflection, artifact CSV hidden-field laundering, self-aliasing nested records, and transparent proxy wrappers around shape-varying envelopes. Each blocker received an exact live five-/sixty-second waiter or protocol red before repair. Capture-first ingress, descriptors-plus-prototype metadata, one cached frozen mutation admission, descriptor-preserving artifact admission, early alias checks, and trap-free Node-host nested proxy rejection retain every red. The final startup suite passes 16/16, the adjacent ring passes 21 files/1,031 tests, and the definitive review returns `VERDICT: NO PRO-22 BLOCKER`.

**Qualification:** the original focused ring passes 9 files and 469 tests; the definitive adjacent preservation ring passes 21 files and 1,031 tests after all reviewer hardening. Complete sequential and official coverage Vitest pass 338 files and 6,995 tests at 49.48% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean task diagnostics, `git diff --check` apart from two line-ending notices, zero root/browser production source maps, and integrated/standalone bundle gates pass. Final sizes are 2,246.5 KB host, 3,151.0 KB main webview, 327.0 KB Markdown webview, and 19,151.6 KB total `dist/`; synchronized baselines move `2184→2197` and `3095→3101` with the fixed 50 KB buffer unchanged. The production gates correctly rejected the prior and interim limits. Native E2E was not repeated because valid mutation shapes, note semantics/persistence, controls, and rendering are unchanged while every live producer, ingress, waiter, correlation, and generic-fallthrough boundary is directly covered.

## Completed Protocol Iteration

### Iteration `PRO-23`: Runtime-Validated Tool-State Snapshot Channel

**Status:** closed on 2026-08-15 with definitive `VERDICT: NO PRO-23 BLOCKER`. The implementation blocker review returned `VERDICT: NO PRO-23 IMPLEMENTATION BLOCKER`.

**Boundary:** `src/shared/toolStateSnapshotProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for exact `requestToolState` and `toolStateResponse`. Host requests are constructed and admitted before the five-second waiter, timer, or transport. Main-webview startup and standalone provider ingress consume the original descriptor snapshot before reentrancy or routing. The webview admits before `getKqlxState()`, Kusto schema-owner mutation, or response effects, and constructs every normal/loading/error response through the shared owner. Host response admission precedes waiter lookup, timer cancellation, SQL comparison-owner reconciliation, and settlement.

**Canonical snapshot contract:** exact nonblank request identity, optional `purpose: 'schema-refresh'`, optional target identity, and optional error strings are preserved without coercion. Sections are a dense canonical array of canonical outer records with exact string `type` and optional string `id`; nested JSON-like state is descriptor-captured into deeply frozen arrays and null-prototype records. Snapshot depth, total values, container entries, and cumulative strings are bounded. Callable, inherited, non-enumerable, accessor, sparse, operational-iterator, custom-prototype, proxy, cyclic, non-finite, extra-field, and oversized traffic fails closed without invoking getters or consuming current work. One immutable admission is cached across startup, provider, and handler hops while direct provider routing remains reference-identical.

**Preserved owners and behavior:** HST-30 retains orchestrator connection/reactivation/disconnect, exact five-second deadline, state request ledger, purpose/target semantics, schema-refresh filtering, exact Kusto resolution, SQL callbacks/reconciliation, disposal, and all three tool routes. `KustoWorkbenchToolOrchestrator`, `SchemaService`, and `SqlEditorLifecycleCoordinator` remain canonical for targeting, schema algorithms, and SQL ownership. PRO-22 Development Note responses retain first claim and generic `toolResponse` remains outside this protocol. Valid Kusto/SQL projection, startup order, file formats, controls, rendering, browser behavior, and tool execution are unchanged.

**Red-first and review hardening:** the required sequence failed exactly 1/8 because matching `sections: {}` settled before 4,999 ms. The final regression proves malformed and oversized matching responses leave the exact waiter, timer, and SQL reconciliation untouched; only the canonical captured array settles. The first pessimistic review found duplicate provider parsing broke HST-30 reference identity, a stale source guard expected inline message construction, and primitive-heavy snapshots were insufficiently bounded. Cached immutable admission, direct-provider preservation, shared-constructor guards, and total/container/string limits fixed all three; the re-review returned `VERDICT: NO PRO-23 IMPLEMENTATION BLOCKER`.

**Qualification:** the hardened core ring passes 7 files and 406 tests; the definitive expanded orchestrator/schema/SQL/PRO-22/provider/startup/protocol ring passes 13 files and 604 tests. Complete sequential and official coverage Vitest pass 339 files and 7,009 tests at 49.49% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics and `git diff --check`, and integrated/standalone bundle gates pass. Final sizes are 2,254.0 KB host, 3,158.5 KB main webview, 332.5 KB Markdown webview, and 19,172.1 KB total `dist/`; synchronized baselines move `2197→2204`, `3101→3109`, and `277→283` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid state semantics, controls, schema algorithms, SQL reconciliation, and rendering remain unchanged while every migrated boundary is directly covered. The definitive documentation-aware review returned `VERDICT: NO PRO-23 BLOCKER`.

## Completed Protocol Iteration

### Iteration `PRO-24`: Runtime-Validated Kusto Execution-Start Handshake

**Status:** closed on 2026-08-15 with definitive `VERDICT: NO PRO-24 BLOCKER`. The final implementation review returned `VERDICT: NO PRO-24 IMPLEMENTATION BLOCKER`. No commit or push was requested or performed.

**Boundary:** `src/shared/kustoExecutionStartProtocol.ts` is the sole descriptor-safe type, constructor, and runtime-admission owner for exact `kustoExecutionStarted` and `kustoExecutionStartedAck`. Host construction captures the reservation before waiter/timer/transport effects. Main startup and standalone ingress admit the original descriptor snapshot before queueing, correlated reentrancy, tracing, or routing. The webview admits before section lookup, target/predecessor checks, comparison binding, `beginQueryExecution()`, acknowledgement, or events; acknowledgements use a scalar-only typed constructor and validate before E2E/transport. Host acknowledgement admission precedes exact ledger lookup, deletion, timer cancellation, and settlement.

**Canonical and comparison contract:** only Copilot starts without `comparisonRun` and comparison starts with complete exact lineage are canonical; manual and tool starts remain preclaimed. Exact reservation, lifecycle, target, producer, query, optional Copilot request, optional predecessor, and comparison identity survive without coercion. A delayed start must match the current lifecycle/target and, whenever an execution is active, its explicit expected predecessor. Tentative comparison binding keeps a unique temporary pin on the old artifact, commits only after the exact section claim succeeds, and restores the old binding before releasing the pin on rejection. Callable, inherited, non-enumerable, accessor, array, custom-prototype, proxy, extra-field, malformed, and impossible producer traffic is inert.

**Red-first and review hardening:** the required sequence started one Copilot execution, delivered exact identity with `accepted: 'yes'`, held 4,999 ms, then delivered canonical `accepted: true`. Before implementation, malformed traffic deleted the sole waiter; the red failed exactly 1/10 while nine neighbors passed. Pessimistic reviews drove trap-free constructor input, direct and captured proxy hard stops, exact producer forms, target/predecessor preflight, exception-safe binding rollback, transactional controller-owner installation, and real `ResultArtifactStore` plus real-controller regressions proving an omitted, refused, or throwing delayed comparison start and its later cancellation cannot prune or replace a newer binding or owner.

**Qualification:** the final core ring passes 7 files and 414 tests; the expanded Kusto execution/Copilot/tool/artifact/persistence/schema ring passes 17 files and 984 tests. Complete sequential and official coverage Vitest pass 340 files and 7,029 tests at 49.53% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics and `git diff --check`, zero production source maps, and both synchronized bundle gates pass. Final sizes are 2,263.9 KB host, 3,166.9 KB main webview, 335.6 KB Markdown webview, and 19,193.6 KB total `dist/`; synchronized baselines move `2204→2214`, `3109→3117`, and `283→286` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid execution, comparison, controls, terminals, artifacts, and rendering remain unchanged while every migrated boundary, the real controller, and the real artifact store are directly covered. Reviews returned `VERDICT: NO PRO-24 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO PRO-24 BLOCKER`.

### Iteration `PRO-25`: Runtime-Validated Power BI Publish Application Handshake

**Status:** closed on 2026-08-15 with definitive `VERDICT: NO PRO-25 BLOCKER`. The implementation review returned `VERDICT: NO PRO-25 IMPLEMENTATION BLOCKER`; the architecture/rescore review returned `VERDICT: NO PRO-25 DOCUMENTATION BLOCKER`.

**Boundary:** `src/shared/powerBiPublishProtocol.ts` is the sole descriptor-safe type, constructor, and runtime-admission owner for exact `publishToPowerBIResult` and `publishToPowerBIAck`. Success and failure results are exclusive frozen unions; exact report/workspace/model/data-mode/schedule values and optional initial-refresh/workspace-name fields survive without coercion, including explicit `undefined` DirectQuery values. Host result construction and both startup/standalone transports validate before publication, queueing, reentrancy, tracing, routing, or delivery. `kw-html-section` and `kw-publish-pbi-dialog` independently validate before request correlation, metadata, document command/CAS, persistence, request retirement, or dialog effects. Acknowledgements use a scalar-only constructor and validate before E2E capture/transport. Host acknowledgement admission precedes request-ID coercion, lease lookup, timer mutation, compensation, cleanup, or finalization.

**Red-first and hardening:** the required created-item sequence failed exactly 1/25 before implementation: matching `accepted: 'yes'` set cleanup/finalization state, cleared the exact 15-second timer, ran cleanup, and removed the exact lease. The fixed regression captures the lease and timer, proves malformed traffic leaves `cleanupRequested` and `finalizationInProgress` false with zero cleanup through 14,999 ms, then permits only canonical `accepted: true` to retire once without cleanup. Pure and boundary regressions reject inherited, non-enumerable, accessor, array, custom-prototype, proxy, extra-field, malformed-union, and matching non-boolean traffic without invoking getters. Additional review hardening preserves frozen host publication, explicit optional DirectQuery fields, exact 15,000 ms expiry, and exact 5,000 ms applied-state rearm.

**Preserved owners and behavior:** HST-1 retains the complete publish application state machine, exact initial deadline/rearm, host-owned apply/compensate CAS, queue-stable cleanup admission, created-item cleanup, disposal, and late suppression. Leave No Trace first-commit admission, Fabric create/update/item recovery/refresh algorithms, portable dashboard compilation, dialog correlation and presentation, legacy persistence, document aggregate ownership, browser behavior, SQL comparison, query terminals, and every other dashboard message remain unchanged and outside this protocol.

**Qualification:** the final core ring passes 8 files and 184 tests; the adjacent dashboard/HTML/persistence/transport ring passes 13 files and 881 tests. Complete sequential and official coverage Vitest pass 341 files and 7,044 tests at 49.55% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, `git diff --check` with two existing line-ending notices only, zero root/browser production source maps, and both synchronized bundle gates pass. Final sizes are 2,270.9 KB host, 3,172.7 KB main webview, 338.0 KB Markdown webview, and 19,208.8 KB total `dist/`; synchronized baselines move `2214→2221`, `3117→3123`, and `286→289` with the fixed 50 KB buffer unchanged. Native UI E2E and live authenticated Fabric publication were not repeated because valid create/update, metadata, document CAS, cleanup, controls, and presentation remain unchanged while every migrated construction, startup, transport, ingress, acknowledgement, lease, and deadline boundary is directly covered. Reviews returned `VERDICT: NO PRO-25 IMPLEMENTATION BLOCKER`, `VERDICT: NO PRO-25 DOCUMENTATION BLOCKER`, and definitive `VERDICT: NO PRO-25 BLOCKER`.

## Next Iteration

**Status:** no implementation slice is selected. After PRO-25, the rescored themes are PRO 35, DOC 34, EXA 32, BRW 30, CMP 29, and KLS 21. PRO has concrete malformed-traffic and identity-aliasing evidence, but no bounded fix until document, registry, lifecycle, execution, artifact, routing, and restore paths share one canonical SQL section-identity contract. DOC has no narrow reversible Kusto/SQL complete-record slice. EXA has a source-confirmed SQL delivery/application gap but no shared manual/Copilot terminal-acknowledgement contract. BRW has a failed-presentation adoption defect but no single pending-generation and parent-acknowledgement owner across root/boot/inline/standalone paths. CMP lacks a durable recovery-record schema plus restart/repair/close interaction. KLS lacks a typed recursive column-fact contract across join/lookup/union. Resume architecture implementation only after one of those missing contracts yields a canonical owner, exact preservation boundary, and complete discriminating red.

## Convergence Loop

Run this loop after every completed architecture slice.

### 1. Reconfirm Product Invariants

- Read the golden outcome and the product behavior touched by the candidate.
- Update the golden outcome only if an implemented product capability or a fundamental platform constraint changed.
- Never weaken the target merely because migration is difficult.

### 2. Refresh Current Evidence

- Read the exact owning code paths and neighboring tests.
- Record concrete competing authorities, untyped boundaries, races, privacy exposure, or change amplification.
- Prefer executable behavior and state ownership over line counts.
- Add newly discovered gaps to the register; retire facts that are no longer true.

### 3. Score And Respect Dependencies

- Score every credible gap using `H/A/R/L/F/E`.
- Select the highest score that has a coherent incremental slice.
- A lower-scoring prerequisite may go first only when the higher gap cannot be safely reached without it; record that dependency explicitly.
- Correctness, security, and privacy discoveries can preempt the queue only when product direction or release policy marks them release-blocking.

### 4. State One Hypothesis And One Discriminating Test

Before editing, write:

- The canonical owner that should decide the behavior.
- The current path that bypasses or duplicates it.
- One falsifiable hypothesis.
- The cheapest test that can disprove it.
- The smallest reversible vertical migration.

If this cannot be stated, the candidate is not ready to implement.

### 5. Characterize The Existing Boundary

- Add or identify a failing cross-layer test for the current divergence.
- Include stale, cancellation, replacement, disposal, and policy-revocation ordering where relevant.
- Do not start with broad module movement.

### 6. Introduce One Owner Behind Existing Contracts

- Preserve public behavior, file formats, and host compatibility.
- Route one complete user journey through the new owner.
- Keep old adapters as anti-corruption layers only while unmigrated callers remain.
- Never dual-write sensitive or canonical state.

### 7. Delete Displaced Authority

- Remove the old mutable map, switch branch, synthetic message, DOM-derived state, or duplicate lifecycle path for the migrated journey.
- Add a static architecture guard or contract test preventing its return.
- A migration is not complete while both old and new paths can decide the same transition.

### 8. Validate In Expanding Rings

1. Failing characterization test.
2. Focused owner/state-machine tests.
3. Neighboring cross-layer tests.
4. Typecheck and lint.
5. Native or authenticated E2E for the touched behavior.
6. Full sequential tests.
7. Production package, browser build when affected, and bundle gates.

Use sequential execution in constrained environments.

### 9. Review Against The Golden Outcome

Ask a pessimistic reviewer only for correctness, privacy, ownership, and regression blockers. Verify:

- Exactly one canonical owner remains.
- All terminals and asynchronous publications are correlated.
- Privacy and identity are revalidated at admission boundaries.
- External behavior and compatibility are preserved.
- The new abstraction removes real complexity rather than renaming it.

### 10. Update This Ledger

- Move the iteration to the completed log with its evidence.
- Update current-alignment facts and gap scores.
- Record deleted authorities and new guards.
- Rescore all gaps; do not automatically continue the same theme.
- Select the next iteration and state its discriminating test only when one bounded slice is ready; otherwise record why no iteration is selected.

## Iteration Definition Of Done

An architecture iteration is complete only when:

- One named canonical owner decides the migrated transitions.
- The old owner cannot still publish or mutate that state.
- The slice is exercised through the real boundary, not only isolated mocks.
- Stale, cancellation, failure, disposal, and privacy paths have explicit outcomes.
- Public file/protocol compatibility remains intentional and tested.
- Focused and broad validation pass.
- Architecture and contributor guidance reflect the new boundary.
- This ledger is rescored and points to exactly one next iteration, or explicitly records why none is ready.

## Convergence Measures

These measures track authority migration, not developer activity:

| Measure | Golden value |
| --- | ---: |
| Manual execution terminal types lacking exact operation identity | 0 |
| Canonical result stores keyed only by section ID | 0 |
| Derived artifacts without source revision and policy lineage | 0 |
| Known section kinds outside the section-definition registry | 0 |
| Durable document fields reconstructed only from DOM state | 0 |
| Host/webview message types without runtime schema | 0 |
| Internal mutable `window` authorities outside the approved adapter allowlist | 0 |
| Temporary transport listeners implementing request ownership | 0 |
| Tool workflows that require a live webview to mutate document state | 0 |
| Browser behaviors implemented by patching full-host globals | 0 |
| Application use cases owned directly by `QueryEditorProvider` | 0 |

Counts should be added only when an automated check can measure them reliably.

## Completed Foundations

These are not full golden-outcome iterations retroactively, but they materially reduce future migration risk.

| Foundation | Outcome |
| --- | --- |
| SQL lifecycle hardening | Explicit target/session ownership, execution broker, STS replay, and Leave No Trace admission |
| Kusto schema lifecycle hardening | Exact section/target/request/model identity and coordinator-owned schema state |
| Shared Kusto worker serialization | One mutation port with physical settlement and inline recovery |
| Kusto/SQL schema separation | Independent language catalogs and invalidation boundaries |
| Compatibility sidecar core | Shared format, lock/CAS store, and session lifecycle abstractions |
| Opaque section-ID hardening | Persistence, removal, and reorder no longer infer ownership from prefixes |

## Completed Iterations

| Iteration | Canonical owner introduced | Authority deleted | Guard added | Validation | Date |
| --- | --- | --- | --- | --- | --- |
| `EXA-1` | `KustoExecutionCoordinator` | Uncorrelated Kusto terminals, box-only cancellation, raw/provisional Kusto Copilot/Optimize output, process-local/fail-open Kusto privacy revision | Exact-envelope cross-layer/protocol/ownership/policy/restore guards | Focused, full sequential, package, integration, and authenticated native gates complete | 2026-07-28 |
| `EXA-2` | `ResultArtifactStore` and exact consumer bindings | Mutable latest-result lineage authority across derived, model, dashboard, share, and CSV consumers | Pin/rebind/revoke, declared-column, producer/policy, and native artifact gates | Focused, full sequential, package, integration, browser, and native gates complete | 2026-07-30 |
| `COD-1` | `kqlxOverlay.ts` lossless codec | Reconstructive serialization and non-exact projection/publication baselines | Exhaustive schema, exact comparison, sidecar matrix, and native codec round-trip guards | Focused, full sequential, package, integration, browser, and native gates complete | 2026-08-01 |
| `COD-2` | `documentSectionCapabilities.ts` + `createSectionWithCapabilities()` | Provider/webview/tool/browser lists, raw automation factories, MDX filtering, compatibility alias lists, and browser parser copy | Every-cell matrix, creation/parser/teardown ownership guards, exact-byte sidecar/manifest gates, acknowledged SQL comparison CAS, inactive-runtime guards, and native MDX/SQLX capability gate | Blocker ring 584, full Vitest 5,543, extension-host 174, native 4/4, production/browser, and definitive review complete | 2026-08-02 |
| `DOC-1` | `MarkdownDocumentAggregate` + `markdownSectionDefinition.ts` | Native Markdown DOM serialization authority and per-panel revision state | Full-projection commands, one URI queue, Save leases, handoff/reopen, lossless Save, and native lifecycle gate | Focused 444, full Vitest 5,565, extension-host 187, native, production/browser, and definitive review complete | 2026-08-03 |
| `DOC-2` | Existing aggregate/client/URI queue + `urlSectionDefinition.ts` | Native URL DOM serialization authority and adapter-owned URL persisted state | Full optimistic projection, pre-read queue reservation, stable mixed order, runtime move/disposal/redirect guards, lossless Save, and native lifecycle gate | Focused 513, full Vitest 5,592, extension-host 188, native, production/browser, and definitive review complete | 2026-08-03 |
| `DOC-3` | Existing aggregate/client/URI queue + `pythonSectionDefinition.ts` | Native Python DOM serialization authority and adapter-owned persisted Python state | Full optimistic projection, exact stale/output admission, inactive settlement, bounded timeout terminal, Monaco retention, lossless Save, and native lifecycle gate | Focused 539, full Vitest 5,615, extension-host 189, native, production/browser, and definitive review complete | 2026-08-03 |
| `DOC-4` | Existing aggregate/client/URI queue + `chartSectionDefinition.ts` | Native Chart DOM serialization authority and adapter-owned persisted Chart configuration | Full optimistic projection, retained ECharts/artifact identity, exact source rebind, detached-instance fencing, lossless Save, arbitrary-ID propagation, and native lifecycle gate | Focused 744, full Vitest 5,626, extension-host 190, native 2/2, production/browser, bundle gates, and definitive post-fix review complete | 2026-08-03 |
| `PRO-1` | `documentViewProtocol.ts` + host-created panel session UUID | Unchecked/duplicated native document lifecycle envelopes and sessionless panel admission | Runtime schema inventory, exactly-once initial projection, stale-session acknowledgement/command/result/barrier fences, provider race test, and native recreation gate | Focused 375, full/coverage Vitest 5,635, extension-host 191, native 3/3, production/browser, bundle gates, and definitive blocker review complete | 2026-08-03 |
| `DOC-5` | Existing aggregate/client/URI queue + `transformationSectionDefinition.ts` | Native Transformation DOM serialization authority and adapter-owned persisted Transformation configuration | Full optimistic projection, retained input pins/lineage, exact source rebind, runtime-height isolation, stale instance fencing, lossless Save, privacy/physical-alias guards, and native lifecycle gate | Focused 729, full/coverage Vitest 5,657, extension-host 200, native 4/4, production/browser, bundle gates, and definitive blocker review complete | 2026-08-04 |
| `DOC-6` | Existing aggregate/client/URI queue + `htmlSectionDefinition.ts` | Native HTML DOM serialization authority and adapter-owned persisted HTML configuration | Full optimistic projection, exact Monaco/iframe/artifact retention, stale-instance/workflow fencing, one-field publish metadata CAS/rollback, queue-stable external cleanup, lossless Save, and native lifecycle gate | Focused 661, full/coverage Vitest 5,703, extension-host 201, native 5/5, production/browser, bundle gates, post-fix blocker reviews, and documented closure decision complete | 2026-08-05 |
| `HST-1` | `HostDashboardApplicationHandler` | Provider-owned dashboard workflow/ack maps, Fabric/export imports, discriminator branches, native publish lease methods, and cleanup transitions | Real-provider injection/forwarding, static displaced-authority, synchronous-decline, direct workflow/race, protocol-sender, and native same-ID cleanup guards | Focused 466, full/coverage Vitest 5,708, extension-host 201, provider lifecycle 21, production/browser, bundle gates, and definitive blocker review complete | 2026-08-05 |
| `HST-2` | `HostArtifactCsvSaveApplicationHandler` | Provider-owned artifact CSV maps, picker/nonce/cancel/deadline/tombstone transitions, discriminator cases, and governed file publication | Real-provider injection/forwarding, static displaced-authority, direct concurrency/replay/cancel/deadline/disposal tests, protocol-sender inventory, browser compatibility, and native exact-byte gate | Focused 310, full/coverage Vitest 5,713, extension-host 201, native exact 26 bytes, production/browser, bundle gates, and definitive blocker review complete | 2026-08-05 |
| `HST-3` | `HostPythonExecutionApplicationHandler` | Provider-owned Python process creation, discriminator branch, stdio accumulation, timeout transitions, and terminal publication | Real-provider injection/forwarding, static displaced-authority, direct fallback/stdio/timeout/cap/disposal tests, protocol-sender inventory, and isolated native execution/persistence gate | Focused 502, full/coverage Vitest 5,728, extension-host 201, native exact output, production/browser, bundle gates, and definitive blocker review complete | 2026-08-05 |
| `HST-4` | `HostImportedCsvSaveApplicationHandler` | Provider-owned imported CSV method, discriminator case, picker/write helpers, notification transitions, and late-picker authority | Real-provider injection/forwarding, static displaced-authority, direct UX/URI/bytes/failure/disposal tests, provider lifecycle disposal, and isolated native exact-byte/picker gate | Focused 128 plus provider lifecycle 101, full/coverage Vitest 5,736, extension-host 201, native exact 36 bytes, production/browser, bundle gates, and definitive blocker review complete | 2026-08-05 |
| `HST-5` | `HostQuerySharingApplicationHandler` | Provider-owned sharing cases/methods, zlib/ADX-link generation, Kusto/SQL formatting, host clipboard effects, response publication, and notifications | Red-first real-provider forwarding, static displaced-authority, direct validation/encoding/formatting/failure/disposal tests, protocol-sender inventory, and native rich/ADX clipboard gate | Focused 454, full/coverage Vitest 5,749, extension-host 201 on rerun, native 2/2 with exact clipboard artifact/screenshot, production/browser, bundle gates, and definitive blocker review complete | 2026-08-05 |
| `HST-6` | `HostUrlContentApplicationHandler` | Provider-owned URL validation, fetch/abort lifecycle, limits, classification/sniffing, response shaping, discriminator case, and URL terminals | Red-first real-provider forwarding, static displaced-authority, direct identity/redirect/classification/cap/timeout/disposal tests, protocol-sender inventory, and unchanged URL component ring | Focused 223, final coverage Vitest 5,768, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-05 |
| `HST-7` | `HostControlCommandSyntaxApplicationHandler` | Provider-owned syntax cache/TTL, Learn fetch/normalization, HTML/entity/`with(...)` parsing, failure shaping, discriminator case, and syntax-result publication | Red-first real-provider forwarding, static displaced-authority, exact cache/URL/parser/failure/disposal tests, protocol-sender inventory, provider lifecycle disposal, and unchanged caret-docs ring | Focused 321, host-handler ring 424, full/coverage Vitest 5,782, extension-host 201, production/browser, bundle gate, and definitive blocker review complete | 2026-08-06 |
| `HST-8` | `HostResourceUriApplicationHandler` | Provider-owned resource cache, path import/normalization, passthrough/base/stat/conversion decisions, discriminator case, failure shaping, and URI-result publication | Red-first real-provider forwarding, static displaced-authority, direct path/cache/order/failure/disposal tests, protocol-sender inventory, provider lifecycle disposal, and unchanged Markdown caller ring | Focused 378, full/coverage Vitest 5,816, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-06 |
| `HST-9` | `HostCopilotContentOpenApplicationHandler` | Provider-owned tool-result document creation/display, Markdown preview URI/command dispatch, failure notifications, discriminator cases, and methods | Red-first exact-object provider forwarding, static displaced-authority, exact coercion/options/failure/disposal tests, response-free protocol inventory, unchanged controller emitters, and provider lifecycle disposal | Focused 154, full/coverage Vitest 5,834, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-06 |
| `HST-10` | `HostInformationNotificationApplicationHandler` | Provider-owned `showInfo` discriminator and direct native information-notification invocation | Red-first exact-object provider forwarding, static displaced-authority, exact text/thenable/throw/disposal tests, response-free protocol inventory, and provider lifecycle disposal | Focused 146, full/coverage Vitest 5,844, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-06 |
| `HST-11` | `HostCachedValuesOpenApplicationHandler` | Provider-owned `seeCachedValues` discriminator and direct knowledge of the Cached Values command ID | Red-first exact-object provider forwarding, static displaced-authority, exact dispatch/settlement/disposal tests, first-launch/viewer ownership guard, response-free protocol inventory, and provider lifecycle disposal | Focused 149, adjacent 208, full/coverage Vitest 5,856, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-06 |
| `HST-12` | `HostCopilotAgentOpenApplicationHandler` | Provider-owned `openCopilotAgent` discriminator and direct shared Agent Chat helper import/invocation | Red-first exact-object provider forwarding, constructor/settlement guards, exact delegation/disposal tests, behavioral helper timing/fallback tests, unchanged caller/emitter guard, response-free protocol inventory, and provider lifecycle disposal | Focused/adjacent 226, full/coverage Vitest 5,873, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-06 |
| `HST-13` | `HostEditorCursorStatusApplicationHandler` | Provider-owned cursor cases, owner prefix/ID construction, visibility/Production gates, snapshot shaping/transport, and panel prefix clearing | Red-first exact-object provider forwarding, direct visibility/identity/snapshot/failure/disposal tests, stale-owner/shared-item guard, unchanged emitters/protocol, sender inventory, and provider lifecycle disposal | Focused 169, full/coverage Vitest 5,883, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-06 |
| `HST-14` | `HostEditingPreferencesApplicationHandler` | Provider-owned preference cases, `STORAGE_KEYS` mapping, then-current truthiness handling, revisioned mutation call, and global/current-panel publication decision; PRO-19 later replaced coercive admission | Red-first exact-object provider forwarding, direct mapping/publication/failure/disposal tests, persistence/configuration/initial-projection/emitter ownership guards, strengthened sender inventory, and provider lifecycle disposal | Focused 553, adjacent 42, full/coverage Vitest 5,893, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-06 |
| `HST-15` | `HostKustoConnectionIntakeApplicationHandler` | Provider-owned discovered/XML intake cases and refresh ordering plus all three displaced `ConnectionService` methods | Red-first exact-object provider forwarding, direct deduplication/normalization/sequentiality/picker/refresh/disposal tests, unchanged parser/emitter guards, protocol sender inventory, and provider lifecycle disposal | Final focused/adjacent 703, full/coverage Vitest 5,909, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-06 |
| `HST-16` | `HostKustoConnectionOnboardingApplicationHandler` | Provider-owned prompt/add/test cases plus all three displaced `ConnectionService` methods | Red-first exact-object provider forwarding, direct validation/account/persistence/selection/refresh/test/trace/disposal tests, preserved-owner/emitter guards, protocol sender inventory, and provider lifecycle disposal | Final focused/adjacent 519, full/coverage Vitest 5,931, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-07 |
| `HST-17` | `HostSqlConnectionOnboardingApplicationHandler` | Provider-owned SQL prompt/direct-add cases and both complete provider methods | Red-first exact-object provider forwarding, direct prompt/cancellation/credential/order/failure/disposal tests, preserved-owner/emitter guards, protocol sender inventory, and provider lifecycle disposal | Final focused/adjacent 414, full/coverage Vitest 5,951, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-07 |
| `HST-18` | `HostSqlFavoritesApplicationHandler` | Provider-owned SQL favorite cases plus every complete `ConnectionService` SQL-favorites method | Red-first exact-object provider forwarding, direct prompt/sanitize/key/order/persistence/publication/failure/disposal tests, provider projection/Connection Manager/emitter guards, sender inventory, and lifecycle disposal | Focused 403, full/coverage Vitest 5,975 at 48.23%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-07 |
| `HST-19` | `HostKustoFavoritesApplicationHandler` | Provider-owned Kusto favorite cases plus every complete `ConnectionService` Kusto-favorites method, registry, and lifecycle state | Red-first exact-object forwarding, migration/principal/merge/unresolved/prompt/confirmation/broadcast/disposal tests, 34-case provider matrix, projection/inference/external-caller/emitter guards, sender inventory, and lifecycle disposal | Focused 596, full/coverage Vitest 6,013 at 48.20%, extension-host 201, production/browser, bundle gates, and closure review complete | 2026-08-07 |
| `HST-20` | `HostSqlDatabaseDiscoveryApplicationHandler` | Provider-owned SQL database routes, target adoption, acquisition/publication method, owner-protection helpers, cache orchestration, notifications, logging, and terminal settlement | Red-first exact-object forwarding, direct target/ticket/principal/privacy/cache/cleanup/transport/disposal tests, preserved-owner/emitter/router/message-shape guards, sender inventory, and lifecycle disposal | Focused 405, full/coverage Vitest 6,060 at 48.20%, extension-host 201, production/browser, bundle gates, and definitive closure review complete | 2026-08-07 |
| `HST-21` | `HostKqlLanguageRequestApplicationHandler` | Provider-owned KQL language-host import/field, request discriminator/method, response construction, logging, and disposal | Red-first reference-identical forwarding, direct admission/method/failure/transport/logger/disposal tests, preserved host/analyzer/webview-ledger guards, sender inventory, constructor slot, and lifecycle disposal | Focused 159, expanded 388, full/coverage Vitest 6,076 at 48.20%, extension-host 201 on clean rerun, production/browser, bundle gates, and definitive closure review complete | 2026-08-07 |
| `HST-22` | `HostSqlLastSelectionApplicationHandler` | Provider-owned `saveSqlLastSelection` discriminator and direct last-connection/last-database application-state writes | Red-first reference-identical forwarding, direct trim/order/undefined-empty/rejection/disposal tests, preserved emitter/onboarding/projection/message guards, response-free sender inventory, constructor slot, and lifecycle disposal | Focused 158, expanded 279, full/coverage Vitest 6,090 at 48.20%, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-08 |
| `HST-23` | `HostDevelopmentNoteMutationApplicationHandler` | Provider-owned development-note resolver map, request IDs, deadline, delivery-failure shaping, matching `toolResponse` settlement, and disposal cleanup | Red-first exact delegation/claimed-response/unclaimed-fallthrough test, direct timeout/delivery/response/disposal tests, preserved Copilot/webview/tool guards, sender inventory, constructor slot, and lifecycle disposal | Focused 156, expanded 950, full/coverage Vitest 6,101 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-24` | `HostCopilotInlineCompletionApplicationHandler` | Provider-owned inline-completion discriminator, Kusto/SQL branch, SQL token assertion/delegation, direct Copilot invocation, and empty SQL fallback construction | Red-first reference-identical Kusto/SQL forwarding, direct owner/fallback/settlement/disposal tests, preserved Copilot/SQL/webview/preference/message guards, sender inventory, constructor slot, and lifecycle disposal | Focused 158, expanded 614, full/coverage Vitest 6,114 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-25` | `HostCopilotAvailabilityApplicationHandler` | Provider-owned availability discriminator and direct Copilot availability invocation | Red-first reference-identical forwarding, direct settlement/rejection/disposal tests, preserved Copilot/caller/router/message guards, response-free sender inventory, constructor slot, and lifecycle disposal | Focused 156, expanded 505, full/coverage Vitest 6,126 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-26` | `HostCopilotWriteQueryPreparationApplicationHandler` | Provider-owned write-query preparation discriminator and direct Copilot preparation invocation | Red-first reference-identical Kusto/SQL forwarding, direct settlement/rejection/disposal tests, preserved model/tool/caller/router/message guards, response-free sender inventory, constructor slot, and lifecycle disposal | Focused 157, expanded 503, full/coverage Vitest 6,140 at 48.20%, extension-host 201, production/browser, bundle gates, and documentation-aware blocker review complete | 2026-08-08 |
| `HST-27` | `HostCopilotConversationClearApplicationHandler` | Provider-owned conversation-clear discriminator, Kusto/SQL flavor branch, Kusto identity validation, and direct clear invocation | Red-first reference-identical Kusto/SQL forwarding, direct identity/compatibility/settlement/rejection/disposal tests, preserved state/caller/start-cancel/message guards, response-free sender inventory, constructor slot, and lifecycle disposal | Focused 160, expanded 671, full/coverage Vitest 6,153 at 48.20%, extension-host 201, production/browser, bundle gates, and documentation-aware blocker review complete | 2026-08-08 |
| `HST-28` | `HostCopilotHistoryRemovalApplicationHandler` | Provider-owned history-removal discriminator and direct Copilot history-removal invocation | Red-first reference-identical Kusto/SQL-originated forwarding, direct settlement/rejection/disposal tests, preserved service mutation/shared-UI/message guards, response-free sender inventory, constructor slot, and lifecycle disposal | Focused 9, expanded 581, final 590, full/coverage Vitest 6,164 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-29` | `HostCopilotChatFirstTimeApplicationHandler` | Provider/CopilotService-owned first-time discriminator, durable-state/modal/helper/result workflow, and helper import | Red-first reference-identical forwarding, direct state/modal/choice/helper/transport/disposal tests, preserved key/projection/caller/router/helper/message guards, sender inventory, constructor slot, and lifecycle disposal | Focused 74, expanded 508, full/coverage Vitest 6,180 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-30` | `HostWorkbenchToolSessionApplicationHandler` | Provider-owned tool token, state resolver map, connect/reactivate/disconnect helpers, schema target projection, SQL callbacks, three tool discriminator cases, and persistence helper | Red-first reference-identical forwarding/thin delegation, direct correlation/schema/SQL/route/disposal tests, HST-23 priority, preserved-owner guards, sender inventory, constructor slot, and lifecycle disposal | Focused 11, expanded 538, full/coverage Vitest 6,192 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-31` | `HostKustoConnectionBrowsingApplicationHandler` | Provider-owned connection list/database browse/refresh/selection cases, request shaping, selection ordering, and diagnostics effect | Red-first reference-identical four-route forwarding, direct shaping/blank/undefined-empty/failure/disposal tests, preserved projection/service/compatibility/diagnostics/message guards, response-free inventory, constructor slot, and lifecycle disposal | Focused 14, expanded 315, full/coverage Vitest 6,205 at 48.20%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-32` | Comparison-summary host bridge retirement | Unused provider maps/waiter/routes, Copilot and SQL lifecycle callbacks, webview sends, and duplicated message types | Red-first zero-transport local render/teardown tests, static dead-bridge guard, immutable-lineage/diff/owner/cancellation preservation, and sender inventory | Focused 505, expanded 846, full/coverage Vitest 6,206 at 48.19%, extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-33` | `HostCopilotQueryWorkflowApplicationHandler` | Provider-owned five-route query/Optimize admission, SQL preflight/token/cancel race and terminals, Kusto cancellation shaping, direct delegation, and disposal | Red-first reference-identical five-route forwarding, direct SQL/Kusto/Optimize race/settlement/lazy-resolution tests, 25-case provider guard, preserved owners/messages/callers, sender inventory, constructor slot, and lifecycle disposal | Focused 111, expanded 738, full/coverage Vitest 6,219 at 48.19%, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-08 |
| `HST-34` | `HostKustoSectionExecutionApplicationHandler` | Provider-owned seven-route lifecycle/acknowledgement/manual execution workflow, two acknowledgement maps, physical/policy admission, direct cancellation/Copilot close effects, and disposal settlement | Red-first reference-identical seven-route forwarding, direct lifecycle/ack/manual terminal/replacement/disposal tests, 18-case provider guard, preserved coordinator/client/caller/message owners, sender inventory, constructor slot, and lifecycle disposal | Focused 806, full/coverage Vitest 6,231 at 48.19%, extension-host 201, production/browser, bundle gates, and definitive blocker review complete | 2026-08-08 |
| `HST-35` | `HostComparisonPreparationApplicationHandler` | Provider-owned three-route comparison transaction, request/Kusto-owner maps, five SQL acknowledgement phases, rollback retry, target/policy revalidation, removal cancellation, and disposal settlement | Red-first three-route/preparation forwarding, direct Kusto/SQL acknowledgement/race/rollback/removal/disposal tests, 15-case provider guard, atomic two-sided finalized-removal cleanup, preserved canonical services/messages, sender inventory, constructor slot, and thin delegation | Final focused 844; full/coverage 6,249 at 48.21%; extension-host 201, production/browser, bundle gates, and blocker review complete | 2026-08-08 |
| `HST-36` | `HostSqlSectionExecutionApplicationHandler` | Provider-owned execute/cancel workflow, synchronous preflight, owner/protection and tool-owner validation, query shaping, physical start, manual terminals, protected logging, and exact cleanup | Red-first execute/cancel forwarding, direct reservation/race/terminal/rotation/cleanup/disposal tests, 13-case provider guard, preserved canonical SQL services/messages, sender inventory, constructor slot, and disposal | Focused 1,053; full/coverage 6,275 at 48.21%; extension-host 201 on rerun, production/browser, bundle gates, and definitive blocker review complete | 2026-08-09 |
| `PRO-2` | `MainWebviewStartupGateway` + explicit dispatcher readiness | Three provider-local startup queues, pre-ready host projection delivery, import-order dispatcher drain, and split disposal admission/drain authority | Three-mode listener/readiness handoff, ordinary/correlated ordering, rejection continuity, immutable retirement admission, stable-tail close, linked-validation cancellation, successor isolation, tutorial transport, and protocol inventory guards | Focused 414; full/coverage 6,293 at 48.25%; compatibility 128, native lifecycle 23, extension-host 209, production/browser, bundle gates, and definitive blocker review complete | 2026-08-09 |
| `BRW-1` | `BrowserViewerRoot` + runtime-validated `BrowserViewerProjection` | Boot-owned parsing/default mutation, synthetic four-message startup replay, persistence-global patches, delayed read-only repair, and convention-only unsupported host work | Native/companion immutable projection, stale/duplicate generation, real bundled boot retry, opaque preservation, capability-owned shell/Monaco/execution, static URL, artifact/CSV, and visible rendering guards | Focused 460; full/coverage 6,303 at 48.23%; extension-host 209, strict types, production/browser, exact CSV, desktop/mobile layout, bundle gates, and definitive blocker review complete | 2026-08-09 |
| `CMP-1` | `CompatSidecarCloseCoordinator` | Duplicated KQL/SQL retired-admission flags, close-start state, final-persist/beforeunload ordering, session/gateway/store cleanup, dirty-close UX, recovery selection, and settlement | Red-first reversed retained traffic, KQL/SQL Save/recovery sanitation, prompt/init/disposal/tail/failure, nested-provider cleanup, and startup-visibility tests plus displaced-authority/PRO-2 guards | Focused 227; compatibility 134; full/coverage 6,338 at 48.33%; extension-host 213, production/browser, bundle gates, and definitive blocker review complete | 2026-08-09 |
| `CMP-2` | `CompatSidecarProjectionCoordinator` | Duplicated KQL/SQL projection generations, active/pending source identity, retry/coalescing, reload currentness, persist-generation admission, source-reload authority, and rollback-failure state | Required held-A/source-B/late-A/newer-edit race, generation-zero, lazy waiter, current-failure recovery, callback/timeout/duplicate/disposal, exact-source/CRLF, displaced-authority, protocol/close/session/store/provider guards | Focused 175; compatibility 135; full/coverage 6,875 at 49.27%; extension-host 217; strict types, production/browser, lint, diagnostics, diff, synchronized bundle gates, and definitive reviews complete | 2026-08-13 |
| `CMP-3` | `CompatSidecarPersistCoordinator` | Duplicated KQL/SQL persist terminal decisions, queue/revision/generation admission, sanitation/materialization ordering, source verification/rollback, draft/state commits, acknowledgement, beforeunload, and final settlement | Required held final-snapshot provider gate, 17-case terminal matrix, two-phase upgrade, source/rollback/ack/disposal/durability tests, provider authority guards, and exact acknowledgement sender inventory | Focused 195; full/coverage 6,923 at 49.27%; extension-host 218; strict types, production/browser, lint, diagnostics, diff, zero maps, synchronized bundle gates, and blocker review complete | 2026-08-13 |
| `PRO-3` | `compatibilityPersistenceProtocol.ts` + host-created compatibility panel UUID | Metadata-free KQL/SQL lifecycle casts and sessionless request/projection, persist, final, acknowledgement, and reload admission | A/B successor traffic, malformed/high-revision state, lost-ack replay, stale-generation, failed-upgrade, unavailable-final, protocol inventory, and preserved sidecar-owner guards | Focused 499; compatibility 134; full/coverage 6,352 at 48.38%; extension-host 215, production/browser, bundle gates, and definitive blocker review complete | 2026-08-10 |
| `DSH-1` | `portableDashboardCompiler.ts` + normalized portable IR | Duplicate provenance/target/source validators, Power BI binding regex rewrites, string-only preflights, and obsolete unsupported-visual route | Cross-path typed diagnostics, standards-tree/source-range and replacement-stability guards, exact-range rewrites, zero-effect export/publish failures, hostile output escaping, protocol/ownership guards, and native warning/chat E2E | Focused 556; full/coverage 6,457 at 48.35%; extension-host 215, production/browser, bundle gates, native 4/4, and definitive blocker review complete | 2026-08-10 |
| `DSH-2` | `powerBiProjectArtifacts.ts` + `powerBiProjectWriter.ts` | Publish temp URI/export/tree-read/delete staging and local writer-owned path/content assembly | Sorted copy-on-read manifest, Windows collision admission, deterministic 17-artifact golden, local/publish byte parity, zero-filesystem publish guard, cancellation, and mocked Fabric transaction tests | Focused 330; adjacent 457; full/coverage 6,465 at 48.35%; extension-host 215, production/browser, bundle gates, and clean implementation review complete | 2026-08-10 |
| `KLS-1` | `analyzeKqlSource()` + fail-closed qualification ranges | Duplicate service and schema-inference scanners plus the webview fallback lexer | Red-first alias/string/comment probe, exact-layout/scopes/management/wrapper/union/range regressions, static ownership guard, and lexical column-scope preservation | Focused 131; full/coverage 6,505 at 48.07%; extension-host 215, production/browser, bundle gate, and definitive blocker review complete | 2026-08-10 |
| `PRO-4` | `installRuntimeAddSectionBridges()` + applied-projection finalization token | Split preload/runtime Add queues and repeatable empty-document finalization | Real preload Markdown handoff, repeated takeover/finalization, capability/default, dispatcher, protocol, and startup guards | Focused 410; full/coverage 6,506 at 48.13%; extension-host 215, production/browser, bundle gates, and definitive blocker review complete | 2026-08-10 |
| `HST-37` | `HostSqlSchemaRequestApplicationHandler` + request-current `SqlSchemaService` cache admission | Provider-owned request reservation/acquisition/result/error publication, reduced lifecycle callback identity, and stale generation/cache publication | Real provider/router B-before-A race, direct request state machine, disk-CAS/cleanup/generation failures, Cached Values ordered locks/applied acknowledgements/Clear All, sender inventory, 12-case provider guard, and disposal | Focused 621; full/coverage 6,526 at 48.24%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and definitive review complete | 2026-08-10 |
| `HST-38` | `HostSqlConnectionsProjectionApplicationHandler` | Provider-owned snapshot route/triggers, revision tail, profile/cache/favorite reads, canonical owner join, payload construction, publication, and disposal | Red-first exact forwarding, all-trigger delegation, target/principal drift, selective/global policy, failed-tail/disposal, 11-case provider, sender inventory, and global-block orphan-favorite guard | Focused 778; full/coverage 6,534 at 48.24%; extension-host 215, production/browser, bundle gate, and blocker review complete | 2026-08-11 |
| `HST-39` | `HostPersistedResultSanitizationApplicationHandler` | Provider-owned sanitation helpers, Kusto/SQL owner checks and locks, comparison/artifact cleanup, lock-held publication, both invalidation emitters, and disposal | Red-first exact state/callback forwarding, direct fail-closed/lock/event/disposal tests, real 100-test sanitation matrix, KQLX/sidecar close ring, displaced-authority guards, and provider disposal | Focused 843; adjacent extension-host 159; full/coverage 6,543 at 48.24%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and definitive review complete | 2026-08-11 |
| `HST-40` | `HostKustoConnectionsProjectionApplicationHandler` | Provider/service-owned snapshot triggers, revision tail, preference/first-time/account/physical-connection/selection/cache/favorite reads, policy lock/retry, payload publication, and disposal | Red-first HST-31 exact forwarding, direct lock/retry/timing/tail/isolation/disposal tests, displaced-authority guards, sender inventory, and provider disposal | Focused 654; full/coverage 6,550 at 48.22%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and definitive review complete | 2026-08-11 |
| `HST-41` | `HostSqlEditorLifecycleApplicationHandler` | Provider-owned nine-route SQL section/target, STS request/document/connect, Production-gating, ordered development-auth forwarding, and disposal settlement | Red-first exact `stsDidChange` forwarding/awaiting, direct nine-route/order/gating/rejection/disposal tests, then-two-case provider guard, preserved lifecycle/auth owners, response-free inventory, and provider disposal | Focused 442; full/coverage 6,563 at 48.22%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and definitive review complete | 2026-08-11 |
| `HST-42` | `HostKustoSchemaRequestApplicationHandler` | Provider-owned primary/cross-cluster Kusto schema cases, argument shaping, direct service calls, and disposal settlement | Red-first exact cross-cluster forwarding/awaiting, direct route/default/lifecycle/rejection/disposal tests, zero-case provider guard, preserved `SchemaService`, response-free inventory, and provider disposal | Focused 669; full/coverage 6,573 at 48.22%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and implementation review complete | 2026-08-11 |
| `PRO-5` | `kustoSchemaProtocol.ts` | Duplicated primary/supplemental request shapes, `unknown` schema delivery transport, and unchecked recognized dispatcher traffic before schema effects | Red-first malformed current supplemental delivery, malformed primary zero-effect, six-discriminator parser, sole-owner/parser-order inventory, legacy/clock-skew compatibility, and preserved inner-identity guards | Focused 693; full/coverage 6,605 at 48.24%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and definitive blocker review complete | 2026-08-11 |
| `PRO-6` | `kustoDatabaseDiscoveryProtocol.ts` | Duplicated Kusto database requests, `unknown` service deliveries, and unchecked dispatcher/E2E-waiter traffic before database effects | Red-first malformed current provenance, four-discriminator parser, malformed array/sparse-list guards, sole-owner/parser-order inventory, typed service delivery, and preserved editor/synthetic/legacy/account/token/provenance identities | Focused 575; clean full rerun/coverage 6,620 at 48.34%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gates, and definitive review complete | 2026-08-11 |
| `PRO-7` | `sqlDatabaseDiscoveryProtocol.ts` | Duplicated SQL database requests, untyped handler deliveries, and unchecked dispatcher/router traffic before request-ledger and UI effects | Red-first malformed current data, five-discriminator parser, array/sparse/inherited-index guards, sole-owner/parser-order inventory, typed delivery, pending-request preservation, and unchanged lifecycle/owner/protected-runtime algorithms | Focused 582; full/coverage 6,639 at 48.35%; extension-host 215, production/browser, lint, diagnostics, diff, bundle gate, and definitive review complete | 2026-08-11 |
| `PRO-8` | `sqlSchemaProtocol.ts` | Duplicated SQL schema request, untyped handler delivery, and unchecked dispatcher/router traffic before catalog and schema-info effects | Red-first malformed current schema, two-discriminator parser, strict branch metadata, dense/sparse/inherited-array guards, sole-owner/parser-order inventory, typed delivery, valid-empty-error fallback, and unchanged HST-37/lifecycle/cache/Cached Values/STS algorithms | Focused 322; adjacent 661; full/coverage 6,655 at 48.42%; extension-host 215; production/browser, lint, diagnostics, bundle gate, and definitive review complete | 2026-08-11 |
| `PRO-9` | `kqlLanguageProtocol.ts` | Duplicated KQL language request/response types, broad webview methods, untyped handler delivery, request coercion, and unchecked matching resolver settlement | Red-first malformed matching table reference, two-discriminator parser, exact IDs/params/results, dense/sparse/inherited-array guards, method-aware settlement, exact timeout, sole-owner/parser-order inventory, and unchanged analysis/Fully Qualify owners | Focused 363; adjacent 913; full/coverage 6,669 at 48.43%; extension-host 215; production/browser, lint, diagnostics, bundle gate, and implementation review complete | 2026-08-11 |
| `PRO-10` | `controlCommandSyntaxProtocol.ts` | Duplicated syntax request types, untyped handler delivery, request/result coercion, and command-only cache/pending/refresh settlement | Red-first malformed/stale/current delivery sequence, exact pair identity, canonical failure branch, dense/sparse/inherited-array guards, scheduler admission, sole-owner/parser-order inventory, and unchanged Learn/cache/presentation/no-timeout owners | Focused 497; adjacent 778; full/coverage 6,694 at 48.75%; extension-host 215; production/browser, lint, diagnostics, diff, bundle gate, and definitive blocker review complete | 2026-08-11 |
| `PRO-11` | `resourceUriProtocol.ts` | Duplicated request types, untyped host results, host request coercion, and unchecked main/plain-Markdown resolver settlement | Red-first dual-path malformed/stale/current sequence, exact IDs/branches, optional/empty values, exact two-second deadlines, request-effect and parser-order guards, real plain-Markdown relative-file resolution, and preserved algorithms/rendering/browser behavior | Focused 356; full 6,707; coverage 6,709 at 48.89%; extension-host 216, production/browser, lint, diagnostics, bundle gates, and implementation review complete | 2026-08-12 |
| `PRO-12` | `urlContentProtocol.ts` | Duplicated URL request types, untyped host deliveries, request/result coercion, and unchecked component settlement before request/artifact/rendering effects | Red-first malformed/stale/current sequence, atomic descriptor snapshots, canonical exclusive branches, callable/inherited/accessor/proxy/cycle guards, dual admission-before-effect inventory, exact URL identity, and preserved fetch/render/artifact/browser behavior | Focused 620; full 6,742; coverage 6,744 at 49.00%; extension-host 216; production/browser, lint, task-owned diagnostics, diff, bundle gates, and definitive blocker reviews complete | 2026-08-12 |
| `PRO-13` | `pythonExecutionProtocol.ts` | Duplicated Python request types, direct Lit transport bypass, untyped host terminals, request/terminal coercion, dead alternate runner, and unchecked reservation consumption before persistence/rendering effects | Red-first malformed/current sequence, atomic descriptor snapshots, request-before-reservation/process, exact box-only consumption, exclusive terminal/cap/proxy guards, dual dispatcher/leaf admission, browser denial, and preserved process/persistence behavior | Focused 619; full 6,769; coverage 6,766 at 49.02%; extension-host 216; strict types, production/browser, lint, diagnostics, diff, bundle gates, and definitive blocker reviews complete | 2026-08-12 |
| `PRO-14` | `artifactCsvSaveProtocol.ts` | Duplicated five-message CSV shapes, untyped host delivery, host/webview/browser identity coercion, unchecked startup reentrancy, and pending nonce/export consumption before canonical validation | Dual red-first malformed transfer/challenge sequences, descriptor/proxy/cycle guards, exact export/box/artifact lookup-before-consume, padded/mismatched/replay tests, typed host delivery, startup snapshot, browser shared-parser build, and sole-owner/source-order inventory | Focused 789; full/coverage 6,787 at 49.05%; extension-host 216; strict types, production/browser, lint, diagnostics, diff, bundle gates, and definitive blocker review complete | 2026-08-12 |
| `PRO-15` | `sqlStsEditorLanguageProtocol.ts` | Duplicated five-request shapes, untyped STS deliveries, local reservation/document/connect mutation before validation, and coercive resolver/diagnostics/readiness settlement | Red-first exact resolver/timer preservation, descriptor-safe eight-message parser, dense opaque diagnostics, expected-owner shape, local/transport/host/dispatcher/router admission, typed coordinator delivery, and sole-owner/source-order inventory | Focused/adjacent 729; full/coverage 6,800 at 49.10%; extension-host 216; strict types, production/browser, lint, diagnostics, diff, bundle gates, and definitive blocker review complete | 2026-08-12 |
| `PRO-16` | `sqlConnectionsProjectionProtocol.ts` | Duplicated SQL snapshot request, untyped delivery, pre-validation revision watermark, coercive projection application, operational-array execution, and prototype-sensitive cache/policy keys | Required malformed/iterator-hostile MAX then canonical recovery, descriptor-captured transport/application, typed publication, complete canonical versus legacy schemas, post-application watermark, zero/equal/lower ordering, failed-application recovery, prototype-safe keys, and sole-owner/source-order inventory | Core 345; adjacent 934; full/coverage 6,821 at 49.11%; extension-host 216; strict types, production/browser, lint, diagnostics, diff, bundle gates, and definitive blocker review complete | 2026-08-13 |
| `PRO-17` | `kustoConnectionsProjectionProtocol.ts` + `runtimeMessageEnvelope.ts` + transactional Kusto projection application | Duplicated Kusto snapshot request, untyped/staged delivery, pre-validation watermark, JSON-unstable selections, descriptor-varying proxy bypass, acknowledgement reentrancy, and non-atomic preference/privacy/result/selector effects | Required malformed MAX then 301 recovery, JSON null selections, descriptor-captured request/delivery/stage/commit, shallow envelope whitelist, exact rollback ledgers, no pre-commit revocation/persistence, commit-timed consumer effects, acknowledgement fence, prototype-safe keys, and real two-section selector containment | Focused reviewer 624; full/coverage 6,861 at 49.27%; extension-host 216; strict types, production/browser, lint, diagnostics, diff, synchronized bundle gates, and definitive blocker review complete | 2026-08-13 |
| `PRO-18` | `querySharingProtocol.ts` | Duplicated query-sharing requests, untyped host delivery, host/dispatcher string coercion, malformed clipboard effects, and redundant unconsumed webview notification | Required accessor/non-string malformed then canonical clipboard sequence, descriptor-safe three-message parser, dense string columns/rows, request-before-E2E/lookup admission, typed/revalidated delivery, parser-before-clipboard guard, and retained native notification | Focused 359; adjacent 578; full/coverage 6,887 at 49.29%; extension-host 217; strict types, production/browser, lint, diagnostics, diff, synchronized bundle gates, reviewer hardening, and definitive re-review complete | 2026-08-13 |
| `PRO-19` | `editingPreferences.ts` | Duplicated setter shapes, truthiness coercion, unchecked host publication, pre-validation revision admission, malformed UI/watermark effects, and unsafe MAX revision growth | Required malformed 999 then canonical 1 plus malformed setters, descriptor-safe four-message parser, exact booleans, frozen snapshots, zero/equal/lower/MAX ordering, MAX saturation, validation before every write/publication/revision/UI effect, and retained HST-14/PRO-17/first-launch owners | Core 265; adjacent 569; full/coverage 6,903 at 49.30%; extension-host 217; strict types, production/browser, lint, diagnostics, zero maps, synchronized bundle gates, reviewer hardening, and definitive closure review complete | 2026-08-13 |
| `PRO-20` | `copilotInlineCompletionProtocol.ts` | Duplicated request shape, untyped result producers, request/result coercion, malformed resolver consumption, and malformed SQL owner admission | Required two-failure malformed result/owner red, descriptor-safe request/result parser, dense canonical completions, outbound/HST/producer/dispatcher/router admission, exact resolver/timer ordering, sole-owner/source-order guards, and sender inventory | Expanded 729; full/coverage 6,940 at 49.27%; extension-host 218; strict types, production/browser, lint, diagnostics, zero maps, synchronized bundle gates, and definitive closure review complete | 2026-08-13 |
| `PRO-21` | `kustoPublicationProtocol.ts` + one-shot `runtimeMessageEnvelope.ts` descriptor capture | Duplicated four-message publication shapes, host acknowledgement coercion, webview stage/timer/payload consumption before validation, permissive startup correlation, and normalized custom-prototype traffic | Required three-surface malformed commit plus exact waiter/deadline red, descriptor-safe host/webview admission, opaque outer payload capture, one-shot proxy guards, startup/provider/listener ordering, generated-ack-before-ledger checks, independent-ledger preservation, and sender inventory | Focused 938; full/coverage 6,957 at 49.33%; extension-host 218; strict types, production/browser, lint, zero maps, synchronized bundle gates, and definitive closure review complete | 2026-08-14 |
| `DOC-7` | Existing aggregate/client/URI queue + `developmentNoteSectionDefinition.ts` | Native Development Notes passthrough array, direct native array mutation/persistence scheduling, stale adapter Save authority, global-barrier mutation settlement, prefix-inferred hidden reorder ownership, and mismatched companion ID normalization | Required `missing-section` red, exact command-scoped settlement, stale rebase/recreation/Save, lossless stable-entry metadata/order, multiple/duplicate notes, KQL/SQL companion compatibility, arbitrary/whitespace hidden IDs, visible prefix IDs, hidden policy, and displaced-authority guards | Focused 974; full/coverage 6,970 at 49.48%; extension-host 218; strict types, production/browser, lint, diagnostics, synchronized bundle gates, reviewer hardening, and definitive review complete | 2026-08-14 |
| `PRO-22` | `developmentNoteMutationProtocol.ts` + capture-first main-editor ingress | Duplicated mutation shapes, caller-overwritable correlation, webview coercion before note effects, malformed matching response consumption, generic fallthrough, repeated nested admission, and descriptor/proxy laundering | Required malformed-then-canonical live waiter/nonce reds, exact requests/typed terminals, descriptors-plus-prototype capture, cached frozen admission, descriptor-preserving CSV, marked-ID hard stop, alias/wrapper/proxy regressions, and sole-owner inventory | Focused 469; adjacent 1,031; full/coverage 6,995 at 49.48%; extension-host 218; strict types, production/browser, lint, diagnostics, zero maps, synchronized bundle gates, and definitive review complete | 2026-08-14 |
| `PRO-23` | `toolStateSnapshotProtocol.ts` + bounded immutable state capture | Duplicated request/response shapes, request coercion before snapshot/schema effects, malformed matching waiter consumption, unchecked SQL reconciliation, and permissive startup reentrancy | Required malformed/oversized then canonical live-waiter reds, exact constructors, bounded deep descriptor capture, cached immutable admission, parser-before-snapshot/reentrancy/settlement guards, provider identity preservation, and sole-owner inventory | Core 406; adjacent 604; full/coverage 7,009 at 49.49%; extension-host 218; strict types, production/browser, lint, diagnostics, synchronized bundle gates, and definitive review complete | 2026-08-15 |
| `PRO-24` | `kustoExecutionStartProtocol.ts` + transactional comparison binding/controller ownership | Duplicated/coercive start/ack shapes, incomplete startup correlation, host waiter deletion before boolean admission, webview effects before validation, unsafe constructor spread, captured-proxy laundering, and rejected/throwing-start binding or owner leaks | Required 4,999 ms malformed-then-canonical live-waiter red, descriptor-safe constructors/admission, exact Copilot/comparison forms, parser-before-startup/effects/settlement guards, scalar typed ack, target/predecessor preflight, captured-proxy rejection, real-store exception-safe temporary-pin rollback, and real-controller late-cancellation rejection | Core 414; adjacent 984; full/coverage 7,029 at 49.53%; extension-host 218; strict types, production/browser, lint, diagnostics, zero maps, synchronized bundle gates, implementation review, and definitive closure review complete | 2026-08-15 |
| `PRO-25` | `powerBiPublishProtocol.ts` + preserved HST-1 application lease/CAS owner | Duplicated result/ack shapes, permissive startup correlation, malformed acknowledgement coercion to rejection, and HTML/dialog/transport/lease effects before canonical runtime admission | Required 14,999 ms created-item malformed-then-canonical exact-lease red, frozen exclusive result constructors, descriptor-safe startup/standalone/HTML/dialog/outbound/host admission, optional DirectQuery fields, exact 15-second expiry and five-second rearm, and sole-owner/source-order inventory | Core 184; adjacent 881; full/coverage 7,044 at 49.55%; extension-host 218; strict types, production/browser, lint, diagnostics, zero maps, synchronized bundle gates, implementation/documentation reviews, and definitive closure review complete | 2026-08-15 |

## Decision Discipline

- Do not create a generic framework before one vertical slice proves the contract.
- Do not move code solely to match the suggested package layout.
- Do not replace mature Kusto schema, SQL lifecycle, sidecar, or dashboard semantics without a demonstrated contract gap.
- Do not pursue global removal before the owning module API exists.
- Do not let temporary compatibility adapters become new authorities.
- Do not run two writers for document state, result artifacts, or privacy-sensitive persistence.
- Prefer one end-to-end behavioral test over many source-shape assertions, then add static guards for dependency direction.
- Keep migrations reversible until the old authority is removed and broad validation passes.