# Use Copilot chat in a query section

Use the Copilot button inside a query section when the task is local to that query. Copilot can use the selected connection and database schema as context while it drafts or refines KQL.

Keep prompts specific. Mention tables, columns, filters, and output shape when you know them. Ask for an explanation when you want to learn the pattern rather than just paste the result.
