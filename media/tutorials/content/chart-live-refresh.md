# Charts refresh when the source query re-runs

Charts stay connected to their data source. Change the query, run it again, and the chart updates in place without rebuilding the visual.

![Chart connected to a live query result](images/tip-chart-live.png)

This makes chart sections useful while you are still exploring. You can tune filters, bin sizes, and summarization in KQL while keeping the same visual frame for comparison.