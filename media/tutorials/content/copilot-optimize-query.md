# Query optimization can compare behavior, not just speed

The optimization flow is designed for confidence. It can compare performance and verify that the optimized query returns the same data, even when row or column order changes.

![Query performance optimization comparison](images/tip-copilot-optimize.png)

Use this when you have a slow query that already works. The goal is not a clever rewrite; the goal is a faster query you can trust.