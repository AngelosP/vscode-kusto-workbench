# Architecture

## Architecture Program

- [GOLDEN_OUTCOME.md](GOLDEN_OUTCOME.md) defines the from-scratch target architecture for the complete implemented product. It is the stable north star, not a description of the current tree.
- [ARCHITECTURE_CONVERGENCE.md](ARCHITECTURE_CONVERGENCE.md) compares the current implementation with that target, ranks remaining ownership gaps, and selects one evidence-driven migration iteration at a time.
- This document describes the implementation that exists today and is updated as convergence work lands.

## Overview

Kusto Workbench is a VS Code extension that provides a notebook-like experience for Kusto Query Language (KQL) and T-SQL. The extension consists of three major subsystems:

1. **Extension host** (Node.js / TypeScript) — manages the VS Code integration, query execution, authentication, and schema handling.
2. **Webview UI** (browser / TypeScript / Lit) — the notebook editor rendered inside a VS Code webview panel.
3. **KQL language service** — a custom, lightweight diagnostics and analysis engine for KQL.

## Extension Host (`src/host/`)

| File | Purpose |
| ---- | ------- |
| `extension.ts` | Entry point. Registers providers, commands, and diagnostics |
| `queryEditorProvider.ts` | Main webview transport/composition adapter. Routes host↔webview messages, composes application handlers and service capabilities, and owns no direct application switch cases |
| `mainWebviewStartupGateway.ts` | Panel-scoped startup transport for native KQLX plus KQL and SQL compatibility editors. Owns listener-first installation, capture-first inert inbound envelopes, explicit dispatcher readiness, ordered startup queues, correlated reply reentrancy, and disposal retirement |
| `dashboardApplicationHandler.ts` | Injected host application owner for dashboard prompts, export, Fabric workspace/item lookup, publish request identity/cancellation, first-commit admission, runtime-validated result/acknowledgement publication, document metadata application/compensation leases, and cleanup decisions |
| `artifactCsvSaveApplicationHandler.ts` | Injected host application owner for governed result-table CSV picker admission, one-use nonce challenges, cancellation, deadlines, replay tombstones, exact artifact correlation, and file publication |
| `pythonExecutionApplicationHandler.ts` | Injected host application owner for Python interpreter fallback, process/stdin/stdout/stderr lifecycle, bounded output, timeout/kill behavior, terminal publication, and disposal |
| `importedCsvSaveApplicationHandler.ts` | Injected host application owner for imported URL-section CSV empty-data UX, native picker admission, URI-preserving extension handling, exact UTF-8 publication, saved-file actions, failures, and disposal |
| `querySharingApplicationHandler.ts` | Injected host application owner for Kusto ADX-link generation, Kusto/SQL rich and plain-text share formatting, host clipboard effects, response publication, notifications, and disposal |
| `urlContentApplicationHandler.ts` | Injected host application owner for URL validation, redirect-following fetch, timeout/abort, text/image byte caps, content classification/sniffing, truncation, response shaping, and disposal |
| `controlCommandSyntaxApplicationHandler.ts` | Injected host application owner for Microsoft Learn control-command syntax lookup, 24-hour caching, HTML/entity extraction, `with(...)` argument parsing, response shaping, and disposal |
| `resourceUriApplicationHandler.ts` | Injected host application owner for local Markdown resource URI normalization, passthrough and base decisions, file existence/cache checks, webview URI conversion, response shaping, and disposal |
| `copilotContentOpenApplicationHandler.ts` | Injected host application owner for opening Copilot tool results as untitled plaintext beside the editor and invoking the built-in Markdown preview, including exact failure notifications and disposal |
| `informationNotificationApplicationHandler.ts` | Injected response-free host application owner for exact `showInfo` information notifications, fire-and-forget native invocation, and disposal |
| `cachedValuesOpenApplicationHandler.ts` | Injected response-free host application owner for awaited `seeCachedValues` command dispatch, exact failure propagation, and disposal |
| `copilotAgentOpenApplicationHandler.ts` | Injected response-free host application owner for awaited zero-argument Kusto Workbench Agent Chat opening through the shared native chat helper, exact failure propagation, and disposal |
| `editorCursorStatusApplicationHandler.ts` | Injected host application owner for cursor-position routing, panel visibility/disposal clearing, unique provider owner prefixes, and development snapshot publication |
| `editingPreferencesApplicationHandler.ts` | Injected host application owner for the three runtime-validated editing-preference mutation routes, exact storage-key mapping and boolean preservation, revisioned mutation delegation, validated all-webview publication with current-panel fallback, and disposal |
| `kustoConnectionIntakeApplicationHandler.ts` | Injected host application owner for discovered-cluster intake, Kusto Explorer XML selection/read/import, exact deduplication and normalization, sequential additions, notifications, post-mutation connection refresh ordering, and disposal |
| `kustoConnectionOnboardingApplicationHandler.ts` | Injected host application owner for the Kusto Add Connection dialog, single-connection validation/persistence/selection/refresh ordering, account-aware draft testing, exact terminals, tracing, and disposal |
| `sqlConnectionOnboardingApplicationHandler.ts` | Injected host application owner for SQL connection prompts and direct form intake, exact AAD/SQL Login credential shaping, selection persistence, acknowledgement publication, and disposal |
| `sqlFavoritesApplicationHandler.ts` | Injected host application owner for Query Editor SQL favorite naming, sanitized mutation and persistence, exact publication, failure containment, projection reads, and disposal |
| `kustoFavoritesApplicationHandler.ts` | Injected host application owner for Query Editor Kusto favorite migration, principal filtering, mutation/persistence, cross-panel/listener publication, confirmation, projection reads, activation, and disposal |
| `sqlDatabaseDiscoveryApplicationHandler.ts` | Injected host application owner for Query Editor SQL database loading, passive/refresh acquisition, exact lifecycle tickets, canonical target/principal/privacy admission, protected one-shot discovery, cache sequencing, terminal reconciliation, and disposal |
| `sqlEditorLifecycleApplicationHandler.ts` | Injected response-free host application owner for SQL section/target and STS request/document/connect forwarding plus Production-gated, ordered development-auth writes |
| `kustoSchemaRequestApplicationHandler.ts` | Injected response-free host application owner for exact Kusto primary and cross-cluster schema request admission, argument shaping, settlement, and disposal |
| `sqlSchemaRequestApplicationHandler.ts` | Injected host application owner for Query Editor SQL schema refresh reservation, same-section supersession, exact lifecycle-owner capture, cache/result/error admission, publication, and disposal |
| `sqlConnectionsProjectionApplicationHandler.ts` | Injected host application owner for every Query Editor SQL connection snapshot trigger, revision ordering, canonical policy/principal joining, cache/favorites filtering, exact `sqlConnectionsData` publication, and disposal |
| `kustoConnectionsProjectionApplicationHandler.ts` | Injected host application owner for every Query Editor Kusto connection snapshot trigger, revision ordering, editing-preference/first-time/account/connection/selection/cache/favorites reads, canonical Leave No Trace admission, exact `connectionsData` publication, retry, and disposal |
| `persistedResultSanitizationApplicationHandler.ts` | Injected cross-language host application owner for synchronous, fresh, fail-closed, and lock-held persisted-result sanitation; Kusto-then-SQL owner admission; comparison/artifact cleanup; persistence invalidation events; and disposal |
| `kqlLanguageRequestApplicationHandler.ts` | Injected host application owner for validated Query Editor KQL language request admission, exact two-method language-host delegation, typed success/fixed-failure response construction, logging, and disposal |
| `sqlLastSelectionApplicationHandler.ts` | Injected response-free host application owner for Query Editor SQL last-connection and optional last-database persistence, exact write ordering, rejection propagation, and disposal |
| `developmentNoteMutationApplicationHandler.ts` | Injected host application owner for Copilot development-note request IDs, five-second correlation deadlines, exact delivery failures, matching `toolResponse` settlement, and disposal |
| `copilotInlineCompletionApplicationHandler.ts` | Injected host application owner for Kusto/SQL inline-completion route admission, SQL owner-token assertion/delegation, exact empty SQL fallback publication, and disposal |
| `copilotAvailabilityApplicationHandler.ts` | Injected response-free host application owner for exact Copilot-availability route admission, awaited capability delegation, exact rejection, and disposal |
| `copilotWriteQueryPreparationApplicationHandler.ts` | Injected response-free host application owner for exact Kusto/SQL Copilot write-query preparation admission, awaited capability delegation, exact rejection, and disposal |
| `copilotConversationClearApplicationHandler.ts` | Injected response-free host application owner for exact Kusto/SQL Copilot conversation-clear admission, Kusto request-identity validation, awaited capability delegation, exact rejection, and disposal |
| `copilotHistoryRemovalApplicationHandler.ts` | Injected response-free host application owner for exact Copilot history-removal route admission, awaited box/entry capability delegation, exact rejection, and disposal |
| `copilotChatFirstTimeApplicationHandler.ts` | Injected host application owner for the complete Copilot Chat first-time state, modal, Agent Chat delegation, result publication, and disposal workflow |
| `workbenchToolSessionApplicationHandler.ts` | Injected host application owner for the panel tool-orchestrator connection, activation, state correlation, schema-refresh targets, SQL tool callbacks, all three tool routes, and disposal |
| `kustoConnectionBrowsingApplicationHandler.ts` | Injected host application owner for Kusto connection listing, passive/interactive database request shaping, selection persistence ordering, best-effort diagnostics refresh, and disposal |
| `copilotQueryWorkflowApplicationHandler.ts` | Injected host application owner for Copilot write-query start/cancel and standalone Optimize prepare/cancel/run admission, SQL owner-preflight races and terminals, exact Kusto cancellation identity, lazy SQL adapter delegation, and disposal |
| `kustoSectionExecutionApplicationHandler.ts` | Injected host application owner for Kusto section open/target/close routing, start/publication acknowledgement ledgers and deadlines, manual execution admission, exact cancellation, physical/policy result admission, and disposal |
| `comparisonPreparationApplicationHandler.ts` | Injected host application owner for cross-engine comparison preparation, SQL application acknowledgements, rollback/retry, target/policy revalidation, removal cancellation, and disposal |
| `sqlSectionExecutionApplicationHandler.ts` | Injected host application owner for manual SQL execution/cancellation, synchronous preflight, owner/protection and tool-owner validation, query-mode shaping, physical start, exact terminals, protected logging, cleanup, and disposal |
| `queryEditorCopilot.ts` | Copilot integration (extracted from provider) |
| `queryEditorConnection.ts` | Connection management (extracted from provider) |
| `queryEditorSchema.ts` | Schema handling (extracted from provider) |
| `kustoConnectionLifecycle.ts` | Serializes Kusto connection and Leave No Trace mutations, revokes affected execution identities, and publishes refreshed connection snapshots |
| `kustoExecutionCoordinator.ts` | Per-editor Kusto reservation, replacement, exact cancellation, dispatch capture, and exactly-once terminal authority |
| `queryRunCoordinator.ts` | Transport-neutral active-query sequence, identity, cancellation, and guarded cleanup |
| `queryEditorTypes.ts` | Shared types, including `IncomingWebviewMessage` |
| `powerBiExport.ts` | Pure Power BI DAX/TMDL/PBIR renderer helpers over the shared portable dashboard IR |
| `powerBiProjectArtifacts.ts` | Pure in-memory compiler for one sorted, collision-safe Power BI project artifact manifest with copy-on-read bytes and Fabric-part projection |
| `powerBiProjectWriter.ts` | VS Code filesystem adapter that writes the compiled Power BI project manifest for local `.pbip` export |
| `powerBiPublish.ts` | Fabric/Power BI service publishing: consumes the in-memory manifest directly and creates or updates SemanticModel and Report items |
| `kustoClient.ts` | Azure Kusto client wrapper. Authentication, cancelable query attempts with immutable dispatch identity, schema fetching, caching |
| `connectionManager.ts` | Persists Kusto cluster connections and revisioned Leave No Trace state in VS Code global state |
| `kustoLeaveNoTracePolicyStore.ts` | Shared file-locked Kusto privacy policy with atomic snapshots, per-cluster revocation generations, recovery, and cross-window watching |
| `connectionManagerViewer.ts` | Connection manager webview panel |
| `kqlxEditorProvider.ts` | Custom editor for `.kqlx` and `.mdx` notebook files |
| `kqlCompatEditorProvider.ts` | Custom editor for `.kql`/`.csl` files (compatibility mode) |
| `compatSidecarFormat.ts` | Shared pure linked-sidecar URI, validation, hydration, and canonicalization helpers for KQL/SQL compatibility editors |
| `compatSidecarStore.ts` | Shared serialized, lock-protected CAS publication, repair, recovery, and drain mechanics for established compatibility sidecars |
| `compatSidecarSession.ts` | Shared revision, persist queue, upgrade barrier, dirty baseline, final-snapshot, reload, beforeunload, and close state machine |
| `compatSidecarProjectionCoordinator.ts` | Panel-scoped shared KQL/SQL compatibility owner for projection generations, active/pending source identity, bounded initial recovery, reload acknowledgement currentness, persist-generation admission, source-reload authority, and rollback-failure fencing |
| `compatSidecarPersistCoordinator.ts` | Panel-scoped shared KQL/SQL compatibility owner for validated persist-snapshot admission, queue/revision application, sanitation/materialization ordering, source verification/rollback, acknowledgement, beforeunload observation, and final settlement |
| `compatSidecarCloseCoordinator.ts` | Panel-scoped shared close owner for KQL/SQL compatibility editors: retired persistence admission, exactly-once startup, gateway/session/store draining, Save/Discard and exact recovery, repair, disposal, and terminal settlement |
| `mdCompatEditorProvider.ts` | Custom editor for `.md` files with embedded KQL |
| `kqlxFormat.ts` | Type definitions for the `.kqlx` JSON file format (`KqlxSectionV1`, `KqlxStateV1`) |
| `schemaCache.ts` | Disk-based caching for database schemas |
| `schemaIndexUtils.ts` | Schema formatting utilities for compact text representation |
| `kqlSchemaInference.ts` | Builds schema-match tokens from canonical physical table references plus masked function calls |
| `queryEditorHtml.ts` | HTML rendering for the query editor webview |
| `selectionTracker.ts` | Tracks text editor selections for compatibility mode |
| `diffViewerUtils.ts` | Utilities for rendering diff views |
| `cachedValuesViewer.ts` | Cached values viewer panel, including generation-leased and application-acknowledged SQL schema/cache publication |
| `kustoWorkbenchTools.ts` | VS Code agent tool registrations |
| `copilotConversationUtils.ts` | Copilot conversation message building utilities |
| `copilotPromptUtils.ts` | Pure prompt template builders for Copilot optimization and tool definitions |
| `kustoClientUtils.ts` | Pure schema parsing (`extractSchemaFromJson`, `finalizeSchema`), cell formatting, error classification |
| `queryEditorUtils.ts` | Pure query helpers: error formatting, control command detection, query mode, cache directives |
| `remoteFileOpener.ts` | Remote file opening support |
| `editingPreferences.ts` | Revisioned application-backed editing preferences shared across Kusto, SQL, and VS Code windows |
| `firstLaunch/firstLaunchState.ts` | Versioned first-use state, legacy-user detection, and resumable write-journal parsing |
| `firstLaunch/editorAssociationManager.ts` | Serialized ownership-aware updates to global `workbench.editorAssociations` |
| `firstLaunch/firstLaunchCoordinator.ts` | Coalesces file, Activity Bar, and command triggers; commits or recovers setup transactions |
| `firstLaunch/firstLaunchSetupPanel.ts` | Secure singleton host for the first-launch setup webview |
| `firstLaunch/firstLaunchTriggers.ts` | Diff- and sidecar-aware supported-file trigger registration |
| `firstLaunch/firstLaunchProfileLease.ts` | `proper-lockfile`-backed profile lease preventing concurrent setup across VS Code windows |
| `sqlConnectionManager.ts` | Persists SQL connections in VS Code global state, passwords in SecretStorage |
| `sqlClient.ts` | Stable SQL data-plane facade over SQL Tools Service: database/schema discovery, cancelable execution |
| `sqlEditorSchema.ts` | SQL schema disk/memory cache with physical-owner, cache-generation, and exact request-current admission |
| `copilotChatFlavor.ts` | Flavor configuration for Copilot chat (Kusto vs SQL) |
| `sql/sqlDialect.ts` | Persisted SQL dialect metadata and shared schema types |
| `sql/mssqlDialect.ts` | MSSQL connection-form metadata (`mssql` ID, auth modes, default port) |
| `sql/sqlDialectRegistry.ts` | Dialect metadata registry used by connection UIs |
| `sql/mssqlSchema.ts` | MSSQL schema catalog queries and transport-neutral parsers |
| `sql/sqlWorkbenchService.ts` | Extension-scoped owner for SQL connections, STS runtime, query broker, and client facade |
| `sql/sqlEditorLifecycleCoordinator.ts` | Editor-scoped SQL lifecycle orchestrator: section incarnations, target transitions, STS documents/replay, request currentness, and connection/principal/privacy invalidation |
| `sql/sqlEditorSessionRegistry.ts` | State-owning editor-scoped SQL target, principal, revocation, comparison, and owner-token authority |
| `sql/sqlExecutionBroker.ts` | Editor-scoped SQL admission, exact execution identity, cancellation, currentness, and guarded lease cleanup shared by manual and Copilot runs |
| `sql/sqlLeaveNoTracePolicyStore.ts` | Versioned, lock-protected, cross-extension-host SQL privacy policy with atomic updates and watcher propagation |
| `sql/sqlAuthState.ts` | Per-connection auth state tracking (AAD vs SQL Login) |
| `sql/stsProtocol.ts` | Typed contracts for the pinned SQL Tools Service JSON-RPC protocol |
| `sql/stsRuntime.ts` | Lazy, extension-scoped SQL Tools Service startup and shutdown |
| `sql/stsQueryService.ts` | Query execution state machine: connect, execute, subset paging, cancel, dispose, disconnect |
| `sql/stsResultAdapter.ts` | Converts STS column/cell envelopes into the shared `QueryResult` shape |
| `sql/stsConnectionOptions.ts` | Purpose-specific STS authentication, TLS, and timeout options |
| `sql/stsProcessManager.ts` | SQL Tools Service (STS) process lifecycle: spawn, restart with backoff, JSON-RPC connection |
| `sql/stsLanguageService.ts` | STS language service client: initialize, completion requests, document sync |
| `sql/stsDownloader.ts` | Downloads, verifies, atomically installs, and caches the STS binary on first use |

## KQL Language Service (`src/host/kqlLanguageService/`)

A custom, lightweight language service for KQL diagnostics and analysis:

| File | Purpose |
| ---- | ------- |
| `sourceAnalysis.ts` | Pure immutable KQL source analysis: offset-preserving comment/string masking, statements, management-command spans, lexical `let`/parameter scopes, and physical table references |
| `service.ts` | Core diagnostics engine. Consumes canonical source analysis and tracks column availability |
| `host.ts` | Bridge between extension and language service. Resolves schema context |
| `protocol.ts` | Compatibility re-export of the shared KQL language protocol types |

`analyzeKqlSource(text)` is the sole host-side lexical and physical-table source owner. `KqlLanguageService.findTableReferences()`, unknown-table diagnostics, schema matching, and Fully Qualify consume its exact UTF-16 ranges. Fully Qualify admits complete, non-overlapping identifier ranges through `kql-table-reference-ranges.ts`; an empty or malformed response is a no-op and never falls back to a second scanner. Column-flow simulation remains in `service.ts` and is intentionally separate from source ownership.

## Webview UI (`src/webview/`)

The notebook UI runs as a VS Code webview, built with Lit web components and legacy bridge modules:

| Directory / File | Purpose |
| ---------------- | ------- |
| `index.ts` | esbuild entry — imports all modules in load order |
| `queryEditor.js` | Pre-load stub (queues clicks before bundle loads) |
| `vscodeApi.js` | `acquireVsCodeApi()` bridge (separate `<script>` tag for browser-ext shim replacement) |
| `core/` | Cross-cutting runtime infrastructure (state, persistence, message dispatcher, keyboard, reorder) |
| `monaco/` | Monaco-specific runtime modules (editor wiring, diagnostics, completions, suggestions) |
| `generated/` | Generated control command and function bridge modules |
| `sections/` | Lit web components for each section type |
| `components/` | Reusable Lit components (`kw-data-table`, `kw-dropdown`, etc.) |
| `shared/` | Pure utility modules importable by both components and modules |
| `styles/` | CSS files |
| `viewers/` | Viewer components (cell viewer, object viewer, etc.) |
| `first-launch/` | Standalone Lit first-launch form for file associations and editing defaults |

### Key Runtime Modules (`src/webview/`)

The webview runtime is split into `core/` and `monaco/`:

- `core/`: cross-cutting infrastructure, global orchestration, and section factory
- `monaco/`: editor-specific integrations

| Module | Purpose |
| ------ | ------- |
| `core/main.ts` | Event handlers and webview-level message orchestration |
| `core/message-handler.ts` | Host `postMessage` dispatcher and routing |
| `core/editing-preferences.ts` | Persistence-free application of revisioned editing preferences to Kusto and SQL controls |
| `core/state.ts` | General webview state and typed Kusto preparation/readiness accessors |
| `core/kusto-editor-schema-coordinator.ts` | Per-editor Kusto section, target, request, model, catalog, preparation, and worker-readiness authority |
| `core/kusto-editor-schema-runtime.ts` | Webview-scoped coordinator instance |
| `core/schema-catalogs.ts` | Typed, language-separated Kusto and SQL schema access |
| `core/kusto-schema-message-router.ts` | Exact lifecycle admission for database and schema deliveries |
| `core/synthetic-request-broker.ts` | Bounded request, timeout, and late-delivery tombstone broker for non-editor schema/database requests |
| `core/kusto-schema-request-state.ts` | Narrow legacy request-token compatibility mirror; stamped request currentness remains coordinator-owned |
| `core/query-section-accessors.ts` | Leaf DOM accessors shared by Monaco and query controllers without a section-factory cycle |
| `core/persistence.ts` | State serialization/restore for `.kqlx` files |
| `core/results-state.ts` | Results display state management |
| `core/keyboard-shortcuts.ts` | Keyboard handlers and clipboard integration |
| `core/drag-reorder.ts` | Section drag-and-drop reorder wiring |
| `core/utils.ts` | Shared runtime utility functions |
| `core/dropdown.ts` | Legacy HTML dropdown/menu rendering and management |
| `core/error-renderer.ts` | Error rendering, navigate-to-line, fallback HTML injection |
| `monaco/monaco.ts` | Monaco editor configuration, KQL integration, column inference |
| `monaco/completions.ts` | Completion providers (columns, functions, tables) |
| `monaco/diagnostics.ts` | Real-time KQL diagnostics overlay |
| `core/section-factory.ts` | Section creation for all types (query, chart, python, URL, etc.), data-source utilities |
| `shared/kusto-worker-mutation-port.ts` | Sole transaction serializer for shared Monaco-Kusto worker mutation |

### First-Launch Setup

First-use setup is profile-scoped and may be triggered by the first supported-file open, Activity Bar reveal, or contributed Kusto Workbench command. The coordinator coalesces concurrent triggers into one panel and defers automatic Did you know? delivery until setup settles.

- File choices are application-scoped extension settings; only `.kql`, `.csl`, `.md`, and `.sql` are configurable. `.kqlx`, `.mdx`, and `.sqlx` always use Workbench.
- Editor associations are read and written only at global scope. Ownership metadata preserves an absent or foreign prior mapping and restores it only while the current mapping remains Workbench-owned.
- Fresh installs write a current-install marker before other extension infrastructure starts. Save writes a transaction journal before settings, associations, or editing preferences. A later activation resumes any valid journal before terminal or legacy-user detection, preventing Close or interrupted setup from being misclassified as an upgrade.
- An atomic lock-directory lease in extension global storage permits only one setup owner across VS Code windows. Waiting windows re-read profile state after the lease is released.
- Editing defaults are application-scoped settings. Legacy global-state values migrate into those settings once and are then removed, so Reset Setting returns to the declared default. Configuration changes produce timestamp-monotonic revisions and update every live webview in every window without invoking document persistence. Legacy root preference fields loaded from old notebooks round-trip unchanged, but new global choices never enter the document signature. Automatic completion and Copilot ghost text are shared by Kusto and SQL; Smart documentation is Kusto-only.
- Closing automatic setup writes nothing and suppresses another automatic prompt for that extension-host session. Skip records a terminal state while preserving inherited editing preferences.

### Lit Section Components (`src/webview/sections/`)

| Component | File | Purpose |
| --------- | ---- | ------- |
| `kw-query-section` | `kw-query-section.ts` | KQL query editor with connection picker, execution, results |
| `kw-query-toolbar` | `kw-query-toolbar.ts` | Query toolbar actions (toggles, share, run modes, tools) |
| `query-connection.controller` | `query-connection.controller.ts` | ReactiveController for connection, database, favorites, and schema management |
| `query-execution.controller` | `query-execution.controller.ts` | ReactiveController for query execution, results visibility, optimization |
| `copilot-chat-manager.controller` | `copilot-chat-manager.controller.ts` | ReactiveController for Copilot chat panel installation, visibility, resize, event wiring, and message delegation |
| `toolbar-overflow.controller` | `toolbar-overflow.controller.ts` | ReactiveController for toolbar overflow detection and resize handling |
| `kw-chart-section` | `kw-chart-section.ts` | Chart builder (line, area, bar, scatter, pie, funnel via ECharts) |
| `chart-data-source.controller` | `chart-data-source.controller.ts` | ReactiveController for data source switching, dataset refresh, and per-source column memory |
| `kw-transformation-section` | `kw-transformation-section.ts` | Data transformation expressions |
| `kw-markdown-section` | `kw-markdown-section.ts` | Rich text / documentation (Toast UI editor) |
| `kw-python-section` | `kw-python-section.ts` | Python code cells |
| `kw-url-section` | `kw-url-section.ts` | Embedded web content / images |
| `kw-html-section` | `kw-html-section.ts` | HTML dashboard editor/preview with provenance, slicers, data bridge, and Power BI actions |

### ReactiveController Pattern

When a Lit component has distinct behavioral concerns, each concern is extracted into a **ReactiveController** co-located with its host component (in `sections/` or `components/`). Controllers own state and lifecycle hooks but do not contain render templates — rendering stays in the host component. This keeps components focused and controllers independently testable.

| Controller | Host | Location |
| ---------- | ---- | -------- |
| `QueryConnectionController` | `kw-query-section` | `sections/query-connection.controller.ts` |
| `QueryExecutionController` | `kw-query-section` | `sections/query-execution.controller.ts` |
| `CopilotChatManagerController` | `kw-query-section` | `sections/copilot-chat-manager.controller.ts` |
| `ToolbarOverflowController` | `kw-query-toolbar` | `sections/toolbar-overflow.controller.ts` |
| `ChartDataSourceController` | `kw-chart-section` | `sections/chart-data-source.controller.ts` |
| `TableSearchController` | `kw-data-table` | `components/table-search.controller.ts` |
| `TableSelectionController` | `kw-data-table` | `components/table-selection.controller.ts` |
| `TableVirtualScrollController` | `kw-data-table` | `components/table-virtual-scroll.controller.ts` |
| `TableRowJumpController` | `kw-data-table` | `components/table-row-jump.controller.ts` |

### Reusable Lit Components (`src/webview/components/`)

| Component | Purpose |
| --------- | ------- |
| `kw-data-table` | Virtual-scrolling data table with sort, filter, search, column jump |
| `kw-dropdown` | Dropdown/menu component with keyboard navigation |
| `kw-section-shell` | Shared section wrapper (drag handle, collapse, remove, name) |
| `kw-copilot-chat` | Copilot chat panel within a query section |
| `kw-popover` | Reusable popover component |
| `kw-filter-dialog` | Column filter dialog |
| `kw-sort-dialog` | Column sort dialog |
| `kw-search-bar` | Reusable search bar with match navigation |
| `kw-object-viewer` | JSON/object viewer modal |
| `kw-publish-pbi-dialog` | Power BI/Fabric publish dialog with workspace selection, update/new mode, and publish status |

### Shared Utilities (`src/webview/shared/`)

| Module | Purpose |
| ------ | ------- |
| `chart-utils.ts` | Number formatting, axis settings, legend normalization, UTC date utilities |
| `transform-expr.ts` | Expression tokenizer/parser/evaluator for transformation sections |
| `data-utils.ts` | Cell value conversion, time axis inference, column name normalization |
| `persistence-utils.ts` | URL normalization, leave-no-trace checks, byte length, result serialization |
| `schema-utils.ts` | `buildSchemaInfo()` pure function |
| `persistence-state.ts` | Shared persistence state object |
| `webview-messages.ts` | Typed `postMessage` wrapper |
| `lazy-vendor.ts` | Lazy loading for vendor libraries |
| `chart-renderer.ts` | ECharts rendering delegation |
| `error-parser.ts` | Pure error parsing: JSON extraction, line positions, error model builder |
| `viewer-utils.ts` | Pure viewer utilities: JSON formatting, syntax highlighting, value classification |

## Host ↔ Webview Communication

Extension host and webview communicate via `postMessage`:

* **Host → Webview:** `QueryEditorProvider` owns panel transport; injected application handlers such as `HostDashboardApplicationHandler`, `HostArtifactCsvSaveApplicationHandler`, `HostPythonExecutionApplicationHandler`, `HostQuerySharingApplicationHandler`, `HostUrlContentApplicationHandler`, `HostControlCommandSyntaxApplicationHandler`, `HostResourceUriApplicationHandler`, `HostSqlConnectionOnboardingApplicationHandler`, `HostSqlFavoritesApplicationHandler`, `HostKustoFavoritesApplicationHandler`, `HostSqlDatabaseDiscoveryApplicationHandler`, `HostKqlLanguageRequestApplicationHandler`, `HostDevelopmentNoteMutationApplicationHandler`, `HostCopilotInlineCompletionApplicationHandler`, `HostCopilotChatFirstTimeApplicationHandler`, `HostWorkbenchToolSessionApplicationHandler`, `HostKustoConnectionsProjectionApplicationHandler`, and development-only snapshot publisher `HostEditorCursorStatusApplicationHandler` send through that transport callback. `HostEditingPreferencesApplicationHandler` publishes the authoritative revisioned preference object through the live tool orchestrator to every connected webview when available and otherwise awaits the same current-panel transport. The response-free `HostCopilotContentOpenApplicationHandler`, `HostInformationNotificationApplicationHandler`, `HostCachedValuesOpenApplicationHandler`, and `HostCopilotAgentOpenApplicationHandler` own their native presentation effects without introducing a host message; `HostSqlLastSelectionApplicationHandler` likewise emits no host response while it owns ordered application-state writes. `HostCopilotAvailabilityApplicationHandler`, `HostCopilotWriteQueryPreparationApplicationHandler`, `HostCopilotConversationClearApplicationHandler`, and `HostCopilotHistoryRemovalApplicationHandler` emit no response while they own route admission. `HostKustoConnectionBrowsingApplicationHandler` also introduces no sender: it keeps exact `getConnections` route ownership while delegating snapshot publication to `HostKustoConnectionsProjectionApplicationHandler`, and delegates database publication to `ConnectionService`. `CopilotService` remains the exact availability/options/status sender and conversation/history-state mutation owner; the first-time handler is the exact `copilotChatFirstTimeResult` sender.
`HostCopilotQueryWorkflowApplicationHandler` emits only the existing SQL preflight owner-change or cancellation `copilotWriteQueryDone` terminal through provider transport. `CopilotService` remains the sender and semantic owner for model execution, status/options/output, history, exact Kusto Optimize publication, and ordinary completion terminals.

`HostKustoSectionExecutionApplicationHandler` emits the existing `kustoExecutionStarted` claim plus `kustoPublicationStage`, `kustoPublicationCommit`, and `kustoPublicationRevoke` messages through provider transport. `KustoExecutionCoordinator` remains the sole logical terminal owner and sender of exact `queryResult`, `queryError`, and `queryCancelled` envelopes.

`kustoExecutionStartProtocol.ts` wraps only `kustoExecutionStarted` and `kustoExecutionStartedAck` around that preserved HST-34 workflow. Host construction descriptor-captures and validates the exact Copilot or lineage-complete comparison reservation before creating a five-second waiter, timer, or transport effect. Main startup and standalone transport admit the original descriptor snapshot before queueing, correlated reentrancy, or routing. The webview admits before section lookup, target/predecessor comparison, comparison-artifact binding, `beginQueryExecution()`, acknowledgement, or event effects; acknowledgements are constructed through the shared typed owner and revalidated before E2E/transport. The host admits acknowledgements before ledger lookup, deletion, timer cancellation, or settlement. Malformed matching traffic is inert, and only a canonical exact acknowledgement can permit physical dispatch.

`powerBiPublishProtocol.ts` wraps only `publishToPowerBIResult` and `publishToPowerBIAck` around the preserved HST-1 publish-application transaction. It solely owns exact frozen success/failure result and acknowledgement types, scalar constructors, descriptor capture, and runtime admission. Successes require exact report, workspace, model, data-mode, schedule, and optional initial-refresh/workspace-name fields; failures carry only exact correlation and error fields. Host results are constructed and revalidated before publication. Main startup and standalone transport admit the original descriptor snapshot before queueing, correlated reentrancy, tracing, routing, or delivery. `kw-html-section` and `kw-publish-pbi-dialog` independently admit recognized results before correlation, metadata, document command, persistence, request retirement, or dialog effects; the section constructs acknowledgements through the shared scalar owner before outbound capture/transport. The host admits acknowledgements before lease lookup, timer mutation, compensation, cleanup, or finalization. Malformed matching traffic is inert, while canonical `accepted: true` retires the application lease without cleanup.

`HostComparisonPreparationApplicationHandler` emits the existing `ensureComparisonBox`, SQL admission/commit/finalize/complete/rollback/release, and `sqlCopilotPolicyChanged` messages through provider transport. It receives exact `sqlComparisonAdmissionAck`, `comparisonBoxEnsured`, and `sqlComparisonRemoved` objects. `SqlEditorLifecycleCoordinator`, `SqlEditorSessionRegistry`, `SqlExecutionBroker`, `KustoExecutionCoordinator`, and `CopilotService` retain target, owner-token, execution, and Copilot cancellation authority.

Native KQLX, KQL compatibility, and SQL compatibility panels compose `QueryEditorProvider` through one `MainWebviewStartupGateway` per panel. The gateway installs the inbound listener before asynchronous provider or linked-file initialization, queues ordinary inbound traffic until the provider handler exists, and queues every outbound main-panel message until the webview explicitly publishes `mainWebviewDispatcherReady`. Identity-shaped waiter replies may re-enter a blocked startup handler, but ordinary messages retain admission order. Disposal rejects ordinary and outbound traffic; explicitly admitted close-critical persistence is serialized through one retired chain. For KQL and SQL compatibility panels, `CompatSidecarCloseCoordinator` alone decides delayed-beforeunload and correlated-final-snapshot retirement eligibility, closes gateway admission to a stable tail, and then drives session/store cleanup. Embedded tutorial messages use the same panel transport.

* **Webview → Host:** `vscode.postMessage({ type: '...', ... })` via `postMessageToHost()` in `webview-messages.ts`

On the host side, incoming messages match the `IncomingWebviewMessage` union type exported from `queryEditorTypes.ts`. On the webview side, the message dispatcher lives in `core/message-handler.ts` (a large `switch` statement). `index.ts` explicitly starts it after runtime and component registration; startup installs the listener, drains preload-buffered host traffic exactly once, and then publishes readiness. The readiness message is consumed by the startup gateway or the standalone provider wrapper and never enters application routing.

Early Add requests have a separate bounded handoff inside the webview. `queryEditor.js` owns only the preload count buffer in `window.__kustoQueryEditorPendingAdds`. When `persistence.ts` installs the runtime Add bridges, it synchronously snapshots supported positive safe-integer counts, merges them into the sole runtime queue at `pState.queryEditorPendingAdds`, clears the preload object, and only then replaces every preload stub. After an applied document projection is acknowledged, capability-aware finalization drains that runtime queue once for the applied projection before considering the document-kind default. Repeated runtime takeover or repeated finalization cannot duplicate an adopted Add or create a later default section.

The native notebook document lifecycle has one narrower runtime-validated channel in `src/shared/documentViewProtocol.ts`. Protocol version 1 and channel `document-view` cover only `documentData`, `documentReloadResult`, host-owned section commands/results, and the Markdown command Save barrier. `KqlxEditorProvider` creates a UUID for each concrete panel incarnation, stamps every in-scope host message, and rejects malformed or mismatched webview envelopes before projection activation, command queue admission, or barrier settlement. The webview adopts the session only from its first valid native `documentData`, permanently tombstones that initial reload request, retains a bounded recent-reload ledger, and rejects malformed, duplicate, or cross-session host traffic before client or DOM adoption. Source generations, command IDs, document/section revisions, the per-URI physical queue, and Save leases remain independent inner fences. Markdown compatibility and browser/legacy viewers remain outside this native channel.

Plain KQL and SQL compatibility persistence has a separate runtime-validated channel in `src/shared/compatibilityPersistenceProtocol.ts`. Protocol version 1 and channel `compatibility-persistence` cover only correlated `requestDocument`/`documentData`, `persistDocument`/`persistDocumentAck`, `requestFinalPersist` responses, and `documentReloadResult`. Each provider creates one panel UUID and injects its envelope into the page before the preload sends the initial request. Both providers parse at `MainWebviewStartupGateway` admission and stamp outbound lifecycle traffic; the webview parses before DOM, persistence, acknowledgement, or waiter effects. Unique section identities and the exact KQL/SQL primary shape are checked in the shared parser, while the existing sidecar codec remains the complete linkage, document-kind, known-field, and lossless-overlay validator before revision or queue admission. Bounded pending/applied request ledgers re-acknowledge identical lost-ack retries without rematerializing, reject lower source generations, and immediately resend newer local state. Markdown compatibility and browser/legacy viewers remain outside this channel.

Kusto schema request and delivery traffic has a separate runtime-validated channel in `src/shared/kustoSchemaProtocol.ts`. It is the sole discriminated type and parser owner for webview requests `prefetchSchema` and `requestCrossClusterSchema` plus host deliveries `schemaData`, `schemaError`, `crossClusterSchemaData`, and `crossClusterSchemaError`. `postMessageToHost()` validates recognized requests before transport, and `HostKustoSchemaRequestApplicationHandler` validates them again at host admission before `SchemaService`. `SchemaServiceHost.postMessage()` accepts only the four typed deliveries. The webview dispatcher parses recognized deliveries at entry before runtime gating, tracing, primary routing, synthetic settlement, catalogs, supplemental coordination, or worker effects. Raw show-schema payloads remain opaque; editor lifecycle, synthetic request, legacy request-token, supplemental request, cache, coordinator, and worker identities remain independent inner authorities. This channel deliberately adds no panel-session envelope and does not include database discovery or SQL schema traffic.

Kusto database-discovery traffic has a parallel runtime-validated channel in `src/shared/kustoDatabaseDiscoveryProtocol.ts`. It solely owns webview requests `getDatabases` and `refreshDatabases` plus host deliveries `databasesData` and `databasesError`. Requests validate before webview transport and again in `HostKustoConnectionBrowsingApplicationHandler` before `ConnectionService`; `ConnectionServiceHost.postMessage()` accepts only the two typed deliveries. The main dispatcher and the bundled E2E database waiter parse recognized deliveries before lifecycle routing, synthetic settlement, timer/listener settlement, or selector/error effects. Shape validation deliberately preserves blank connection IDs, optional and partial lifecycle fields, legacy tokenless traffic, optional account partitions, empty dense database arrays, and `authoritative` / `fallback` provenance. Existing editor, synthetic, legacy, account-partition, request-token, cache, fetch, authentication, tracing, recovery, and presentation owners remain authoritative.

SQL database-discovery traffic has a parallel runtime-validated channel in `src/shared/sqlDatabaseDiscoveryProtocol.ts`. It solely owns webview requests `getSqlDatabases` and `refreshSqlDatabases` plus host deliveries `sqlDatabasesLoading`, `sqlDatabasesData`, and `sqlDatabasesError`. Requests validate before E2E capture or VS Code transport and again in `HostSqlDatabaseDiscoveryApplicationHandler` before target adoption. The handler publishes only the typed delivery union. The main dispatcher and direct SQL router parse recognized deliveries before `beginDatabaseRequest`, `acceptDatabaseResponse`, completion, or database UI effects. Exact request, section, target-generation, and connection identity plus dense empty database arrays are preserved without normalization; known-discriminator arrays, sparse or inherited-index arrays, and non-string database entries fail closed. Existing SQL lifecycle, canonical target/principal/privacy admission, protected STS, cache/CAS, retry, terminal, and disposal owners remain authoritative.

SQL schema traffic has a parallel runtime-validated channel in `src/shared/sqlSchemaProtocol.ts`. It solely owns webview request `prefetchSqlSchema` and host delivery `sqlSchemaData`. Requests validate before E2E capture or VS Code transport and again in `HostSqlSchemaRequestApplicationHandler` before target adoption. The handler publishes only the typed delivery. The main dispatcher and direct SQL router parse recognized deliveries before catalog or schema-info effects. Complete section, target-generation, connection, database, and server identity; strict success/error metadata; dense string table/view arrays; string-valued nested column maps; and canonical stored-procedure entries are validated without cloning valid objects. Malformed recognized arrays, sparse or inherited-index arrays, mixed success/error metadata, and noncanonical schema entries fail closed. HST-37 request reservation/supersession, lifecycle owner admission, `SqlSchemaService` cache/currentness, Cached Values, and STS lifecycle algorithms remain authoritative.

SQL Tools Service editor-language traffic has a parallel runtime-validated channel in `src/shared/sqlStsEditorLanguageProtocol.ts`. It solely owns webview requests `stsRequest`, `stsDidOpen`, `stsDidChange`, `stsDidClose`, and `stsConnect` plus host deliveries `stsResponse`, `stsDiagnostics`, and `stsConnectionState`. Monaco providers and `kw-sql-section` validate before request reservation or local document/connect state; `postMessageToHost()` validates before E2E capture/transport; `HostSqlEditorLifecycleApplicationHandler` validates before lifecycle effects; and `SqlEditorLifecycleCoordinator` publishes typed parsed deliveries. The central dispatcher and direct SQL router repeat admission before resolver, timer, diagnostics, readiness, owner, or UI effects. Results and marker records remain opaque while dense marker containers and complete owner/expected-owner identity are validated. Canonical wrong-owner responses retain intentional null settlement; malformed traffic leaves current work untouched. The host-internal STS JSON-RPC/data-plane protocol remains separate.

SQL connection snapshot traffic has a parallel runtime-validated channel in `src/shared/sqlConnectionsProjectionProtocol.ts`. It solely owns webview request `getSqlConnections` and host delivery `sqlConnectionsData`. The outbound wrapper descriptor-captures a validated request before E2E capture or transport, and `HostSqlConnectionsProjectionApplicationHandler` validates again before projection effects. The handler constructs and validates only the typed delivery union. The dispatcher validates and descriptor-captures the complete snapshot before revision comparison, connection/cache/favorite/Leave No Trace state, selectors, or pending-result restoration. Revisioned snapshots are complete; intentionally supported unrevisioned snapshots retain their legacy partial/minimal shape. Ordinary valid parser results retain exact object identity, while operational array overrides, accessors, hostile proxies, sparse containers, and malformed nested values fail closed. SQL cache keys use a null-prototype record and own data properties, so saved IDs such as `__proto__` remain ordinary keys. Present revisions, including zero, participate in ordering; equal revisions may reapply, lower revisions are stale, legacy unrevisioned traffic does not move the watermark, and a revision is committed only after every application effect succeeds. HST-38 revision-tail, canonical principal/privacy joining, filtering, selectors, restoration, and mutation workflows remain separate owners.

Kusto connection snapshot traffic has a parallel runtime-validated channel in `src/shared/kustoConnectionsProjectionProtocol.ts`. It solely owns webview request `getConnections` and host delivery `connectionsData`. Revisioned deliveries require the complete physical connection, account, JSON-stable selection, cache, favorites, editing-preference, first-time, and Leave No Trace projection; intentionally supported unrevisioned deliveries retain the legacy partial shape. `HostKustoConnectionBrowsingApplicationHandler` admits requests before HST-40 effects, and `HostKustoConnectionsProjectionApplicationHandler` publishes only descriptor-captured canonical values under its existing policy lock and correlated retry. The dispatcher captures recognized deliveries before revision or projection effects and captures staged payloads before storage and commit without spreading unchecked objects. Application uses an in-flight fence and a transactional preference/privacy/result snapshot: destructive policy revocation, consumer events, rendered-result clearing, and persistence are staged until commit; selector reconciliation runs only after commit and contains each section failure. Applied acknowledgement precedes revision-watermark movement while synchronous reentrant connection snapshots are rejected. Revision zero, equal replay, stale lower revisions, unrevisioned legacy traffic, dense empty collections, prototype-sensitive record keys, and malformed-MAX recovery are explicit contracts.

Kusto publication transactions have a shared runtime-validated channel in `src/shared/kustoPublicationProtocol.ts`. It solely owns `kustoPublicationStage`, `kustoPublicationAck`, `kustoPublicationCommit`, and `kustoPublicationRevoke` across the main Query Editor, Connection Manager, and Cached Values. All three hosts validate descriptor-captured stage/commit/revoke messages before transport and acknowledgements before waiter lookup, timer cancellation, or settlement. All three webviews validate before stage replacement, timer creation/cancellation, payload storage/application, revoke/completed-ledger lookup, or acknowledgement effects. Stage payloads are captured as opaque outer records; existing Kusto terminal, connection projection, search, schema, snapshot, privacy, and application validators remain authoritative for nested semantics. Exact IDs, phases, booleans, finite deadlines, five-second host deadlines, ten-second completed ledgers, retry/revoke behavior, startup reentrancy, and each surface's independent ledger/disposal ownership remain unchanged.

Every main-webview top-level message first passes through `src/shared/runtimeMessageEnvelope.ts`, which captures one inert own-enumerable-data value plus its original one-shot descriptor snapshot before protocol discrimination. Private captured-envelope metadata lets strict domain protocols retain raw-shape authority without exposing the hostile object to generic routing. Kusto publication consumes the original snapshot directly; artifact CSV reconstructs all original own descriptors before parsing so hidden/accessor fields cannot disappear; and Development Note mutation derives and caches one complete frozen admission so startup reentrancy and provider settlement cannot inspect top-level or nested proxy state twice. Unrelated payload families still receive only the shallow captured value and retain their own domain protocols. Outbound messages are additionally checked against a source-guarded whitelist equal to `OutgoingWebviewMessage['type']`. This prevents unknown-to-known discriminator proxies and accessors from re-entering protocol routing, but it is not a global schema claim.

KQL language traffic has a parallel runtime-validated channel in `src/shared/kqlLanguageProtocol.ts`. It solely owns webview request `kqlLanguageRequest`, host response `kqlLanguageResponse`, both supported methods, parameters, diagnostics, table references, and response branches. Requests validate before E2E capture or VS Code transport and again in `HostKqlLanguageRequestApplicationHandler` before analysis; the handler publishes only the typed response union. Nonblank request IDs and valid parameter/result objects retain exact identity and values. The dispatcher parses recognized responses and verifies the result branch against the pending method before resolver settlement, timer cancellation, or deletion. Dense empty diagnostic/reference arrays remain valid; malformed, sparse, inherited-index, mixed-branch, blank-ID, and wrong-method responses fail closed without consuming the pending request. `KqlLanguageServiceHost`, `KqlLanguageService`, the 1,500 ms webview resolver deadline, and fail-closed Fully Qualify range admission remain authoritative.

Control-command syntax traffic has a parallel runtime-validated channel in `src/shared/controlCommandSyntaxProtocol.ts`. It solely owns webview request `fetchControlCommandSyntax` and host result `controlCommandSyntaxResult`. Requests validate before E2E capture or VS Code transport and again in `HostControlCommandSyntaxApplicationHandler` before cache lookup or Learn acquisition; the handler publishes only the typed result union. Valid request, command, href, syntax, and argument values retain exact identity without normalization. Results validate dense string `withArgs` and canonical empty failure payloads, then require the exact pending `(commandLower, requestId)` pair before cache, pending-ledger, or active-caret-doc refresh effects. The 24-hour host cache, Learn URL/fetch/parser and failure-cache behavior, caret-doc request deduplication and presentation, valid empty syntax, and absence of a timeout remain authoritative.

Resource URI traffic has a parallel runtime-validated channel in `src/shared/resourceUriProtocol.ts`. It solely owns webview request `resolveResourceUri` and host result `resolveResourceUriResult` across both the main Query Editor and plain `.md` compatibility adapters. Requests validate before E2E capture or VS Code transport and again before either host path, stat, or cache path. Both webviews parse recognized results before resolver lookup, deletion, timer cancellation, promise settlement, or Markdown rendering. Exact valid strings, optional or empty `baseUri`, empty-path failures, both existing path-resolution algorithms, two-second deadlines, stale and late-result behavior, request deduplication, Markdown rendering, and browser no-op behavior remain authoritative.

URL content traffic has a parallel runtime-validated channel in `src/shared/urlContentProtocol.ts`. It solely owns webview request `fetchUrl` and host deliveries `urlContent` and `urlError`. Requests are atomically classified and snapshotted before E2E capture or VS Code transport and again in `HostUrlContentApplicationHandler` before request coercion or fetch effects; the handler publishes only the typed delivery union. The central dispatcher and each independent `kw-url-section` listener perform the same atomic delivery admission before correlation, request retirement, state mutation, artifact revocation/publication, or rendering. Descriptor-captured plain messages preserve exact scalar identity while inherited, accessor, non-enumerable, callable, proxy-trapped, and mixed branch shapes fail closed. Successful content requires a 2xx status and nonblank request/resolved identity; empty text bodies, canonical nonblank errors, and exclusive image (`dataUri`) versus CSV/HTML/text (`body` plus `truncated`) branches are preserved. Existing fetch, timeout, cap, classification, debounce, rendering, artifact, imported CSV, and browser-static behavior remains authoritative.

Python execution traffic has a parallel runtime-validated channel in `src/shared/pythonExecutionProtocol.ts`. It solely owns webview request `executePython` and host terminals `pythonResult` and `pythonError`. The live `kw-python-section` snapshots a canonical request before reserving its box-only run or entering `Running...`, then sends through `postMessageToHost()`, which repeats admission before E2E capture or VS Code transport. `HostPythonExecutionApplicationHandler` admits again before workspace-directory or process effects and publishes only the typed terminal union. The central dispatcher and the leaf terminal functions repeat terminal admission before reservation consumption, running/output mutation, host-owned persistence, or legacy rendering. Required fields are own enumerable data properties; callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters. Results retain exact stdout/stderr strings within independent 200 KB UTF-8 caps and a safe-integer-or-null exit code; errors and results are exclusive. The existing one-outstanding-run contract, box-only identity, interpreter/process behavior, projection retirement, inactive-terminal consumption, and browser execution denial remain authoritative.

Artifact CSV saving has a parallel runtime-validated channel in `src/shared/artifactCsvSaveProtocol.ts`. It solely owns webview messages `requestArtifactCsvSave`, `artifactCsvSaveData`, and `cancelArtifactCsvSaveIntent` plus host deliveries `requestArtifactCsvSaveData` and `cancelArtifactCsvSave`. Requests are atomically classified and snapshotted before E2E capture, startup queue/reentrancy decisions, picker or intent effects, nonce consumption, and file publication; handler output is typed and revalidated before transport. Host deliveries validate at dispatcher and leaf admission before pending-export consumption, timer cancellation, artifact binding, response publication, or browser download. Exact export, nonce, box, and artifact identities are preserved without normalization. Callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters. The native picker-first workflow and browser-local adapter retain their existing limits, deadlines, tombstones, policy gates, and download behavior.

Query sharing has a parallel runtime-validated channel in `src/shared/querySharingProtocol.ts`. It solely owns webview requests `copyAdeLink` and `shareToClipboard` plus host delivery `shareContentReady`. Requests are descriptor-captured before E2E capture or VS Code transport and again in `HostQuerySharingApplicationHandler` before connection lookup, ADX-link generation, or formatting. The handler publishes only a typed, revalidated delivery. The dispatcher admits that delivery before `ClipboardItem`, rich clipboard, or plain-text fallback effects. Required scalar fields preserve exact valid values; `columns` and `rowsData` are dense string-only snapshots. Callable, inherited, non-enumerable, accessor, sparse, operational-iterator, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters. Existing artifact admission, row caps, ADX encoding, Kusto/SQL formatting, clipboard behavior, and native notification wording remain authoritative.

Editing preferences have a parallel runtime-validated channel in `src/shared/editingPreferences.ts`. It solely owns webview requests `setCaretDocsEnabled`, `setAutoTriggerAutocompleteEnabled`, and `setCopilotInlineCompletionsEnabled` plus host delivery `editingPreferencesData`. The outbound wrapper descriptor-captures requests before E2E capture or transport, and `HostEditingPreferencesApplicationHandler` validates again before configuration writes. Host snapshots, HST-14 publication, first-launch broadcasts, and configuration-refresh broadcasts validate to frozen descriptor snapshots before transport. The dispatcher and `applyEditingPreferencesData()` independently admit delivery before revision comparison, toolbar state, user-set flags, documentation presentation, or watermark movement. Exact booleans and non-negative safe-integer revisions are required; lower revisions are stale, equal revisions intentionally reapply for cross-window convergence, and host revision growth saturates at `Number.MAX_SAFE_INTEGER`. Callable, inherited, non-enumerable, accessor, array-shaped, descriptor-trapping, revoked, cyclic, and unbounded-prototype traffic fails closed without invoking getters. Application-scoped configuration, optimistic toolbar state, first-launch transactions, PRO-17 embedded snapshot fields, and browser-local defaults remain separate unchanged owners.

Dashboard workflows remain outside the six-kind document-view discriminator set. `QueryEditorProvider` offers each typed inbound message to its injected `DashboardApplicationHandler`; the handler synchronously declines unrelated traffic so Kusto/SQL admission timing is unchanged, and it is the sole dashboard discriminator/workflow owner. HTML sections send request-correlated `exportDashboard`, `getPbiWorkspaces`, `checkPbiItemExists`, and `publishToPowerBI` messages; cancellation and every host response carry the exact request ID and concrete HTML owner. The host replies with `openPublishPbiDialog`, `pbiWorkspacesResult`, `pbiItemExistsResult`, and the runtime-validated `publishToPowerBIResult`, which route only to the originating `kw-html-section`/`kw-publish-pbi-dialog` incarnation. The matching `publishToPowerBIAck` is runtime-validated before startup reentrancy or HST-1 lease effects.

Governed result-table CSV saving remains outside the document-view protocol. `HostArtifactCsvSaveApplicationHandler` owns native picker admission, eight active intents, one-use UUID nonce challenges, 60-second transfer deadlines, bounded 10-minute/256-intent replay tombstones, exact box/artifact matching, UTF-8 file publication, and disposal. `artifactCsvSaveProtocol.ts` wraps all five intent/challenge/transfer/cancel messages around that application boundary. `QueryEditorProvider` only constructs or accepts the handler, offers validated typed messages synchronously, supplies panel transport, and disposes it. The webview artifact/table-generation gate and the browser-local download adapter remain separate owners; the browser shim bundles the same shared parser rather than duplicating its wire schema.

Python execution remains outside the document-view protocol. `HostPythonExecutionApplicationHandler` is the sole native owner of `executePython`, interpreter fallback (`python`, `python3`, `py`), child process and stdin/stdout/stderr lifecycle, independent 200 KB UTF-8 output caps, the 15-second timeout/kill terminal, `pythonResult` / `pythonError` publication, and disposal. Timeout and disposal retire terminal authority before process termination, so synchronous or late close/error events cannot publish twice. `QueryEditorProvider` only constructs or accepts the handler, offers typed messages synchronously, supplies panel transport, and disposes it. The webview one-request admission registry and host-owned persisted Python output remain unchanged authorities.

`pythonExecutionProtocol.ts` wraps that application boundary with one request/terminal contract. The component and `postMessageToHost()` atomically admit `executePython` before reservation, running-state, E2E, or transport effects; the handler repeats admission before native effects and uses a typed terminal transport. Dispatcher and leaf admission precede box-only reservation consumption, state, persistence, and rendering. A malformed matching terminal cannot retire current work; only a canonical terminal for the current exact box settles it.

Imported URL-section CSV saving remains outside the document-view protocol and the governed artifact CSV workflow. `HostImportedCsvSaveApplicationHandler` is the sole native owner of `saveImportedCsv`, including empty-data UX, native picker admission, workspace/home default selection, URI-preserving `.csv` extension handling, exact UTF-8 file publication, Open File / Show in Folder actions, notification failure containment, write-failure messaging, and disposal. `QueryEditorProvider` only constructs or accepts the handler, offers typed messages synchronously, and disposes it. URL fetch identity/content acquisition, `kw-url-section`, browser downloads, Connection Manager preview export, and message shapes remain unchanged adapters.

Query sharing remains outside the document-view protocol. `HostQuerySharingApplicationHandler` is the sole native owner of `copyAdeLink` and `shareToClipboard`, including input validation, Kusto gzip/base64 Azure Data Explorer links, SQL shares without an ADX-link promise, exact HTML/plain-text formatting and escaping, host clipboard writes, typed `shareContentReady` publication, native notifications, and disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers typed messages, supplies connection lookup and panel transport, and disposes it. `querySharingProtocol.ts` validates all three messages before transport or effects. The unconsumed handler-emitted webview `showInfo` duplicate is retired; the exact native success notification remains. The `share:clipboard:result` artifact binding and policy admission, share modal, browser clipboard write, row caps, Kusto/SQL execution and connection ownership remain unchanged adapters.

URL content acquisition remains outside the document-view protocol. `HostUrlContentApplicationHandler` is the sole native owner of `fetchUrl`, including HTTP/HTTPS validation, redirect-following fetch, the 15-second timeout and abort lifecycle, 100 MB text/CSV and 5 MB image caps, 200,000-character truncation, CSV/HTML/text/image classification and HTML body sniffing, exact original/resolved URL identity, `urlContent` / `urlError` publication, and disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers typed messages, supplies panel transport, and disposes it. `kw-url-section` request identity, stale rejection, rendering, immutable URL CSV artifact publication, debounce, autosizing, teardown, imported CSV saving, browser behavior, and `resolveResourceUri` remain unchanged adapters.

`urlContentProtocol.ts` wraps that application boundary with one request/delivery contract. `postMessageToHost()` and the handler atomically admit `fetchUrl` before transport or acquisition effects; handler transport is typed. Both the central dispatcher and the independent URL component listener atomically admit recognized deliveries before any request-ledger, loading, artifact, or rendering effect. Malformed matching and valid stale traffic cannot consume current work; only a canonical current delivery settles it.

Control-command syntax lookup remains outside the document-view protocol. `HostControlCommandSyntaxApplicationHandler` is the sole native application owner of `fetchControlCommandSyntax`, including the 24-hour command cache, Microsoft Learn URL normalization and fetch, Syntax-section/first-`pre` HTML extraction, entity decoding, case-insensitive `with(...)` argument de-duplication, existing failure-cache behavior, exact request/command identity, `controlCommandSyntaxResult` publication, and disposal. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers shared-protocol messages, supplies panel transport, and disposes it. Caret-documentation request de-duplication, presentation caching, rendering, Kusto control-command grammar/detection/execution, generated command data, browser behavior, and `resolveResourceUri` remain unchanged adapters.

Local Markdown resource resolution remains outside the document-view protocol. `HostResourceUriApplicationHandler` owns the main Query Editor resolution algorithm, including HTTP/HTTPS/data/blob/webview passthrough, local-file base validation, workspace-root and relative/absolute path normalization, file stat and exact base/path caching, live-panel webview URI conversion, and disposal/late-publication suppression. Plain `.md` compatibility retains its independent document-directory resolution algorithm in `mdCompatEditorProvider.ts`. `resourceUriProtocol.ts` now supplies both adapters with one request/result contract and parser-before-effect admission, while each webview retains its own exact request ledger and two-second deadline. Markdown rendering, browser behavior, URL acquisition, navigation, and linked queries remain unchanged.

Copilot content opening remains outside the document-view protocol. `HostCopilotContentOpenApplicationHandler` is the sole native owner of `openToolResultInEditor` and `openMarkdownPreview`, including existing tool/content coercion, untitled `plaintext` document creation, `preview: true` beside-column display, local file URI construction, `markdown.showPreview`, and exact failure prefixes. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the original typed messages, and disposes it. Disposal suppresses later requests but does not cancel or silence an already accepted native open or preview effect. The Copilot controller retains presentation intent and emits the unchanged message objects; generation, conversations, tools, browser behavior, navigation policy, message shapes, and Kusto/SQL execution remain unchanged.

Host information notification remains outside the document-view protocol. `HostInformationNotificationApplicationHandler` is the sole native owner of `showInfo`: it receives the original typed object, invokes `vscode.window.showInformationMessage(message.message)` exactly once with the exact text, discards the returned thenable, and emits no response. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the message, and disposes it. A synchronous native throw still rejects the provider's async message call; disposal claims and suppresses later notifications without canceling or adopting an already invoked native notification. Cached Values, Agent Chat, all emitters, message shapes, browser behavior, and Kusto/SQL lifecycle remain unchanged.

HST-10 qualification passed the final four-file focused ring (146 tests), complete sequential Vitest and the official coverage gate (237 files, 5,844 tests; 48.19% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`). The first full Vitest run had one unrelated `proper-lockfile` release race in Kusto Leave No Trace recovery; that file passed unchanged at 8/8 and the complete rerun passed. Production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, both bundle gates, and the implementation blocker review passed. Final production sizes are 1,912.6 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; existing synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because this was a host-only ownership extraction with unchanged message shapes and emitters. The reviewer returned `VERDICT: NO HST-10 IMPLEMENTATION BLOCKER`.

Cached Values opening remains outside the document-view protocol. `HostCachedValuesOpenApplicationHandler` is the sole provider-route owner of `seeCachedValues`: it receives the original typed object, invokes `vscode.commands.executeCommand('kusto.seeCachedValues')` exactly once with zero arguments, awaits completion, discards the resolved value, propagates the exact rejection, and emits no response. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the message, awaits claimed work, and disposes it. Disposal claims and suppresses later requests without changing an accepted command's settlement. `extension.ts` retains first-launch command registration and `CachedValuesViewerV2` retains viewer singleton/lifecycle ownership; the handler neither imports nor opens the viewer.

HST-11 qualification passed the final four-file focused ring (149 tests), the adjacent first-launch/viewer ring (208 tests), complete sequential Vitest and the official coverage gate (239 files, 5,856 tests; 48.19% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, both bundle gates, and the implementation blocker review passed. Final production sizes are 1,913.0 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,863/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because this was a host-only ownership extraction with unchanged command registration, viewer behavior, message shape, and emitter. The reviewer returned `VERDICT: NO HST-11 IMPLEMENTATION BLOCKER`.

Kusto Workbench Agent Chat opening remains outside the document-view protocol. `HostCopilotAgentOpenApplicationHandler` is the sole provider-route owner of `openCopilotAgent`: it receives the original typed object, invokes `openKustoWorkbenchAgentChat()` exactly once with zero arguments, awaits completion, discards the resolved boolean, propagates the exact rejection, and emits no response. `QueryEditorProvider` only constructs or accepts the handler, synchronously offers the message, awaits claimed work, and disposes it. Disposal claims and suppresses later requests without changing an accepted helper call's settlement. `copilotChatOpenUtils.ts` retains the first native chat-open command, 150 ms mode reapplication, Kusto Workbench mode, optional query/submit shaping, and fallback semantics shared by extension commands, dashboard prompts, and Copilot workflows.

HST-12 qualification passed the final six-file focused/adjacent ring (226 tests), complete sequential Vitest and the official coverage gate (242 files, 5,873 tests; 48.19% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the implementation blocker review passed. Final production sizes are 1,913.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,863 -> 1,864 KB while the 50 KB buffer remained unchanged. Native E2E was not required because this was a host-only ownership extraction with unchanged helper behavior, callers, emitter, and message shape. The reviewer returned `VERDICT: NO HST-12 IMPLEMENTATION BLOCKER`.

Editor cursor status remains outside the document-view protocol. `HostEditorCursorStatusApplicationHandler` is the sole provider-route owner of `editorCursorPositionChanged` and development-only `getEditorCursorStatusSnapshot`, plus panel-hidden and panel-disposal clearing. It owns one unique `queryEditor:<sequence>:` prefix, trimmed `boxId` / `editorKind` owner selection with `editor` fallback, visible-panel admission, exact snapshot request IDs, hidden fallback without an adapter, awaited failure-contained snapshot transport, and idempotent disposal. `QueryEditorProvider` only constructs or accepts the handler, forwards exact typed objects plus panel visibility, awaits claimed snapshot work, and disposes it. `EditorCursorStatusBar` remains the shared status-item, formatting, accessibility, stale-owner, and global-disposal owner; webview emitters and message shapes are unchanged.

HST-13 qualification passed the seven-file focused ring (169 tests), complete sequential Vitest and the official coverage gate (244 files, 5,883 tests; 48.19% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,914.0 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,864 -> 1,865 KB while the 50 KB buffer remained unchanged. Native E2E was not required because status formatting, shared item ownership, emitters, and protocol shapes were unchanged. The reviewer returned `VERDICT: NO HST-13 BLOCKER`.

Editing-preference mutation remains outside the document-view protocol. `HostEditingPreferencesApplicationHandler` is the sole provider-route owner of `setCaretDocsEnabled`, `setAutoTriggerAutocompleteEnabled`, and `setCopilotInlineCompletionsEnabled`. It admits the shared request shape before mapping each discriminator to the existing `STORAGE_KEYS` key, preserves the exact boolean, awaits exactly one `setEditingPreference`, validates the returned authoritative `editingPreferencesData` object, and publishes it through the live tool orchestrator or awaited current-panel fallback. `QueryEditorProvider` only constructs or accepts the handler, forwards each original typed object, awaits claimed work, and disposes it. `editingPreferences.ts` retains application configuration, persistence, safe revision growth, snapshots, and migration; `extension.ts` retains configuration-change refresh; and `HostKustoConnectionsProjectionApplicationHandler` reads the current preference snapshot for `connectionsData`. Disposal suppresses later requests without changing already accepted mutation or publication settlement.

HST-14 qualification recorded the requested red-first real-provider failure at zero fake-handler calls, then passed the unchanged forwarding test. The final focused ring passed 10 files and 553 tests; the adjacent first-launch/preference ring passed 5 files and 42 tests. Complete sequential Vitest and the official coverage gate passed 246 files and 5,893 tests at 48.19% statements. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`) passed. The protocol sender guard now inventories option-backed handler transports and every `postToAllWebviews` call. Final production sizes are 1,914.4 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; existing synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because application scope, configuration listeners, initial projection, emitters, webview application, and message shapes were unchanged. The definitive blocker review returned `VERDICT: NO HST-14 BLOCKER`.

Kusto connection intake remains outside the document-view protocol. `HostKustoConnectionIntakeApplicationHandler` is the sole provider-route owner of `addConnectionsForClusters`, `promptImportConnectionsXml`, and `importConnectionsFromXml`. It owns short-cluster and cluster-plus-authority deduplication, HTTPS/name/authority normalization, sequential additions, the `%LOCALAPPDATA%\Kusto.Explorer` picker default with home fallback, exact picker options, UTF-8 XML delivery, import notifications, and one awaited post-mutation refresh for accepted add/import requests. Rejected discovered-cluster mutation does not refresh, picker traffic never refreshes, accepted work settles across disposal, and later related traffic is suppressed. `ConnectionManager` remains the persistent connection owner, and `HostKustoConnectionsProjectionApplicationHandler` is the injected revisioned refresh effect. Webview XML parsing and all message shapes remain unchanged.

HST-15 qualification recorded the requested red-first real-provider failure at zero fake-handler calls, then passed reference-identical forwarding for all three messages. The final focused and complete host-handler rings passed with one worker, including direct deduplication, sequentiality, picker/read/notification, refresh, rejection, disposal, parser, protocol, and provider-lifecycle coverage. Complete sequential Vitest and the official coverage gate passed 248 files and 5,909 tests at 48.19% statements. Host/webview typechecks, integration compilation, the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,915.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,865 -> 1,866 KB with the 50 KB buffer unchanged. Native E2E was not required because native picker options, response objects, webview XML parsing, and emitters remained unchanged. The blocker review returned `VERDICT: NO HST-15 BLOCKER`.

Kusto single-connection onboarding remains outside the document-view protocol. `HostKustoConnectionOnboardingApplicationHandler` is the sole provider-route owner of `promptAddConnection`, `addConnection`, and `testKustoConnection`. It preserves exact dialog identity; add validation, HTTPS/name/database/authority normalization, explicit-account resolution and fallback, persistent addition, last-selection ordering, revisioned connection refresh, and success/error objects; and draft-test trace, transient-auth preference, interactive non-persisting database discovery, database normalization, and exact success/warning/authentication/error terminals. `ConnectionManager`, `KustoAuthPreferenceService`, `KustoQueryClient`, `ConnectionService` selection storage, `HostKustoConnectionsProjectionApplicationHandler`, and webview controls/message shapes remain separate owners. Accepted work settles across disposal, while later onboarding requests are claimed and suppressed.

HST-16 qualification recorded the required red-first real-provider failure with zero fake-handler calls, then passed unchanged with reference-identical forwarding for all three exact objects and awaited exact settlement. The definitive focused and adjacent ring passed 10 files and 519 tests. Complete sequential Vitest and the official coverage gate passed 250 files and 5,931 tests at 48.18% statements. Host/webview typechecks, integration compilation, the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five existing warnings, and both bundle gates passed. Final production sizes are 1,916.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,866 -> 1,867 KB with the 50 KB buffer unchanged. Native E2E was not required because dialog controls, message shapes, response application, selection storage, auth/client adapters, and tracing remained unchanged. The blocker review returned `VERDICT: NO HST-16 BLOCKER`.

SQL single-connection onboarding remains outside the document-view protocol. `HostSqlConnectionOnboardingApplicationHandler` is the sole provider-route owner of `promptAddSqlConnection` and `addSqlConnection`. It preserves the exact SQL Server address, authentication, username, password, and optional-name prompt sequence; AAD and SQL Login shaping; direct-form trimming/defaults; separate password delivery to `SqlConnectionManager`; committed manager add, `sql.lastConnectionId`, and `sqlConnectionAdded` ordering; exact rejection propagation; synchronous decline; and disposal settlement. `SqlConnectionManager` retains persistent connection and SecretStorage ownership. `HostSqlConnectionsProjectionApplicationHandler` retains revisioned, policy/principal-aware `sqlConnectionsData`; `SqlWorkbenchService`, `SqlEditorLifecycleCoordinator`, webview controls, response application, and message shapes remain unchanged.

HST-17 qualification recorded the requested red-first real-provider failure at zero fake-handler calls, then passed unchanged with reference-identical forwarding and exact settlement. The final focused and adjacent ring passed 6 files and 414 tests. Complete sequential Vitest and the official coverage gate passed 252 files and 5,951 tests at 48.18% statements. Host/webview typechecks, integration compilation, the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,916.7 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,867 -> 1,868 KB with the 50 KB buffer unchanged. Native E2E was not required because prompt controls, emitters, response application, manager/SecretStorage behavior, and message shapes remained unchanged. The blocker review returned `VERDICT: NO HST-17 BLOCKER`.

Query Editor SQL favorites remain outside the document-view protocol. `HostSqlFavoritesApplicationHandler` is the sole provider-route owner of `requestAddSqlFavorite` and `removeSqlFavorite`. It preserves exact prompt defaults and cancellation; sanitized `sql.favorites` reads; trimmed connection plus case-insensitive database identity; in-place replacement with submitted database spelling or append; removal persistence even when unchanged; storage-before-publication ordering; add-only originating `boxId`; awaited `sqlFavoritesData` transport with exact warning containment; synchronous decline; and disposal settlement. `QueryEditorProvider` only constructs or accepts the handler, forwards exact typed objects, exposes its sanitized favorites as an injected capability to `HostSqlConnectionsProjectionApplicationHandler`, and disposes it. Connection Manager SQL-favorites routes, Kusto favorites, webview controls, response application, emitters, and message shapes remain unchanged.

HST-18 qualification recorded the requested zero-call red failure before production changes, then passed unchanged with ordered reference-identical forwarding. The focused handler/provider/helper/lifecycle/protocol/projection ring passed 7 files and 403 tests. Complete sequential Vitest and the official coverage gate passed 254 files and 5,975 tests at 48.23% statements. Host/webview typechecks, integration compilation, the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates passed. Final production sizes are 1,916.6 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because prompt controls, storage/message shapes, emitters, response application, and Connection Manager behavior were unchanged.

Query Editor Kusto favorites remain outside the document-view protocol. `HostKustoFavoritesApplicationHandler` is the sole provider-route owner of `requestAddFavorite`, `removeFavorite`, and `confirmRemoveFavorite`. It owns exact prompt and confirmation UX, exact trimmed connection-ID plus case-insensitive database mutation, account-partition capture and revalidation, migration and active-principal filtering, hidden-principal merge, unresolved-entry fail-closed persistence, cross-panel/listener publication, add-only originating `boxId`, exact failure containment, reactivation, and disposal settlement. `QueryEditorProvider` only constructs or accepts the handler, forwards exact typed objects, exposes its read-only projection to `HostKustoConnectionsProjectionApplicationHandler` and the unchanged schema-inference orchestration, reactivates it with the panel, and disposes it. Connection Manager Kusto-favorites routes remain an independent unchanged workflow over the shared pure helpers; SQL favorites, webview controls, response handling, emitters, and message shapes are unchanged.

HST-19 qualification recorded the requested red-first zero-call failure before production changes, then passed the unchanged provider contract with ordered reference-identical forwarding and exact settlement. The final focused handler/provider/helper/Connection Manager/cleanup/protocol/lifecycle/webview ring passed 9 files and 596 tests. Complete sequential Vitest and the official coverage gate passed 256 files and 6,013 tests at 48.20% statements. Host/webview/browser typechecks, integration compilation, the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five existing warnings, and both bundle gates passed. Final production sizes are 1,918.0 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines and the 50 KB buffer remain unchanged. Native E2E was not required because prompt controls, emitters, response application, message shapes, and Connection Manager routes were unchanged.

Query Editor SQL database discovery remains outside the document-view protocol. `HostSqlDatabaseDiscoveryApplicationHandler` is the sole provider-route owner of `getSqlDatabases` and `refreshSqlDatabases`, including synchronous target adoption, passive versus explicit refresh, monotonic lifecycle request tickets, bounded loading admission, exact terminal completion/reconciliation, canonical physical-target/principal/Leave No Trace admission, protected one-operation STS acquisition and cleanup, sorted results, cache CAS sequencing/fallback, notifications, detail-free protected diagnostics, and disposal. `SqlEditorLifecycleCoordinator` retains targets, generations, tickets, and currentness; `SqlWorkbenchService` retains canonical principal/privacy dispatch; `sqlDatabaseCache.ts` retains cache ownership/CAS; and `SqlConnectionManager`, `SqlQueryClient`, `StsQueryService`, and the protected runtime retain physical adapters. `QueryEditorProvider` only constructs/injects, forwards exact typed objects, supplies transport, and disposes the handler. SQL connection snapshots/favorites, schema, execution, STS language lifecycle, persistence, comparisons, webview emitters/router, and message shapes remain unchanged owners.

HST-20 qualification recorded the required zero-call red failure before production changes, then passed unchanged with reference-identical forwarding, deferred settlement, and exact rejection. The definitive focused SQL owner ring passed 15 files and 405 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 258 files and 6,060 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,928.9 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,868 -> 1,879 KB while the 50 KB buffer remained unchanged. Native E2E was not required because controls, emitters, response routing, and message shapes were unchanged.

Query Editor KQL language requests remain outside the document-view protocol. `HostKqlLanguageRequestApplicationHandler` is the sole provider-route owner of `kqlLanguageRequest`, including shared-protocol admission, exact request-ID and parameter preservation, exact `textDocument/diagnostic` and `kusto/findTableReferences` delegation, typed success and fixed user-safe failure responses, existing fire-and-forget transport behavior, failure logging, and disposal. Malformed recognized requests are claimed and dropped before analysis. `QueryEditorProvider` only constructs/injects, reference-identically forwards exact typed objects, awaits claimed handler work, supplies transport/logger capabilities, and disposes the handler. `KqlLanguageServiceHost` retains selection fallback, cached-schema and auth-partition resolution, and analysis delegation; `KqlLanguageService` retains diagnostics/reference algorithms; `extension.ts` retains independent text-editor diagnostics; and the webview retains request timeout/resolver ownership and response application. Accepted language analysis settles across disposal while late publication/logging and later recognized work are suppressed.

HST-21 qualification recorded the required red-first zero-call failure before production changes, then passed the unchanged real-provider contract. The final focused ring passed 4 files and 159 tests; the expanded handler/provider/analyzer/webview ring passed 6 files and 388 tests. Complete sequential and official coverage Vitest passed 260 files and 6,076 tests at 48.20% statements. The definitive VS Code 1.132.0 extension-host rerun passed 201/201 with `--timeout 5000`; the first run had two unrelated unchanged sidecar timing/lock failures, both passed alone before the clean complete rerun. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, protocol sender inventory, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,929.4 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,879 -> 1,880 KB with the 50 KB buffer unchanged. Native E2E was not required because the analyzer, schema/auth resolution, webview resolver, emitters, and message shapes were unchanged. The reviewer returned `VERDICT: NO HST-21 BLOCKER`.

Query Editor SQL last-selection persistence remains outside the document-view protocol. `HostSqlLastSelectionApplicationHandler` is the sole provider-route owner of `saveSqlLastSelection`: it trims and requires `sqlConnectionId`, awaits `sql.lastConnectionId` before optionally writing `sql.lastDatabase`, treats `database === undefined` as no database write, preserves `database === ''` as an explicit clearing write, propagates exact rejection, emits no response, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler in the constructor slot immediately after KQL language requests, reference-identically offers the typed request, awaits claimed work, and disposes it. `SqlSectionSessionController` retains target transitions and emission, SQL onboarding retains its independent post-add selection write, and `HostSqlConnectionsProjectionApplicationHandler` retains revisioned policy/principal-aware `sqlConnectionsData` reads of both keys.

HST-22 qualification recorded the required red-first two-test failure with zero fake-handler calls and legacy resolved rejection behavior, then passed the unchanged provider contract. The focused ring passed 4 files and 158 tests; the expanded SQL-owner ring passed 10 files and 279 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 262 files and 6,090 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, protocol sender inventory, and both bundle gates passed. Final production sizes are 1,929.8 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,880/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because selection emitters, message shapes, application-state keys, projection reads, SQL lifecycle, and connection behavior were unchanged. The definitive blocker-only reviewer returned `VERDICT: NO HST-22 BLOCKER`.

Copilot development-note mutation correlation remains outside the document-view protocol. `HostDevelopmentNoteMutationApplicationHandler` is the sole owner of outbound `updateDevNotes` request IDs, the five-second deadline, exact unavailable/rejected/failed delivery results, matching `toolResponse` settlement, and disposal settlement. `QueryEditorProvider.updateDevelopmentNotes()` is a thin `CopilotServiceHost` delegate; the provider offers each `toolResponse` to the handler and forwards every unclaimed response unchanged to `toolOrchestrator`. `CopilotService` retains add/supersede/remove shaping, note IDs, tool-result copy, and conversation history. Native notebook mutations validate and enter the existing revisioned document-command client, then construct `toolResponse` only from the exact command's settlement. Sidecar-enabled KQL/SQL companions retain their metadata-free persistence adapter; plain compatibility and read-only hosts reject mutation. Agent tools retain their independent response path.

Development-note mutation traffic has a bounded runtime-validated channel in `src/shared/developmentNoteMutationProtocol.ts`. It is the sole descriptor-safe type and admission owner for exact add, supersede, and remove `updateDevNotes` requests plus only their correlated `toolResponse` subset. Copilot and agent producers construct canonical requests before transport; callers cannot override generated request identity or add alternate fields. `MainWebviewStartupGateway` and the standalone `QueryEditorProvider` panel listener capture raw inbound traffic as their first operation and route only an inert own-data envelope while `runtimeMessageEnvelope.ts` retains original descriptors plus direct prototype identity in private metadata. The mutation protocol derives one frozen complete response admission from that snapshot and caches it by captured-envelope identity so startup reentrancy, provider routing, and both correlation owners consume the same decision. Nested entries, results, and related-section arrays reject exact self-aliases and Node-host proxy/wrapper objects before any nested reflection; browser webview messages have already crossed structured-clone transport and cannot retain proxies. Matching malformed traffic is claimed without waiter lookup, timer cancellation, or settlement; inherited data-property correlation can be identified without invoking getters, accessor-backed traffic is quarantined only while note work is pending, and the generic agent resolver cannot settle an ID marked as development-note work. Every mutation terminal uses the shared typed response constructor, and canonical unmatched tool responses retain generic fallthrough. DOC-7 aggregate/client/URI queue/acknowledged projection/Save ownership, HST-23 request IDs and five-second deadline, agent targeting, note semantics/history, compatibility admission, hidden policy, and show-notes behavior remain separate unchanged owners.

HST-23 qualification recorded the requested red-first one-test failure with exactly zero fake-handler calls, then passed the unchanged provider contract. The focused ring passed 4 files and 156 tests; the expanded Copilot/webview/codec ring passed 16 files and 950 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 264 files and 6,101 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, protocol sender inventory, and both bundle gates passed. Final production sizes are 1,930.5 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,880 -> 1,881 KB while the 50 KB buffer remained unchanged. Native E2E was not required because note semantics, validation, persistence, emitters, response shapes, and tool orchestration were unchanged. The blocker-only reviewer returned `VERDICT: NO HST-23 BLOCKER`.

PRO-22 qualification began with the required malformed matching response failing exactly 1/10 because it consumed the live Copilot waiter before boolean result validation. Pessimistic reviews then retained reds for revoked nested arrays, inherited/accessor generic fallthrough, startup and standalone repeated reflection, request-ID/type traps, top-level and nested descriptor/prototype variation, hidden artifact fields, direct and wrapped identity aliases, and Kusto self/wrapped payloads. Capture-first ingress, descriptors-plus-prototype metadata, one cached frozen response admission, descriptor-preserving artifact admission, the marked-ID generic hard stop, and trap-free nested proxy rejection close those paths. The original focused ring passes 9 files and 469 tests; the definitive adjacent ring passes 21 files and 1,031 tests. Complete sequential and official coverage Vitest pass 338 files and 6,995 tests at 49.48% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, `git diff --check` apart from two line-ending notices, zero root/browser production source maps, and both synchronized bundle gates pass. Final sizes are 2,246.5 KB host, 3,151.0 KB main webview, 327.0 KB Markdown webview, and 19,151.6 KB total `dist/`; synchronized baselines move `2184 -> 2197` and `3095 -> 3101` with the fixed 50 KB buffer unchanged. The production gates correctly rejected the prior and interim limits. Native E2E was not repeated because valid request/response shapes, Development Note semantics and persistence, controls, and rendering remain unchanged while every live producer, ingress, correlation owner, and generic-fallthrough boundary is directly covered. The definitive reviewer returned `VERDICT: NO PRO-22 BLOCKER`.

Copilot inline-completion traffic remains outside the document-view protocol. `src/shared/copilotInlineCompletionProtocol.ts` is the sole descriptor-safe type/runtime-admission owner for webview `requestCopilotInlineCompletion` and host `copilotInlineCompletionResult`. Requests validate before E2E capture or VS Code transport and again in `HostCopilotInlineCompletionApplicationHandler` before SQL owner assertion or model effects. Both host result producers construct typed results and revalidate before transport. The main dispatcher and direct SQL router revalidate before owner admission, resolver lookup, ten-second timer cancellation, or UI effects. Exact request, box, text, flavor, owner, completion text, and optional error values survive without normalization; dense empty completions remain valid. Callable, inherited, non-enumerable, accessor, sparse, operational-iterator, descriptor-trapping, revoked, cyclic, custom-prototype, and malformed recognized traffic fails closed without invoking getters or consuming work.

`HostCopilotInlineCompletionApplicationHandler` remains the sole provider-route/application owner: it distinguishes Kusto from SQL, asserts SQL owner-token admission through the existing lifecycle capability, delegates with the exact issued owner and token, preserves the existing empty SQL fallback, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler, reference-identically offers requests, awaits claimed work, and supplies narrow callbacks. `CopilotService` retains model selection/cache, prompts, context trimming, eight-second cancellation, replacement, streaming, owner revalidation, cleanup, and normal result construction. SQL lifecycle/workbench services retain owner-token, principal, privacy, and dispatch authority. Monaco retains request creation, spinner/application behavior, and the ten-second timeout; `copilot-inline-completion-runtime.ts` is its narrow exact resolver-settlement function. Editing preferences and stale/duplicate behavior are unchanged.

HST-24 qualification recorded the requested red-first two-test failure with zero fake-handler calls before production edits. Implementation validation exposed one extra positional placeholder in the new test harness; after removing it so the structural fake occupied the required slot, the behavior assertions passed. The focused ring passed 4 files and 158 tests; the expanded Copilot/SQL/webview ring passed 13 files and 614 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 266 files and 6,114 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, protocol sender inventory, and both bundle gates passed. Final production sizes are 1,931.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved 1,881 -> 1,882 KB while the 50 KB buffer remained unchanged. Native E2E was not required because model behavior, SQL owner/privacy semantics, webview resolution/application, preferences, emitters, and message shapes were unchanged. The blocker-only reviewer returned `VERDICT: NO HST-24 BLOCKER`.

PRO-20 qualification recorded exactly two intended red failures while 254 neighboring tests passed: malformed non-string `insertText` consumed the live resolver, and an array owner token reached SQL owned-message admission. The final expanded protocol/application/Copilot/SQL/preferences/provider/inventory ring passes 13 files and 729 tests. Complete sequential and official coverage Vitest pass 336 files and 6,940 tests at 49.27% statements; VS Code 1.133.0 extension-host passes 218/218. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics and `git diff --check`, zero production source maps, and both synchronized bundle gates pass. Watcher-free final sizes are 2,225.2 KB host, 3,132.2 KB main webview, 320.7 KB Markdown webview, and 19,105.3 KB total `dist/`. Native E2E was not repeated because valid request/application behavior, model prompts, SQL owner/privacy semantics, the eight/ten-second deadlines, controls, and rendering are unchanged while every migrated boundary is directly covered. Reviews returned `VERDICT: NO PRO-20 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO PRO-20 BLOCKER`.

PRO-21 qualification recorded the required four-failure red while 249 neighbors passed: malformed array publication IDs consumed each current webview stage or the host deadline. Reviewer-driven raw-boundary probes then failed five tests while 285 neighbors passed because generic envelope/startup/provider paths could normalize or route malformed transaction traffic before domain admission. The final expanded protocol/transport/provider/startup/three-host/three-webview/persistence/execution/inventory ring passes 18 files and 938 tests. Complete sequential and official coverage Vitest pass 337 files and 6,957 tests at 49.33% statements; VS Code 1.133.0 extension-host passes 218/218. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean `git diff --check` apart from two existing line-ending notices, zero root/browser production source maps, and both synchronized bundle gates pass. Final sizes are 2,230.9 KB host, 3,137.9 KB main webview, 324.2 KB Markdown webview, and 19,120.2 KB total `dist/`; synchronized baselines move `2176→2181`, `3083→3088`, and `271→275` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid publication shapes, five-second deadlines, ten-second replay entries, privacy locks, controls, and rendering are unchanged while every live ingress, waiter, ledger, startup, and bundled-listener boundary is directly covered. Reviews returned `VERDICT: NO PRO-21 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO PRO-21 BLOCKER`.

Copilot availability admission remains outside the document-view protocol. `HostCopilotAvailabilityApplicationHandler` is the sole provider-route owner of `checkCopilotAvailability`: it receives the original typed object, delegates the exact `boxId` to the existing Copilot capability, awaits settlement, propagates exact rejection, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler in the constructor slot immediately after inline completion, reference-identically offers requests, awaits claimed work, and disposes it. `CopilotService` retains model discovery through `vscode.lm.selectChatModels({ vendor: 'copilot' })`, no-model and failure-to-false behavior, and exact `copilotAvailability` response construction/publication. Global startup plus Kusto and SQL section callers, response routing/application, and message shapes remain unchanged.

HST-25 qualification recorded the requested red-first 2/2 intended failures with zero fake-handler calls before production edits and legacy resolved rejection behavior, then passed the unchanged provider contract. The focused ring passed 4 files and 156 tests; the expanded Copilot/provider/protocol/webview ring passed 8 files and 505 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 268 files and 6,126 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, protocol sender inventory, `git diff --check`, and both bundle gates passed. Final production sizes are 1,931.6 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,882/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because model discovery, caller timing, response routing/application, and message shapes were unchanged. The blocker-only reviewer returned `VERDICT: NO HST-25 BLOCKER`.

Copilot write-query preparation admission remains outside the document-view protocol. `HostCopilotWriteQueryPreparationApplicationHandler` is the sole provider-route owner of `prepareCopilotWriteQuery`: it receives the original typed Kusto or SQL object, delegates that exact object to the existing Copilot capability, awaits settlement, propagates exact rejection, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler in the constructor slot immediately after Copilot availability, reference-identically offers requests, awaits claimed work, and disposes it. `CopilotService` retains box-ID normalization, Copilot model discovery, ID filtering, label sorting, preferred/default and persisted-model selection, Kusto/SQL local-tool selection, unavailable status, and exact `copilotWriteQueryOptions` / `copilotWriteQueryStatus` publication. Kusto and SQL chat-manager callers plus response routing/application and message shapes remain unchanged.

HST-26 qualification recorded the requested red-first 2/2 failures with zero structural-handler calls and legacy resolved rejection behavior, then passed the unchanged provider contract. The focused ring passed 4 files and 157 tests; the expanded Copilot/provider/protocol/webview ring passed 8 files and 503 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 270 files and 6,140 tests at 48.20% statements. The first full run had one unrelated unchanged SQL account-map watcher timing failure; that file passed 17/17 alone and the complete rerun passed unchanged. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, and ESLint with zero errors and five existing warnings passed. Final sizes are 1,932.2 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; intentional host growth moved only the synchronized extension baseline from 1,882 to 1,883 KB with the 50 KB buffer unchanged, and both bundle gates pass. Native E2E was not required because preparation semantics, callers, response application, and message shapes were unchanged. The documentation-aware blocker review returned `VERDICT: NO HST-26 BLOCKER`.

Copilot conversation-clear admission remains outside the document-view protocol. `HostCopilotConversationClearApplicationHandler` is the sole provider-route owner of `clearCopilotConversation`: it receives the original Kusto or SQL object, distinguishes the flavors, validates the complete Kusto request identity before delegation, preserves metadata-free SQL compatibility, awaits the appropriate existing Copilot clear capability, propagates exact rejection, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler in the constructor slot immediately after write-query preparation, reference-identically offers requests, awaits claimed work, and disposes it. `CopilotService.clearCopilotConversation()` retains box normalization and deletion of general-rules, development-note, and conversation-history state; `clearKustoCopilotConversation()` retains exact active-owner matching and owner deletion. Kusto and SQL chat-manager callers, conversation creation/history semantics, start/cancel execution, response routing, and message shapes remain unchanged.

HST-27 qualification recorded the requested red-first 2/2 failures with zero structural-handler calls and the legacy direct Kusto-path rejection, then passed the unchanged provider contract. The focused ownership ring passed 4 files and 160 tests; the expanded Copilot/provider/protocol/webview ring passed 10 files and 671 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 272 files and 6,153 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, response-free sender inventory, and both bundle gates passed. Final sizes are 1,932.8 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,883/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because conversation-state semantics, callers, response routing, and message shapes were unchanged. The documentation-aware blocker review returned `VERDICT: NO HST-27 BLOCKER`.

Copilot history-removal admission remains outside the document-view protocol. `HostCopilotHistoryRemovalApplicationHandler` is the sole provider-route owner of `removeFromCopilotHistory`: it receives the original typed object, delegates the exact unnormalized `boxId` and `entryId` to the existing Copilot capability, awaits settlement, propagates exact rejection, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler in the constructor slot immediately after conversation clear, reference-identically offers requests, awaits claimed work, and disposes it. `CopilotService.removeFromCopilotHistory()` retains box/entry normalization, history lookup, eligible `tool-call` / `general-rules` decisions, and the `removed` mutation. `kw-copilot-chat` retains immediate local removal and the shared Kusto/SQL chat manager retains exact box/entry emission. Conversation creation, clear semantics, first-time UX, start/cancel execution, optimization, response routing, and message shapes remain unchanged.

HST-28 qualification recorded the requested red-first 2/2 failures with zero structural-handler calls and the exact legacy direct-route rejection, then passed the unchanged provider contract. The focused ring passed 2 files and 9 tests; the expanded Copilot/provider/protocol/webview ring passed 10 files and 581 tests after its first run exposed only the line-sensitive reviewed sender allowlist; the final post-documentation/baseline ring passed 11 files and 590 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 274 files and 6,164 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, response-free sender inventory, and both bundle gates passed. Final sizes are 1,933.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; intentional host growth moved only the synchronized extension baseline from 1,883 to 1,884 KB with the 50 KB buffer unchanged. Native E2E was not required because service semantics, shared UI behavior, emitters, response routing, and message shapes were unchanged. The blocker-only reviewer returned `VERDICT: NO HST-28 BLOCKER`.

Copilot Chat first-time behavior remains outside the document-view protocol. `HostCopilotChatFirstTimeApplicationHandler` is the sole provider-route and workflow owner of `copilotChatFirstTimeCheck`: it receives the original typed object, reads `copilotChatFirstTimeDismissed`, writes that key before showing the unchanged modal, delegates the Agent choice to `openKustoWorkbenchAgentChat()`, and publishes exact `proceed`, `openedAgent`, or `dismissed` results with the existing fire-and-forget transport semantics. `QueryEditorProvider` only constructs or accepts the handler immediately after history removal, reference-identically offers requests, awaits claimed work, and disposes it. `STORAGE_KEYS`, the `HostKustoConnectionsProjectionApplicationHandler` initial read, the shared Kusto/SQL chat-manager caller, webview local-flag/result application, and the native Agent Chat helper remain unchanged owners. The complete first-time method and helper import are absent from `CopilotService`.

HST-29 qualification recorded the requested red-first 2/2 intended failures with zero structural-handler calls plus the exact legacy workflow rejection, then passed reference-identical forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct provider state/modal/helper/transport effects. The focused ownership/protocol ring passed 5 files and 74 tests; the expanded complete first-time journey ring passed 12 files and 508 tests with `--maxWorkers=1`. An initial full-suite attempt exposed only a missing HST-29 disposable in the prototype-based provider lifecycle harness and was terminated after the failed callback left its test timers live; that file passed 99/99 after repair, and the clean complete sequential plus official coverage runs passed 276 files and 6,180 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, both bundle gates, and the blocker-only review passed. Final sizes are 1,933.7 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized 1,884/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because modal copy/actions, state ordering, helper behavior, shared caller, response application, and message shapes were preserved and directly covered. The reviewer returned `VERDICT: NO HST-29 BLOCKER`.

Workbench panel tool sessions remain outside the document-view protocol. `HostWorkbenchToolSessionApplicationHandler` is the sole owner of the panel's extension-scoped orchestrator token, initial connection and visible-panel reactivation, guarded disconnect, five-second `requestToolState` correlation, exact `toolStateResponse` settlement, Kusto schema-refresh target projection, SQL connection/ready-owner callbacks, and `toolExecutionStarted` / unclaimed `toolResponse` / `toolStateResponse` routing. `HostDevelopmentNoteMutationApplicationHandler` retains first claim on matching development-note responses. `QueryEditorProvider` only constructs or accepts the handler immediately after the first-time workflow, activates and disposes it, reference-identically offers exact tool messages, and keeps `requestSectionsFromWebview()` as a thin `CopilotServiceHost` delegate. Persistence reconciles comparison owners directly through `SqlEditorLifecycleCoordinator`.

Tool-state snapshot traffic has a bounded runtime-validated channel in `src/shared/toolStateSnapshotProtocol.ts`. It is the sole type and descriptor-safe admission owner for exact `requestToolState` and `toolStateResponse`. Host construction validates before waiter, timer, or transport effects. Capture-first startup and standalone ingress validate the original descriptor snapshot before correlated reentrancy or routing. The webview validates before section serialization or Kusto schema-owner mutation and constructs every normal/loading/error response through the shared owner. The host validates before waiter lookup, timer cancellation, SQL comparison-owner reconciliation, or settlement. Dense section arrays are deeply captured and frozen with bounded depth, value count, container entries, and cumulative strings; malformed or oversized matching traffic is inert. HST-30 connection lifecycle, the exact five-second deadline, purpose/target semantics, schema filtering, Kusto/SQL projection, SQL reconciliation, disposal, and direct provider forwarding remain unchanged owners. Generic `toolResponse`, `toolExecutionStarted`, and every PRO-22 Development Note boundary remain outside this protocol.

HST-30 qualification recorded the required red-first 2/2 failures with zero structural-handler calls while the legacy provider still owned state settlement, then passed constructor-slot identity, reference-identical three-route forwarding, deferred settlement, zero legacy-map/global-orchestrator effects, and the thin state delegate. The final focused handler/provider ring passed 2 files and 11 tests; the expanded orchestrator/schema/SQL/HST-23/webview/protocol/lifecycle ring passed 9 files and 538 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 278 files and 6,192 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, sender inventory, and both bundle gates passed. Final sizes are 1,935.2 KB host and 2,801.9 KB webview; intentional host growth moved the synchronized extension baseline from 1,884 to 1,886 KB with the 50 KB buffer unchanged. Native E2E was not required because orchestrator command semantics, schema acquisition, SQL ownership, webview routing, and message shapes were unchanged. The blocker-only reviewer returned `VERDICT: NO HST-30 BLOCKER`.

PRO-23 qualification began with the required matching `sections: {}` response failing exactly 1/8 because it consumed the live waiter before 4,999 ms. Reviewer hardening preserved direct provider identity and added bounded immutable admission for primitive-heavy snapshots. The final core ring passes 7 files and 406 tests; the definitive expanded HST-30/orchestrator/schema/SQL/PRO-22 ring passes 13 files and 604 tests. Complete sequential and official coverage Vitest pass 339 files and 7,009 tests at 49.49% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics and diff, and both synchronized bundle gates pass. Final sizes are 2,254.0 KB host, 3,158.5 KB main webview, 332.5 KB Markdown webview, and 19,172.1 KB total `dist/`; synchronized baselines move `2197→2204`, `3101→3109`, and `277→283` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid state behavior, controls, schema/SQL algorithms, and rendering are unchanged while every live request, snapshot, startup, transport, and settlement boundary is directly covered. Reviews returned `VERDICT: NO PRO-23 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO PRO-23 BLOCKER`.

Kusto execution-start traffic has a bounded runtime-validated channel in `src/shared/kustoExecutionStartProtocol.ts`. It is the sole type, constructor, and descriptor-safe admission owner for exact `kustoExecutionStarted` and `kustoExecutionStartedAck`. Canonical starts are only Copilot starts without comparison lineage or comparison starts with a complete exact `KustoComparisonRunIdentity`; manual and tool executions remain preclaimed and never enter this handshake. Required lifecycle, target, execution, producer, reservation, query, optional Copilot request, optional expected predecessor, and comparison identities are exact own enumerable data properties. Callable, inherited, non-enumerable, accessor, custom-prototype, proxy, extra-field, malformed, and impossible producer traffic fails closed without invoking getters or consuming work.

Comparison starts validate target and predecessor before artifact effects. A tentative comparison rebind temporarily pins the prior artifact, commits only after the exact section claim succeeds, and otherwise restores the prior binding before releasing its temporary pin. A delayed start with an omitted or mismatched predecessor cannot touch a newer execution or binding. HST-34 retains the exact five-second acknowledgement ledger, transport-failure/disposal/late settlement, manual execution, cancellation, and publication behavior. `KustoExecutionCoordinator`, physical dispatch, PRO-21 publication, Copilot/Optimize identity, and preclaimed manual/tool behavior remain unchanged.

PRO-24 qualification began with the required exact-identity `accepted: 'yes'` acknowledgement deleting the sole live waiter before 4,999 ms; the red failed exactly 1/10 while nine neighboring tests passed. Pessimistic reviews drove trap-free constructor input, producer-dependent start forms, stale/predecessor preflight, real-store transactional comparison-binding retention, captured-proxy rejection, exception-safe binding rollback, and transactional `QueryExecutionController` owner installation before late cancellation. The final core ring passes 7 files and 414 tests; the expanded Kusto execution/Copilot/tool/artifact/persistence/schema ring passes 17 files and 984 tests. Complete sequential and official coverage Vitest pass 340 files and 7,029 tests at 49.53% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics and `git diff --check`, zero root/browser production source maps, and both synchronized bundle gates pass. Final sizes are 2,263.9 KB host, 3,166.9 KB main webview, 335.6 KB Markdown webview, and 19,193.6 KB total `dist/`; synchronized baselines move `2204→2214`, `3109→3117`, and `283→286` with the fixed 50 KB buffer unchanged. Native E2E was not repeated because valid execution, comparison, controls, terminals, artifacts, and rendering remain unchanged while every start construction, startup, ingress, transport, effect, waiter, deadline, controller-owner, and real artifact-binding boundary is directly covered. Reviews returned `VERDICT: NO PRO-24 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO PRO-24 BLOCKER`.

Kusto connection browsing remains outside the document-view protocol. `HostKustoConnectionBrowsingApplicationHandler` is the sole provider-route owner of `getConnections`, `getDatabases`, `refreshDatabases`, and `saveLastSelection`. It preserves passive versus interactive database shaping, exact request/lifecycle fields, service-owned blank discovery behavior, trimmed blank-selection rejection, ordered selection persistence, `undefined` versus empty database values, and best-effort `kusto.refreshTextEditorDiagnostics` only after successful persistence. `QueryEditorProvider` only constructs or accepts the handler immediately after the Workbench tool-session handler, reference-identically offers exact messages, awaits claimed work, injects `HostKustoConnectionsProjectionApplicationHandler` plus `ConnectionService` database/selection capabilities, and disposes it. `ConnectionService` retains cache, authentication, discovery, tracing, recovery, terminals, selection persistence, and schema inference; `extension.ts` retains diagnostics command registration; plain `.kql` compatibility retains file-cache and inferred-selection updates before forwarding.

HST-31 qualification recorded the required red-first 2/2 intended failures with zero structural-handler calls and the legacy resolved injected rejection, then passed reference-identical four-route forwarding, deferred settlement, exact rejection, constructor-slot identity, and zero direct projection/service/diagnostics effects. The focused ring passed 2 files and 14 tests; the expanded owner/caller ring passed 11 files and 315 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 280 files and 6,205 tests at 48.20% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, both bundle gates, and the blocker-only review passed. Final sizes are 1,936.0 KB host and 2,801.9 KB webview; intentional host growth moved the synchronized extension baseline from 1,886 to 1,887 KB with the 50 KB buffer unchanged. Native E2E was not required because request/response shapes, provider projection, `ConnectionService`, compatibility caching, diagnostics registration, and webview behavior were preserved and directly covered. The reviewer returned `VERDICT: NO HST-31 BLOCKER`.

Kusto connection snapshots are owned by `HostKustoConnectionsProjectionApplicationHandler`. Every webview, lifecycle, onboarding, intake, execution, authentication-preference, and public refresh trigger enters one serialized revision tail. The handler reads editing preferences, Copilot first-time state, accounts, current physical connection identities/incarnations, selection, cached databases, and favorites; admits the complete payload under the canonical Kusto Leave No Trace lock; preserves the existing two-attempt correlated `policyRequestId` retry and development isolation path; awaits application-acknowledged publication; and owns disposal. `HostKustoConnectionBrowsingApplicationHandler` remains the exact `getConnections` route owner, while `ConnectionService` retains selection, database cache/discovery, tracing, recovery, terminals, and schema inference. `QueryEditorProvider` only composes capabilities, delegates public refresh, and disposes the projection owner.

HST-40 qualification recorded the required red-first real-provider failure at zero structural projection calls, then passed exact policy-request forwarding and held settlement with no provider state, service, account, connection, favorite, cache, policy-lock, or transport effects. The final focused/adjacent ring passes 20 files and 654 tests with `--maxWorkers=1`; complete sequential Vitest and the official coverage gate pass 306 files and 6,550 tests at 48.22% statements. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, message inventory, diagnostics, `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,155.0 KB host and 3,020.6 KB webview within unchanged synchronized limits. Native E2E was not required because controls, message shapes, application acknowledgement, policy/auth/cache/favorites algorithms, and webview application are unchanged. The definitive documentation-aware review returned `VERDICT: NO HST-40 BLOCKER`.

Copilot query generation and standalone Optimize routing remain outside the document-view protocol. `HostCopilotQueryWorkflowApplicationHandler` is the sole provider-route owner of `startCopilotWriteQuery`, `cancelCopilotWriteQuery`, `prepareOptimizeQuery`, `cancelOptimizeQuery`, and `optimizeQuery`. It reserves SQL owner preflight before asynchronous token assertion, preserves exact owner-change and cancellation terminals plus cancellation-wins races, forwards complete Kusto cancellation identity, invokes cancel capabilities synchronously, awaits start/prepare/run delegation, resolves SQL manager/schema/client lazily at accepted delegation, and owns disposal. `QueryEditorProvider` only constructs or accepts the handler immediately after Kusto connection browsing, reference-identically offers exact messages, awaits claimed settlement, supplies existing capabilities, and disposes it. `CopilotService`, `SqlExecutionBroker`, `SqlEditorLifecycleCoordinator`, SQL managers/adapters, callers, execution, artifacts, persistence, and message shapes remain unchanged owners.

HST-33 red-first coverage failed exactly 2/2 with zero structural-handler calls and legacy resolved rejection behavior, then passed unchanged. The corrected focused ring passed 3 files and 111 tests, including lazy adapter timing; the expanded and final post-documentation owner/caller/router ring passed 13 files and 738 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 283 files and 6,219 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, sender inventory, and both bundle gates passed. Final sizes are 1,934.7 KB host and 2,801.6 KB webview; synchronized 1,887/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because model, prompt, execution, identity, caller, response-application, persistence, artifact, and browser behavior were unchanged and the SQL/Kusto ordering contracts were directly covered. The implementation review returned `VERDICT: NO HST-33 IMPLEMENTATION BLOCKER`; the definitive documentation-aware review returned `VERDICT: NO HST-33 BLOCKER`.

Kusto section execution and application-acknowledged publication remain outside the document-view protocol. `HostKustoSectionExecutionApplicationHandler` is the sole provider-route owner of `kustoSectionOpen`, `kustoSectionTarget`, `kustoSectionClose`, `kustoExecutionStartedAck`, `kustoPublicationAck`, `executeQuery`, and `cancelQuery`. It owns both acknowledgement ledgers and five-second deadlines, exact lifecycle routing, manual execution query shaping and selection ordering, physical target/account/Leave No Trace admission, exact cancellation, late acknowledgement suppression, and disposal settlement. `QueryEditorProvider` only constructs or accepts the handler immediately after the Copilot query workflow, reference-identically offers exact messages, exposes thin `CopilotServiceHost` delegates, and disposes it. `KustoExecutionCoordinator`, `KustoQueryClient`, `ConnectionService`, `ConnectionManager`, and `CopilotService` retain their canonical algorithms and state.

HST-34 red-first coverage failed exactly 2/2 with zero structural-handler calls and legacy resolved rejection behavior, then passed unchanged. The final expanded Kusto execution/Copilot/tool/webview/persistence/schema/protocol ring passed 15 files and 806 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 285 files and 6,231 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, and both bundle gates passed. Final sizes are 1,936.9 KB host and 2,801.6 KB webview; synchronized 1,887/2,752 KB baselines and the 50 KB buffer remain unchanged. An initial root-level browser build command targeted the wrong package and failed before compilation; the unchanged command run from `browser-ext` passed. Native E2E was not required because coordinator/client algorithms, callers, message shapes, terminal application, artifacts, persistence, schema, SQL, comparisons, and browser behavior were unchanged and the exact controller-provider-window journey is directly covered. Reviews returned `VERDICT: NO HST-34 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO HST-34 BLOCKER`.

Cross-engine comparison preparation remains outside the document-view protocol. `HostComparisonPreparationApplicationHandler` is the sole provider-route owner of `sqlComparisonAdmissionAck`, `comparisonBoxEnsured`, and `sqlComparisonRemoved`, the pending request and Kusto comparison-owner maps, the 20-second preparation deadline, all five SQL acknowledgement phases, rollback retry, exact Kusto/SQL target and SQL policy revalidation, removal cancellation, lifecycle rejection, and disposal settlement. `QueryEditorProvider` constructs or accepts the handler immediately after Kusto section execution, reference-identically offers exact messages, and keeps `ensureComparisonBoxInWebview()` as a thin `CopilotServiceHost` delegate. The SQL lifecycle, registry, broker, Kusto coordinator, Copilot service, artifacts, persistence, callers, and message shapes remain unchanged owners.

HST-35 red-first coverage failed exactly 2/2 with zero structural-handler calls and legacy resolved routing, then passed constructor identity, exact forwarding, deferred settlement, exact rejection, and zero direct provider effects. Direct handler coverage passed 14 tests, the focused owner/service/caller/router/artifact/persistence/protocol ring passed 16 files and 832 tests, and the definitive post-review ring passed 19 files and 844 tests with `--maxWorkers=1`. The first full run passed 286/287 files and 6,244/6,245 tests, failing only a stale ownership guard that still expected the engine choice in the provider; the pre-review full and coverage runs passed 287 files and 6,245 tests at 48.19% statements. The documentation-aware review then found an inherited finalized-removal race. Pre-finalization removal still follows exact acknowledged rollback and restores reusable state; after an atomically accepted finalization, the host terminally settles cancellation while `sql-comparison-admission-runtime.ts` lets the webview ledger clear its request/box correlation, persistence snapshot, admission attribute, and read-only state before detachment. Late completion is rejected and retry admission remains available. The definitive post-fix full and official coverage runs passed 287 files and 6,249 tests at 48.21% statements. VS Code 1.132.0 extension-host passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, sender inventory, and both bundle gates passed. Task-owned files had clean diagnostics; VS Code still reports 73 pre-existing ambient `Window` declaration diagnostics in `section-factory.ts` even though strict webview TypeScript passes. `git diff --check` found no whitespace errors and reported only existing LF-to-CRLF conversion warnings. The first production package failed only because the intentional 1.6 KB host growth exceeded the prior limit; synchronized host baselines moved 1,887 -> 1,889 KB with the 50 KB buffer unchanged. Final sizes are 1,938.5 KB host and 2,802.0 KB webview. Native E2E was not required because external comparison semantics, canonical services, artifacts, persistence, callers, and message shapes remain unchanged and the complete host/webview transaction is covered directly. The implementation review returned `VERDICT: NO HST-35 IMPLEMENTATION BLOCKER`; the first documentation-aware review returned `VERDICT: HST-35 BLOCKED`, its two-sided removal-settlement blocker was fixed, and the definitive review returned `VERDICT: NO HST-35 BLOCKER`.

Manual SQL section execution remains outside the document-view protocol. `HostSqlSectionExecutionApplicationHandler` is the sole provider-route owner of `executeSqlQuery` and `cancelSqlQuery`, including synchronous preflight, exact owner-token and Leave No Trace mode admission, tool-owner validation, query-mode shaping, broker promotion and lease acquisition, physical start, ordinary and protected result/error/cancellation publication, detail-free protected logging, and exact preflight/admission/lease cleanup. `QueryEditorProvider` constructs or accepts it immediately after comparison preparation, reference-identically offers exact messages, supplies narrow lifecycle/broker/workbench/client/transport capabilities, and disposes it. The provider has no SQL execution discriminator, private execution method, query-mode or cancellation import, manual terminal helper, or execution cleanup decision; at HST-36 closure its live switch inventory was exactly 13.

HST-36 was red first at 2/2 intended failures with zero structural-handler calls and legacy resolved routing. Direct handler coverage passes 23 tests; the definitive focused owner/service/caller/router/artifact/persistence/protocol ring passes 28 files and 1,053 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 289 files and 6,275 tests at 48.21% statements. The first full run failed only a stale terminal-owner guard, which passed 8/8 after naming the new handler; the complete rerun passed. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, message sender inventory, and both bundle gates pass. The first production package rejected the intentional 1.0 KB host growth at the old limit; synchronized host baselines moved 1,889 -> 1,890 KB with the 50 KB buffer unchanged. Final sizes are 1,939.5 KB host and 2,802.0 KB webview. The first VS Code 1.132.0 extension-host run reached 200/201 on an unchanged bounded-close timeout; that case passed alone in 912 ms and the complete unchanged rerun passed 201/201 with `--timeout 5000`. Reviews returned `VERDICT: NO HST-36 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO HST-36 BLOCKER`. Native E2E was not required because controls, STS/client algorithms, webview terminal application, artifacts, persistence, callers, browser behavior, and message shapes remain unchanged and the complete host workflow is directly covered.

Comparison-summary presentation is webview-local. `displayComparisonSummary()` computes from the exact immutable comparison artifact and its `comparison-source` lineage, renders the banner and diff link, and updates local controls without sending a host summary. Target retirement removes the banner locally while Kusto/SQL owner cancellation and artifact teardown continue through their existing authorities. The retired host bridge has no protocol message, provider map/waiter/route, `CopilotServiceHost` member, or SQL lifecycle callback.

HST-32 red-first coverage proved the two legacy sends were the only failing effects while 74 neighboring tests passed; the same pair then passed 76/76 after retirement. The focused owner/protocol/lifecycle ring passed 9 files and 505 tests, and the final expanded comparison-preservation ring passed 13 files and 846 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest passed 281 files and 6,206 tests at 48.19% statements. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, sender inventory, and both bundle gates passed. Final sizes are 1,933.8 KB host and 2,801.6 KB webview; synchronized 1,887/2,752 KB baselines and the 50 KB buffer remain unchanged. Native E2E was not required because visible local rendering and teardown were directly covered while execution, artifacts, lineage, persistence, and browser composition remained unchanged. The implementation review returned `VERDICT: NO HST-32 IMPLEMENTATION BLOCKER`, and the definitive documentation-aware review returned `VERDICT: NO HST-32 BLOCKER`.

## Window Bridges (Legacy)

Webview modules communicate via window globals declared in `window-bridges.d.ts`. This is a legacy pattern from when modules were loaded as separate `<script>` tags. The codebase is being progressively migrated to ES module imports between modules.

## File Formats

### `.kqlx` / `.sqlx` / `.mdx` (Notebooks)

JSON format with a `sections` array. Each section has a `type` discriminator and type-specific fields. Type definitions live in `kqlxFormat.ts`; `kqlxOverlay.ts` owns the lossless overlay codec. `.kqlx` files can contain any mix of section types (query, sql, chart, markdown, etc.). `.sqlx` files use the same JSON schema and allow SQL plus chart, transformation, Python, URL, HTML, and markdown sections, but not Kusto query sections. `.mdx` files are markdown-oriented notebooks.

`src/shared/documentSectionCapabilities.ts` is the sole transport-neutral authority for known document/section compatibility, canonical aliases, visible add kinds, and default section kinds:

| Document kind | Persisted known section kinds | User-addable kinds |
| ------------- | ----------------------------- | ------------------ |
| `.kqlx` | `query`, `sql`, `chart`, `transformation`, `markdown`, `python`, `url`, `html`, `devnotes` | All except hidden `devnotes` |
| `.sqlx` | `sql`, `chart`, `transformation`, `markdown`, `python`, `url`, `html`, `devnotes` | All except hidden `devnotes` |
| `.mdx` | `markdown`, `url`, `transformation`, `devnotes` | All except hidden `devnotes` |

Legacy `copilotQuery` canonicalizes to `query` and is accepted only where `query` is accepted. Unknown future section kinds are deliberately not classified as incompatible: they remain opaque COD-1 data and retain their relative order. A known incompatible section is never filtered. Parsing reports the document kind, section index/ID, and canonical type, and the custom editor opens script-disabled and read-only without installing a persistence channel. The notebook provider also requires the root `kind` to match the URI-derived `.kqlx`/`.sqlx`/`.mdx` kind, so parsing and projection cannot select different matrix rows.

Host `allowedSectionKinds`, defaults, and KQL/SQL/Markdown compatibility upgrade destinations derive from the shared matrix. `core/document-capabilities.ts` intersects those host projections with the same matrix before add buttons, insert zones, default creation, restored-state validation, or tool dispatch. `createSectionWithCapabilities()` in `core/persistence.ts` is the sole non-restore creation gateway: UI creation may request an explicit compatibility upgrade, while tools, Copilot auto-create/Insert, and comparison preparation reject before mutation when the document or host does not admit the kind. SQL optimization comparisons persist and restore as `sql` sections carrying `comparisonSourceBoxId`, so SQLX never contains a Kusto query section. Removing a comparison unregisters its webview mapping, clears the source backlink, publishes `sqlComparisonRemoved`, and defensively retires its own host owner before a same-source comparison can be recreated. The browser extension bundles `browser-ext/src/viewer-document.ts`, which delegates structural and capability validation to the host's pure `parseKqlxText()`, requires the first companion section to link to the exact opened raw file, fails visibly for invalid or unrelated companions, and preserves opaque future sections; its production build runs a strict browser TypeScript check before esbuild.

Parsing validates the root kind/version and document-kind capability row, requires a section array, rejects malformed section records, duplicate non-empty IDs, invalid link values, multiple linked-query owners, malformed known primitive/container values, and missing required nested identities. Unknown sections and explicitly opaque payloads remain forward-compatible data. The codec preserves unknown root/state fields, extension fields on known sections and nested known objects, opaque unknown sections, and their relative order. Ordinary persistence overlays edited known state onto the exact current file revision; known field omission remains authoritative while future fields survive. Nested arrays distinguish stable identities (artifacts, lineage, development notes) from explicitly renameable configuration items, and fail closed when future metadata cannot be correlated safely. Root/state/section extension comparison and durable persistence equality are JSON-semantic and hostile-key safe, separate from semantic UI-default normalization.

Legacy id-less sections receive stable opaque IDs per raw document revision before webview projection, and the same projected baseline is used for persistence. Every host projection carries a source generation and becomes active only after the webview acknowledges successful section materialization; persistence echoes that generation. KQL/SQL compatibility sidecars deterministically materialize IDs after overlay, enrich edits from the exact locked baseline before privacy sanitation, and use CAS/retry for writes, repair, and recovery. Their accepted physical identity is captured during load or returned by the locked creation/adoption publication itself, then retained through later draft writes. Privacy sanitation removes explicitly modeled row-bearing fields without granting authority over future extensions. Linked queries support one plain `.kql`/`.csl` target, use exact structural ownership, reject self/non-query targets, fence asynchronous loads by document generation, and reject stale or previously missing linked-file writes until reload establishes an exact baseline. Native linked Save holds one serialized content lease across buffer mutation and durable publication, rejects any other dirty open document that aliases the target's physical file, and treats buffer/disk rollback as independent CAS surfaces so a newer direct edit is never overwritten.

Native notebook Markdown, URL, Python, Chart, Transformation, HTML, and hidden Development Notes persisted state are host-owned. `markdownSectionDefinition.ts`, `urlSectionDefinition.ts`, `pythonSectionDefinition.ts`, `chartSectionDefinition.ts`, `transformationSectionDefinition.ts`, `htmlSectionDefinition.ts`, and `developmentNoteSectionDefinition.ts` own validation and patch semantics, while the transitional `MarkdownDocumentAggregate` owns their ordered application state, document revision, per-section revisions, and add/patch/remove transitions together. The webview `markdown-document-client.ts` uses one optimistic full-projection ledger and command queue for all seven persisted kinds. The six visual Lit components remain views; Development Notes have no DOM component. Native persistence reads acknowledged host projections, including hidden note sections, and never consults component serialization or a passthrough note array. Plain `.md` compatibility and metadata-free browser behavior retain existing serializers; sidecar-enabled KQL/SQL companions intentionally retain a narrow metadata-free Development Notes adapter.

Each normalized notebook URI has one physical document-command queue shared before and after projection acknowledgement, across transient panels, owner replacement, Save, rollback, close, and reopen. A command reserves that queue before any asynchronous source read. Every successful result must match the complete predicted post-command projection, including all owned sections, revisions, and mixed section order; malformed or inconsistent results reload without adoption. Rejected commands may adopt a complete authoritative projection, reconcile all six visual view kinds plus hidden Development Notes, and finish with one stable whole-container ordering pass. Development-note callers settle from their exact pending command rather than the global queue-empty Save barrier, so a later rejected command cannot retroactively fail an earlier committed note. A projection candidate becomes canonical only after a live reload request, exact source/generation checks, and application acknowledgement. Every valid command ID receives one terminal result. Native Save converts the webview barrier into a queue lease held through the matching non-canonical commit; rejected barriers abort, overlapping Saves serialize, canonical result-restoration Saves cannot release user leases, and disposal settles accepted leases. Live panel ownership is distinct from retained aggregate state: canonical-panel disposal reprojects a survivor without rewinding revisions, while text-document cleanup deletes retained owner and queue only after the tail stabilizes and an atomic final check confirms that the URI did not reopen. Final serialization overlays the aggregate snapshot onto the exact current codec baseline, preserving COD-1 future data and order.

DOC-7 qualification established the requested red first: one new aggregate regression failed while 11 neighbors passed because `devnotes_owner` returned `missing-section`. The final focused ownership/protocol/client/persistence/HST-23/Copilot/tool/codec/capability ring passes 15 files and 974 tests. Complete sequential Vitest and the official coverage gate pass 337 files and 6,970 tests at 49.48% statements. VS Code 1.133.0 extension-host passes 218/218. The real provider lifecycle applies an authoritative note replacement at section revision 1, then proves a stale final adapter note cannot overwrite it across panel recreation and lossless native Save; exact order, future section metadata, opaque data, and replacement-ID metadata isolation survive. Reviewer findings for command-scoped settlement, sidecar-enabled KQL/SQL compatibility, prefix-inferred hidden ownership during reorder, and normalized companion hidden IDs were fixed and retained as regressions. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics and `git diff --check`, and both synchronized bundle gates pass. Final sizes are 2,233.5 KB host, 3,144.6 KB main webview, 326.7 KB Markdown webview, and 19,132.0 KB total `dist/`; synchronized baselines move `2181→2184`, `3088→3095`, and `275→277` with the fixed 50 KB buffer unchanged. Native UI E2E was not repeated because Development Notes remain hidden with no new control or rendering behavior, while the real provider lifecycle directly covers mutation, recreation, Save, and lossless persistence.

PRO-1 qualification passed the seven-file focused Vitest ring (375 tests), complete sequential Vitest and sequential coverage (217 files, 5,635 tests, 47.78% statements), both TypeScript projects, integration compilation, the complete VS Code 1.131.0 extension-host suite (191/191 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates. Provider lifecycle coverage passed 16/16 and the compatibility sidecar matrix passed 117/117. Native run `20260803-213448` passed all three `host-owned-markdown-lifecycle` scenarios. Reviewed `1280x1000` screenshots, JSON artifacts, and logs proved distinct panel-session UUIDs, one initial projection per session, exactly one stamped command/result per session, no terminal or mutation from a prior-session command, an accepted stamped Save barrier, clean persisted state, and unchanged DOC-3/DOC-4 lossless/runtime contracts. The blocker-only reviewer returned `VERDICT: CLOSE PRO-1`. Final production sizes are 1,872.6 KB for `extension.js` and 2,752.8 KB for `webview.bundle.js`; synchronized baselines are 1,823 KB and 2,703 KB with the 50 KB buffer unchanged.

PRO-2 qualification passes the final 11-file focused ring (414 tests), direct gateway suite (12 tests), complete compatibility sidecar matrix (128 tests), native KQLX lifecycle suite (23 tests), full sequential and official coverage Vitest (291 files, 6,293 tests; 48.25% statements), and the complete VS Code 1.132.0 extension-host suite (209/209 with `--timeout 5000`). Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, protocol inventory, diagnostics, and both bundle gates pass. Final production sizes are 1,946.2 KB host and 2,802.1 KB webview; synchronized baselines are 1,897/2,753 KB with the 50 KB buffer unchanged. The definitive documentation-aware review returned `VERDICT: NO PRO-2 BLOCKER`.

URL fetch identity, resolved redirect URL, downloaded content, loading/error state, CSV artifacts, iframe/DOMPurify rendering, debounce, and autosizing remain view/runtime effects. Only the authored URL and persisted presentation fields enter the aggregate. Same-document projections retain unchanged URL elements so artifacts, tables, iframes, observers, and pending debounce work survive synchronous DOM moves; teardown is deferred until genuine removal. Detached or replaced instances cannot emit persistence commands because the current connected element must own the exact section ID.

Python Monaco instances and local-code policy remain view/runtime adapter effects. `HostPythonExecutionApplicationHandler` owns native execution dispatch, interpreter fallback, child-process/stdin/stdout/stderr acquisition, bounded output, timeout/kill behavior, and terminal publication. Only authored `name`, `code`, `output`, `expanded`, and `editorHeightPx` enter the aggregate. `pythonExecutionProtocol.ts` validates the complete three-message channel, while `python-execution-admission.ts` fences the box-ID-only execution contract to one outstanding request per section, retaining exact component identity and code. Projection, removal, replacement, disconnect, and document invalidation retire publication authority while preserving a tombstone until the canonical terminal is consumed; malformed traffic has no effect and inactive terminals settle without publishing output. The handler publishes one bounded timeout terminal even if process termination or close never settles. Same-document projections retain unchanged Python elements and Monaco instances, genuine removal disposes them, and detached/replaced instances cannot persist stale code or output.

HST-3 qualification passed the final 10-file focused ring (502 tests), complete sequential Vitest and coverage (224 files, 5,728 tests; 48.18% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, `git diff --check`, and both bundle gates passed. Final production sizes are 1,908.2 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline is 1,859 KB with the 50 KB buffer unchanged. Native run `20260805-194937` executed built-in Python through the real Run button, rendered exact `HST3:24`, saved/reopened raw Windows stdout, and proved normalized output plus `dirty:false`; its foreground-valid screenshot and two JSON artifacts were reviewed clean. The definitive blocker-only review returned `VERDICT: NO HST-3 BLOCKER`.

HST-4 qualification passed the focused eight-file HST ring (128 tests), the provider lifecycle suite (101 tests), complete sequential Vitest and the official coverage gate (225 files, 5,736 tests; 48.18% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.132.0 extension-host suite (201/201 with `--timeout 5000`). A preceding npm-wrapper run incorrectly passed `5000` as a positional argument and hit the known two-second close-bound test at 200/201; the direct five-second CLI run passed unchanged. Production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, both bundle gates, and the blocker review passed. Final production sizes are 1,908.5 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; existing baselines and the 50 KB buffer remain unchanged. Native run `20260805-211100` passed 1/1 against the reviewed build, driving the real imported-table Save button and Windows picker, writing the exact 36-byte UTF-8 file `Name,City\nJosé,東京\nZoë,München`, and showing the saved notification with Open File / Show in Folder in a foreground-valid `1280x1000` screenshot. Two preceding harness attempts established the correct product output but failed on literal Unicode-escape substring handling and the documented controller notification-capture limitation; the final scenario uses exact host-read bytes plus visual notification evidence. The definitive blocker-only review returned `VERDICT: NO HST-4 BLOCKER`.

HST-5 qualification passed the expanded 12-file HST/share ring (454 tests), complete sequential Vitest and the official coverage gate (227 files, 5,749 tests; 48.18% statements), host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, and both bundle gates. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000` on unchanged rerun; the preceding run reached 199/201 after one unrelated sidecar repair timeout and its cascading setup failure, and both cases passed alone before the clean rerun. Final production sizes are 1,909.1 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved by 1 KB to 1,860 KB while the 50 KB buffer remained unchanged. Definitive native run `20260805-231107` passed 2/2 `share-result-artifacts` scenarios against the reviewed build: an exact extension-host JSON artifact proved clipboard artifact B while denied/revoked rows were absent, and the real host clipboard contained the expected gzip/base64 ADX URL prefix. Its foreground-valid `1280x1000` screenshot visibly showed the exact ADX-link success toast. A preceding identity-checklist run stopped before clipboard on an unrelated Connection Manager Favorites assertion; a later controller notification assertion missed the visibly present toast after exact clipboard success. The final isolated scenario uses exact clipboard bytes plus visual notification evidence. The definitive blocker-only review returned `VERDICT: NO HST-5 BLOCKER`.

HST-6 qualification passed the definitive seven-file URL ring (223 tests). Complete sequential Vitest passed 229 files and 5,767 tests before the review-added disposal regression; the official sequential coverage gate then passed the final 229 files and 5,768 tests at 48.18% statements. Host/webview/browser typechecks, integration compilation, production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, and both bundle gates passed. The complete VS Code 1.132.0 extension-host suite passed 201/201 with `--timeout 5000`. Final production sizes are 1,910.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved by 1 KB to 1,861 KB while the 50 KB buffer remained unchanged. The blocker-only reviewer returned `VERDICT: NO HST-6 BLOCKER`; its stale-ledger and abort-rejection test follow-ups were addressed. Native E2E was not required for this host-only ownership extraction because message shapes and the `kw-url-section` runtime remained unchanged and are covered by the focused component ring.

Chart ECharts instances, immutable result-artifact bindings, source refresh/rebind behavior, rendering, zoom, tooltips, column memory, and dependency scheduling remain adapter effects. Only the persisted Chart configuration enters the aggregate. An unchanged authoritative projection retains the exact Chart element, global renderer state, ECharts instance, zoom state, and artifact binding without rendering or emitting a command. A genuine source change synchronously transitions the artifact binding even while collapsed, while rendering remains visibility-dependent. Renderer-only automatic layout does not persist defaults. Genuine removal disposes ECharts and releases the binding; detached/replaced same-ID elements and delayed Lit updates cannot command, render into, or remove the current owner. Transformation column rename propagation identifies Charts by component type rather than ID prefix and commits the resulting configuration through the same host command client.

Transformation primary and join-right immutable artifact bindings, expression evaluation, derived publication and lineage, dependency scheduling, refresh cascades, table/CSV rendering, and runtime warnings remain adapter effects. Only persisted Transformation configuration enters the aggregate. Equal projections retain the exact element, both input bindings, derived lineage, and rendered state without commands or publication. Configuration-only changes recompute from the existing pins; a genuine source/type change synchronously rebinds only the affected role and republishes even while collapsed. The established dependent-refresh boundary explicitly rebinds current upstream revisions. Renderer-driven auto-fit remains local and cannot hitchhike into a later host patch; explicit fit/resize heights persist and restore. Genuine removal clears output and releases both inputs, while detached/replaced same-ID instances cannot command, publish, unbind, or remove the current owner. Downstream rename propagation identifies Transformations by component type and commits arbitrary IDs through the host client.

HTML Monaco instances, sandboxed iframe/scripts, provenance parsing, data-bridge serialization, immutable fact binding, slicers, validation, Power BI export, and Fabric REST remain adapter effects. Only persisted HTML configuration enters the aggregate. Equal projections retain the exact element, Monaco editor, iframe, and fact binding with no rerender or command; a genuine source/code change retires stale dashboard workflows and rebinds through the established artifact refresh path. Native tools, rename/collapse/resize, and Save use host commands/projections; metadata-free browser/legacy serialization remains intentional.

Power BI create/update is a separate external transaction. One captured Microsoft account is reused for Fabric operations, refresh schedule, and initial refresh. Final Leave No Trace admission is held through the first mutating request. New publishes use UUID staging names, exact paginated ID recovery, and report-before-model compensation. Returned model/report IDs enter the document through an exact one-field HTML command. The host captures the authoritative pre-apply metadata and accepts compensation only as that exact object, or `null` when none existed, at the exact document/section revisions. The component provisionalizes the new tuple so concurrent rename/collapse/resize patches cannot overwrite it with stale metadata, then reconciles from the acknowledged host projection on failed apply/compensation. Cleanup runs in the same URI queue and deletes newly created items only after a queue-stable source proves that no HTML section references the exact workspace/model/report tuple.

Definitive DOC-6 qualification passed the final 20-file focused ring (661 tests), complete sequential Vitest and coverage (220 files, 5,703 tests, 48.18% statements), all TypeScript projects, integration compilation, and the complete VS Code 1.131.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds passed; ESLint reported zero errors and five pre-existing warnings. Final production sizes are 1,905.5 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; synchronized baselines are 1,856 KB and 2,752 KB with the 50 KB buffer unchanged, and both gates pass. Definitive native run `20260805-043534` passed all five `host-owned-markdown-lifecycle` scenarios after one documented harness file-not-found rerun. Five fresh foreground `1280x1000` screenshots and all eight DOC-6 artifacts were reviewed: exact successful/stale terminals, equal editor/iframe/binding identity with zero rerender, dirty host state, accepted Save barrier, zero serializer/commands, exact recreation, clean lossless order, nested future publish/notice fields, and opaque preservation. Focused publish tests additionally prove exact old-metadata restoration and the concurrent rename-during-apply race. Logs contained only existing Git/CSP/Mermaid noise. The post-fix blocker reviews returned `VERDICT: NO DOC-6 BLOCKER`; the documented closure decision is `VERDICT: CLOSE DOC-6`. No live authenticated Fabric tenant run was performed.

Definitive DOC-5 qualification passed the final 19-file focused ring (729 tests), complete sequential Vitest and coverage (217 files, 5,657 tests, 48.11% statements), both TypeScript projects, integration compilation, the complete VS Code 1.131.0 extension-host suite (200/200 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates. Provider lifecycle passed 20/20 and compatibility sidecars passed 124/124, including pending-authority pre-lease rejection, retry projection handshakes, privacy durability, canonical restoration, overlapping Saves, and dirty physical-alias rejection. Native run `20260804-144628` passed all four `host-owned-markdown-lifecycle` scenarios. Four reviewed foreground `1280x1000` screenshots were clean; all 25 reviewed strict JSON artifacts proved exact successful/stale terminals, accepted stamped Saves, zero Transformation serializer/equal-projection commands, A pins retained through configuration while source currents advanced to B, explicit refresh rebinding both roles to B, exact recreated height/configuration, lossless nested future metadata/order, distinct panel sessions, and `dirty:false`. Logs contained only existing Git/CSP/Mermaid noise. The definitive blocker-only reviewer returned `VERDICT: CLOSE DOC-5`. Final production sizes are 1,886.5 KB for `extension.js` and 2,773.4 KB for `webview.bundle.js`; synchronized baselines are 1,837 KB and 2,724 KB with the 50 KB buffer unchanged.

Definitive DOC-2 qualification passed the final 11-file focused ring (513 tests), complete sequential Vitest (215 files, 5,592 tests), both TypeScript projects, integration compilation, the complete VS Code 1.131.0 extension-host suite (188/188 with `--timeout 5000`), production extension and browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates. The provider lifecycle matrix passed 13/13, including reversed session source reads. Native run `20260803-104840` passed `default/host-owned-markdown-lifecycle`; its reviewed screenshot showed restored Markdown and collapsed URL sections without errors, and both reviewed JSON artifacts proved zero component serialization, stale-command rejection, exact `markdown_doc1|url_doc1|future_doc1` order, future root/state/known-section/opaque preservation, and `dirty:false`. The definitive pessimistic review returned `VERDICT: CLOSE DOC-2`. Final production sizes are 1,857.1 KB for `extension.js` and 2,724.2 KB for `webview.bundle.js`; synchronized baselines are 1,808 KB and 2,675 KB with the 50 KB buffer unchanged.

Definitive DOC-3 qualification passed the final 12-file focused ring (539 tests), complete sequential Vitest (216 files, 5,615 tests), both TypeScript projects, integration compilation, the complete VS Code 1.131.0 extension-host suite (189/189 with `--timeout 5000`), production extension and browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates. The provider lifecycle matrix passed 14/14, including Python-only Save before projection acknowledgement. Native run `20260803-124847` passed the extended `default/host-owned-markdown-lifecycle` scenario. Its reviewed screenshot showed restored `Host owned` Markdown plus collapsed URL and `Host owned Python` sections without error UI; three reviewed JSON artifacts proved zero Markdown/URL/Python serializer calls, stale-command rejection, exact `markdown_doc1|url_doc1|python_doc1|future_doc1` order, all five Python fields, future root/state/known-section/opaque preservation, and `dirty:false`. The definitive post-qualification review returned `VERDICT: CLOSE DOC-3`. Final production sizes are 1,859.0 KB for `extension.js` and 2,731.4 KB for `webview.bundle.js`; synchronized baselines are 1,810 KB and 2,682 KB with the 50 KB buffer unchanged.

DOC-4 qualification passed the expanded 17-file focused ring (744 tests), complete sequential Vitest (216 files, 5,626 tests), both TypeScript projects, integration compilation, the complete VS Code 1.131.0 extension-host suite (190/190 with `--timeout 5000`), production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates. The provider lifecycle matrix passed 15/15, including Chart-only Save before projection acknowledgement and a command arriving after a barrier response but before lease reservation. Native run `20260803-194348` passed both `default/host-owned-markdown-lifecycle` scenarios with a clean extension-host log. Its reviewed screenshots showed restored Markdown/URL/Python and the four collapsed owned sections including arbitrary-ID `Host owned Chart`, with no error UI or dirty indicator. Eleven reviewed JSON artifacts proved four exact successful Chart terminals with no reload, an explicitly accepted Save barrier, zero Chart serializer calls, zero equal-projection commands, retained runtime identity, stale-command rejection, exact `markdown_doc4|url_doc4|python_doc4|sales-chart|future_doc4` order, complete nested Chart configuration, future root/state/known-section/nested-setting/opaque preservation, and `dirty:false` immediately after Save and after reopen. Full-projection comparison follows JSON transport semantics, so omitted and `undefined` object fields cannot cause a false reload. Final production sizes are 1,863.5 KB for `extension.js` and 2,744.2 KB for `webview.bundle.js`; intentional DOC-4 growth moved synchronized baselines from 1,810 to 1,814 KB and from 2,682 to 2,695 KB with the 50 KB buffer unchanged.

The webview sets `documentRuntimeActive` to false before buffered host messages can drain and keeps it false throughout document replacement. It becomes true only after capability validation and section materialization succeed. Before that point, publications, terminals, tools, notes, and state requests fail with an explicit loading response. A malformed replacement retires Kusto and SQL lifecycle owners, purges result and owner maps, resolves pending comparison work only through rollback or forward completion, and leaves any retained last-good DOM inert and non-executable.

Definitive COD-2 qualification passed the final 10-file blocker ring (584 tests), complete sequential Vitest (211 files, 5,543 tests), both host/webview TypeScript projects, strict browser typechecking, integration compilation, the complete VS Code 1.131.0 extension-host suite (174/174), production extension and browser builds, ESLint with zero errors and five pre-existing warnings, and both bundle gates. Compatibility sidecars validate against their destination kind before parse, hydration, repair, build, adoption, or write; malformed SQL `linkedQueryPath`, multiple linked owners, and other invalid KQL/SQL companions project read-only without prompts or byte changes. Empty host capabilities remain empty, nonempty visual/hidden/opaque documents never receive a default section, all browser providers and the production `<all_urls>` manifest route MDX/raw URLs, browser validation reuses the host parser and exact companion linkage, and legacy aliases canonicalize through the shared owner across restore, sanitation, tools, and preloaders. All non-restore creation goes through the capability-aware owner; SQL optimization comparisons persist as SQL and retire exact owners on removal. Native run `20260802-103341` passed all four `default/document-capabilities` scenarios; three reviewed MDX screenshots and exact DOM/file assertions proved actionable byte-preserving rejection, exact controls, opaque-only behavior, future-data preservation, and real host SQL comparison save/reopen/nested-reject/remove/recreate/reopen with one fresh valid comparison and lineage. The definitive pessimistic review returned `VERDICT: CLOSE COD-2`. Final production sizes are 1,835.2 KB for `extension.js` and 2,704.9 KB for `webview.bundle.js`; synchronized baselines are 1,786 KB and 2,655 KB with the existing 50 KB buffer.

### Section Types

| Type | Component | Purpose |
| ---- | --------- | ------- |
| `query` | `kw-query-section` | KQL query editor with execution and results |
| `markdown` | `kw-markdown-section` | Rich text / documentation |
| `python` | `kw-python-section` | Python code cells |
| `url` | `kw-url-section` | Embedded web content |
| `chart` | `kw-chart-section` | Visualization configs (ECharts) |
| `transformation` | `kw-transformation-section` | Data transformation expressions |
| `html` | `kw-html-section` | HTML + JS dashboard sections with preview, slicers, data bindings, and Power BI export/publish |
| `sql` | `kw-sql-section` | T-SQL query cells (SQL Server / Azure SQL) |

> A legacy `copilotQuery` type also exists for backward compatibility. It is treated as `query` at load time and should not be used in new code.

### Key Types

```typescript
KqlxSectionV1   // Union type for all section kinds
KqlxStateV1     // Root document state with sections array

DatabaseSchemaIndex {
  tables: string[];
  columnTypesByTable: Record<string, Record<string, string>>;
  functions?: KustoFunctionInfo[];
  rawSchemaJson?: unknown;
}

KustoConnection {
  id: string;
  name: string;
  clusterUrl: string;
  database?: string;
}

SqlConnection {
  id: string;       // prefix: sql_
  name: string;
  dialect: string;   // e.g. 'mssql'
  serverUrl: string;
  port?: number;
  database?: string;
  authType: string;  // 'aad' | 'sql-login'
  username?: string;
}

HtmlSectionData {
  id?: string;
  type: 'html';
  name?: string;
  code?: string;
  mode?: 'code' | 'preview';
  expanded?: boolean;
  editorHeightPx?: number;
  previewHeightPx?: number;
  previewHeightUserSet?: boolean;
  dataSourceIds?: string[];
  pbiPublishInfo?: PbiPublishInfo;
  powerBiUpgradeNotice?: PowerBiUpgradeNoticeState;
}

PbiPublishInfo {
  workspaceId: string;
  workspaceName?: string;
  semanticModelId: string;
  reportId: string;
  reportName: string;
  reportUrl: string;
  dataMode?: 'import' | 'directQuery';
}
```

## HTML Dashboard Sections

HTML sections are authored in `kw-html-section.ts`; `htmlSectionDefinition.ts` owns their persisted shape and patch semantics. They store `name`, source `code`, display `mode`, expansion and editor/preview height state, referenced `dataSourceIds`, optional `pbiPublishInfo`, and optional `powerBiUpgradeNotice`. They do not persist query result data in the HTML section; data remains owned by source query/transformation sections and is read at runtime.

Dashboard data binding is declared with a provenance block embedded in the HTML source:

```html
<script type="application/kw-provenance">
{
  "version": 1,
  "model": {
    "fact": { "sectionId": "query_...", "sectionName": "..." },
    "dimensions": [
      { "column": "Day", "label": "Date", "mode": "between" }
    ]
  },
  "bindings": {
    "total-calls": { "display": { "type": "scalar", "agg": "COUNT" } }
  }
}
</script>
```

The provenance `model.fact` identifies the event-grain source query section. `model.dimensions` describes slicer columns and modes (`dropdown`, `list`, `between`). `bindings` map `data-kw-bind` element names to display definitions such as scalar, table, repeated table, pivot, and supported chart outputs. Preview rendering injects a sandboxed `window.KustoWorkbench` bridge with helpers such as `getData`, `onDataReady`, `agg`, `bind`, `bindHtml`, `renderChart`, `renderTable`, `renderRepeatedTable`, and formatting utilities.

`src/shared/portableDashboardCompiler.ts` is the sole portable-dashboard admission owner. It uses the standards-based `parse5` tree builder and source locations to parse provenance v1, discover browser-created `data-kw-bind` targets, prove exactly one authored target and stable replacement semantics, validate display/target shapes and fact-source columns (including pre-aggregation, table cells, charts, slicers, and tooltips), and return typed diagnostics plus normalized renderer-neutral IR. Admission rejects source-less generated clones, intersecting or reparented ranges, parser-state-changing form boundaries, hidden/inert contexts, and targets that cannot retain representative generated output. Malformed provenance and source schemas fail closed with diagnostics rather than throwing. `htmlDashboardUpgrade.ts` is now a compatibility-notice adapter over typed compiler diagnostics rather than a second interpreter.

Slicers are generated from compiler-admitted provenance dimensions for preview. They filter the fact rows client-side and compose with AND semantics before bindings are evaluated. Exportable scalar `bind()`, chart, table, and repeated-table calls resolve only compiler-admitted bindings; `bindHtml()` remains the explicit preview-only active-content extension. Exportable preview charts should call `KustoWorkbench.renderChart(bindingId)`, which renders registered bar, pie, and line bindings as inline SVG using the same chart dimensions, palette, ordering, and label rules as the Power BI DAX/SVG backend. Exportable preview tables should call `KustoWorkbench.renderTable(bindingId)`, which renders provenance table bindings into `<table>` or `<tbody>` targets and supports `columns[].cellBar` stacked SVG bars plus `columns[].cellFormat` conditional badges/highlights inside cells. Exportable repeated tables should call `KustoWorkbench.renderRepeatedTable(bindingId)` into a visible container target. Power BI export uses the same admitted IR but preserves the existing DAX/SVG/HTML Content renderers; JavaScript-only DOM updates that are not represented by admitted bindings do not survive the Power BI render path.

The Kusto Workbench agent keeps detailed dashboard authoring rules in `copilot-instructions/html-dashboard-rules.md` and exposes them through the `getHtmlDashboardGuide` tool. The `validateHtmlDashboard` tool asks the active webview for the same export context used by HTML dashboard export/publish, then returns the portable compiler's typed diagnostics and compatibility issue strings. Preview preflight, agent validation, local export, and publish preparation therefore report the same diagnostic identity.

## Power BI Dashboard Export and Publishing

HTML dashboards can be saved as standalone HTML or exported as a folder-based Power BI project (`.pbip`). `powerBiProjectArtifacts.ts` compiles the complete project in memory; `powerBiProjectWriter.ts` writes it for local export. The manifest contains PBIR report files, TMDL semantic model files, a `_KW_HtmlMeasures` measure table, and an `HTML Dashboard` measure rendered through the marketplace-signed HTML Content visual (`htmlContent443BE3AD55E043BF878BED274D3A6855`). The implementation intentionally targets `.pbip`/PBIR/TMDL, not `.pbix` files.

Project compilation validates authored HTML, applies the existing CSS/wrapper transformation, recompiles the transformed HTML, and precomputes every generated artifact before any external effect. Compiler-owned provenance and target source ranges drive exact DAX rewrites; every admitted binding must be rewritten exactly once, and dynamic scalar/table/pivot/chart text is HTML/XML escaped before insertion. The immutable manifest is ordinally sorted, rejects filesystem-equivalent path collisions, and exposes fresh byte copies. Local export performs only directory/file publication through the VS Code filesystem adapter. Publish preparation converts the same manifest bytes directly to sorted Fabric definition parts before authentication or Fabric calls and performs no temporary filesystem reads or writes. Admission failures cross both adapters as `PortableDashboardAdmissionError` with the compiler's typed diagnostics. Raw standalone HTML remains a distinct authored-code export and is not rewritten through the portable IR.

Exported data sources are generated from referenced Kusto query sections. The semantic model uses `AzureDataExplorer.Contents`, maps Kusto column types to TMDL types, and can generate Kusto tables in Import or DirectQuery mode. Local `.pbip` export and new Power BI service publishing default to Import mode, while legacy republish preserves DirectQuery compatibility unless a mode is selected explicitly. Provenance slicers are emitted as native Power BI visuals bound directly to fact-table columns so filter context reaches DAX measures without generated dimension-table joins. Scalar/table/repeatedTable/pivot/chart dashboard values are generated from the provenance binding definitions, including table visual-cell helpers such as stacked `cellBar` columns and numeric-threshold `cellFormat` styles. Custom JavaScript table bodies produced with `bindHtml()` are preview-only unless the same cells are represented in the table or repeated-table provenance spec.

Power BI service publishing is implemented in `powerBiPublish.ts` using Fabric REST APIs. It projects report and semantic-model definition parts directly from the shared in-memory manifest, excluding local `.platform` files and patching only the service `definition.pbir` connection. Publishing creates or updates SemanticModel and Report items in a selected workspace, supports republishing to existing stored IDs, can detect whether the stored report still exists, and persists returned workspace/model/report metadata in `pbiPublishInfo`. Refresh schedule configuration is attempted after publish and treated as non-fatal if it fails.

DSH-2 qualification passes the final three-file manifest/export/publish/ownership ring at 330/330 tests and the adjacent nine-file dashboard/protocol/compensation ring at 457/457 with `--maxWorkers=1`. Full sequential and official coverage Vitest pass 297 files and 6,465 tests at 48.35% statements; the complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean task diagnostics and `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,147.4 KB host and 3,020.2 KB webview within unchanged synchronized baselines. Complete path/SHA-256 goldens cover 17 representative artifacts, and mocked create publishing verifies sorted direct parts plus the unchanged SemanticModel-then-Report transaction. Native E2E was not required because controls, messages, renderer behavior, and Fabric transaction semantics are unchanged; no live authenticated Fabric tenant publish was performed.

`HostDashboardApplicationHandler` composes these unchanged export/publish adapters. It owns all nine dashboard request/cancel/ack routes, exact request abort controllers, successful publish application leases, first-external-commit Leave No Trace admission, stale publish retirement, and cleanup finalization. `KqlxEditorProvider` supplies three explicit native-document hooks for publish apply/compensate admission, terminal settlement, and queue-stable cleanup. `QueryEditorProvider` retains only handler construction/injection, transport offering, and disposal; it has no dashboard workflow map, Fabric adapter import, or publish state transition.

PRO-25 adds only the shared result/application-acknowledgement contract around that transaction. The required red proved matching `accepted: 'yes'` previously requested cleanup and retired the exact created-item lease/timer. The fixed regression holds the same lease and 15-second timer through 14,999 ms, then permits only canonical `accepted: true` to retire once without cleanup. Separate fake-clock tests pin expiry at 15,000 ms and the applied-state rearm at 5,000 ms. The final core ring passes 8 files and 184 tests; the adjacent dashboard/HTML/persistence/transport ring passes 13 files and 881 tests. Complete sequential and official coverage Vitest pass 341 files and 7,044 tests at 49.55% statements. VS Code 1.133.0 extension-host passes 218/218 with explicit `.vscode-test.mjs`, `--timeout 5000`, and `--fail-zero`. Strict host/webview/browser types, integration compilation, production extension/browser builds, ESLint with zero errors and five existing warnings, zero production source maps, diagnostics, `git diff --check` with two existing line-ending notices only, and both synchronized bundle gates pass. Final production sizes are 2,270.9 KB host, 3,172.7 KB main webview, 338.0 KB Markdown webview, and 19,208.8 KB total `dist/`; synchronized baselines move `2214→2221`, `3117→3123`, and `286→289` with the fixed 50 KB buffer unchanged. Native E2E and live Fabric publication were not repeated because valid create/update, presentation, metadata, CAS, cleanup, and Fabric behavior remain unchanged while every migrated construction, startup, transport, ingress, acknowledgement, lease, and deadline boundary is directly covered. Reviews returned `VERDICT: NO PRO-25 IMPLEMENTATION BLOCKER`, `VERDICT: NO PRO-25 DOCUMENTATION BLOCKER`, and definitive `VERDICT: NO PRO-25 BLOCKER`.

HST-1 qualification passed the final 10-file focused ring (466 tests), complete sequential Vitest and coverage (221 files, 5,708 tests, 48.18% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.131.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds passed; ESLint reported zero errors and five pre-existing warnings. Final production sizes are 1,905.8 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`, within the unchanged synchronized limits. The real provider lifecycle passed 21/21, including publish apply, same-ID HTML replacement, stale compensation rejection, exact prior-metadata restoration, and queue-stable cleanup. The definitive documentation-aware reviewer returned `VERDICT: CLOSE HST-1`. No live authenticated Fabric tenant run was performed.

## Schema Caching

* **In-memory:** `schemaCache` Map in `KustoQueryClient`
* **On-disk:** SHA1-hashed JSON files in `globalStorageUri/schemaCache/`
* **Version:** `SCHEMA_CACHE_VERSION` constant triggers cache invalidation on format changes

### Kusto Schema Lifecycle and Preparation Readiness

Every active `<kw-query-section>` claims an immutable section lease identified by `{ boxId, sectionInstanceId }`. Selecting a different connection/database or explicitly invalidating authentication advances `targetGeneration`. Database and schema requests carry `{ sectionInstanceId, targetGeneration, requestToken }`; the host echoes that identity, and `kusto-schema-message-router.ts` admits a delivery only when the section incarnation, target, and request stream are still exact. Recreating the same persisted `boxId` therefore cannot revive an old response.

`KustoEditorSchemaCoordinator` is the canonical per-editor owner for section/model leases and all target-scoped Kusto state: schema, metadata, preparation, pending worker updates, base worker readiness, enhancement readiness, apply requirements, and waiters. A DOM disconnect caused by section reorder is not disposal. Only explicit section removal closes the lease, settles waiters, clears owned state, and leaves a tombstone against late writes. Auth invalidation and reapply scheduling enumerate coordinator-registered sections, so restored sections are covered before their Monaco models exist.

Kusto and SQL catalogs are intentionally separate. Kusto consumers use `schema-catalogs.ts` accessors backed by the coordinator; SQL consumers use `sqlSchemaByBoxId`. There is no mutable `window.schemaByBoxId` authority. `section-factory.ts` composes query sections, Monaco, and controllers, but Monaco and `QueryConnectionController` stay below it through leaf accessors and direct module APIs.

The extension host treats the physical Kusto connection identity (cluster plus authority) as part of response ownership. Host connection projections carry `connectionRevision` and `connectionIdentityKey`; section lifecycle targets retain those stamps so a request queued against saved connection A cannot silently run against replacement B that reused the same ID. Restored sections can establish their logical target before that projection arrives, so `setConnections()` republishes the same connection/database with its host stamp without clearing target-bound rows. Schema prewarm goes through the section target API and is keyed by target generation. Within that exact generation, schema responses match the logical connection/database target because the physical stamps are host-owned lifecycle state rather than duplicated response fields. Database and schema services capture the same physical identity before asynchronous work and suppress publication if the connection is removed or repointed. `KustoConnectionLifecycle` synchronously invalidates physical targets before its queued projection refresh, then serializes Copilot/schema invalidation, publishes `kustoAuthIdentityChanged`, and sends the refreshed connection snapshot.

`KustoQueryClient` coalesces concurrent direct database or schema discovery only when the complete target, principal preference, refresh, interaction, and persistence policy match; owner-gated dispatch remains isolated. An SDK client is held by an operation lease until every in-flight sibling settles, so account retry, invalidation, cancellation, or disposal can evict the cache entry without closing a client still serving metadata. Disposal and authentication currentness are checked before every physical schema command and again before publication. Schema discovery tries JSON and tabular formats within one authenticated attempt before considering another account, rejects empty or malformed results, and normalizes successful or historical compact cache entries into structurally validated `.show schema` JSON before they can become worker-ready. Shared disk-cache reads are request-brokered per exact cache key.

Kusto query sections expose coordinator-owned preparation state through typed accessors in `src/webview/core/state.ts`. A section is not worker-ready merely because `schemaData` reached the webview. Readiness requires the current primary database schema to match the current operation generation, delivery revision, schema key/signature, and Monaco model, with these blockers settled:

1. Required database discovery and schema delivery are complete.
2. A usable cached fallback is available, or a required foreground schema load has completed. Silent cache-expiry refreshes never hold section readiness.
3. The schema is applied to the current Monaco Kusto worker model.
4. Deferred function-output inference is tracked separately as best-effort background enrichment. It can improve function-result completions and is retried after cancellation or failure, but it does not hold `aria-busy` or the toolbar progress indicator once the exact base worker schema can already serve semantic completions.

Because Monaco-Kusto uses a shared worker schema, another section targeting the same cluster/database can adopt an already-loaded schema for its exact model and become ready without waiting for a duplicate host fetch. Stale-cache validation and changed-schema refreshes continue silently and never replace the usable ready fact with a foreground preparation blocker.

Multi-section restore intentionally avoids pushing every unfocused model into the shared Monaco worker, because those writes can race the first real focus and install the wrong primary database context. Once raw schema is fetched and exact delivery ownership is retained, an inactive section therefore becomes `deferred / waiting-focus`: it keeps the pending worker update and preparation token, but has no active blocker or worker operation. Focusing that editor promotes the same token back to `preparing / waiting-worker`, applies the pending schema for the exact model, and reaches true `ready` only after worker readiness is committed.

The state is reflected by `kw-query-section` through `data-preparation-state`, `data-preparation-stage`, and `aria-busy`. While the state is `preparing`, `queryEditor.css` renders an indeterminate blue segment over the bottom border of the Kusto editor toolbar. Deferred, ready, idle, and terminal-error states use the normal solid toolbar border. Reduced-motion mode uses a static accent, and forced-colors mode uses `Highlight`.

Operation generations and delivery revisions prevent late database/schema/worker responses from completing newer work. Removal leaves a monotonic tombstone so recreating a persisted section ID cannot be revived by an old async operation. Worker-wide schema replacement invalidates other model readiness; those sections remain dormant until focus prepares them again.

All shared Monaco-Kusto worker writes pass through `KustoWorkerMutationPort`. A queued operation receives the exact transaction that owns its physical worker calls and records a commit only after each mutation succeeds. Queued primary intent and started primary intent are distinct: enqueueing a later primary apply cannot invalidate the primary transaction that is currently executing, while a transaction that actually starts supersedes the prior started intent. Primary intent generation, committed worker revision, and destructive replacement epoch are distinct counters. Supplemental/enhancement leases may time out logically while an uncancelable worker call remains in flight; the detached transaction cannot commit afterward, and the queue remains occupied through physical settlement and inline primary-schema recovery before any later mutation can begin.

Each active primary worker-preparation episode has one 30-second absolute budget, shared by retries and fallback paths. Deferred, ready, error, and idle states retire the episode so focus-time or later worker preparation receives a fresh budget, while stale older generations cannot reset a newer active deadline. Expiry moves the owning section to a terminal error state so `aria-busy` and the toolbar indicator stop, but it does not cancel or release the underlying Monaco worker operation; that operation remains serialized until it settles because Monaco-Kusto exposes no worker-mutation cancellation API.

### Exact Kusto Execution Ownership

Every section-publishing Kusto run carries one `KustoExecutionRequestIdentity`: `{ engine, boxId, sectionInstanceId, targetGeneration, executionId, connectionId, database, producer }`. `src/shared/kustoExecution.ts` defines that execution identity, while `src/shared/kustoExecutionStartProtocol.ts` solely owns the Copilot/comparison start handshake. The webview publishes exact section open/target/close lifecycle messages, and `KustoExecutionCoordinator` mirrors the current target for each section incarnation.

The host reserves synchronously before selection persistence, authentication, or any other await. A reservation receives a monotonic `reservationSequence`; replacement atomically installs the new reservation, cancels the previous transport, and publishes the previous reservation's correlated `superseded` terminal. Reservation, pre-start admission, and dispatch capture compare the host-issued physical connection owner, so same-ID replacement cannot redirect stale work. Immediately before each actual SDK submission, `KustoQueryClient` captures immutable dispatch identity: attempt number, connection revision and identity, endpoint/authority, authenticated account partition and session generation, Leave No Trace revision, and client activity ID. Success is admitted only when that dispatch exists and its physical target, account session, and policy revisions remain current.

`KustoExecutionCoordinator` is the only Kusto logical terminal publisher. Success, error, explicit cancellation, target retirement, policy retirement, and supersession carry the complete reservation plus dispatch identity when physical submission occurred. Physical SDK and server cancellation remain best-effort, but logical settlement is immediate and idempotent. Manual runs, Run Function, Copilot final runs, performance comparisons, and agent-tool runs use this coordinator whenever they publish into a query section. Canonical Copilot and lineage-complete comparison starts use the runtime-validated `kustoExecutionStarted` / `kustoExecutionStartedAck` handshake before physical dispatch; manual and tool starts remain preclaimed, and tool invocations separately acknowledge their exact owner so cancellation works before or after that acknowledgement. Model-context-only Copilot queries remain separate because they never publish a generic section terminal.

Kusto Copilot requests have a second exact identity for their whole generation/write lifecycle: `{ boxId, sectionInstanceId, targetGeneration, copilotRequestId }`. Standalone Optimize uses the parallel `{ boxId, sectionInstanceId, targetGeneration, optimizeRequestId }` identity. The webview creates and owns these requests, the host echoes them on every status/options/output/terminal message, and the section admits output only for the active request and current lifecycle. Retarget, removal, replacement, or panel disposal cancels exact model, direct-query, final-run, comparison, and Optimize owners. Delegated tools capture the exact Copilot request owner after send and use a request-scoped pre-start cancellation tombstone, so delayed cancellation cannot affect a newer request in the same section. Queued output cannot mutate or settle a recreated same-ID section.

Successful terminals require a runtime-validated reservation and physical dispatch envelope; the coordinator retires a dispatchless success attempt instead of publishing rows. Request admission compares connection, database, and producer in addition to section/execution identity. Linked comparison sections retire and exactly cancel any old-target owner before synchronously adopting the source connection, database, and lifecycle target whenever the source retargets or the comparison is reused. That retirement clears rendered rows, shared result state, persisted `resultJson`, comparison summaries, and exact target-bound Copilot conversation history before mutation. Manual and Copilot comparison dispatch fail closed if the source and comparison data targets differ.

The webview must successfully claim the exact owner before sending `executeQuery`. `QueryExecutionController` retains ownership while cancellation is pending and retains every superseded identity until its exact terminal is admitted once. SQL routing runs first; afterward, a terminal targeting a registered Kusto section is rejected unless it carries a complete Kusto execution identity. A removed tool section may consume its matching retired terminal through the internal terminal event so the tool request settles without rendering into a missing or recreated section.

Connection, authority/account, database target, section removal, panel disposal, and Kusto Leave No Trace changes revoke affected reservations before publication. First-time automatic account establishment (`none -> A`) is not a revocation: an added Microsoft session establishes the baseline, and account-partition metadata refresh invalidates schema ownership without retargeting or clearing the exact run that established it. A true automatic rotation (`A -> B`), removed or recreated session, explicit selection change, or forgotten account revokes only mapped connections and clears their retained data and Copilot state.

`KustoLeaveNoTracePolicyStore` is the canonical cross-window privacy authority. It stores atomically committed snapshots under extension global storage, serializes changes with a filesystem lock, watches other extension hosts, and advances a per-cluster revocation generation. Physical dispatch starts and all data-bearing publication read that generation under the same shared lock; asynchronous publication callbacks are awaited before the lock is released. Model-context Copilot posts rows and appends history inside that callback, and Connection Manager table preview captures the dispatch generation and admits its rows under the same lock. A remote toggle therefore invalidates only affected clusters even before watcher delivery and cannot interleave between admission and publication.

Database/schema discovery, table preview, Connection Manager search, Cached Values, and direct agent schema tools use exact metadata owners rather than connection IDs alone. Those owners include physical connection identity/incarnation, account partition, authentication-session generation, per-cluster Leave No Trace generation, and the relevant database/schema cache generations. `KustoQueryClient` invokes authenticated metadata gates inside the canonical Kusto policy lock immediately before the SDK call, closing the authentication-to-submission race. Persisted Connection Manager search rows carry their own `kustoSearchOwner` proof and are revalidated individually; whole-profile fingerprints and whole-policy versions remain only a fail-closed fallback for legacy rows.

Sensitive host-to-webview deliveries use application-level publication leases: stage, commit, and revoke/status reconciliation. The webview acknowledges staging only after it owns the exact payload and acknowledges application only after the mutation occurs. A bounded completed-publication ledger lets a lost applied acknowledgement converge on the already-rendered result instead of producing a contradictory cancellation or second terminal. Transport-level `postMessage()` success is never treated as proof that rows were applied.

Snapshots that combine Kusto and SQL ownership use canonical lock order Kusto then SQL. `SqlWorkbenchService` exposes one-attempt owner-snapshot acquisition; callers release Kusto admission while SQL is contended and retry the complete ordered acquisition. This prevents Connection Manager, Cached Values, Query Editor sanitation, or publication from holding the Kusto privacy lock during SQL lock backoff.

Persisted Kusto result restoration waits for the first canonical policy snapshot. Protected or globally blocked jobs are discarded before rendering; later policy changes purge matching queued jobs, shared/rendered rows, and stored `resultJson`. If committed state and backup are unrecoverable after migration, recovery remains globally fail closed through ordinary edits and dominates any older process's higher unblocked version: result admission is rejected, the first connection snapshot waits for policy initialization, and every current Kusto connection is advertised non-persistable. `ConnectionManager` mirrors healthy shared state into legacy global state for compatibility and maps changed cluster keys to local connection IDs for lifecycle cancellation.

Reorder disconnect/reconnect is not disposal and does not release the execution or Copilot owner.

Admitted Kusto successes also publish their exact producer dispatch and privacy owner into the immutable result-artifact layer described below. Transport-specific execution coordination remains separate from artifact publication; SQL does not yet publish the same exact artifact provenance.

Focused coverage lives in `kustoLeaveNoTracePolicyStore.test.ts`, `connectionManager.test.ts`, `connectionManagerViewerSearch.test.ts`, `cachedValuesViewer.test.ts`, `kustoAuthPreferenceService.test.ts`, `kustoExecutionCoordinator.test.ts`, `kustoClient.test.ts`, `queryEditorProviderCancel.test.ts`, `query-execution-run-function.test.ts`, `query-section-accessors.test.ts`, `kw-query-section-loading.test.ts`, `message-handler.test.ts`, `message-protocol.test.ts`, `persistence-roundtrip.test.ts`, `queryEditorCopilotFunctionExecution.test.ts`, `toolOrchestratorConnect.test.ts`, `kw-cached-values.test.ts`, `kw-connection-manager.test.ts`, and `kusto-schema-ownership.test.ts`. Authenticated native coverage lives in `kusto-execution-contract` and `query-cancel`.

Final EXA-1 qualification on VS Code 1.130.0 passed production and integration compilation, the 27-suite focused ring (1,270 tests), full sequential Vitest (195 files, 5,098 tests), the full extension-host integration suite (113 tests), production extension and browser builds, and both bundle-size gates. Authenticated `kusto-execution-contract` passed all three normal/rerun/retarget scenarios on its unchanged rerun; the preceding first-scenario timeout remains recorded as a flake suspect. Authenticated `query-cancel` passed both physical-cancellation/race-recovery scenarios. Every final JSON artifact and all nine passing-run screenshots were reviewed.

### Immutable Result Artifacts

`src/shared/resultArtifact.ts` owns the runtime `ResultArtifactStore` and the compact `PersistedResultArtifactV1` descriptor. Each publication deep-clones and freezes columns, rows, metadata, producer, policy, and lineage; advances a monotonic per-source revision; and replaces only the source's mutable current pointer. Explicit consumer bindings retain an older revision across later source publications. Rebinding moves that consumer to the selected current revision and prunes unreferenced history. Clearing a source synchronously revokes all of its consumer bindings and current artifact, while an ordinary rerun preserves pinned consumers.

`core/results-state.ts` is the compatibility facade for latest-result presentation and presence checks. Every accepted `setResultsState` first publishes an immutable artifact revision; publication failure clears presentation and rejects the result atomically. Cross-section computation, external egress, and governed table interactions do not treat the mutable facade as lineage authority.

`shared/chart-renderer.ts` resolves only the chart's bound artifact. A normal source rerun therefore cannot silently change a pinned render; the existing dependent-refresh cascade explicitly rebinds the chart before refreshing it, preserving automatic live-chart behavior. Source selection rebinds through rendering, clearing the selection releases the binding, and chart removal releases it permanently.

Transformations are the first derived producer. `kw-transformation-section` owns separate immutable bindings for its primary and join-right inputs, computes only from those bound revisions, and publishes a new artifact with direct input roles and flattened leaf-source policy ancestry. Formula and tool edits retain existing bindings; source selection and the established dependent-refresh path explicitly rebind. Join refresh observes either input. Removal releases both inputs and clears the derived output even when the element is already detached.

Lineage participates in retention and revocation. A source revision remains available while any derived artifact references it. Clearing a source follows artifact lineage transitively, removes affected derived revisions and their consumer bindings synchronously, and clears mutable compatibility rows only when that source's current artifact was revoked. An old T(A) revision is therefore revoked with A while a retargeted current T(B) survives. Differently owned join inputs retain exact per-source policy stamps instead of fabricating one common owner; a top-level owner stamp is inherited only when every policy-bearing leaf agrees.

Kusto comparisons use one `KustoComparisonRunIdentity` across the source and comparison executions: source box, source execution, and comparison box. Manual Compare and standalone Optimize wait for the admitted source terminal before dispatching the comparison; Copilot optimization reuses the same source execution identity across retries. The source artifact is pinned by producer execution, the comparison start claims that exact pin, and output publication verifies source target, principal session, connection incarnation, and Leave No Trace policy against its dispatch before rendering. The comparison artifact then retains `comparison-source` lineage and leaf policy ancestry, so summaries resolve the exact source revision even after the source current pointer advances. Cancellation, failure, retarget, removal, and abandoned dispatch release temporary pins.

HTML dashboard previews bind their provenance fact source through `html:<boxId>:fact` and serialize rows only from that immutable artifact revision. Ordinary source publication does not move the binding; the existing dependent-refresh cascade explicitly rebinds. Active iframe data requires `policy.exposeToActiveContent === true`. Kusto and owner-admitted SQL results publish that compatibility decision explicitly, transformations promote it only when every leaf source allows exposure, and restored descriptors must match the locally recomputed post-owner-admission decision. Missing or mixed permission fails closed.

Source/lineage revocation reports affected consumer IDs synchronously. A connected dashboard immediately hides and blanks its sandboxed iframe before deferred refresh; provenance source changes do the same for the old source. Reconnect after reorder revalidates the retained binding, and section removal releases it even after DOM detachment. Passive Power BI/export metadata remains separate and continues to read only schema/query metadata.

Kusto model/tool result responses bind the artifact for their exact producer execution under `model:<requestId>:result` and require the independent `policy.sendToModel === true` decision. Configure-and-execute previews and delegated Copilot responses never fall back to mutable latest rows or terminal payloads. Delegated execution starts carry the host-captured query text, so the response cannot pair execution A's rows with later editor text B. Row caps and response formats remain unchanged. Success, denial, cancellation, timeout, failed start, owner invalidation, callback exceptions, and panel cleanup all settle once and release listeners, timers, owners, and bindings.

Share Results uses the singleton `share:clipboard:result` consumer. Opening the modal binds the current immutable result and, when `policy.shareToClipboard === true`, takes query, connection, and database from that artifact's producer rather than the mutable editor. A rerun retires mutable/current state without pruning an already-bound A; copying closes and releases A, and reopening binds B. Denial, source revocation, owner removal, malformed/applied document replacement, and modal dismissal release the binding; title/query-only sharing remains available when rows are denied. Kusto shares retain their Azure Data Explorer link, while direct and derived SQL shares use SQL formatting and no ADX-link promise.

After webview artifact admission, `HostQuerySharingApplicationHandler` formats the exact typed payload and publishes `shareContentReady`; the webview writes both HTML and plain-text clipboard flavors. The same handler owns direct Kusto ADX-link clipboard writes. Provider transport cannot reformat, relink, or publish sharing responses independently.

Result-table CSV export uses a separate `policy.exportToCsv` capability. Kusto, SQL, and transformation sections register `csv:<boxId>:table` against the exact rendered artifact and receive a unique table-generation token. Save remains hidden until that exact binding is admitted. The table serializes its TanStack sorted/filtered row model by declared column index, so ragged rows cannot export undeclared cells, then `artifact-csv-export.ts` verifies the artifact ID, generation token, binding, and policy before sending an intent without bytes.

`HostArtifactCsvSaveApplicationHandler` opens the native picker, creates a one-use nonce, and asks the webview to provide the projection. The webview revalidates the active table and creates a short-lived `csv:<boxId>:save` binding before returning bytes. One table may retain one projection, with eight pending projections globally. The handler accepts only the matching nonce, box, and artifact, consumes it once, retains a bounded completed-intent tombstone against request replay, and writes immediately to the selected URI. Replacement, transitive result revocation, removal, every applied document transition (including malformed replacement), and panel disposal clear both sides and hide Save through a token-scoped release event. Picker cancellation or the visible 60-second transfer timeout cancels only that attempt, leaving an otherwise valid table retryable. URL-imported CSV routes `saveImportedCsv` to `HostImportedCsvSaveApplicationHandler`; Connection Manager preview export retains its separate protocol.

HST-2 qualification passed the final 11-file focused ring (310 tests), complete sequential Vitest and coverage (223 files, 5,713 tests; 48.18% statements), host/webview/browser typechecks, integration compilation, and the complete VS Code 1.131.0 extension-host suite (201/201 with `--timeout 5000`). Production extension and strict browser builds, ESLint with zero errors and five pre-existing warnings, diagnostics, `git diff --check`, both bundle gates, and the definitive blocker review passed. Final production sizes are 1,906.3 KB for `extension.js` and 2,801.9 KB for `webview.bundle.js`; the synchronized extension baseline moved by 1 KB to 1,857 KB while the 50 KB buffer remained unchanged. Native run `20260805-164304` exercised denied/allowed Save admission, the real Windows picker, exact intent/nonce/box/artifact correlation, zero cancellation, and the exact 26-byte file `Name,Score\nalpha,1\nbravo,2`. The permanent scenario's fresh screenshots were blocked before picker execution by the test framework's foreground-HWND validation; its previously reviewed clean screenshots remain the visual baseline.

Read-only browser viewers are a distinct local authority because they intentionally have no live Kusto/SQL connection owner. `browser-ext/src/browser-viewer-root.ts` is the sole browser document-adoption and composition owner. It consumes a `LoadedBrowserFile` snapshot, rejects stale or duplicate load generations, delegates native and companion parsing to `viewer-document.ts` and the shared notebook codecs, deep-freezes one typed `BrowserViewerProjection`, and applies query/SQL collapse plus Markdown/Chart preview defaults only to a cloned presentation state. The immutable parsed document retains opaque root, state, known-section extension, and future-section data unchanged.

`src/shared/browserViewerProjection.ts` defines and runtime-validates the cross-bundle projection plus the browser capability set. The browser can read a document and download an admitted derived file; editing, persistence, authentication, Kusto execution, SQL execution, and Copilot are explicitly false. Authored HTML and script behavior is trusted and unchanged. Independent artifact restoration still strips persisted active-content exposure claims. `src/webview/core/browser-viewer-presentation.ts` applies the projection directly through the existing section restoration and artifact paths, disables persistence, initializes unavailable host-backed state locally, and never sends the projection through the VS Code host-message dispatcher. `viewer-boot.js` owns only load admission plus loading, banner, error, and height presentation. It does not synthesize `persistenceMode`, `connectionsData`, `copilotAvailability`, or `documentData`, patch persistence globals, or repair editor writability with timers.

The shared section shell omits rename, reorder, remove, edit-mode, connection, and authentication controls when document mutation is unavailable. Monaco construction and the existing writability guard derive `readOnly` and `domReadOnly` from the same capability, so delayed writable normalization cannot reopen a browser editor. Browser CSS removes the remaining light-DOM host execution and editor-tool rows while retaining static source display, section show/hide and fit controls, result inspection, sorting/filtering, and CSV Save. Kusto, SQL, and Python do not register browser Run shortcuts and reject direct execution before reservation or host publication. URL sections render their authored URL as static selectable text and do not request unavailable host fetch work. Persisted `resultJson` rows are restored into a fresh runtime artifact carrying only `exportToCsv: true`; persisted producer identity and model, clipboard, or active-content capability claims are not adopted. `vscode-shim.js` implements only the browser CSV nonce/replay/download adapter and supplies no synthetic response for unsupported host work. Parent handlers reject messages from every other source window.

Standalone browser handoff never uses a process-global latest payload. `background.js` assigns each open request a `crypto.randomUUID()` token, stores payloads in a one-shot TTL-bounded map, and embeds only that token in the standalone URL. The wrapper requests and consumes exactly its own token, so concurrent opens cannot receive each other's document.

Content-script load generations cross both inline and standalone payload delivery. The browser root presents one accepted generation; a standalone retry for that same generation is acknowledged without replaying the projection or changing loading/banner state, while a stale generation cannot acknowledge or alter the accepted presentation. The wrapper's exact iframe-source acknowledgement, retry, resize, and CSV-download behavior remains unchanged.

BRW-1 qualification passes the definitive 16-file focused ring with 460 tests, complete sequential and official coverage Vitest with 292 files and 6,303 tests at 48.23% statements, strict host/shared, webview, and browser TypeScript, integration compilation, and the complete VS Code 1.132.0 extension-host suite at 209/209 with `--timeout 5000`. Production extension and strict browser builds pass; ESLint reports zero errors and five existing warnings. The integrated and standalone bundle gates pass at 1,946.2 KB host and 2,807.1 KB webview; synchronized baselines are 1,897/2,758 KB with the fixed 50 KB buffer. The final browser build emits a 32.2 KB viewer boot bundle. Built-browser verification proves one stable accepted banner across duplicate/stale delivery, six mixed section views, read-only Kusto/SQL/Python editors, static URL text, exact Kusto and SQL restored rows, no mutation/authentication/execution controls, zero forbidden host/synthetic messages, and exact CSV bytes `City,Count\nSeattle,42\nLondon,31\nTokyo,27`. A desktop screenshot is clean; a `390x844` Playwright layout check reports no horizontal overflow, no error surface, and non-overlapping Python/static-URL sections. The integrated screenshot surface reset to its fixed desktop viewport during mobile capture, so mobile evidence is DOM/layout plus accessibility rather than a retained screenshot.

Kusto success publication carries reservation, execution, lifecycle target, physical dispatch, connection, principal session, Leave No Trace policy, and optional comparison-source lineage from the admitted terminal. Owner-admitted SQL manual/tool/Copilot and derived-comparison successes carry the exact executed query, connection, and database needed by row-sharing consumers. SQL Copilot execution publishes required start ownership before transport, revalidates after asynchronous delivery, and retires stale comparison rows before replacing a comparison query. Bounded persistence continues to store rows once in `resultJson` and may pair them with a row-free `resultArtifact` descriptor. Restore waits for the existing Kusto/SQL owner and policy admission, validates descriptor source and policy claims, renders accepted rows, and only then installs the restored artifact identity. Positive capability claims require a matching local decision. Restored Kusto comparisons additionally wait for their admitted source artifact and must exactly match locally reconstructed lineage and flattened leaf policies; cyclic dependencies are discarded. Restored SQL results and comparisons must match the live query, connection, database, principal, and revocation generation, including comparison-first documents and whitespace-exact queries. Legacy descriptors retain only capabilities they already carried; they never gain model or clipboard permission. The next live publication advances beyond the highest runtime revision. A descriptor never admits rows by itself; every row purge and host privacy sanitizer removes the descriptor atomically, and diffs treat it as result noise.

Declared-column projection happens before presentation, search, persistence, artifact publication, model/share/HTML egress, and derived computation. Governed Kusto, SQL, transformation, and Diff tables retain local copy only while their exact rendered generation is live; revocation synchronously purges rows, TanStack/search state, selection, object/unique-values viewers, chart interactions, and copy authority. URL CSV sections publish immutable source artifacts for chart/transformation consumption through a request-correlated fetch protocol, while their local table and Connection Manager previews retain separate ungoverned direct-data copy/save workflows. EXA-2 is closed; this boundary does not persist transformation payloads or merge the Kusto and SQL execution coordinators.

Focused coverage lives in `result-artifact.test.ts`, `results-state.test.ts`, `artifact-csv-export.test.ts`, `browser-csv-shim.test.ts`, `transformation-join.test.ts`, `chart-renderer-zoom-pan.test.ts`, `chart-datasets.test.ts`, `kw-html-section-slicer.test.ts`, `dashboard-chart-renderer.test.ts`, `kustoExecutionCoordinator.test.ts`, `message-handler.test.ts`, `query-execution-run-function.test.ts`, `queryEditorCopilotFunctionExecution.test.ts`, `kw-query-section-loading.test.ts`, `kw-query-toolbar-share.test.ts`, `kw-sql-section-loading.test.ts`, `kw-data-table-visibility.test.ts`, `kw-url-section.test.ts`, `savedResultsCsvNotification.test.ts`, `sql-section-session.controller.test.ts`, `persistence-roundtrip.test.ts`, `queryEditorProviderCancel.test.ts`, `kqlxFormat.test.ts`, and `diffViewerUtils.test.ts`. Native `default/chart-regressions`, `default/transformation-artifacts`, `default/comparison-artifacts`, `default/html-artifact-bridge`, `default/model-result-artifacts`, `default/share-result-artifacts`, and `default/csv-result-artifacts` contracts prove pinning, explicit rebind, derived/comparison lineage, independent exposure/model/clipboard/CSV admission, exact projections, row caps, and synchronous revocation through the real VS Code webview.

Final model-result qualification on VS Code 1.130.0 passed the 21-file focused ring (1,140 tests), full sequential Vitest (196 files, 5,163 tests), both type projects, production extension and browser builds, integration compilation, the full extension-host suite (113 tests), and all existing bundle gates without a threshold increase. ESLint completed with zero errors and five pre-existing warnings outside touched files. Native `default/model-result-artifacts` passed on its first run. Final pessimistic review found no blocking issue.

Final clipboard-share qualification passed the 12-file focused ring (610 tests), full sequential Vitest (197 files, 5,182 tests), both type projects, production extension and browser builds, integration compilation, and the full extension-host suite (113 tests) on VS Code 1.130.0 and 1.131.0. ESLint completed with zero errors and the same five pre-existing warnings. The intentional webview runtime and native-contract growth raised only the synchronized webview bundle baseline by 10 KB (2,595 to 2,605 KB); every final bundle remains within its threshold. Native `default/share-result-artifacts` passed both runs, including the final production build. Final pessimistic review found no blocking issue.

CSV-export qualification passed the 14-file focused ring (586 tests), full sequential Vitest (199 files, 5,224 tests), both type projects, production extension/browser builds, integration compilation, the full extension-host suite (113 tests), and both bundle gates. ESLint completed with zero errors and five pre-existing warnings. Repeated first-attempt proper-lockfile/SQL credential lock failures were unrelated; each affected file passed unchanged and every complete rerun passed. Final production sizes are 1,762.9 KB for the extension and 2,660.1 KB for the webview. Native `default/csv-result-artifacts` exercised denied and allowed Save visibility, the real Windows picker, an exact 26-byte payload, and the saved notification; both screenshots and the JSON byte artifact were reviewed. Built browser inline/standalone Kusto, SQL, and SQL-derived comparison fixtures in both section orders restored and downloaded exact CSV bytes without live connections.

### Supplemental Fully Qualified Schemas

Schemas referenced through fully qualified expressions such as `cluster(...).database(...)` are supplemental and intentionally excluded from section-level preparation. They never extend the blue primary preparation indicator. `KustoSupplementalSchemaCoordinator` in `src/webview/shared/kusto-supplemental-schema-coordinator.ts` is the per-Monaco-model authority for reference generations and the `scheduled`, `fetching`, `fetched`, `waiting-primary`, `applying`, `loaded`, and `failed` states. A schema-keyed broker in `monaco.ts` deduplicates host fetches and retains raw schema payloads, but application readiness remains model-scoped.

The webview discovers references when a model is registered or changed, and again when primary preparation becomes ready. Model changes schedule a model-independent pump, so inactive sections can fetch and apply supplemental schemas without taking focus; readiness and request completion events resume waiting work without a permanent polling timer. Background requests use `requestSource: 'background'`; `SchemaService` and `KustoQueryClient` enforce `allowInteractive: false`, so opening a document cannot show an authentication prompt. Explicit Ctrl+Space escalates the exact model/reference to `requestSource: 'autocomplete'`, may retry interactively, and retains the bounded supplemental-completion fallback. A stale disk schema remains usable during silent refresh; Ctrl+Space can force an interactive live revision from any fetched nonterminal state, and a failed refresh restores the stale loaded fallback. Fetch and apply deadlines restore diagnostics on terminal failure instead of suppressing them indefinitely.

While an owned attempt is active or loaded, the Monaco marker boundary suppresses only explicit `KS207` and `KS208` diagnostics contained by that fully qualified cluster/database call. Other diagnostics, broad statement-level markers, database-only references, message-only warnings, and terminal failures remain visible. After load or failure, `monaco.ts` calls the Kusto worker's `doValidation()` only when exact primary preparation is ready, then republishes markers guarded by model/version, primary preparation identity, worker-schema revision, and broker revisions. Monaco-Kusto stores one shared schema per worker, so each broker revision mutates the worker once; exact-primary-ready model owners adopt that shared revision and retain independent loaded/revalidation state. Every worker-wide replacement invalidates supplemental applications and synchronously restores other ready primary schemas from the authoritative primary cache before readiness is republished. Supplemental schemas never enter the primary `SchemaTracker` cache.

Supplemental worker mutation has a 12-second lease so app state can move to a terminal outcome without granting a late operation commit authority. The transaction port still holds the shared-worker queue until the detached call settles, then runs recovery in that same serialized slot. Recovery forces an active authoritative primary schema through `setSchemaFromShowSchema`, removing any partial supplemental mutation before readiness is trusted or later writes run. Monaco-Kusto 14.1.0 exposes no public worker-cancellation or restart API, so a permanently nonresponsive worker cannot be recovered in-process; it requires webview/window recreation.

Supplemental traces are bounded and sanitized: model, schema, request, box, cluster, and database identities are hashed; query text, raw schemas, aliases, credentials, and backend messages are not recorded. Host responses carry distinct request policy (`requestSource`), delivery provenance (`deliverySource`), and structured failure kinds so diagnostics and retries do not infer policy from cache provenance or user-facing error text.

## SQL Section Architecture

SQL sections provide a near-identical notebook experience for T-SQL queries against SQL Server / Azure SQL databases. The system mirrors the Kusto architecture with full separation.

### Dialect Metadata

* **`SqlDialect`** (`sql/sqlDialect.ts`) — persisted/UI metadata contract: ID, display name, default port, and authentication modes
* **`MssqlDialect`** (`sql/mssqlDialect.ts`) — Microsoft SQL Server / Azure SQL metadata; saved connections continue to use `dialect: "mssql"`
* **`SqlDialectRegistry`** (`sql/sqlDialectRegistry.ts`) — register/get/list metadata for connection forms and viewers

### Host Services

* **`SqlConnectionManager`** — CRUD for SQL connections. IDs use `sql_` prefix. Connections in `globalState`, passwords in `SecretStorage`
* **`SqlWorkbenchService`** — one extension-scoped SQL owner shared by editors, Connection Manager, Cached Values, Copilot, and tools
* **`SqlEditorLifecycleCoordinator`** — one per editor/webview. Owns section incarnation and STS sequencing, composes the registry and execution broker, reacts to connection/principal/privacy/runtime events, and exposes narrow owner/currentness APIs to provider adapters
* **`SqlEditorSessionRegistry`** — one state-owning editor-scoped authority for section/comparison targets, generations, result ownership, lineage, and owner tokens
* **`SqlExecutionBroker`** — one editor-scoped authority for SQL preflight reservations, single-attempt transport admission, exact cancellation, currentness, and guarded release. Manual and Copilot callers retain query shaping, retries, user-facing terminals, and conversation history
* **`SqlQueryClient`** — stable caller-facing facade over STS database discovery, schema queries, execution, and cancellation
* **`HostSqlEditorLifecycleApplicationHandler`** — one per editor/webview. Owns exact admission and settlement for section open/target retirement, STS request/document/connect forwarding, Production gating, ordered development account-map/token writes, and disposal
* **`HostSqlSchemaRequestApplicationHandler`** — one per editor/webview. Synchronously reserves each section's schema request, aborts and supersedes prior same-section work, captures the exact section/target/result owner, and admits cache/result/error publication only while that request remains current
* **`HostSqlConnectionsProjectionApplicationHandler`** — one per editor/webview. Owns webview, lifecycle, execution, and Copilot snapshot triggers; serialized revisions; canonical policy/principal joining; cache/favorites filtering; exact payload publication; global-block fail-closed favorite handling; and disposal
* **`HostKustoConnectionsProjectionApplicationHandler`** — one per editor/webview. Owns every Kusto snapshot trigger; serialized revisions; editing-preference, first-time, account, physical connection, selection, cache, and favorite reads; canonical policy admission; exact payload publication/retry; and disposal
* **`HostPersistedResultSanitizationApplicationHandler`** — one per editor/webview. Owns cross-language synchronous, fresh, fail-closed, and lock-held publication sanitation; exact Kusto-then-SQL lock ordering; target, principal, account, and revocation admission; comparison and artifact cleanup; both persistence invalidation events; and disposal
* **`SqlSchemaService`** (`sqlEditorSchema.ts`) — disk + memory schema cache. Physical target/principal ownership, cache generation, and the injected exact request-current assertion are revalidated inside disk CAS, before memory publication, and before return

### SQL Tools Service (STS) — Language And Data Engine

SQL sections use Microsoft's SQL Tools Service, the same engine behind the official SQL Server extension. A lazily started shared process communicates over JSON-RPC and serves normal language sessions and the SQL data plane. Leave No Trace data operations use a separate one-operation process instead.

* **`StsDownloader`** — pins release `6.0.20260409.1`, verifies each platform archive with SHA-256, installs through a locked staging directory, and validates the cached binary manifest
* **`StsProcessManager`** — spawns STS, establishes a `vscode-jsonrpc` connection, exposes process epochs, settles failed readiness generations, rebinds subscriptions after restart, and supports per-request timeouts
* **`StsLanguageService`** — editor-scoped LSP client with session-unique owner URIs, document lifecycle, schema-ready handling, restart replay, and disposal
* **`StsQueryService`** — extension-scoped data-plane broker using `connection/connect`, `connection/listdatabases`, `query/executeString`, `query/complete`, paged `query/subset`, `query/cancel`, `query/dispose`, and `connection/disconnect`
* **Protected STS runtime** — each Leave No Trace query/database-discovery operation gets an isolated OS-temp sandbox and process. `TEMP`, `TMP`, home, app-data, cache, working, and log paths point into that sandbox. Cleanup disconnects/disposes the STS owner, stops or kills the process, recursively deletes the sandbox before returning results, and removes abandoned dead-process sandboxes on the next activation.

Every query execution gets a unique owner URI. The public `QueryResult`, host/webview messages, and `.kqlx`/`.sqlx` formats remain shared with Kusto and unchanged. STS results are converted before entering shared table, chart, transformation, HTML, comparison, and persistence paths.

### SQL Editor Lifecycle Boundary

`sqlStsEditorLanguageProtocol.ts` wraps the five STS lifecycle routes and three host deliveries around the preserved HST-41 application boundary. Every local sender, webview transport, host handler, coordinator delivery, central dispatcher, and direct router validates before its own effects. `stsResponse.result` and marker records remain opaque; dense diagnostics, exact owner tokens/generations, ready/error branches, and optional complete tool-owner proofs are structural protocol concerns, while lifecycle currentness, runtime replay, principal/privacy checks, deadlines, and canonical wrong-owner settlement remain with their existing owners.

`QueryEditorProvider` remains the webview and cross-language adapter. `HostSqlEditorLifecycleApplicationHandler` owns exact admission and settlement for `sqlSectionOpen`, `retireSqlTarget`, both development-auth routes, and all five STS request/document/connect routes; the provider only constructs/injects, reference-identically offers, awaits, and disposes it. `SqlEditorLifecycleCoordinator` retains every section, target, STS document/request/connect, replay, principal, policy, and runtime algorithm, while `sqlAuthState.ts` retains account-map/token helper behavior. The provider also constructs, forwards to, and disposes `HostSqlSchemaRequestApplicationHandler`; both webview `prefetchSqlSchema` and lifecycle-triggered refreshes use that exact owner. `HostSqlConnectionsProjectionApplicationHandler` owns `getSqlConnections`, every lifecycle/execution/Copilot refresh, revision ordering, profile-state reads, canonical SQL owner joining, cache/favorites filtering, and `sqlConnectionsData` publication. The parallel `HostKustoConnectionsProjectionApplicationHandler` owns every Kusto snapshot trigger, state read, physical projection, policy-locked `connectionsData` publication, retry, and disposal. `HostPersistedResultSanitizationApplicationHandler` owns synchronous, fresh, fail-closed, and lock-held persisted-result sanitation plus both invalidation events; the provider preserves its four public sanitation methods and two event properties only as exact delegates for native KQLX and compatibility callers. Manual execution and cancellation are delegated to `HostSqlSectionExecutionApplicationHandler`; SQL single-connection prompting and intake are delegated to `HostSqlConnectionOnboardingApplicationHandler`; Query Editor SQL favorites are delegated to `HostSqlFavoritesApplicationHandler`; SQL database discovery is delegated to `HostSqlDatabaseDiscoveryApplicationHandler`; cross-engine comparison preparation is delegated to `HostComparisonPreparationApplicationHandler`; and editor-local identity and sequencing remain delegated to `SqlEditorLifecycleCoordinator`.

HST-41 qualification recorded the required red-first real-provider failure at zero structural-handler calls, then passed reference-identical forwarding and held `stsDidChange` settlement with no direct lifecycle, auth-helper, or transport effect. The definitive focused/adjacent SQL lifecycle, STS language/runtime/query, development-auth, protocol, webview-routing, provider-inventory, and disposal ring passes 18 files and 442 tests with `--maxWorkers=1`. Complete sequential and official coverage Vitest pass 308 files and 6,563 tests at 48.22% statements. VS Code 1.132.0 extension-host passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, clean diagnostics, clean `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,155.8 KB host and 3,020.6 KB webview within unchanged synchronized baselines and the fixed 50 KB buffer. Native E2E was not required because controls, message shapes, coordinator/STS/auth algorithms, routing, and rendering are unchanged. Reviews returned `VERDICT: NO HST-41 IMPLEMENTATION BLOCKER` and definitive `VERDICT: NO HST-41 BLOCKER`.

`HostKustoSchemaRequestApplicationHandler` owns exact admission, argument shaping, settlement, and disposal for `prefetchSchema` and `requestCrossClusterSchema`. `QueryEditorProvider` reference-identically offers both messages, awaits claimed work, and has no direct application switch cases or direct schema-service calls. `SchemaService` retains every schema, cache, request, physical-owner, response, and publication algorithm. PRO-5 adds `kustoSchemaProtocol.ts` around that preserved application boundary: malformed recognized requests are claimed and dropped before service effects, every service delivery is typed, and malformed recognized host deliveries are dropped at dispatcher entry before primary or supplemental schema effects. Opaque raw-schema data plus editor, synthetic, legacy, cache, coordinator, and worker identities are preserved. PRO-6 applies the same bounded contract to Kusto database discovery through `kustoDatabaseDiscoveryProtocol.ts` while preserving `HostKustoConnectionBrowsingApplicationHandler`, `ConnectionService`, and all inner database identities and algorithms. PRO-7 applies it to SQL database discovery through `sqlDatabaseDiscoveryProtocol.ts` while preserving `HostSqlDatabaseDiscoveryApplicationHandler` and every SQL lifecycle, owner, protected-runtime, cache, retry, terminal, and disposal algorithm. PRO-8 applies it to SQL schema traffic through `sqlSchemaProtocol.ts` while preserving HST-37 reservation/supersession, lifecycle owner admission, `SqlSchemaService` cache/currentness, Cached Values, and STS lifecycle algorithms. PRO-9 applies it to KQL language requests and responses through `kqlLanguageProtocol.ts` while preserving analysis, resolver deadlines, and Fully Qualify admission. PRO-10 applies it to control-command syntax requests and results through `controlCommandSyntaxProtocol.ts` while preserving Learn acquisition, the 24-hour cache, caret-doc deduplication/presentation, and the no-timeout contract. HST-42, HST-31, HST-20, HST-21, HST-37, and HST-7 remain the application-ownership baselines; PRO-5 through PRO-10 qualification is recorded in the convergence ledger.

PRO-11 applies the same bounded protocol contract to resource URI requests and results through `resourceUriProtocol.ts` across main and plain-Markdown adapters while preserving both path algorithms, exact two-second deadlines, rendering, and browser behavior.

PRO-12 applies the same bounded protocol contract to URL content requests and deliveries through `urlContentProtocol.ts` while preserving host acquisition, URL-section rendering and runtime retention, immutable CSV artifacts, imported CSV saving, and browser static behavior.

PRO-13 applies it to Python request/terminal traffic through `pythonExecutionProtocol.ts`; PRO-14 applies it to artifact CSV intent/transfer traffic through `artifactCsvSaveProtocol.ts`; PRO-15 applies it to the SQL Tools Service editor-language channel through `sqlStsEditorLanguageProtocol.ts`; and PRO-16 applies it to SQL connection snapshot traffic through `sqlConnectionsProjectionProtocol.ts`. Each slice preserves its existing process, execution, lifecycle, policy, rendering, and inner-correlation owners.

HST-39 qualification passes the final 14-file focused sanitation/privacy/persistence/artifact/lifecycle/protocol ring at 843/843 tests and the adjacent native KQLX plus compatibility sidecar extension-host ring at 159/159 with `--timeout 5000`. The first adjacent run had four timing-sensitive failures; each passed unchanged alone in 104–369 ms, and the complete unchanged rerun passed. Full sequential and official coverage Vitest pass 304 files and 6,543 tests at 48.24% statements; VS Code 1.132.0 extension-host passes 215/215. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and integrated/standalone bundle gates pass. Final production sizes are 2,153.7 KB host and 3,020.6 KB webview within unchanged baselines. Native E2E was not required because file formats, persistence callers, policy stores, lifecycle algorithms, message shapes, controls, and rendering are unchanged. The definitive blocker review returned `VERDICT: NO HST-39 BLOCKER`.

SQL schema cache generation is part of publication authority. A request-current callback participates in `SqlSchemaService` disk publication and memory admission, so aborting a superseded request is not the only fence. If stale post-rename cleanup cannot delete the canonical file, the shared generation advances while the generation lock is held; readers using either a fresh or previously captured generation reject that file. Cached Values holds the same generation through Kusto-then-SQL owner admission and staged/applied webview acknowledgement for schema and snapshot successes. SQL Clear All retires older snapshot revisions before mutation, advances schema generation even if database-cache clearing fails, and publishes one fresh snapshot or an explicit pending-state terminal.

The coordinator composes, but does not replace, the canonical authorities:

* `SqlEditorSessionRegistry` owns targets, monotonic generations/tombstones, comparison lineage, canonical result owners, and owner tokens.
* `SqlExecutionBroker` owns preflight/admission, exact execution IDs, cancellation, currentness, and guarded release over the provider's shared `QueryRunCoordinator`.
* `SqlWorkbenchService` remains extension-scoped and owns connections, principals, Leave No Trace policy, shared STS runtime, query service, and client.
* `StsLanguageService` owns the per-editor JSON-RPC document protocol. The coordinator owns when documents open, change, close, and replay.

Target changes cancel work and exact-close the old STS owner inside the registry's pre-commit callback, while the old token and target are still current. Runtime replacement recreates language state without rotating the logical target generation. Leave No Trace invalidation cancels correlated work before revoking tokens and publishing policy, then closes shared language state and issues an execution-only token only when the final target remains protected.

The coordinator receives provider effects as callbacks and never imports `QueryEditorProvider`, `CopilotService`, the extension entry point, or document providers. This prevents panel-scoped state from flowing into extension-scoped services and keeps lifecycle tests independent of VS Code webview construction.

SQL comparison preparation is a request-ID-scoped, application-acknowledged transaction owned by `HostComparisonPreparationApplicationHandler`. The webview snapshots the complete live descriptor, query text/revision, result revision, persisted rows and artifact, and active execution identity, then advances only through `staged`, `committed`, `finalized`, and `completed` acknowledgements. Admission makes the comparison read-only, fences delayed Monaco unlocks, and cancels/tombstones exact active work. Before completion, failure retries an acknowledged rollback to the unchanged snapshot; after completion, late rollback is rejected. The handler preserves the previous comparison owner and token until the completed acknowledgement, then revalidates both source and comparison targets/generations before installing derived ownership. Completed and rolled-back proofs are released explicitly. Nested comparison sources and duplicate section identities reject before mutation.

### Compatibility Sidecars

Plain `.kql`/`.csl` and `.sql` editors share one compatibility-sidecar architecture while retaining language-specific adapters:

* **`CompatSidecarFormat`** owns linked-path resolution, exact linkage validation, primary-text hydration, and canonical sidecar serialization.
* **`CompatSidecarStore`** owns established-sidecar write serialization, cross-window locks, compare-and-swap publication, three-attempt repair, recovery files, and drain barriers.
* **`CompatSidecarSession`** owns revision admission, persist sequencing, upgrade barriers, dirty baselines, correlated final snapshots/reloads, beforeunload observation, close transition primitives, and waiter settlement.
* **`CompatSidecarProjectionCoordinator`** owns monotonic projection generations, generation-zero compatibility before first acknowledgement, active/pending source identity, same-source pending supersession, bounded initial retry/coalescing with one follow-up, reload acknowledgement currentness, `persistDocument` source-generation admission, source-reload authority, and terminal rollback-failure fencing. A current failed reload demotes initialization so the next request re-enters bounded recovery; a superseded failure cannot demote a successful successor.
* **`CompatSidecarPersistCoordinator`** owns the complete post-protocol `persistDocument` application transaction: live final-request admission, load/unavailable/noop/closing/stale terminals, two-phase validation across upgrades, projection-generation and session-queue admission, revision timing, sanitation/materialization sequencing, EOL and blank-file policy, source verification and projection-owned rollback, retryable-draft retention, state/materialized commit ordering, one live acknowledgement attempt, beforeunload observation, and exactly-once final settlement. It directly composes the existing session and projection coordinators and creates no queue, waiter, revision store, rollback authority, or durable write path.
* **`CompatSidecarCloseCoordinator`** owns panel disposal admission, delayed-beforeunload and correlated-final-snapshot retirement eligibility, exactly-once close startup, final-persist/beforeunload ordering, stable gateway-admission closure, session persist/upgrade drain, subscription disposal, dirty Save/Discard, exact attempted-draft recovery, final repair, exact store drain, initialization-failure cleanup, and terminal settlement.
* **`CompatibilityPersistenceProtocol`** owns the six KQL/SQL lifecycle discriminators, runtime schemas, host-created panel envelope, request/projection correlation, and both-direction stamping. Validation occurs before revision adoption, persist admission, primary-text or sidecar mutation, final-persist settlement, or reload settlement.
* **Providers** inject language-specific projection construction and typed physical persist effects, and retain primary-text edit mechanics, sidecar materialization/publication, KQL inference/connection caching, language-specific sidecar creation/adoption prompts, diff UI, protocol names, user-facing copy, and durable Save/recovery/repair/notification state updates. They cannot admit revisions or generations, queue a snapshot, decide transaction success, acknowledge, settle final waiters, or invoke projection rollback. They resynchronize session visibility immediately before installing the view-state listener, but decide neither projection/reload currentness nor the close transaction.

Compatibility files reserve stable primary IDs (`compat_primary_query` and `compat_primary_sql`) and admit exactly one structurally valid primary section. Projection currentness compares exact source text; rollback recovery retains its established EOL-normalized comparison. Candidate sidecars must preserve primary identity and ordering and pass destination-kind validation before a primary-text edit is applied. Load, create, adoption, repair, and publication retain the exact accepted physical sidecar identity. If final sanitation fails transiently, a metadata-only draft is retained only when its primary text still equals the source, so a later Save can retry without publishing a mismatched companion.

CMP-1 qualification passes the final seven-file focused owner/session/store/gateway/protocol/nested-lifecycle ring at 227/227 tests and the complete parameterized compatibility matrix at 134/134 with `--timeout 5000`. Full sequential and official coverage Vitest pass 294 files and 6,338 tests at 48.33% statements; the complete VS Code 1.132.0 extension-host suite passes 213/213 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, and both bundle gates pass. Final production sizes are 1,948.7 KB host and 2,816.9 KB webview; synchronized baselines are 1,899/2,767 KB with the 50 KB buffer unchanged. The host increase belongs to CMP-1; the webview baseline synchronization includes disjoint externally committed autocomplete work at `747199c07b74d17de16d939094aafacf32c72250`. CMP-1 was later committed locally at `ff178e9025c803a1923ab19654670e1bb73f7e40` and has not been pushed. No native E2E was required because CMP-1 leaves controls, rendering, message shapes, primary-text behavior, and sidecar formats unchanged; the real-provider extension-host matrix covers the migrated close boundary, including hidden-to-visible startup resynchronization. A permanently non-settling external `QueryEditorProvider.initializeWebviewPanel()` promise remains an upstream startup-cancellation residual; rejected initialization and every admitted close path settle through the coordinator.

CMP-2 qualification passes the final eight-file projection/session/store/format/close/protocol/provider-guard ring at 175/175 tests and the complete parameterized compatibility matrix at 135/135 with `--timeout 5000`. Full sequential and official coverage Vitest pass 332 files and 6,875 tests at 49.27% statements. The clean complete VS Code 1.133.0 extension-host rerun passes 217/217 with `--timeout 5000` and `--fail-zero`; the first run had one unchanged linked-notebook Save timing failure, which passed alone before the complete rerun. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, `git diff --check`, and the synchronized bundle gates pass. Final production sizes are 2,206.7 KB host, 3,114.2 KB main webview, 310.6 KB Markdown webview, and 19,058.7 KB total `dist/`. Intentional CMP-2 host growth moves only the synchronized host baseline from 2,153 to 2,157 KB; main/Markdown baselines remain 3,065/261 KB and the fixed 50 KB buffer is unchanged. No native E2E was required because controls, valid message shapes, file formats, primary-text behavior, and rendering are unchanged while real KQL/SQL providers directly cover generation, recovery, Save, rollback, and close behavior. The definitive implementation and documentation-aware reviews returned `VERDICT: NO CMP-2 BLOCKER` and `VERDICT: NO CMP-2 DOCUMENTATION BLOCKER`.

CMP-3 qualification passes the direct 17-test terminal matrix and the nine-file coordinator/session/projection/close/store/format/protocol/provider/inventory ring at 195/195 tests. The complete parameterized VS Code 1.133.0 extension-host suite passes 218/218 with `--timeout 5000` and `--fail-zero`, including the required real KQL/SQL provider gate, Save, upgrades, source races, retryable drafts, and close behavior. Complete sequential and official coverage Vitest pass 335 files and 6,923 tests at 49.27% statements. Strict host/webview/browser types, integration compilation, production extension and strict browser builds, ESLint with zero errors and five existing warnings, task-owned diagnostics, `git diff --check`, zero production source maps, and both synchronized bundle gates pass. Final sizes are 2,218.7 KB host, 3,125.2 KB main webview, 317.3 KB Markdown webview, and 19,088.4 KB total `dist/`. Intentional CMP-3 growth moves the host baseline from 2,168 to 2,169 KB; a preserved concurrent main-webview change independently moves its synchronized baseline from 3,074 to 3,076 KB. The fixed 50 KB buffer is unchanged. Native E2E was not required because controls, valid message shapes, file formats, primary-text semantics, and rendering are unchanged while every migrated terminal and both real providers are directly covered. The implementation blocker review returned `VERDICT: NO CMP-3 IMPLEMENTATION BLOCKER`.

PRO-3 qualification passes the final twelve-file protocol/webview/persistence/format/session/store/coordinator/gateway ring at 499/499 tests and the complete parameterized compatibility matrix at 134/134 with `--timeout 5000`. Full sequential and official coverage Vitest pass 295 files and 6,352 tests at 48.38% statements; the complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and both synchronized bundle gates pass. Final production sizes are 1,957.7 KB host and 2,826.6 KB webview; synchronized baselines are 1,908/2,777 KB with the 50 KB buffer unchanged. The definitive blocker review returned `VERDICT: NO PRO-3 IMPLEMENTATION BLOCKER`. Native E2E was not required because controls, rendering, file formats, primary-text behavior, and provider ownership remain unchanged; real-provider extension-host and bundled webview boundary tests cover the migrated lifecycle channel.

PRO-4 qualification passes the final six-file preload/persistence/dispatcher/capability/protocol/startup ring at 410/410 tests. Full sequential and official coverage Vitest pass 299 files and 6,506 tests at 48.13% statements; the complete VS Code 1.132.0 extension-host suite passes 215/215 with `--timeout 5000`. Strict host/webview/browser types, integration compilation, production extension and browser builds, ESLint with zero errors and five existing warnings, diagnostics, `git diff --check`, and both synchronized bundle gates pass. Final production sizes are 2,147.5 KB host and 3,020.6 KB webview within unchanged baselines and the 50 KB buffer. The red-first real-preload regression proves one Markdown Add, zero default Query sections, both queues cleared, and no duplication after repeated takeover or finalization. The definitive review returned `VERDICT: NO PRO-4 BLOCKER`. Native E2E was not required because controls, message shapes, startup transport, and section behavior remain unchanged.

### Webview Components

* **`kw-sql-section`** — hybrid light/shadow DOM Lit component (mirrors `kw-query-section`): Monaco editor, server+database dropdowns, action bar with Run/Cancel, results table (`kw-data-table`), Copilot chat pane
* **`kw-sql-toolbar`** — light DOM toolbar: Undo, Redo, Comment, Prettify (`sql-formatter`), Search, Replace, Copilot toggle
* **`sql-copilot-chat-manager.controller.ts`** — ReactiveController managing Copilot chat lifecycle for SQL sections

### Copilot Flavor System

Both Kusto and SQL share the same `CopilotChatManagerController` and `CopilotService` infrastructure. Differences are captured in flavor objects:

* **Host-side:** `CopilotChatFlavor` in `copilotChatFlavor.ts` — `kustoCopilotFlavor` / `sqlCopilotFlavor`. Controls role, language, rules file, feature flags
* **Webview-side:** `WebviewCopilotFlavor` in `copilot-chat-flavor.ts` — `kustoWebviewFlavor` / `sqlWebviewFlavor`. Controls DOM IDs, message types, tool names, CSS classes

SQL Copilot rules: `copilot-instructions/sql-query-rules.md`, optimization rules: `copilot-instructions/optimize-sql-rules.md`

### Agent Tools

4 SQL-specific tools registered in `kustoWorkbenchTools.ts`: `list-sql-connections`, `configure-sql-section`, `get-sql-schema`, `ask-sql-copilot`. The `add-section` tool also accepts `'sql'` as a type. The `list-sections` tool returns `serverUrl` for SQL sections (instead of `clusterUrl`).

### Key Patterns

* SQL events use `sql-` prefix (e.g. `sql-connection-changed`, `sql-database-changed`)
* SQL state is separate: `sqlConnections` / `sqlCachedDatabases` in `state.ts`
* Connection resolution matches by `serverUrl` (lowercase) instead of Kusto's hostname normalization
* SQL Tools Service is downloaded on first SQL use; it is not bundled in the VSIX
* Data-plane TLS uses mandatory encryption with certificate validation; language connections preserve their existing compatibility policy
* SQL Leave No Trace policy is versioned in extension global storage, serialized with a filesystem lock, atomically replaced, and watched by every extension host so toggles propagate across VS Code windows
* SQL Leave No Trace keeps manual queries and database discovery usable through the protected one-shot STS runtime. Shared language STS, schema/cache publication, Copilot execution, durable result state, restoration, and data-bearing output logs remain blocked. Results render in memory only and are admitted after sandbox deletion.
* A terminally exhausted STS manager is replaced by `StsRuntime`; open editors recreate language services and replay their latest document/target state against the replacement manager
* File format: `.kqlx` supports mixed Kusto+SQL; `.sqlx` supports SQL plus derived and presentation sections, but not Kusto query sections

## Diagnostic Codes

Custom diagnostics use codes like:

* `KW_EXPECTED_PIPE` — Missing pipe operator
* `KW_UNKNOWN_COLUMN` — Column not found in schema
* See `service.ts` for the full list

## Error Message Formatting

User-facing errors are formatted via `formatQueryExecutionErrorForUser()` in `queryEditorUtils.ts`. This converts raw Kusto errors into actionable, user-friendly guidance. The function is pure (takes an error message string, cluster URL, and optional database name) and is independently testable.

## Popup & Dropdown Dismiss-on-Scroll Policy

All floating UI elements (popups, dropdowns, menus, tooltips) are **dismissed on scroll**, never anchored to move with the viewport. This matches VS Code's native behavior.

| Category | Behavior | Examples |
| -------- | -------- | -------- |
| **Ephemeral** | Close immediately on any scroll | Monaco autocomplete, context menus, tooltips, caret docs, hover info |
| **Interactive** | Close when scroll exceeds **20px threshold** | Dropdowns (favorites, clusters, databases, chart type, chart columns), cache settings, run-mode menu, tools menu, share modal |
| **Never anchor** | Never attempt to reposition a popup to follow scroll | All categories — anchoring is explicitly prohibited |

### Why Not Anchor?

Anchoring (repositioning popups on every scroll frame) was considered and rejected because:
- The webview scroll container's rendering pipeline is not under our control, causing visible lag
- `requestAnimationFrame` repositioning still produces janky movement on fast scrolls
- The threshold-based dismiss approach provides a better UX with far less complexity
- VS Code's own dropdown menus use the same dismiss-on-scroll pattern

## Responsive Layout (CSS Container Queries)

The query section header toolbar uses **CSS Container Queries** for responsive layout, not JavaScript. This ensures correct layout immediately when sections are added, without race conditions.

### Breakpoints

Defined in `queryEditor.css` on `.query-header-row-bottom` (which has `container-type: inline-size`):

| Container Width | Layout Mode | Behavior |
| --------------- | ----------- | -------- |
| > 420px | Full | Dropdowns show icon + text |
| ≤ 420px | Minimal | Dropdowns collapse to icon-only (32px) |
| ≤ 200px | Ultra-compact | Also hides refresh, favorite, and schema buttons |

### Why Not JavaScript?

Previously, a 500ms `setInterval` polled element widths using `getBoundingClientRect()`. This caused a race condition: if the timer fired while a newly-added section was in the DOM but not yet laid out (width = 0), incorrect styles were applied. CSS Container Queries are synchronous with layout, eliminating this issue.

### Legacy Classes

The `.is-minimal` and `.is-ultra-compact` classes are still supported in CSS for backwards compatibility, but JavaScript no longer adds them. The container queries handle everything automatically.

## Leave No Trace

"Leave no trace" is a privacy feature that allows users to mark specific Kusto clusters as sensitive. When a cluster is marked:

* **Query results are not persisted**: Tabular results from queries executed against these clusters are never saved to `.kqlx` files or session storage
* **Derived data is also excluded**: Any data derived from query results (chart previews, transformations, etc.) is not persisted
* **Configuration is preserved**: Section configurations (query text, chart settings, etc.) are still saved—only the data itself is excluded

### Implementation

* **Storage**: The canonical policy is an atomically committed, file-locked snapshot under extension global storage; `kusto.leaveNoTraceClusters` is maintained as a legacy compatibility mirror
* **Execution fencing**: Effective policy changes advance a per-cluster revocation generation, propagate across extension hosts, revoke active runs for matching connections before refreshed snapshots, and guard both dispatch and result admission under the shared lock
* **Connection Manager UI**: Clusters section shows a "Mark as Leave no trace" action on hover. A dedicated "Leave No Trace" accordion section displays marked clusters.
* **Persistence Logic**: Before saving, check if a query section's `clusterUrl` matches a leave-no-trace cluster. If matched, strip `resultJson` from that section. Also strip data from chart/transformation sections that reference such query sections.

Key files: `connectionManagerViewer.ts`, `connectionManager.ts`, `core/persistence.ts`, `persistedResultSanitizationApplicationHandler.ts`.

## Section Resize, Max Height & Fit-to-Contents

Applies to all section types that contain a Monaco editor and show tabular results (Kusto query sections and SQL sections). Three concepts govern the resize behavior:

### Max Heights (Absolute Ceilings)

These ceilings cannot be exceeded by any operation — not manual sash dragging, not fit-to-contents, not auto-resize.

| Concept | Definition |
| ------- | ---------- |
| **monaco-editor-max-height** | 750px. The absolute maximum height of the Monaco editor wrapper, regardless of content length. |
| **section-max-height** (results area) | If tabular results are visible: the height needed to display all data rows plus the 10px gap (`padding-bottom` on `.results-wrapper`). If no tabular results: the results sash is disabled. |

### Fit-to-Contents & Double-Click on Resize Sashes

Fit-to-contents and double-clicking the resize sashes are **different entry points to the same logic**. They always produce the same result.

- **Double-click on the editor sash** → fit the Monaco editor to the height needed to display all rows without a scrollbar, or `monaco-editor-max-height` (750px), whichever is smaller.
- **Double-click on the results sash** → fit the results area to `section-max-height` or 750px, whichever is smaller.
- **Fit-to-contents button** (on the section shell) → equivalent to double-clicking the editor sash and then the results sash. When tabular results are hidden, only the editor is adjusted.

### Manual Sash Drag

- **Editor sash**: capped at `monaco-editor-max-height` (750px). The user cannot drag the editor wrapper beyond 750px.
- **Results sash**: capped at `section-max-height` (content height + 10px gap). The user can drag up to the full content height but not beyond it.

### Auto-Resize (Grow-Only)

The editor wrapper grows automatically as the user types, up to `monaco-editor-max-height` (750px). It never shrinks below the current height (to avoid jarring collapses). Auto-resize is disabled once the user manually resizes via the sash.

### Key Constants

| Constant | Value | Location | Purpose |
| -------- | ----- | -------- | ------- |
| `FIT_CAP_PX` | 750 | `section-factory.ts`, `kw-sql-section.ts`, `resize.ts` | `monaco-editor-max-height` and fit-to-contents cap for results |
| `FIT_SLACK_PX` | 5 | `section-factory.ts`, `resize.ts` | Extra pixels below editor content |
| `GAP_PX` | 10 | CSS `padding-bottom` on `.results-wrapper` | Gap between table end and section end |

Key files: `core/section-factory.ts`, `monaco/monaco.ts`, `monaco/resize.ts`, `sections/kw-sql-section.ts`, `sections/kw-query-section.ts`.

## Copilot Chat Feature

The Copilot Chat feature integrates with an LLM via VS Code's Copilot API. The UX is a chat window alongside the query editor (Kusto or SQL), inside a section. It has a main content view for the conversation, a textbox for user input, and Send/Cancel buttons. The header includes a Clear button to reset conversation history.

The LLM has access to two categories of tools:

* **Optional**: Tools the LLM can choose to use zero or multiple times
* **Final Step**: Tools the LLM must use to provide its response

### Conversation History Management

1. Each Kusto section maintains its own independent conversation history.
2. At the start of every conversation (and after reset), the contents of `copilot-instructions/general-query-rules.md` are included with the first user request. This is displayed with distinctive styling and hover/click actions.
3. Every message, response, and tool call is remembered and included in subsequent messages, unless the user explicitly removes a tool call result or clears the entire history.
4. Tool calls are represented with consistent styling, an inspection action, and the ability to remove the response from history.
5. Notifications not part of the conversation history have distinct styling to indicate the LLM does not see them.
6. Each user message includes the current Kusto query from the editor (if non-blank), displayed with styling similar to tool calls and removable from history.

## Copilot Integration

The extension integrates with VS Code's Copilot APIs for query generation (`startCopilotWriteQuery`) and query optimization (`optimizeQuery`).

### Local Tools (via `getCopilotLocalTools()`)

* `get_extended_schema`
* `get_query_optimization_best_practices` (reads `optimize-query-rules.md`)
* `execute_kusto_query`
* `respond_to_query_performance_optimization_request`
* `respond_to_all_other_queries`
* `ask_user_clarifying_question`

### VS Code Agent Tools (via `registerKustoWorkbenchTools()`)

Tools are contributed in `package.json` and registered with `vscode.lm.registerTool()` in `kustoWorkbenchTools.ts`; the manifest `name` must match the registration ID. Current registrations cover connection/schema discovery, section lifecycle and configuration, query/chart/transformation/HTML/SQL configuration, delegation to Kusto or SQL Copilot, file creation, and development notes.

HTML dashboard-relevant tools include:

| Tool ID | Tool Reference Name | Purpose |
| ------- | ------------------- | ------- |
| `kusto-workbench_add-section` | `addSection` | Adds notebook sections, including `html` sections |
| `kusto-workbench_configure-html-section` | `configureHtmlSection` | Sets HTML section code/name/mode; dashboard prompts should use `application/kw-provenance` and `data-kw-bind` |

Schema-specific tools still include `kusto-workbench_refresh-schema` and `kusto-workbench_search-cached-schemas`, but they are no longer the full agent tool surface.

## Dependencies

* `@kusto/monaco-kusto` — Monaco Editor KQL language support
* `azure-kusto-data` — Official Kusto client SDK
* `monaco-editor` — Code editor
* `@toast-ui/editor` — WYSIWYG markdown editor
* `echarts` — Charting library
* `vscode-jsonrpc` — JSON-RPC protocol for STS communication
* Microsoft SQL Tools Service — downloaded and integrity-checked on first SQL use; provides IntelliSense and query execution

## Test Coverage

Tests are organized under `tests/`:

* **Vitest unit tests** (`tests/webview/`): Fast tests that run without VS Code. Covers webview components, shared utilities, and pure host-side logic.
  - `tests/webview/` — webview component and utility tests
  - `tests/webview/host/` — pure host-side logic (no VS Code dependency) tested via Vitest
* **Integration tests** (`tests/integration/`): Run inside VS Code's extension host. Reserved for tests that genuinely need VS Code APIs (webview panel faking, filesystem via `vscode.workspace.fs`, compiled-output extraction).

### Host-side pure utility tests (`tests/webview/host/`)

| Test File | Coverage |
| --------- | -------- |
| `kustoClientUtils.test.ts` | Cell formatting, error classification, schema JSON parsing |
| `queryEditorUtils.test.ts` | Error message formatting, control command detection, query mode, cache directives |
| `kqlxEditorUtils.test.ts` | State normalization, deep equality, section sanitization |
| `copilotPromptUtils.test.ts` | Prompt template building, tool definition enumeration |
| `copilotConversationUtils.test.ts` | Conversation history sanitization, tool call result insertion |
| `queryEditorConnection.test.ts` | URL normalization, connection naming, cluster key generation |
| `kqlSchemaInference.test.ts` | Canonical physical-table and masked function tokens for schema matching |
| `kqlSourceAnalysis.test.ts` | Exact-layout masking, statements/management spans, lexical scopes, physical references, immutability, and scanner ownership |
| `kqlxFormat.test.ts` | `.kqlx` file parsing, serialization, creation |
| `schemaIndexUtils.test.ts` | Schema formatting, column counting, token-budget pruning |
| `kqlDiagnostics.test.ts` | KQL error detection, lexical table/column scopes, pipe operator validation, and statement splitting |
| `kql-table-reference-ranges.test.ts` | Fail-closed exact range admission and replacement for Fully Qualify |
| `message-protocol.test.ts` | Host↔webview message type alignment, payload shape contracts, including dashboard export/publish messages |
| `power-bi-publish-protocol.test.ts` | Exact frozen Power BI publish success/failure/acknowledgement construction, descriptor/prototype/accessor/proxy rejection, optional DirectQuery fields, one-shot envelope authority, and sole contract ownership |
| `queryEditorProviderDashboardHandler.test.ts` | Real-provider dashboard handler injection/forwarding and static displaced-authority guard |
| `queryEditorProviderQuerySharingHandler.test.ts` | Real-provider query-sharing injection/forwarding and static displaced-authority guard |
| `querySharingApplicationHandler.test.ts` | ADX encoding, Kusto/SQL rich/plain formatting, escaping, validation, notifications, clipboard failures, synchronous decline, and disposal |
| `queryEditorProviderUrlContentHandler.test.ts` | Real-provider URL-content injection/forwarding and static displaced-authority guard |
| `urlContentApplicationHandler.test.ts` | URL validation, redirect identity, HTTP/fetch errors, classification/sniffing, truncation, byte caps, timeout/abort, synchronous decline, and disposal |
| `url-content-protocol.test.ts` | Exact URL request/delivery identity, empty text bodies, exclusive success branches, canonical errors, malformed recognized traffic, and sole runtime parser behavior |
| `queryEditorProviderControlCommandSyntaxHandler.test.ts` | Real-provider control-command syntax injection/forwarding and static displaced-authority guard |
| `controlCommandSyntaxApplicationHandler.test.ts` | Learn URL normalization, syntax/entity extraction, `with(...)` parsing, exact cache expiry/failure behavior, synchronous decline, and disposal/late-settlement suppression |
| `queryEditorProviderResourceUriHandler.test.ts` | Real-provider resource URI injection/forwarding and static displaced-authority guard |
| `resourceUriApplicationHandler.test.ts` | Passthrough schemes, local-base validation, Windows/POSIX/workspace path behavior, exact cache/stat/panel ordering, failures, transport containment, and disposal/late-settlement suppression |
| `resource-uri-protocol.test.ts` | Exact resource request/result identity, optional/empty values, canonical branches, malformed recognized traffic, and sole runtime parser behavior |
| `queryEditorProviderCopilotContentOpenHandler.test.ts` | Real-provider exact-object forwarding and static displaced-authority guard for both Copilot content-open messages |
| `copilotContentOpenApplicationHandler.test.ts` | Exact content coercion, untitled plaintext/beside options, Markdown URI/command dispatch, failures, synchronous decline, disposal, and accepted in-flight native effects |
| `queryEditorProviderInformationNotificationHandler.test.ts` | Real-provider exact-object information-notification forwarding and displaced-authority guard |
| `informationNotificationApplicationHandler.test.ts` | Exact notification text/thenable/throw behavior, synchronous decline, and disposal |
| `queryEditorProviderCachedValuesOpenHandler.test.ts` | Real-provider exact-object Cached Values forwarding and command-owner guard |
| `cachedValuesOpenApplicationHandler.test.ts` | Exact zero-argument command dispatch, settlement, synchronous decline, and disposal |
| `queryEditorProviderCopilotAgentOpenHandler.test.ts` | Real-provider exact-object Agent Chat forwarding, constructor-slot, settlement, helper-caller, and emitter guards |
| `copilotAgentOpenApplicationHandler.test.ts` | Exact zero-argument helper delegation, settlement, synchronous decline, and disposal |
| `queryEditorProviderEditorCursorStatusHandler.test.ts` | Real-provider exact-object cursor update/snapshot forwarding, visibility lifecycle, and displaced-authority guards |
| `editorCursorStatusApplicationHandler.test.ts` | Visibility, owner identity/prefix, snapshot transport, fallback, stale-owner, and disposal contracts |
| `queryEditorProviderEditingPreferencesHandler.test.ts` | Red-first real-provider exact-object preference forwarding, awaited settlement, constructor-slot, initial-projection/configuration-owner, emitter, and displaced-authority guards |
| `editingPreferencesApplicationHandler.test.ts` | Exact preference key mapping, malformed-request rejection before writes, boolean preservation, validated authoritative publication identity, live orchestrator and current-panel paths, failures, synchronous decline, and disposal contracts |
| `queryEditorProviderKustoConnectionOnboardingHandler.test.ts` | Red-first real-provider exact-object onboarding forwarding, awaited settlement, displaced-authority, preserved-owner, emitter, and disposal guards |
| `kustoConnectionOnboardingApplicationHandler.test.ts` | Prompt, add validation/normalization/account/selection/refresh ordering, draft testing, tracing, exact terminals, transient-auth fallback, and disposal contracts |
| `queryEditorProviderSqlConnectionOnboardingHandler.test.ts` | Red-first real-provider exact-object SQL onboarding forwarding, awaited settlement, displaced-authority, preserved-owner, emitter, and disposal guards |
| `sqlConnectionOnboardingApplicationHandler.test.ts` | Exact AAD/SQL Login prompts, cancellations, credential separation, direct-form defaults, manager/selection/acknowledgement ordering, failures, and disposal contracts |
| `queryEditorProviderSqlFavoritesHandler.test.ts` | Red-first real-provider exact-object SQL favorites forwarding, awaited settlement, projection, displaced-authority, Connection Manager, emitter, and disposal guards |
| `sqlFavoritesApplicationHandler.test.ts` | Exact SQL favorite prompt/default/cancellation, sanitized mutation, keying/order/casing, persistence/publication, failure containment, and disposal contracts |
| `queryEditorProviderKustoFavoritesHandler.test.ts` | Red-first real-provider exact-object Kusto favorites forwarding, awaited settlement, read delegation, displaced-authority, external-caller, emitter, and disposal guards |
| `kustoFavoritesApplicationHandler.test.ts` | Migration, principal filtering, hidden-owner merge, prompts, mutation, unresolved-entry protection, cross-panel/listener publication, confirmation, resolver isolation, reactivation, and disposal contracts |
| `queryEditorProviderSqlDatabaseDiscoveryHandler.test.ts` | Red-first real-provider exact-object SQL discovery forwarding, awaited settlement, displaced-authority, preserved-owner, sender, response-router, and disposal guards |
| `sqlDatabaseDiscoveryApplicationHandler.test.ts` | Passive/refresh discovery, exact tickets and terminals, canonical target/principal/privacy admission, protected STS cleanup, cache sequencing/fallback, concurrent sections, transport retry, policy/principal races, logging, and disposal contracts |
| `queryEditorProviderSqlConnectionsProjectionHandler.test.ts` | Red-first real-provider exact-object SQL connection snapshot forwarding, all-trigger delegation, awaited settlement, zero direct profile/store/transport effects, displaced-authority, and disposal guards |
| `sqlConnectionsProjectionApplicationHandler.test.ts` | Exact payload and revision ordering, canonical target/principal joins, selective and global Leave No Trace filtering, failed-tail continuation, and disposal contracts |
| `queryEditorProviderPersistedResultSanitizationHandler.test.ts` | Red-first real-provider reference-identical sanitation/publication forwarding, held settlement, event/invalidation delegation, zero direct policy/lifecycle/connection/publication effects, displaced-authority, and disposal guards |
| `persistedResultSanitizationApplicationHandler.test.ts` | Exact admitted-owner preservation, fail-closed fallback, Kusto-then-SQL lock-held publication, invalidation events, disposal, and close-time sanitation contracts |
| `queryEditorProviderKqlLanguageRequestHandler.test.ts` | Red-first real-provider reference-identical KQL language request forwarding, deferred settlement, exact rejection, displaced-authority, preserved-owner, response-ledger, sender, constructor-slot, and disposal guards |
| `kqlLanguageRequestApplicationHandler.test.ts` | Exact diagnostics/reference delegation and correlation identity, malformed/unknown request rejection before analysis, typed success/fixed-failure responses, unawaited transport, logger failure, synchronous decline, and disposal contracts |
| `queryEditorProviderSqlLastSelectionHandler.test.ts` | Red-first real-provider reference-identical SQL last-selection forwarding, deferred settlement, exact rejection, zero direct provider writes, constructor-slot, preserved-owner, message-shape, and disposal guards |
| `sqlLastSelectionApplicationHandler.test.ts` | Connection trimming/admission, ordered application-state writes, undefined/empty database semantics, exact rejection, synchronous decline, and disposal contracts |
| `queryEditorProviderSqlEditorLifecycleHandler.test.ts` | Red-first real-provider reference-identical nine-route forwarding, held settlement, exact rejection, zero direct lifecycle/auth/transport effects, zero-case provider inventory, and disposal ownership |
| `queryEditorProviderKustoSchemaRequestHandler.test.ts` | Red-first real-provider reference-identical Kusto schema forwarding, held settlement, exact rejection, zero direct schema/transport effects, zero-case provider inventory, and disposal ownership |
| `kustoSchemaRequestApplicationHandler.test.ts` | Exact primary/cross-cluster schema argument shaping, lifecycle completeness, rejection, synchronous decline, and disposal contracts |
| `sqlEditorLifecycleApplicationHandler.test.ts` | Exact section/target and STS delegation, Production gating, ordered account-map/token writes, rejection, synchronous decline, and disposal contracts |
| `queryEditorProviderDevelopmentNoteMutationHandler.test.ts` | Red-first real-provider exact mutation delegation, reference-identical claimed-response offering, zero provider resolver-map effects, unclaimed tool-orchestrator delivery, constructor-slot, preserved-owner, and disposal guards |
| `development-note-mutation-protocol.test.ts` | Exact add/supersede/remove requests, correlated boolean response subset, one-shot descriptor admission, inherited/non-enumerable/accessor fields, canonical dense arrays, custom/revoked/cyclic/proxy traffic, and sole protocol ownership |
| `mainWebviewStartupGateway.test.ts` | Capture-first startup ordering, blocked-drain correlation, outer accessor/type-get/top-level and nested descriptor/prototype variation, frozen mutation-admission reuse, exact waiter/timer preservation, hidden-field artifact nonce preservation, canonical-only settlement, and queue/retirement behavior |
| `developmentNoteMutationApplicationHandler.test.ts` | Exact request IDs/outbound mutation, malformed matching waiter/timer preservation, inherited correlation, malformed producer rejection, unavailable and delivery failures, five-second timeout, late response behavior, and disposal settlement |
| `queryEditorProviderCopilotInlineCompletionHandler.test.ts` | Red-first real-provider reference-identical Kusto/SQL forwarding, deferred settlement, exact rejection, zero direct owner/Copilot/fallback effects, constructor-slot, preserved-owner, and disposal guards |
| `copilotInlineCompletionApplicationHandler.test.ts` | Descriptor-captured Kusto/SQL route admission, malformed-request suppression, issued SQL owner/token delegation, validated empty fallback, transport settlement, rejection, synchronous decline, and disposal contracts |
| `copilot-inline-completion-protocol.test.ts` | Exact request/result values, dense completion arrays, optional fields, hostile descriptors/prototypes/proxies, malformed traffic, and sole runtime parser behavior |
| `queryEditorProviderCopilotAvailabilityHandler.test.ts` | Red-first real-provider reference-identical availability forwarding, deferred settlement, exact rejection, zero direct Copilot/transport effects, constructor-slot, preserved-owner/caller/router/message-shape, and disposal guards |
| `copilotAvailabilityApplicationHandler.test.ts` | Exact availability delegation, settlement and rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotent disposal contracts |
| `queryEditorProviderCopilotWriteQueryPreparationHandler.test.ts` | Red-first real-provider reference-identical Kusto/SQL preparation forwarding, deferred settlement, exact rejection, zero direct Copilot/transport effects, constructor-slot, preserved-owner/caller/router/message-shape, and disposal guards |
| `copilotWriteQueryPreparationApplicationHandler.test.ts` | Exact Kusto/SQL preparation delegation, settlement and rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotent disposal contracts |
| `queryEditorProviderCopilotConversationClearHandler.test.ts` | Red-first real-provider reference-identical Kusto/SQL conversation-clear forwarding, deferred settlement, exact rejection, zero direct Copilot/transport effects, constructor-slot, preserved-state/caller/message-shape, and disposal guards |
| `copilotConversationClearApplicationHandler.test.ts` | Exact Kusto identity validation, Kusto/SQL delegation, metadata-free SQL compatibility, settlement and rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotent disposal contracts |
| `queryEditorProviderCopilotHistoryRemovalHandler.test.ts` | Red-first real-provider reference-identical Kusto/SQL-originated history-removal forwarding, deferred settlement, exact rejection, zero direct Copilot/transport effects, constructor-slot, preserved-state/shared-UI/message-shape, and disposal guards |
| `copilotHistoryRemovalApplicationHandler.test.ts` | Exact box/entry delegation, settlement and rejection, synchronous decline, disposal-crossing settlement, later suppression, and idempotent disposal contracts |
| `queryEditorProviderCopilotChatFirstTimeHandler.test.ts` | Red-first real-provider reference-identical first-time-check forwarding, deferred settlement, exact rejection, zero direct state/modal/helper/transport effects, constructor-slot, preserved caller/projection/router/message/helper owners, and disposal guards |
| `copilotChatFirstTimeApplicationHandler.test.ts` | Already-seen state, write-before-modal ordering, exact copy/actions, all choices, Agent Chat settlement/failure, result shaping, fire-and-forget transport, synchronous decline, and disposal contracts |
| `queryEditorProviderWorkbenchToolSessionHandler.test.ts` | Red-first real-provider three-route forwarding, HST-23 response priority, thin state delegation, constructor-slot identity, zero legacy/global effects, displaced-authority guards, and disposal ownership |
| `workbenchToolSessionApplicationHandler.test.ts` | Connect/reactivate/disconnect, exact state correlation and deadlines, schema target filtering, SQL callbacks, all three tool routes, publication failures, late responses, and disposal contracts |
| `tool-state-snapshot-protocol.test.ts` | Exact request variants, bounded immutable Kusto/SQL section snapshots, descriptor/prototype/accessor/proxy/cycle/size rejection, original-envelope authority, and sole protocol ownership |
| `queryEditorProviderKustoConnectionBrowsingHandler.test.ts` | Red-first real-provider four-route forwarding, deferred settlement, exact rejection, constructor-slot identity, zero direct projection/service/diagnostics effects, preserved-owner guards, and disposal ownership |
| `kustoConnectionBrowsingApplicationHandler.test.ts` | Connection projection delegation, passive/interactive request shaping, blank inputs, undefined/empty database persistence, failure containment, disposal-crossing settlement, and later suppression |
| `queryEditorProviderCopilotQueryWorkflowHandler.test.ts` | Red-first real-provider five-route reference-identical forwarding, deferred settlement, exact rejection, zero direct Copilot/preflight/token/transport effects, zero-case provider inventory, and disposal ownership |
| `copilotQueryWorkflowApplicationHandler.test.ts` | Exact Kusto start/cancel, SQL preflight success/failure/cancellation races, Optimize prepare/cancel/run, lazy adapter resolution, disposal-crossing settlement, later suppression, and idempotence |
| `queryEditorProviderKustoSectionExecutionHandler.test.ts` | Red-first real-provider seven-route reference-identical forwarding, deferred settlement, exact rejection, zero direct coordinator/client/selection/transport/acknowledgement/Copilot-close effects, zero-case inventory, and disposal ownership |
| `kustoSectionExecutionApplicationHandler.test.ts` | Section lifecycle, exact start/publication acknowledgements and deadlines, manual success/failure/cancellation/replacement, late suppression, disposal-crossing settlement, and idempotence |
| `queryEditorProviderComparisonPreparationHandler.test.ts` | Red-first real-provider three-route and preparation forwarding, deferred settlement, exact rejection, zero direct map/lifecycle/broker/coordinator/Copilot effects, zero-case inventory, and disposal ownership |
| `comparisonPreparationApplicationHandler.test.ts` | Kusto/SQL preparation, all five SQL acknowledgement phases, rollback retry, target/policy races, removal cancellation, lifecycle rejection, deadlines, late suppression, and disposal contracts |
| `queryEditorProviderSqlSectionExecutionHandler.test.ts` | Red-first real-provider execute/cancel forwarding, constructor identity, deferred settlement, exact rejection, zero direct lifecycle/broker/workbench/client/transport effects, zero-case inventory, and disposal ownership |
| `sqlSectionExecutionApplicationHandler.test.ts` | Synchronous preflight, exact cancellation/replacement, owner/protection and tool-owner validation, query-mode shaping, ordinary/protected terminals, rotation suppression, logging, cleanup, and disposal contracts |
| `comparisonSummaryHostBridgeRetirement.test.ts` | Static guard that forbids host summary maps, waiters, routes, callbacks, and message types while preserving local artifact-backed rendering and banner teardown |
| `copilotChatOpenUtils.test.ts` | Shared native chat command sequence, 150 ms reapplication, query/submit shaping, and fallback semantics |
| `queryEditorProviderPowerBiPublishHelp.test.ts` | Dashboard application handler prompts, cancellation, workspace/item responses, publish leases, compensation, exact metadata restoration, and cleanup admission |
| `powerBiExport.test.ts` | HTML dashboard provenance parsing, DAX generation, PBIR/TMDL output, native slicers, Import/DirectQuery generation, CSS patching, sorted manifest bytes, Windows path admission, local writes, cancellation, and complete artifact goldens |
| `powerBiPublishCancellation.test.ts` | Direct manifest-to-Fabric part parity, zero publish filesystem staging, create transaction ordering, PBIR service patching, cancellation, exact recovery helpers, and pre-effect admission failures |
| `mssqlDialect.test.ts` | MSSQL persisted/UI dialect metadata |
| `mssqlSchema.test.ts` | MSSQL schema catalog query and parser compatibility |
| `sqlDialectRegistry.test.ts` | Dialect registry: register, get, list, unknown dialect handling |
| `sqlFormat.test.ts` | `.sqlx` file parsing, serialization, section type validation |
| `sqlClient.test.ts` | SQL client error contract |
| `stsProcessManager.test.ts` | STS process epochs, request timeouts, and restart-safe subscriptions |
| `stsQueryService.test.ts` | STS connection, execution, paging, cancellation, cleanup, and database listing |
| `stsResultAdapter.test.ts` | STS column and cell conversion into shared query results |
| `stsConnectionOptions.test.ts` | STS authentication, TLS, server/port, and timeout options |
| `sqlLeaveNoTrace.test.ts` | SQL Leave No Trace fail-closed policy before STS startup |
| `sqlPrettify.test.ts` | SQL formatting via sql-formatter |
| `sqlAuthState.test.ts` | Per-connection auth state tracking |
| `sqlFavorites.test.ts` | SQL favorites: add, remove, match, persistence |
| `sqlEditorUtils.test.ts` | SQL editor utilities: query mode, error formatting |

### Webview/component tests (`tests/webview/`)

| Test File | Coverage |
| --------- | -------- |
| `kw-html-section-slicer.test.ts` | HTML dashboard preview slicer normalization and filtering behavior |

### Integration tests (`tests/integration/`)

| Test File | Coverage |
| --------- | -------- |
| `kqlCompatInference.test.ts` | Schema inference for `.kql` compatibility mode |
| `kqlSidecar.test.ts` | Sidecar `.kql.json` file strategy |
| `schemaCache.test.ts` | Disk-based schema cache read/write |
| `kqlPrettify.test.ts` | KQL prettification (via compiled output extraction) |
| `kqlCompletionColumns.test.ts` | Column completion inference |
| `kqlCompletionColumnsInFunctionArgs.test.ts` | Column inference inside function calls |
| `kqlCompletionFunctions.test.ts` | Function completion |

### Coverage gate

`npm run test:coverage-gate` fails the build if Vitest statement coverage drops below the recorded baseline. The baseline is stored in `scripts/coverage-gate.mjs`.

## Build System

* **esbuild** bundles the extension (`esbuild.js`)
* Two build targets: extension host bundle and webview bundle
* Development: `npm run watch` (runs `watch:tsc` and `watch:esbuild` in parallel)
* Production: `npm run compile` (type-check + lint + esbuild)
* Distribution: `npm run vsix`
